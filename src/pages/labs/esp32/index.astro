---
// 자리 페이지(공통 기반 단계, 2026-09-16). ESP32 실습실 단계(PLAN §8.3 Phase 3)에서 실제 화면으로 바꾼다.
import ComingSoon from '../../../components/common/ComingSoon.astro';
import { getPage } from '../../../config/nav.ts';
import BaseLayout from '../../../layouts/BaseLayout.astro';

const page = getPage('labs-esp32');
---

<BaseLayout title={page.title} description={page.description}>
  <h1 class="page-title">{page.title}</h1>
  <p class="lead">{page.description}</p>
  <ComingSoon what={page.label} when="ESP32 실습실을 만드는 단계(Phase 3)" />
</BaseLayout>
