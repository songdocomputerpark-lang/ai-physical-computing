---
// 자리 페이지(공통 기반 단계, 2026-09-16). 영상처리 실습실 단계(PLAN §8.2 Phase 2)에서 실제 화면으로 바꾼다.
import ComingSoon from '../../../components/common/ComingSoon.astro';
import { getPage } from '../../../config/nav.ts';
import BaseLayout from '../../../layouts/BaseLayout.astro';

const page = getPage('labs-vision');
---

<BaseLayout title={page.title} description={page.description}>
  <h1 class="page-title">{page.title}</h1>
  <p class="lead">{page.description}</p>
  <ComingSoon what={page.label} when="영상처리 실습실을 만드는 단계(Phase 2)" />
</BaseLayout>
