---
// 자리 페이지(공통 기반 단계, 2026-09-16). 시작하기 담당이 실제 화면으로 바꾼다(PLAN §8.1 P1-08).
import ComingSoon from '../../../components/common/ComingSoon.astro';
import { getPage } from '../../../config/nav.ts';
import BaseLayout from '../../../layouts/BaseLayout.astro';

const page = getPage('start-student');
---

<BaseLayout title={page.title} description={page.description}>
  <h1 class="page-title">{page.title}</h1>
  <p class="lead">{page.description}</p>
  <ComingSoon what="학생용 시작 안내" when="사이트 뼈대를 만드는 지금 단계(Phase 1)" />
</BaseLayout>
