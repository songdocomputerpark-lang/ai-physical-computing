# 원본 자료(운영자 PC에만 있음 — 비공개 저장소 ai-physical-computing-materials에 보관)
/2022_인피컴_교수,학습운영계획/
/HW_소스코드/
/HW설치파일/
/dll 오류/
/pyautugoui_소스코드/
/교과서_소스코드/
/교과서_안/
/블루투스통신_소스코드/
/블루투스통신_수업교안/
/materials/

# 전체 요구사항 문서(6행에 학교 이름·직위가 있어 공개 저장소에 올리지 않는다 — 운영자 할 일 7번).
# 저장소 검사(scripts/lib/repo-check.mjs의 FORBIDDEN_TRACKED_FILES)가 한 번 더 막지만, 그건 커밋을 시도한 뒤에 걸리는 관문이라
# 실수로 스테이징되는 일 자체를 여기서 먼저 막는다(2026-09-17 검토 반영).
/docs/SPEC.md

# 빌드·의존성
node_modules/
dist/
.astro/
.cache/
# 같은 사이트에서 내보낼 외부 자산(MediaPipe WASM, Pyodide 예비본) — npm run dev·build 앞에서 scripts/vendor-assets.mjs가 npm 패키지·CDN에서 복사한다(PD-02·PD-13)
/public/vendor/
coverage/
playwright-report/
test-results/
blob-report/

# 환경·편집기
.env
.env.*
.DS_Store
Thumbs.db
.vscode/
.idea/
