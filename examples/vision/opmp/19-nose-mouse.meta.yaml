# 예제 설명(사이드카). 원본 코드 파일은 줄 번호를 지키려고 고치지 않고, 제목·설명은 여기에 적어요(src/lab/README.md 3절).
# 처음 한 번은 scripts/import-examples.mjs가 만들고, 그 뒤로는 사람이 고쳐요(스크립트가 덮어쓰지 않아요).
title: "계단 19: 코 끝으로 마우스 움직이기"
description: 코 끝 위치를 화면 크기에 맞춰 마우스 커서를 옮겨요(가상 데스크톱).
source_id: f127
tags:
  - 얼굴
  - 코
  - pyautogui
packages:
  - opencv-python

# ── 실습실에서 돌릴 때(P2-09에서 브라우저로 확인, 2026-09-16) ──
# 필요한 입력: '재생 입력' + 재생 동작 '고개 돌리기'. 마우스는 가상 데스크톱(pyautogui 모듈)에서 움직여요.
# 출력 창 'Face' · ESC로 끝내요.
# 주의: 코 위치로 커서를 옮기므로 가상 모니터 안에서만 움직여요(진짜 마우스는 그대로예요).
