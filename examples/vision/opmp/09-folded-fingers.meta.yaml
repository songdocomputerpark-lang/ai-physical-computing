# 예제 설명(사이드카). 원본 코드 파일은 줄 번호를 지키려고 고치지 않고, 제목·설명은 여기에 적어요(src/lab/README.md 3절).
# 처음 한 번은 scripts/import-examples.mjs가 만들고, 그 뒤로는 사람이 고쳐요(스크립트가 덮어쓰지 않아요).
title: "계단 9: 접힌 손가락 알아채기"
description: 손가락마다 끝과 뿌리의 위치를 비교해 접혔는지 펴졌는지 글자로 써요.
source_id: f136
tags:
  - 손
  - 손가락 판별
packages:
  - opencv-python

# ── 실습실에서 돌릴 때(P2-08에서 브라우저로 확인, 2026-09-16) ──
# 필요한 입력: '재생 입력(합성 손 좌표)' — 카메라가 없어도 돼요. 웹캠으로 하려면 public/models/hand_landmarker.task가 있어야 해요.
# 출력 창 'Hand Tracking' · ESC로 끝내요.
# 주의: 재생 동작을 '손가락 0~5개 펴기'로 두면 화면 왼쪽 위 숫자(00000~11111)가 차례로 바뀌어요.
# 주의: 접힘 판별 반복문이 손 반복문 밖이라 손이 여럿이면 마지막 손만 판별돼요(원본 그대로, max_num_hands=1이라 지금은 영향 없음).
