# 예제 설명(사이드카). 원본 코드 파일은 줄 번호를 지키려고 고치지 않고, 제목·설명은 여기에 적어요(src/lab/README.md 3절).
# 처음 한 번은 scripts/import-examples.mjs가 만들고, 그 뒤로는 사람이 고쳐요(스크립트가 덮어쓰지 않아요).
title: "계단 2: 캠 화면 크기 바꾸기"
description: 카메라에 너비를 부탁하고 실제로 받은 크기를 읽어요.
source_id: f129
tags:
  - 카메라
  - 해상도
packages:
  - opencv-python

# ── 실습실에서 돌릴 때(P2-08에서 브라우저로 확인, 2026-09-16) ──
# 필요한 입력: 아무거나(웹캠·샘플·재생 입력). 손 인식을 쓰지 않는 단계예요.
# 출력 창 'Hand Tracking' · ESC로 끝내요.
# 주의: cap.set으로 가로 1080을 부탁해도 브라우저 카메라는 640×480으로 열려요. cap.get이 실제 크기를 돌려주니 그 값을 쓰면 돼요.
