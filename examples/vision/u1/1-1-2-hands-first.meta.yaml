# 예제 설명(사이드카). 원본 코드 파일은 줄 번호를 지키려고 고치지 않고, 제목·설명은 여기에 적어요(src/lab/README.md 3절).
# 처음 한 번은 scripts/import-examples.mjs가 만들고, 그 뒤로는 사람이 고쳐요(스크립트가 덮어쓰지 않아요).
title: "1-1-2 기본 실습: 손 관절 점 그리기(첫 손 인식)"
description: 웹캠 영상에서 손을 찾아 관절 점과 이음선을 그려요. 원본은 색 변환 없이 인식해요(교사용 참고).
lesson: 1-1-2
page: 17
source_id: f026
tags:
  - 손
  - mediapipe
packages:
  - opencv-python

# ── 실습실에서 돌릴 때(P2-08에서 브라우저로 확인, 2026-09-16) ──
# 필요한 입력: '재생 입력(합성 손 좌표)' — 카메라가 없어도 돼요. 웹캠으로 하려면 public/models/hand_landmarker.task가 있어야 해요.
# 출력 창 'Hand' · q 키로 끝내요(출력 창을 한 번 눌러 초점을 준 뒤).
# 주의: 색 변환 없이 BGR 배열을 그대로 process()에 넣어요(원본 그대로 — PC에서도 같아요). 실습실도 넘겨받은 배열로 인식해 원본 동작을 재현해요.
# 주의: Hands()에 인자가 없어 손을 2개까지 찾아요(레거시 기본값).
