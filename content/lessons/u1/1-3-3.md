---
title: 내 얼굴에 맞춤 필터 만들기
unit: 1
order: 9
kind: textbook
label: 1-3-3
description: 얼굴 가장자리의 점 네 개로 얼굴을 감싸는 상자를 그리고, 투명한 부분이 있는 가면 그림을 얼굴 크기에 맞춰 영상 위에 겹쳐 그려 얼굴 필터를 만들어요.
pages: "파일명 p75·p78"
standards: ["12인피01-03"]
duration: 50
materials: ["웹캠(없으면 실습실의 재생 입력)"]
difficulty: 3
lab: vision
examples:
  - file: vision/u1/1-3-3-face-box.py
    title: 기본 실습 — 얼굴 둘레에 상자 그리기(파일명 p75)
  - file: vision/u1/1-3-3-adv-face-mask-site.py
    title: 심화 실습(사이트판) — 얼굴에 가면 그림 씌우기(파일명 p78)
quiz:
  - q: 가면 그림을 cv2.imread("mask.png", cv2.IMREAD_UNCHANGED)로 읽는 까닭은 무엇일까요?
    choices: ["투명한 정도(알파 채널)까지 네 번째 숫자로 읽으려고", "그림을 흑백으로 읽으려고", "그림 크기를 얼굴에 맞추려고"]
    answer: 0
    explain: IMREAD_UNCHANGED는 PNG 그림의 알파 채널까지 그대로 읽어요. 이 설정이 없으면 파랑, 초록, 빨강 세 숫자만 읽어서 투명한 곳을 알 수 없어요. 크기는 cv2.resize()가 맞춰요.
  - q: 가면 그림에서 알파 값이 0인 픽셀은 영상 위에서 어떻게 보일까요?
    choices: ["가면 색만 보여요.", "가면 색과 원래 색이 반씩 섞여 보여요.", "원래 영상 색이 그대로 보여요."]
    answer: 2
    explain: a = 0이면 결과 = 가면 색 × 0 + 원래 색 × 1이라 원래 영상 색이 그대로예요. 알파 255(a = 1)면 가면 색만, a = 0.5면 반씩 섞여요.
  - q: 기본 실습에서 얼굴 상자의 왼쪽 위 모서리 (x1, y1)은 어느 점의 좌표로 정할까요?
    choices: ["454번의 x와 152번의 y", "234번의 x와 10번의 y", "1번의 x와 1번의 y"]
    answer: 1
    explain: x1은 얼굴 왼쪽 끝인 234번의 x, y1은 이마 위 끝인 10번의 y로 정해요. 454번의 x와 152번의 y는 오른쪽 아래 모서리 (x2, y2)예요.
---

## 학습목표

- 얼굴 가장자리의 랜드마크 네 개로 얼굴을 감싸는 상자를 그릴 수 있어요.
- 투명한 정도(알파 채널)가 있는 그림을 영상 위에 겹쳐 그리는 원리를 설명할 수 있어요.
- 가면 그림을 얼굴의 크기와 자리에 맞춰 얹는 얼굴 필터 프로그램을 만들 수 있어요.

## 왜 배울까

사진 앱에서 얼굴에 가면이나 안경, 동물 귀를 씌우는 필터를 써 본 적 있나요? 필터가 얼굴을 따라다니려면, 컴퓨터가 프레임마다 얼굴이 어디에 얼마나 크게 있는지 알아내 그림의 크기와 자리를 맞춰야 해요. 오늘은 얼굴의 점으로 가면 그림을 얹는 얼굴 필터를 직접 만들어요.

<figure>
<img src="/images/lessons/1-3-3/face-filter-idea.svg" alt="스마트폰 화면 두 개를 나란히 그린 그림: 왼쪽은 정면을 보는 얼굴의 두 눈 위에 보라색 가면이 얹혀 있고, 오른쪽은 얼굴이 옆으로 옮겨 가고 작아졌는데 가면도 따라 옮겨 가고 작아져 여전히 두 눈 위에 있다. 가운데 화살표에 얼굴을 따라가요라고 적혀 있다" width="640" height="316">
<figcaption>얼굴이 움직이거나 카메라에서 멀어져도 가면이 따라가요. 프레임마다 얼굴의 자리와 크기를 새로 재기 때문이에요. 사이트가 직접 그린 그림이에요.</figcaption>
</figure>

## 핵심 개념

### 점 네 개로 얼굴을 감싸는 상자

얼굴의 자리와 크기를 알려면 얼굴 가장자리의 점을 봐요. :용어[페이스 메시]의 점 가운데 10번은 이마 위 끝, 152번은 턱 끝, 234번은 얼굴 왼쪽 끝, 454번은 얼굴 오른쪽 끝이에요. 이 네 점으로 얼굴을 딱 감싸는 네모를 그릴 수 있어요. 물체를 둘러싼 이런 네모를 **바운딩 박스**(bounding box)라고 해요.

<figure>
<img src="/images/lessons/1-3-3/face-box.svg" alt="정면을 보는 얼굴의 이마 위 끝(10번), 턱 끝(152번), 얼굴 왼쪽 끝(234번), 얼굴 오른쪽 끝(454번)에 빨간 점을 찍고, 네 점에 닿는 초록 네모로 얼굴을 감싼 그림. 왼쪽 위 모서리는 (x1, y1), 오른쪽 아래 모서리는 (x2, y2)이고, 오른쪽에 x1 = 234번의 x × w = 209, y1 = 10번의 y × h = 81, x2 = 454번의 x × w = 430, y2 = 152번의 y × h = 379라는 계산이 있다" width="640" height="380">
<figcaption>왼쪽 위 모서리는 234번의 x와 10번의 y, 오른쪽 아래 모서리는 454번의 x와 152번의 y로 정해요. 숫자는 실습실 재생 입력의 정면 얼굴을 가로 640, 세로 480 화면에서 잰 것이에요. 사이트가 직접 그린 그림이에요.</figcaption>
</figure>

| 모서리 | x | y |
| --- | --- | --- |
| 왼쪽 위 (x1, y1) | 234번(얼굴 왼쪽 끝)의 x × 너비 | 10번(이마 위 끝)의 y × 높이 |
| 오른쪽 아래 (x2, y2) | 454번(얼굴 오른쪽 끝)의 x × 너비 | 152번(턱 끝)의 y × 높이 |

네 점은 모두 :용어[정규화 좌표]라서, 1-2-2에서처럼 화면의 너비와 높이를 곱해 픽셀 좌표로 바꿔요. 이 상자의 가로 길이가 얼굴의 너비, 세로 길이가 얼굴의 높이예요.

### 투명한 곳이 있는 그림: 알파 채널

컬러 사진의 픽셀은 파랑, 초록, 빨강 세 숫자로 색을 나타내요. PNG 그림은 여기에 네 번째 숫자를 하나 더 가질 수 있는데, 이 숫자를 :용어[알파 채널]이라고 해요. 알파는 그 픽셀이 얼마나 불투명한지를 0부터 255까지로 나타내요. 0이면 완전히 투명하고, 255면 완전히 불투명해요.

<figure>
<img src="/images/lessons/1-3-3/alpha-channel.svg" alt="투명한 곳을 바둑판 무늬로 보인 가면 그림에서 보라색 몸통 픽셀은 (184, 63, 91, 255)로 알파 255라 가면 색이 보이고, 눈 구멍 픽셀은 (0, 0, 0, 0)으로 알파 0이라 투명하다는 설명 그림. 오른쪽에 가면 색과 원래 색을 겹친 결과가 a = 1이면 가면 색만, a = 0.5면 반반 섞인 색, a = 0이면 원래 색 그대로인 색 칸 세 개가 있고, 아래에 결과 = 가면 색 × a + 원래 색 × (1 − a), a = 알파 ÷ 255라는 식이 있다" width="640" height="384">
<figcaption>숫자 네 개의 차례는 파랑, 초록, 빨강, 알파예요. 알파를 255로 나눈 a만큼 가면 색을 쓰고, 나머지(1 − a)만큼 원래 영상 색을 써요. 사이트가 직접 그린 그림이에요.</figcaption>
</figure>

그림을 겹쳐 그릴 때는 픽셀마다 이렇게 계산해요.

```text
a = 알파 ÷ 255              (0부터 1 사이의 수)
결과 색 = 가면 색 × a + 원래 영상 색 × (1 − a)
```

a가 1이면 가면 색만, 0이면 원래 영상 색만 남아요. 그래서 가면의 눈 구멍과 바깥처럼 알파가 0인 곳에서는 뒤의 얼굴이 그대로 보여요. `cv2.imread("mask.png", cv2.IMREAD_UNCHANGED)`처럼 `IMREAD_UNCHANGED`를 붙여야 이 네 번째 숫자까지 읽어요.

### 가면 그림을 얼굴 크기에 맞춰 얹기

가면 그림을 얼굴에 맞추는 일은 세 단계예요.

1. **얼굴의 너비와 높이 재기:** 234번과 454번 사이의 거리가 얼굴 너비, 10번과 152번 사이의 거리가 얼굴 높이예요. 1-2-3에서 배운 :용어[유클리드 거리]로 재요.
2. **가면 그림 크기 바꾸기:** `cv2.resize(가면, (너비, 높이))`로 가면 그림을 얼굴 크기로 늘이거나 줄여요. 괄호 안은 (가로, 세로) 차례예요.
3. **자리 정해 겹치기:** 가면 그림의 왼쪽 위를 얼굴 상자의 왼쪽 위(234번의 x, 10번의 y)에 맞추고, 그 자리부터 픽셀마다 알파로 섞어 그려요.

<figure>
<img src="/images/lessons/1-3-3/mask-fit.svg" alt="가로 400, 세로 500인 mask.png를 resize 화살표로 줄여, 가로 640, 세로 480 화면 속 얼굴 위에 얼굴 너비 220, 얼굴 높이 297 크기로 겹친 그림. 겹친 파란 점선 네모의 왼쪽 위 모서리는 (209, 81)이고, 가면은 네모의 위쪽, 두 눈 위에 온다" width="640" height="372">
<figcaption>실습실이 넣어 주는 mask.png는 그림의 위쪽에만 가면이 있고 나머지는 투명해요. 그래서 얼굴 높이만큼 늘여 이마부터 그리면 가면이 눈 높이에 와요. 사이트가 직접 그린 그림이에요.</figcaption>
</figure>

## 따라하기

예제 두 개를 차례로 실행해요. 기본 실습은 얼굴 둘레에 상자를 그리고, 심화 실습은 얼굴에 가면 그림을 씌워요. 아래 줄 번호는 실습실에 보이는 예제 파일의 줄 번호예요.

심화 실습은 사이트판을 써요. 교과서 코드는 픽셀을 하나씩 세는 반복문으로 가면을 겹쳐서, 브라우저에서는 한 장을 그리는 데 1초 넘게 걸려 영상이 뚝뚝 끊겨요. 사이트판은 결과는 같고 계산 방법만 빨라요(실행 결과 아래 "왜 이런 결과가 나올까" 상자). 가면 그림 `mask.png`는 사이트가 직접 그린 그림이고, 실습실이 파이썬 작업 폴더에 넣어 줘요.

카메라가 없거나 얼굴을 비추고 싶지 않으면 입력 소스를 '재생 입력(합성 좌표)'으로 골라요. 재생 동작은 '고개 돌리기'로 저절로 바뀌어요.

:::주의[가면을 씌운 화면도 내 얼굴 사진이에요]
- 필터를 씌워도 화면에는 내 얼굴이 담겨 있어요. 화면을 찍거나 파일로 저장해 올리지 않아요.
- 친구 얼굴에 필터를 씌울 때는 먼저 허락을 받아요.
:::

### 기본 실습 코드 읽기

- 2\~3행: OpenCV와 MediaPipe를 불러와요. 6행: 웹캠에 연결해요.
- 7\~8행: 얼굴을 1개만 찾는 `FaceMesh` 도구를 만들어요.
- 11\~14행: 웹캠이 열려 있는 동안 프레임을 읽어요.
- 16\~19행: 좌우로 뒤집고, 프레임의 높이(`h`)와 너비(`w`)를 꺼내고, RGB로 바꿔 얼굴의 점을 찾아요.
- 22\~24행: 얼굴을 찾았으면 얼굴마다 점 목록(`landmarks`)을 꺼내요.
- 27\~30행: 얼굴 왼쪽 끝(234번), 오른쪽 끝(454번), 이마 위 끝(10번), 턱 끝(152번) 점을 꺼내요.
- 33\~34행: 왼쪽 위 모서리 (x1, y1)과 오른쪽 아래 모서리 (x2, y2)를 픽셀 좌표로 구해요.
- 37행: `cv2.rectangle()`로 두께 2인 초록 네모를 그려요.
- 40행: 'Face Bounding Box' 창에 보여 주고, 42\~43행: q 키를 누르면 끝내요.

### 심화 실습(사이트판) 코드 읽기

- 12행: 배열 계산 라이브러리 `numpy`를 `np`라는 이름으로 불러와요.
- 16\~20행: `FaceMesh`를 영상 모드, 얼굴 1개, 신뢰도 기준 0.5로 만들어요.
- 23행: `cv2.imread()`로 가면 그림을 알파 채널까지 읽어요.
- 29행: 얼굴에 가면을 겹치는 함수 `overlay_mask_on_face(frame, landmarks, mask)`를 만들어요.
- 30\~33행: 이마(10번), 턱(152번), 왼쪽 뺨(234번), 오른쪽 뺨(454번) 점을 꺼내요.
- 35\~43행: `np.linalg.norm()`으로 두 뺨 사이의 거리(얼굴 너비)와 이마와 턱 사이의 거리(얼굴 높이)를 구해 픽셀로 바꿔요. `norm`은 두 점의 차이로 유클리드 거리를 구하는 함수예요.
- 46\~47행: 얼굴이 너무 작으면 가면을 그리지 않고 그대로 돌려줘요(사이트판에서 더한 줄).
- 49행: `cv2.resize()`로 가면 그림을 얼굴 크기로 바꿔요.
- 51\~52행: 가면을 그리기 시작할 왼쪽 위 자리(234번의 x, 10번의 y)를 구해요.
- 55\~61행: 가면 가운데 화면 밖으로 나가는 부분을 잘라 내고, 화면 안에 들어오는 조각(`piece`)만 남겨요.
- 66\~68행: 알파를 255로 나눠 0\~1의 a로 만들고(66행), 원래 영상의 같은 자리를 꺼내(67행), "가면 색 × a + 원래 색 × (1 − a)"를 한 번에 계산해 영상에 다시 넣어요(68행).
- 77\~79행: 좌우로 뒤집고, RGB로 바꿔 얼굴의 점을 찾아요.
- 82\~85행: 얼굴을 찾았으면 함수로 가면을 겹쳐요.
- 87행: 'Virtual Mask Filter' 창에 보여 주고, 89\~90행: q 키를 누르면 끝내요.

### 예제 코드

두 예제의 전체 코드예요. [이 자리에서 실습실 열기]를 누르면 이 자리에 실습실이 열리고, [실습실에서 열기]를 누르면 실습실 페이지로 가요.

::예제

### 기본 실습 실행하고 결과 보기

첫 번째 예제에서 [이 자리에서 실습실 열기]를 누르고 [실행]을 눌러요. 브라우저가 카메라 사용을 물으면 "허용"을 눌러요. 'Face Bounding Box' 창에서 얼굴을 감싸는 초록 네모가 보이고, 얼굴이 움직이면 네모도 따라 움직여요. 카메라에 가까이 가면 네모가 커지고, 멀어지면 작아져요.

:::확인
- 'Face Bounding Box' 창에서 얼굴 둘레에 초록 네모가 보여요.
- 얼굴이 움직이면 네모가 따라 움직이고, 크기도 달라져요.
:::

:::왜그럴까
19행이 프레임마다 얼굴의 점을 새로 찾고, 33\~34행이 네 점으로 네모의 두 모서리를 다시 구해요. 그래서 얼굴이 움직이거나 카메라와의 거리가 달라지면 네모의 자리와 크기가 따라 바뀌어요.

고개를 옆으로 기울이면 네모가 얼굴에 꼭 맞지 않아요. 네모는 늘 화면과 나란하게 그려지는데, 기울어진 얼굴의 네 점은 네모의 네 변에 딱 닿지 않기 때문이에요.
:::

### 심화 실습 실행하고 결과 보기

두 번째 예제에서 [이 자리에서 실습실 열기]를 누르고 [실행]을 눌러요. 'Virtual Mask Filter' 창에서 두 눈 위에 보라색 가면이 얹혀요. 가면의 눈 구멍으로 내 눈이 보이고, 얼굴이 움직이거나 멀어지면 가면이 따라 움직이고 크기가 바뀌어요. 실습실이 넣어 준 `mask.png`는 실습실의 파일 패널에 "사이트 제공"으로 보여요.

:::확인
- 'Virtual Mask Filter' 창에서 얼굴의 두 눈 위에 보라색 가면이 보여요.
- 가면의 눈 구멍과 가면 바깥으로는 원래 영상이 그대로 보여요.
- 얼굴이 움직이면 가면도 따라 움직여요.
:::

:::왜그럴까
23행이 가면 그림을 네 겹(파랑, 초록, 빨강, 알파)으로 읽고, 49행이 얼굴 크기로 바꾸고, 51\~52행이 이마와 왼쪽 뺨의 자리에 맞춰요. 68행이 픽셀마다 "가면 색 × a + 원래 색 × (1 − a)"를 계산하니, 알파가 255인 보라색 부분은 가면 색이 되고 알파가 0인 눈 구멍과 바깥은 원래 영상 그대로예요.

교과서 코드는 이 계산을 반복문 세 겹(세로 × 가로 × 색 3개)으로 픽셀마다 해요. 가면이 가로 220, 세로 297이면 한 장에 20만 번쯤 계산하니, 브라우저 안 파이썬에서는 한 장에 1초가 넘게 걸려요. 사이트판의 `numpy`는 같은 계산을 배열 전체에 한 번에 시켜서, 실습실에서 1초에 12장쯤 그려요. 계산 결과는 똑같아요.
:::

## 바꿔보기

:::바꿔보기
1. 기본 실습 37행 `cv2.rectangle(frame, (x1, y1), (x2, y2), (0, 255, 0), 2)`의 마지막 `2`를 `-1`로 바꿔 보세요. 얼굴이 어떻게 보이나요?
2. 심화 실습 66행 끝에 `* 0.5`를 붙여 `alpha = piece[:, :, 3:4].astype("float32") / 255.0 * 0.5`로 바꿔 보세요. 가면이 어떻게 보이나요?
3. 심화 실습 52행을 `top_left_y = int(landmarks[159].y * frame.shape[0])`로 바꿔 보세요. 159번은 한쪽 눈의 위 눈꺼풀 점이에요. 가면이 어디로 옮겨 가나요?
:::

:::왜그럴까[왜 알파에 0.5를 곱하면 가면이 비쳐 보일까?]
0.5를 곱하면 가면에서 가장 진한 곳도 a가 0.5가 돼요. 그러면 결과 = 가면 색 × 0.5 + 원래 색 × 0.5라서 가면 색과 얼굴이 반씩 섞여 가면 너머로 얼굴이 비쳐 보여요. 알파가 0이던 곳은 0 × 0.5 = 0이라 그대로 투명해요.

바꿔보기 1의 두께 `-1`은 "선이 아니라 속을 채워 그려라"라는 뜻이에요. 그래서 얼굴이 초록 네모로 가려져요. 바꿔보기 3처럼 그리기 시작하는 자리를 눈 높이로 내리면, 가면 그림 전체가 아래로 내려가 가면이 코와 입 쪽을 덮어요.
:::

:::오류
`cv2.error: … !ssize.empty() in function 'resize'`가 나면 가면 그림을 읽지 못한 거예요. 23행의 파일 이름에 오타가 있으면 `cv2.imread()`가 그림 대신 `None`(빈값)을 돌려주고, 49행의 `cv2.resize()`가 빈 그림을 받아 이 오류가 나요. 파일 이름을 `mask.png`로 바르게 적었는지 확인해요. 오류 메시지의 뜻은 [오류 사전](/help/errors/#cv2-empty-image)에서 찾아볼 수 있어요.
:::

## 도전 과제

::::도전[도전 과제 1: 코드로 선글라스 필터 그리기]
가면 그림 파일을 쓰지 않고, 코드로 투명한 그림을 만들어 선글라스를 그려 보세요. `np.zeros()`로 모든 픽셀이 0인(완전히 투명한) 네 겹 그림을 만들고, 그 위에 알파 255로 동그라미 두 개를 그리면 돼요.

:::힌트
심화 실습 23행을 아래 네 줄로 바꿔요. 줄이 3개 늘어나니, 그 아래 줄 번호는 3씩 밀려요.

```python
mask_image = np.zeros((500, 400, 4), dtype=np.uint8)                  # 세로 500, 가로 400, 네 겹 — 처음엔 모두 투명
cv2.circle(mask_image, (120, 216), 60, (40, 40, 40, 255), -1)        # 왼쪽 렌즈(알파 255)
cv2.circle(mask_image, (280, 216), 60, (40, 40, 40, 255), -1)        # 오른쪽 렌즈
cv2.line(mask_image, (175, 216), (225, 216), (40, 40, 40, 255), 10)   # 두 렌즈를 잇는 다리
```

(120, 216)과 (280, 216)은 실습실의 가면 그림에서 두 눈 구멍이 있던 자리예요. 색의 네 번째 숫자 255가 알파라서 렌즈만 보이고 나머지는 투명해요. 색이나 모양을 바꿔 나만의 필터를 만들어 봐요.
:::
::::

::::도전[도전 과제 2: 얼굴을 가려 주는 모자이크 필터]
사진을 올리기 전에 다른 사람의 얼굴을 가려 주는 기능은 개인정보를 지키는 데 쓰여요. 기본 실습의 얼굴 상자 안을 모자이크로 가려 보세요. 상자 안을 아주 작게(8×8) 줄였다가 다시 원래 크기로 늘리면 네모난 조각 무늬가 돼요.

:::힌트
기본 실습 34행 아래에 아래 줄을 넣어요(12칸 들여쓰기).

```python
            x1, x2 = max(0, min(x1, x2)), min(w, max(x1, x2))
            y1, y2 = max(0, y1), min(h, y2)
            if x2 > x1 and y2 > y1:
                small = cv2.resize(frame[y1:y2, x1:x2], (8, 8))
                frame[y1:y2, x1:x2] = cv2.resize(small, (x2 - x1, y2 - y1), interpolation=cv2.INTER_NEAREST)
```

첫 두 줄은 두 모서리를 작은 값, 큰 값 차례로 맞추고 화면 밖으로 나가지 않게 잘라요. `frame[y1:y2, x1:x2]`는 영상에서 상자 안만 꺼낸 조각이에요. `INTER_NEAREST`는 늘릴 때 옆 픽셀과 섞지 않아서 조각 무늬가 또렷하게 남아요. `(8, 8)`을 `(16, 16)`으로 바꾸면 조각이 더 잘아져요.
:::
::::

### 다음은 무엇을 배울까

페이스 매시 중단원은 여기까지예요. 다음 차시 [1-4-1 몸으로 말해요! 동작 감지 인공지능](/learn/u1/1-4-1/)에서는 얼굴 대신 몸의 관절 33곳을 찾아, 손을 들었는지 알아채는 프로그램을 만들어요.

## 확인 퀴즈

오늘 배운 내용을 세 문제로 확인해 보세요.

::퀴즈

## 교사용

:::교사용
### 지도안 요약(50분, 사이트가 제안하는 흐름)

1. 도입(5분): 사진 앱의 얼굴 필터를 써 본 경험을 나누고, 따라하기의 주의 상자(필터를 씌운 화면도 얼굴 사진)를 함께 읽어요.
2. 전개 1(10분): 얼굴 상자 그림으로 네 점과 두 모서리를 짚은 뒤 기본 실습을 실행하고 바꿔보기 1(속 채우기)을 해요.
3. 전개 2(12분): 알파 채널 그림으로 "가면 색 × a + 원래 색 × (1 − a)"를 색 칸 세 개와 함께 설명하고, 가면 맞추기 세 단계(재기, 크기 바꾸기, 자리 정하기)를 그림으로 정리해요.
4. 전개 3(15분): 심화 실습을 실행하고 바꿔보기 2·3을 해요. 교과서 코드(반복문)와 사이트판(`numpy`)의 속도 차이를 "왜 이런 결과가 나올까" 상자로 짚어요.
5. 정리(8분): 확인 퀴즈와 자기 평가를 하고, 도전 과제 2(모자이크)로 얼굴 영상과 개인정보 이야기를 나눠요.

### 평가 포인트

이 차시는 원고가 없어 원고의 자기 평가 문항이 없어요. 아래는 다른 차시와 같은 모양으로 사이트가 제안하는 자기 평가 문항이에요(학생이 세 단계로 스스로 표시).

- 얼굴 가장자리 랜드마크로 얼굴을 감싸는 상자를 그릴 수 있는가?
- 알파 채널로 두 그림을 겹쳐 그리는 원리를 설명할 수 있는가?
- 얼굴의 크기와 자리에 맞춰 그림을 얹는 필터 프로그램을 만들 수 있는가?

관찰할 곳: 알파 0, 0.5, 1일 때의 결과를 식으로 설명하는지, 가면이 얼굴을 따라가는 까닭을 "프레임마다 점을 새로 찾아 크기와 자리를 다시 잰다"로 말하는지 봐요. 도전 과제 2에서는 얼굴을 가리는 기능이 왜 필요한지 함께 이야기해요.

### 자주 막히는 곳

- 가면을 씌우는 교과서 파일 그대로인 예제('1-3-3 심화 실습: 얼굴에 마스크 그림 씌우기')는 픽셀마다 반복문을 돌아 브라우저에서 1초에 한 장도 그리지 못해요(실습실 화면에 "0fps"). 수업에서는 사이트판을 쓰고, 교과서 코드는 "왜 느릴까"를 이야기할 때 보여 주세요.
- `cv2.resize(가면, (너비, 높이))`의 괄호 안 차례가 (가로, 세로)라서, 넘파이 배열의 모양 `(세로, 가로, 4)`와 반대예요. 도전 과제 1의 `np.zeros((500, 400, 4))`도 (세로, 가로, 겹 수) 차례예요.
- 23행 파일 이름을 바꾸면 `cv2.error`가 나요(오류 상자). 내 그림을 쓰려면 실습실 파일 패널의 [파일 넣기]로 투명 배경 PNG를 먼저 넣고 그 이름을 적어요.
- 고개를 크게 돌리면 두 뺨 사이 거리가 줄어 가면이 좁아져요. 얼굴 상자와 같은 원리예요.

### 바꿔보기와 도전 과제 확인 결과(재생 입력)

- 바꿔보기 1: 얼굴 자리가 속이 찬 초록 네모로 가려져요.
- 바꿔보기 2: 가면이 반쯤 비쳐 보여요. 바꿔보기 3: 가면의 위쪽 끝이 눈 높이로 내려가, 가면이 코와 입 쪽을 덮어요.
- 도전 과제 1: 두 눈 위에 짙은 회색 렌즈 두 개가 얹혀요. 도전 과제 2: 얼굴 상자 안이 8×8 조각 무늬로 가려지고, 그 위에 초록 네모가 그려져요.
- 23행 파일 이름을 `mask2.png`처럼 없는 이름으로 바꾸면 49행에서 `cv2.error`와 오류 풀이 카드("OpenCV에 빈 이미지(None)가 들어갔어요")가 나와요.

### 원고와 코드 파일, 사이트판

- 원고(교과서 052\~111쪽)가 자료에 없어, 코드 파일(파일명 p75·p78, 코드 id f038·f039)을 근거로 사이트가 본문을 새로 썼어요. 원고를 받으면 원고 문장으로 바꿔요.
- 기본 실습은 코드 파일 그대로예요. 심화 실습은 사이트판을 썼고, 교과서 파일 그대로인 판도 실습실 예제 목록에 있어요([교과서 코드 열기](/labs/vision/?example=vision%2Fu1%2F1-3-3-adv-face-mask.py)). 사이트판은 머리말 8줄을 더하고, 얼굴이 너무 작을 때 넘어가는 세 줄(45\~47행)을 넣고, 픽셀마다 도는 반복문을 `numpy` 한 번 계산(54\~68행)으로 바꿨어요. 바뀐 줄에는 `# [사이트판]`이라고 적혀 있어요.

| 코드 | 교과서 파일(p78) | 사이트판 |
| --- | --- | --- |
| `cv2.imread("mask.png", …)` | 15행 | 23행 |
| `def overlay_mask_on_face` | 21행 | 29행 |
| 얼굴 너비, 높이 계산 | 27\~35행 | 35\~43행 |
| `cv2.resize()` | 37행 | 49행 |
| 그리기 시작 자리 | 39\~40행 | 51\~52행 |
| 겹쳐 그리기 | 42\~51행(반복문 세 겹) | 54\~68행(`numpy`) |
| `cv2.flip()` | 60행 | 77행 |
| `cv2.imshow()` | 70행 | 87행 |

- 교과서 코드에는 `mask.png` 파일이 자료에 없어서, 사이트가 가면 그림을 직접 그려(SVG) 실습실이 PNG로 바꿔 넣어 줘요. 교과서 코드는 그리기 시작 자리가 화면 왼쪽이나 위쪽 밖이면(음수) 가면이 반대편 가장자리에 그려지는 결함이 있어요. 사이트판은 화면 안의 조각만 그려서 이 문제가 없어요.
- 그림은 모두 사이트가 그렸고, 얼굴은 실습실 재생 입력과 같은 계산으로 만든 선 그림이에요(사람 사진 없음).
:::
