# 바뀐 내용(CHANGELOG)

「AI 피지컬 컴퓨팅 오픈랩」의 판(버전)마다 무엇이 바뀌었는지 적어요. 가장 최근 판이 맨 위예요.

- **판 번호 규칙**은 [MAINTENANCE.md](MAINTENANCE.md) 10절이에요. 요약: 사이트를 만드는 동안은 Phase N이 끝날 때마다 `0.N.0`, Phase 6(품질과 유지보수)이 끝나면 첫 정식 판 `1.0.0`. 그 뒤로는 고침 = 셋째 자리, 차시·예제·기능 더하기 = 둘째 자리, 교육과정 개정이나 주소가 바뀌는 큰 변화 = 첫째 자리를 올려요.
- 지금 판은 사이트 바닥글의 "버전"과 점검 페이지 [결과 복사] 글의 "사이트 버전"에 보여요. 값은 `package.json`의 `version` 한 곳에서 와요.
- 날짜는 한국 시간이에요. "커밋" 줄은 그 판에 들어간 커밋 범위예요 — 저장소에서 `git log 앞..뒤`로 볼 수 있어요.
- 0.1.0~0.5.0은 판 번호 규칙을 정한 Phase 6(2026-09-26)에 기록(PROGRESS.md·git log)을 되짚어 붙인 번호예요. 그동안 바닥글은 0.1.0으로 보였고, Phase 6 통합(2026-09-26)에서 `package.json`을 0.5.0으로 맞췄어요.

## 다음 판(아직 올리지 않음)

<!-- 새로 바꾼 것을 여기에 한 줄씩 적고, 판을 올릴 때 제목을 "## 1.0.1 — 날짜"처럼 바꿔요(MAINTENANCE.md 10절). -->

- 아직 없어요.

## 1.0.0 — 준비 중(Phase 6 품질과 유지보수)

Phase 6이 끝나 완료 기준(새 차시를 코드 수정 없이 더해 push 뒤 2분 안에 주소가 열림 — 시나리오 E, `sources.yaml` 누락 0)을 확인하면 날짜를 적고 첫 정식 판으로 올려요. 아래는 지금까지 들어간 것이에요.

**더한 것**

- 빌드 환경 변수 `APC_BASE`(사이트 하위 경로 — 오프라인판은 `/`)·`APC_OUT_DIR`(결과 폴더)를 `src/config/site.ts` 한 곳에서 읽어, 링크·서비스 워커·Pyodide 예비본·빌드 뒤 검사가 같은 값을 따르게 했어요.
- 새 명령: `npm run test:a11y`(접근성 검사), `npm run perf:measure`(성능 측정), `npm run build:offline`·`npm run test:offline`(오프라인판 만들기·확인), `npm run handouts:compare`(가린 편집본 원본 쪽 대조). 접근성 검사 도구 `@axe-core/playwright` 4.13.0(개발 도구).
- 마크다운 출력 다듬기 자리(`src/lib/rehype-lesson-polish.mjs`)와 플러그인 목록 한 곳(`src/lib/markdown-plugins.mjs`) — 빌드와 차시 틀 검사가 같은 출력을 봐요.
- 유지보수 문서: MAINTENANCE.md에 배포 확인, 빌드가 실패했을 때(원인별 실제 메시지), 판 올리기(Pyodide·MediaPipe·Blockly·MQTT.js·esptool-js·MicroPython 펌웨어·Astro), 도메인 연결, 판 번호 규칙, 연 1회 점검 목록, GitHub 웹 화면으로 올리는 법. CONTRIBUTING.md를 완성하고 이 CHANGELOG를 새로 만들었어요.
- 이슈 양식을 네 가지로 나눴어요: 실습실·사이트 오류, 차시 내용 오류, 새 예제·자료 제안, 개인정보·저작권 제보(점검 페이지 [결과 복사] 칸과 개인정보 주의 포함). 풀 리퀘스트 양식을 더했어요.
- 오프라인 배포판(`npm run build:offline`): 인터넷이 막힌 교실용 zip(약 68MB) — 풀어서 `시작하기.bat`를 누르면 Windows에 들어 있는 PowerShell 작은 서버가 이 컴퓨터 안에서만 사이트를 열어요. 첫 실습·가상 보드·같은 컴퓨터 탭 통신·차시·검색이 인터넷 없이 돼요(P6-07). 음성 예제는 오프라인판에서 글자 입력만 보여요.
- 성능 측정 검사(느린 3G에서 학습 페이지 첫 그리기 3초 안, 실습실 밖 쪽이 무거운 라이브러리를 받지 않는지)와 접근성 검사(모든 쪽 axe, 누른 뒤 화면, 키보드만·확대 200%·글자 200%·움직임 줄이기)를 더했어요.
- 출처와 라이선스(`/credits/`)에 "다시 나누는 파일에 꼭 함께 알리는 것"과 "고지 전문 파일 모음"(27개)을 더하고, 펌웨어·Pyodide 코어·휠·검색 엔진·MediaPipe 실행 파일 안 구성요소의 고지 전문 5개를 새로 실었어요. 가린 편집본 교안 두 개의 끝에 출처·라이선스 쪽을 붙였어요(블루투스 교안 92쪽, PyAutoGUI 슬라이드 24쪽).
- 보드 라이브러리 `buzzer.py` 사이트판과 교과서 157쪽 확인 예제 — 가상 보드에서 징글벨이 울려요.
- 4단원 통합 화면을 주소 하나로 짝지어 열기(`/labs/unit4/?pair=4-1-4` 등), 차시 예제마다 적은 난이도·낱말을 예제 갤러리 카드에.
- 대시보드 위젯을 끌지 않고 [설정]의 자리·크기 단추로도 옮겨요(WCAG 2.2 2.5.7).

**바뀐 것**

- 사이트 판 번호를 CHANGELOG와 맞춰 0.1.0에서 0.5.0으로 올렸어요(바닥글 "버전"에 "바뀐 점 기록" 링크).
- 느린 3G에서도 글이 빨리 보이게, 쪽마다 CSS를 HTML 안에 넣고 글꼴 CSS가 첫 그리기를 막지 않게 했어요(학습 페이지 첫 그리기 약 5초 → 약 2.3초 — 측정은 PROGRESS). 통신 모듈은 코드가 통신을 쓸 때만 받아요.
- 가상 RGB LED 색 이름을 켜진 비율로(`(255, 94, 0)` → 주황), 가상 MP3 곡이 긴 `sleep` 안에서도 곡 끝에 멈춰요, 가상 데스크톱 메모장 저장 창이 이름이 골라진 채 열려요(수업 슬라이드 코드 그대로).
- 퀴즈 18문항의 오답 보기를 정답과 비슷한 길이로 다듬었어요("정답이 가장 긴 보기" 사이트 문항 51 → 33).
- 저장소 검사가 MicroPython이 찍은 기기 주소 모양과 그림에 숨은 바이트까지 잡고, 작업 폴더·git 기록·빌드 결과를 손으로 훑는 선택이 생겼어요.

**고친 것(접근성)**

- 실습실 콘솔 이름, 휴대폰 폭에서 옆으로 넘치는 표·기록 칸을 키보드로 밀어 보기, 두 방향 표의 빈 머리 칸, 배우기 목록의 같은 영역 이름, 갤러리 결과 줄이 초점을 가리던 것, 블록 모드 초점 테두리·주석 글자 대비, 실행 전 빈 핀 표, 강제 색 모드의 "지금 위치".

**커밋:** `3f08878..` (Phase 6 병렬 제작 준비 `46afd3f`부터)

## 0.5.0 — 2026-09-26 — 교육과정 차시 45편(Phase 5)

교과서와 수업 자료를 사이트 차시로 옮겼어요.

**더한 것**

- 차시 45편: 교과서 31편, 보충 9편(V1~V5·P1·C1~C3), 읽기 자료 2편(2-1-R·IV-프로젝트), 대단원 마무리 3편. `/learn/`의 카드가 모두 차시로 열려요. 원고가 없는 12편은 예제 코드와 교안을 바탕으로 사이트가 새로 썼어요(교사용 칸에 저절로 표시).
- 차시 틀: 확인 퀴즈(3문항, 바로 채점), 교사용 접기 끝의 "이 차시의 원고와 자료"(저절로), 발표 모드(긴 단계는 저절로 나눠 "(이어서)"), 예제를 코드 읽기 바로 아래에 두는 `::예제[n]`, 긴 예제 발췌(`focus`), 접히는 `:::더알아보기`, `:::생성형AI` 상자.
- 차시 틀 검사 `npm run check:lessons`(엄격 모드 — CI "테스트" 워크플로의 한 단계, 빌드는 경고만).
- 원고 그림 추출 도구 `npm run images:show`·`images:extract`·`images:check` — 차시마다 따로인 그림 목록에 적은 그림만 메타데이터 없는 WebP로 꺼내고, 한 장씩 눈으로 확인한 기록을 남겨요. 스톡 그림은 쓰지 않아요(결정 C11).
- 교사용 자료실(`/teacher/`): 차시별 지도 요약, 성취기준과 평가 방향, 교과서 원고 정정 목록, 진짜 PC에서 돌리기, 선생님 FAQ, 개인정보를 가린 편집본 교안 PDF 두 개(쪽마다 눈 확인 기록 114쪽).
- 용어사전 87개.

**바뀐 것**

- 예제 코드(`examples/`)의 라이선스를 CC BY-NC-SA 4.0에서 MIT로 넓혔어요(운영자 결정 O13). `ESP32BLE.py`에 MIT 고지를 실었어요(결정 C12).
- 출판사 조판 삽화를 운영자 자료로(O10), 운영자·공동 저자 본인 얼굴이 나온 실행 화면과 교안 표지 학교명을 공개로(O9·O11) 바꿨어요.
- 키트 업체 제품 사진·그림 3장을 제3자 자료로 옮겼어요(권리 확인은 운영자 할 일 24번).

**고친 것**

- 두 번째 [실행]에 뜨던 영어 Traceback 알림, 블루투스 연결 뒤 꺼지던 가상 보드 상태 LED, 발표 도중 단계 수가 바뀌던 것, 차시 코드 칸을 복사하면 빈 줄이 사라지던 것.

**확인:** `npm run check:lessons -- --complete` 45편 통과(오류 0), 브라우저 테스트 568 통과·0 실패, 사이트 안 링크 8,023개 통과, CI 세 워크플로 성공.
**커밋:** `6296be5..3f08878`(58개)

## 0.4.0 — 2026-09-25 — 통신 실습실(Phase 4)

인공지능이 알아낸 결과를 통신으로 보드에 보내는 실습을 더했어요.

**더한 것**

- 통신 브릿지: 영상처리 실습실과 ESP32 실습실을 잇는 통로 — 같은 컴퓨터의 탭끼리(기본, 인터넷 없이), 블루투스, USB 시리얼, MQTT. 메시지 규칙(한 줄 글자, 바뀔 때만 초당 10회)을 모든 통로가 같게 써요.
- 가상 블루투스(BLE)·가상 와이파이·umqtt 흉내, 실제 기기용 Web Bluetooth와 USB 데이터 포트(두 번째 시리얼 포트).
- 통신 실습실 안내(`/labs/iot/`), 대시보드(`/labs/iot/dashboard/` — 그래프·게이지·스위치·기록), 4단원 통합 실습실(`/labs/unit4/`), 예제 갤러리(`/labs/gallery/` — 통신 방식·부품으로 거르기).
- 통신 템플릿 3종과 블록 모드 "통신" 칸, 오류 사전 통신 묶음 21항목, 통신 예제 30개 이관(기기 주소는 자리표시자로 가림).
- 공개 MQTT 중계 서버는 고를 때만 쓰고 "누구나 보고 보낼 수 있어요" 경고를 늘 보여요. 실제 보드는 무작위 접두어 없이 보내지 않아요(PD-29).

**고친 것(검토 반영 42건 가운데 40건)**

- 준비 중에 누른 [실행]이 보드 라이브러리보다 먼저 돌아 거짓 오류로 멈추던 것, 보낼 차례가 같은 값을 잘못 합쳐 메시지를 삼키던 것 등.

**확인:** 시나리오 D(대시보드)·F(손가락 수만큼 네오픽셀 — 카메라 없이 재생 입력) 통과, 브라우저 테스트 538 통과·0 실패.
**커밋:** `4f8d5fb..6296be5`(54개)

## 0.3.0 — 2026-09-18 — ESP32 실습실(Phase 3)

보드가 없어도 화면 속 가상 ESP32 보드로 피지컬 컴퓨팅을 끝까지 할 수 있게 했어요.

**더한 것**

- ESP32 실습실(`/labs/esp32/`)과 가상 보드: 교과서 키트와 같은 30핀 보드 그림, `machine.Pin`·`Timer`·PWM·ADC·I2C·UART·RTC 흉내, 가상 시계. 부품 15개(내장 LED, BOOT 버튼, 터치 센서, 진동 모터, RGB LED, 레이저, 버저, 서보, 팬 모터, 4채널 터치, 문자 LCD, OLED, 네오픽셀, USB-UART 변환기, MP3). 예제마다 배선도와 배선 검사.
- 블록 모드: 블록을 끌어 놓아 교과서와 같은 MicroPython 코드를 만들어요(태블릿용 블록 전용 호환 모드 포함).
- 실제 보드: [실제 보드] 탭으로 같은 코드를 USB 보드에 보내고, [보드에 저장]으로 전원만 넣어도 돌게 해요(Web Serial).
- 보드 준비 페이지(`/start/board/`): 연결 확인과 버튼 하나로 MicroPython 펌웨어 굽기(v1.29.0 — GPL 스텁은 싣지 않음, PD-38).
- 실물 점검 도우미(`/labs/esp32/check/`): 키트가 사이트 예제대로 움직이는지 항목 22개로 확인하고 [결과 복사].
- 교과서 2단원 예제 이관, 오류 사전 보드 묶음.

**고친 것(검토 반영)**

- 콘솔이 화면 밖일 때 "콘솔에 결과가 나왔어요" 알림, 긴 줄 접기, 배선 어긋남을 보드 그림 옆에, [보드에 저장] 전에 확인 창, 고칠 것이 없는 안내는 파란 상자로.

**확인:** 브라우저 테스트 393 통과·0 실패, 실사이트 시나리오 B(블록 → 코드 → 가상 LED) 통과.
**커밋:** `f6aa4dd..4f8d5fb`(61개)

## 0.2.0 — 2026-09-17 — 영상처리 실습실(Phase 2)

설치 없이 브라우저에서 파이썬(OpenCV)으로 웹캠 영상을 처리하는 실습실을 열었어요.

**더한 것**

- 파이썬 실행기: Pyodide 314.0.7을 브라우저 워커에서 돌리고, 실행 중 입력(카메라 프레임·키·조절 막대)을 받으며, [정지]로 1초 안에 멈춰요.
- 영상처리 실습실(`/labs/vision/`): 코드 편집기(CodeMirror 6), 자동 저장, 공유 링크, .py 내려받기, 웹캠·샘플·내 그림 입력, 출력 창, 코드 주석으로 만드는 조절 막대(`# @slider`).
- 손·얼굴·자세 인식 흉내(MediaPipe 0.10.35 — 모델은 같은 사이트)와 카메라 없이도 되는 합성 재생 입력, 가상 데스크톱(pyautogui), 음성 인식 흉내(기본은 글자 입력).
- 한국어 파이썬 오류 사전(`/help/errors/`)과 실습실 오류 풀이 카드.
- 느린 학교망 대비: 같은 사이트 Pyodide 예비본, 서비스 워커 캐시, 단계별 진행률과 1분 개념 카드, [이 컴퓨터에 실습 파일 미리 받기].
- 보충 차시 V1~V5, 교과서 예제 57개를 고치지 않고 이관(PD-33), 차시 안에서 여는 실습실.

**고친 것(검토 반영 26건)**

- 준비 중에 누른 [실행]을 예약하고, 실행하면 결과 창과 조절 막대로 화면을 옮기며, 오류가 나면 풀이 카드로 옮겨요. 서비스 워커가 파이썬 엔진 파일을 SHA-256으로도 대조해요.

**확인:** 실사이트 시나리오 A(홈 → 첫 에지 결과 → 조절 막대) 17.0초.
**커밋:** `387b41e..f6aa4dd`(53개)

## 0.1.0 — 2026-09-16 — 사이트 뼈대와 첫 배포(Phase 1)

**더한 것**

- Astro 7.3.2 정적 사이트와 GitHub Pages 배포(첫 배포 2026-09-15), 홈(큰 버튼 3개와 흐름 그림), 배우기 목록과 차시 틀, 용어사전과 풀이 말풍선, 시작하기 4쪽과 브라우저·학교 네트워크 점검, 사이트 검색(Pagefind), 기여·문의, 404.
- 출처 등록부(`sources.yaml`)와 출처 검사, 자동 출처 페이지(`/credits/`), 저장소 안전 검사(커밋 전 훅 + CI — 원본 형식·개인정보 모양·그림 눈 확인 기록).
- 라이선스(`LICENSE` MIT, `LICENSE-CONTENT.md` CC BY-NC-SA 4.0)와 안내 문서, 이슈 양식 3종, 배포와 테스트를 나눈 워크플로(PD-35).

**커밋:** `2db7b4f..387b41e`(26개)

## 계획 — 2026-09-15(Phase 0)

- 요구사항·자료 목록·코드 대응표·계획서(`docs/`)를 쓰고 공개 저장소에 첫 커밋을 올렸어요(`2db7b4f`). 사이트는 아직 없었어요.
