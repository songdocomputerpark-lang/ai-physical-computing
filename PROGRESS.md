# PROGRESS.md — 진행 상황

> 새 세션은 `CLAUDE.md` → `docs/SPEC.md` → `docs/DECISIONS.md` → 이 파일 → `docs/PLAN.md` 순서로 읽는다. 작업 묶음이 끝날 때마다 이 파일을 고친다.

마지막 갱신: 2026-09-18 13시 50분 KST (운영자 PC 로컬 세션, **Phase 4 병렬 제작 준비** — 통신 예제 30개 이관(BLE 주소 5곳 자리표시자로), 확장 자리(보드 이벤트 `board.uart.tx`·machine 확장 `ext/ble`·`ext/network`·오류 사전 `comm` 묶음·갤러리 태그 규약), MQTT.js 5.15.2 확인·등록·설치, 구역 표 `src/lab/README.md` 5.2. 검증: 단위 1,617건·`check` 0오류·빌드·링크 2,090개·새 예제 26개 브라우저 스모크 기대 일치. 그 앞은 **Phase 3 검토 반영 — 적대적 검토 3명의 지적 26건(치명 1·중요 8·사소 17)을 판단·반영**: 콘솔이 화면 밖일 때 결과 칸이 알리기(치명), 편집칸 줄 접기, `board.notice`로 배선 어긋남을 그림 옆에, 오류 사전 `level: notice`(파랑 안내 상자), [보드에 저장] 덮어쓰기 확인 창, 보드 그림 최소 폭 32rem·넘침 표시, 예제 한 줄 설명, 편집칸 `# @part` 다시 읽기, 초안 고지 빌드 차단·`repo-check.yml`, `repo-check.mjs` NUL 제거, 자르기 호환 오탐·조사·터치 막대·`dir(machine)`·부록 B-2 번호·오류 사전 항목 수. 검증: 빌드·`check` 0오류·단위 1,489건·링크 2,088개·**브라우저 393 통과·0 실패(32.7분)**, CI 테스트 35293745594·배포 35293745626·새 저장소 검사 35293745653 성공, 실사이트 시나리오 B 통과. 그 앞은 P3-03~P3-11 여섯 구역 통합 — CI 테스트 35274533190·배포 35274533239 성공)

## 한눈에 보기

- **지금:** **Phase 2 영상처리 실습실을 마쳤다(본체 2026-09-16, 검토 반영 2026-09-17).** P2-01~P2-14로 실습실을 만든 뒤, 적대적 검토 3명(완료 기준·안전·사용성)이 낸 **지적 26건(치명 3·중요 8·사소 15)을 근거를 확인해 모두 반영**했다. 이제 학생이 실습실에 들어가자마자 [실행]을 눌러도 버려지지 않고 "준비되면 실행돼요…"로 예약되며, 실행하면 결과 창과 조절 막대 쪽으로, 오류가 나면 한국어 풀이 카드 쪽으로 화면이 옮겨 간다. 파이썬을 받는 동안에는 진행률과 1분 개념 카드가 편집칸 위에 보이고, 손 인식·음성·파일 패널은 그 기능을 쓰는 예제에서만 열린다. 서비스 워커는 파이썬 엔진 파일을 크기뿐 아니라 SHA-256으로도 대조하고, 같은 사이트에서 다시 배포하는 numpy·OpenCV 휠의 고지를 모두 실었다. 교과서 3·4단원 PC 쪽 예제 8개(f090~f097)를 더 옮겨 **원본 예제 57개가 실습실에서 고치지 않고** 돈다(스모크 테스트). 실사이트 시나리오 A(홈 → 준비 중에 [실행] → 첫 에지 → 슬라이더)는 총 17.0초로 통과(준비 중에 [실행]을 눌러 예약 → 준비 4.6초 → 첫 에지 8.1초 → 슬라이더 효과 11.1초, 흰 픽셀 0.86% → 0.96% → 0.85%). 다음은 Phase 3 ESP32 실습실이다. **Phase 3 첫 묶음 P3-00(2026-09-17)에서 가상 보드 엔진을 Pyodide로 확정했다(PD-04 유지, 미해결 42)** — MicroPython 공식 WebAssembly 포트 v1.29.0은 모듈 워커에서 뜨지만 `time.sleep`이 바쁜 대기이고 `input()`·브라우저 인터럽트가 없어 실행 중 버튼 입력·[정지]를 받을 수 없다. **P3-01(2026-09-17)에서 ESP32 실습실을 열었다** — `/labs/esp32/`에서 `Pin(2, Pin.OUT).on()`을 [실행]하면 화면 속 가상 보드의 내장 LED가 켜지고 핀 표에 GPIO2 = 1이 나오며, BOOT 버튼을 마우스로 누르고 있거나 Tab 뒤 Space를 누르고 있으면 코드가 0을 읽는다. `time.sleep_ms`·`ticks_ms`는 가상 시계로 돌고, `Timer` 콜백은 코드가 끝나도 [정지]까지 돈다. 이 실습실은 파이썬 엔진만 받고(OpenCV·numpy 없음, 12.9MB), 새 부품은 폴더 하나로 더한다(결정 근거 미해결 43~50). **P3-02(2026-09-17)에서 보드 그림과 첫 부품을 더했다** — 가상 보드가 교과서 키트와 같은 30핀 개발 보드 모양(핀 번호·GND·3V3·5V, 스트래핑 핀 ▲)으로 그려지고, 코드가 쓰는 핀은 흰 고리·1이면 노랗게 빛난다. 터치 센서·진동 모터 같은 바깥 부품을 쓰는 예제를 고르면 보드 아래 브레드보드에 부품과 선(배선도)이 그려지고, 터치 센서는 마우스·손가락·키보드로 누르고 있으면 1, 진동 모터는 켜지면 떨린다(움직임 줄이기면 떨지 않고 "진동 중"). 배선이 실물에서 문제가 될 때(스트래핑 핀, 한 핀에 입력·출력 부품 등)와 코드가 배선과 다르게 핀을 쓸 때 한국어로 알린다. 교과서 예제 f046·f015·f053이 고치지 않고 돌고, f052는 문자 LCD가 생기는 P3-04 전까지 2번 줄에서 한국어 안내와 함께 멈춘다(미해결 52). 진동 알림 새 예제(2-2-1)와 [그림 크게 보기](휴대폰)도 있다. **병렬 제작 준비(2026-09-17 밤)를 마쳤다** — 다음 여섯 구역(A PWM·ADC 부품, B I2C 표시 장치, C 네오픽셀·UART·MP3, D 블록 모드, E 실제 보드 연결·실행·저장, F 펌웨어 굽기·보드 준비 페이지)이 서로 같은 파일을 고치지 않도록, 교과서 2단원 예제 36개를 미리 옮기고(지금은 대부분 "아직 준비 중" 안내로 멈추는 것이 기대 결과), 부품이 보드 핵심을 고치지 않고 쓸 자리(PWM 밝기·아날로그 값·LCD 같은 부품 상태·조작 칸·소리·보드 라이브러리)와 실제 보드가 같은 [실행] 단추를 받는 자리, 블록 모드의 태블릿용 기다리기 자리, 실제 보드 없이 연결·실행을 시험하는 모의 시리얼(가짜 USB 포트와 MicroPython 보드)을 만들어 브라우저(Edge)에서 확인했다. 펌웨어 굽기 도구의 GPL 스텁은 싣지 않기로 했다(PD-38).
- **Phase 3 통합(2026-09-18):** 여섯 구역(A PWM·ADC 부품, B I2C 표시 장치, C 네오픽셀·UART·MP3, D 블록 모드, E 실제 보드, F 펌웨어 굽기·보드 준비 페이지)이 만든 것을 한 자리에 합쳤다. 이제 `/labs/esp32/`에서 ① 가상 보드에 부품 15개(내장 LED·BOOT 버튼·터치·진동 모터·RGB LED·레이저·버저·서보·팬·4채널 터치·문자 LCD·OLED·네오픽셀·USB-UART 변환기·MP3)가 돌고 ② 코드 칸 위 [블록]으로 블록을 끌어 놓아 같은 코드를 만들고 ③ 입력·출력 칸 위 [실제 보드]로 USB 보드에 같은 코드를 보내고 [보드에 저장]으로 전원만 넣어도 돌게 하고 ④ `/start/board/`에서 펌웨어를 버튼 하나로 굽고 ⑤ `/labs/esp32/check/`에서 키트가 사이트 예제대로 움직이는지 항목 22개로 확인한다(선생님용, [결과 복사]). 이관 예제 97개가 모두 사이드카 기대대로 돈다(영상처리 57·ESP32 40, 문제 0).
- **Phase 3 검토 반영(2026-09-18):** 적대적 검토 3명이 낸 지적 **26건(치명 1·중요 8·사소 17)**을 근거를 확인해 판단하고 24건을 반영했다. 학생이 바로 느끼는 것: [실행]했는데 화면이 그대로여서 "안 된다"로 보이던 것을 결과 칸의 "콘솔에 결과가 나왔어요"(마지막 3줄 + [콘솔 보기 ↓])로 고쳤고, 편집칸이 긴 줄을 접어 안내 주석이 잘리지 않으며, 코드와 배선이 어긋나면 그 까닭이 보드 그림 옆에 뜨고, [정지]처럼 고칠 것이 없는 안내는 빨간 오류 카드 대신 파란 안내 상자가 된다. 선생님이 느끼는 것: [보드에 저장]이 보드의 `main.py`를 바꾸기 전에 물어보고, 점검 도우미에 "안전하게 쓰기" 칸이 생겼다. 남은 두 가지는 파일을 받아야 끝난다 — 펌웨어 고지 전문(운영자 할 일 19번)과 실물용 OLED 드라이버(미해결 64).
- **운영자 할 일:** 아래 표이며, 모두 작업을 막지 않는다. 지금 해 볼 수 있는 것은 1번(실제 웹캠으로 시나리오 A, 약 10분), 18번(음성 예제에서 [기기 안 인식 되는지 확인], 약 5분), 17번(MediaPipe 모델 파일 넣기, 10분), 3번(교실 컴퓨터에서 점검 페이지 열기), 14번(저장소 룰셋, 5분)이다. 16번(남은 미리 보기 서버)은 2026-09-17에 남은 서버가 없음을 확인해 끝냈다.
- **다음에 볼 수 있는 결과:** 홈의 [카메라로 바로 해보기] → 준비 중에 눌러 둔 [실행]이 저절로 돌고 결과·슬라이더로 화면이 옮겨 감, 오류가 나면 안내 줄과 풀이 카드, 예제 목록의 "3단원 교과서 실습(손으로 컴퓨터 조작)"·"4단원 프로젝트 실습(얼굴로 마우스 조작)", `/help/errors/`의 찾기 칸, 차시 V1~V5의 [이 자리에서 실습실 열기]와 실습실의 "이 예제가 나오는 차시" 링크, **`/labs/esp32/`의 가상 보드**(내장 LED·BOOT 버튼·핀 표, 예제 3개 — Timer 예제는 코드가 끝나도 [정지]까지 깜빡임). **Phase 3:** `/labs/esp32/`의 예제 목록(부품 15개·이관 예제 40개)과 코드 칸 위 [블록], 입력·출력 칸 위 [실제 보드] 탭, `/start/board/`의 [보드 연결]·[펌웨어 굽기 시작], `/labs/esp32/check/`의 항목 22개와 [결과 복사].

## 운영자 할 일 (유일한 목록)

다른 문서(PLAN §1.3·§12.1·부록 B-2, INVENTORY §10.8)는 이 표의 번호만 가리킨다. 답이 없으면 오른쪽 기본값으로 계속 진행한다.

| # | 언제 | 무엇을 (예/아니오로 답할 수 있게) | 어떻게 | 답이 없을 때 기본값 |
|---|---|---|---|---|
| 1 | 끝남(통과, 2026-09-17) | ~~운영자 PC 크롬에서 실제 웹캠으로 시나리오 A가 되나요?~~ **통과** — EShare 가상 카메라가 Chromium에서 실제 카메라를 가리고 있었고, 장치 관리자에서 그 가상 카메라를 "디바이스 사용 안 함"으로 바꾼 뒤 에지·슬라이더·q 키가 모두 됐다. 이 해결 방법을 `/help/`의 "카메라는 켜졌는데 화면이 까맣게만 보여요"와 `/start/teacher/` 체크리스트에 실었다(영상처리 실습실의 카메라 고르기·검은 영상 감지 코드는 다음 묶음 — 미해결 121) | 홈 → [카메라로 바로 해보기] → [실행] → 카메라 허용 → 출력 창에 테두리 영상이 보이는지, 오른쪽 아래 조절 패널의 threshold 슬라이더를 움직이면 테두리가 달라지는지, q 키·[정지]로 멈추는지 한 줄로 알려 주기(약 10분) | 가짜 카메라 자동 테스트(`tests/e2e/scenario-a.spec.ts`)까지만 "통과", 실물은 "확인 필요" |
| 2 | **지금 할 수 있음**(Phase 3 끝 약 4시간, 나눠도 됨 — 도우미 항목 22개가 약 2시간 12분 + 보드 준비·굽기 확인 약 1시간 15분), Phase 4(약 40분) | 키트 보드로 "실물 점검 도우미"의 항목이 모두 "예"인가요? | **`/labs/esp32/check/`가 P3-11에서 만들어졌다** — 보드를 꽂고 [보드 연결]을 누른 뒤 항목마다 [보드에 보내기] → 눈으로 보고 예/아니오 → 마지막에 [결과 복사]한 글을 이 표 아래에 붙여 주기. 배선이 필요한 항목 12개에는 배선 그림이 함께 보인다. 굽기·드라이버 확인(부록 B-2 24~30번)은 `/start/board/`에서 | 가상 보드·모의 시리얼 테스트까지만 통과, 핀·프레임·드라이버 사실은 "실물 확인 전" |
| 3 | Phase 1 뒤 | 교실 PC(가능하면 학교 태블릿 한 대 포함)에서 점검 페이지 결과가 모두 "지원"인가요? | `/start/check/`를 열고 [결과 복사](기기당 5~10분) | 안내문 기준. JSPI가 없는 기기는 제한 모드와 블록 전용 호환 모드(PD-27) |
| 4 | 나중에 | 얼굴이 나온 실행 캡처가 운영자·공동 저자 본인인가요? — 교과서 1단원 원고 029·034·035·037·040쪽, 3단원 원고 194·200·202·204·206쪽, 블루투스 교안 p9·p84·p91 | 쪽 번호별로 예/아니오 | 계속 공개하지 않음(손만 잘라 쓰거나 그림으로 대체) |
| 5 | 나중에 | 교과서가 인천광역시교육청 누리집 교육과정정보센터 > 교육감승인과목의 "인공지능과 피지컬 컴퓨팅"(2024.10.04. 등록) 교육과정을 따른 것이 맞나요? 원문 hwp와 대조하려고 그 첨부 파일을 내려받아도 되나요? | 예/아니오 두 개 | 맞는 것으로 보고 성취기준 15개와 차시 대응표 초안(PLAN §2.2)을 쓴다. 파일은 내려받지 않고 게시물 문서뷰어 내용 기준 |
| 6 | 나중에 | 추가 자료가 있나요? 평가·운영계획서, 1단원 03·04와 4단원 원고, 자료에 없는 라이브러리 원본(`servo_library.py`, PWM판 `gorillacell_dcmotors.py`, `ESP32BLE_LIB.py`, OLED 드라이버), II·III 대단원 마무리 정답 | 있으면 빈 폴더 `2022_인피컴_교수,학습운영계획` 등 원본 폴더에 넣고 알려 주기 | 코드와 원고 스크린숏 기준으로 새로 쓴다 |
| 7 | 나중에(클라우드 루틴을 다시 켜기 전에는 필요) | `docs/SPEC.md` 6행의 학교명·직위와 블루투스 교안 표지(p1)의 학교명을 공개 저장소·교사용 편집본에 올려도 되나요? | 예/아니오 | 공개하지 않는다: SPEC.md는 공개 커밋에서 빼고 로컬에만 두며, 편집본 p1은 가린다 |
| 8 | 나중에(P1-10 전이면 좋음) | 교과서·수업 자료에서 옮긴 예제 코드(`examples/`)를 MIT(상업적 이용도 허용)로 넓혀도 되나요? | 예/아니오 | CC BY-NC-SA 4.0(콘텐츠와 같은 조건). 넓히기는 나중에도 되지만 먼저 넓게 공개하면 되돌릴 수 없다. CC FAQ는 소프트웨어에 CC 라이선스를 권하지 않는다는 점도 참고 |
| 9 | P1-03에서 API로 Pages 켜기가 거절될 때만 | 저장소 Settings > Pages > Source에서 "GitHub Actions"를 한 번 골라 주시겠어요? | https://github.com/songdocomputerpark-lang/ai-physical-computing/settings/pages | 워크플로만 만들어 두고 첫 배포 확인은 보류, 나머지 Phase 1 작업은 계속 |
| 10 | 클라우드 야간 루틴을 다시 쓸 때만 | claude.ai/code에서 GitHub 연결을 해 주시겠어요? | claude.ai/code 설정(야간 루틴 저장 실패 HTTP 401의 원인) | 운영자 PC 로컬 세션으로 작업(O8) |
| 11 | 나중에 | 개인정보 제보를 공개 이슈 대신 비공개로 받을 연락 창구(예: 사이트 전용 메일)를 둘까요? | 예/아니오(예라면 받을 주소) | 두지 않는다: 이슈 양식과 기여·문의 페이지에 "개인정보는 옮겨 적지 말고 위치만" 안내(GitHub 비공개 보고 기능은 보안 취약점 신고용이라 쓰지 않음) |
| 12 | 편할 때 | 이 PC에 Playwright 전용 브라우저(Chromium)를 내려받아도 되나요? CI와 같은 브라우저로 로컬 테스트를 돌리게 돼요 | 예/아니오(예라면 받기 직전에 파일 크기를 알려 드림) | 설치된 Microsoft Edge로 로컬 테스트, CI(테스트 워크플로)는 Chromium |
| 13 | 나중에(Phase 5 원고 이미지 작업 전이면 좋음) | 교과서 원고를 출판사가 조판할 때 넣은 삽화(예: 1-1-1 차시의 인식·학습·추론 그림 3장, 원고 009쪽)의 저작권이 저자(운영자)에게 있나요? 그 삽화를 웹에 다시 올리고 CC BY-NC-SA 4.0으로 공개해도 출판 계약과 부딪히지 않나요? | 예/아니오 두 개 | 제3자 자료로 등록(사이트 라이선스 제외, `/credits/` 제3자 목록에 표시)해 두고, 문제 제기가 오면 사이트가 그린 그림(INVENTORY §5.3 S06)으로 바꿈 |
| 14 | 편할 때(5분) | 저장소 Settings > Rules > Rulesets에서 `main` 브랜치에 "강제 push 금지(Block force pushes)·삭제 금지(Restrict deletions)" 룰셋을 만들어 주시겠어요? Claude는 저장소 설정을 바꾸지 않아요 | https://github.com/songdocomputerpark-lang/ai-physical-computing/settings/rules | 규칙(PD-32, OVERNIGHT §3 기록 재작성 금지)으로만 지킴 |
| 15 | 나중에 | GitHub 계정 이름(`songdocomputerpark-lang`)에 지역 이름과 성이 들어 있어 Pages 주소·커밋 작성자에 그대로 보여요. 이대로 두어도 되나요? 바꾸려면 Pages 주소가 함께 바뀌어 Phase 1 직후가 가장 싸요 | 예(그대로)/아니오(새 이름) | 그대로 둠(DECISIONS C10: PD-37의 비공개 대상은 학교 전체 이름·직위·학교 자료 캡처) |
| 16 | 끝남(2026-09-17 확인) | ~~병렬 제작 때 남은 미리 보기 서버 3개(포트 4330·4336·4339)를 띄운 터미널 창을 닫아 주시겠어요?~~ | 2026-09-17 세션 시작 때 노드 프로세스가 하나도 없고 4300~4600번 포트에서 듣는 서버가 없음을 확인했어요(컴퓨터를 다시 켠 것으로 보여요) | 할 일 없음 |
| 17 | 끝남(완료, 2026-09-18 배치) | ~~MediaPipe 모델 파일 5개를 `public/models/`에 내려받아 두시겠어요?~~ **완료** — 운영자가 2026-09-17에 공식 주소에서 받아 MD5를 검증했고, P3-11 통합에서 `public/models/`로 옮겨 크기·MD5를 다시 대조했다(hand 7,819,105·face 3,758,596·pose_full 9,398,198·pose_lite 5,777,746·blaze_face 229,746바이트, 모두 일치). 옛 물음 그래야 실제 웹캠에서 손·얼굴·자세를 찾아요(Claude는 파일 내려받기를 하지 않아요 — 위임의 예외) | 받는 곳·크기·MD5·PowerShell 명령은 `.cache/phase2-requests/mediapipe.md` 1번. 5MB 넘는 네 개는 `scripts/repo-allowlist.yaml`에 이미 적어 두었으니 파일만 넣으면 커밋돼요 | 모델 없이 간다: 실습실이 "모델 없음"을 한국어로 알리고 합성 재생 입력(카메라 없이도 되는 손·얼굴·자세 좌표)으로 실습을 이어요 |
| 18 | 끝남(확인, 2026-09-17) | ~~음성 예제의 [기기 안 인식 되는지 확인]을 누르면 무엇이 나오나요?~~ **확인함** — 운영자 PC 크롬에서 "내 기기 안 인식: 안 됨"이었다. 교실 기본은 글자 입력이고, 마이크 실습은 교사가 `/settings/`에서 서버 인식을 켠 브라우저에서만 선택지가 생긴다(미해결 38에 실측 기록). 옛 물음 "가능"이면 방식을 "기기 안 인식"으로 바꿔 한 마디가 글자로 바뀌나요? | 영상처리 실습실 → 예제 "1-4-3 기본 실습: 말을 글로 바꾸기" → 음성 패널의 [기기 안 인식 되는지 확인] → 결과 한 줄(가능/내려받기 필요/안 됨)을 알려 주기. 가능하면 방식 "기기 안 인식" → [실행] → 말하기 → 콘솔에 "인식된 내용:"이 나오는지 | 글자 입력 방식만 "통과". 기기 안 인식·실제 마이크는 "확인 필요"(Edge 153 자동 확인은 `unavailable`, CI의 Chromium은 확인 단추를 누르면 탭이 닫힘 — 미해결 38) |
| 19 | **지금 할 수 있음**(10분, 파일 3개 내려받기) | 펌웨어 고지에 넣을 공식 라이선스 문서를 받아 주시겠어요? — ① MicroPython v1.29.0 `LICENSE`(https://raw.githubusercontent.com/micropython/micropython/v1.29.0/LICENSE) ② 같은 태그의 `lib/berkeley-db-1.xx/LICENSE`(BSD-4-Clause, 광고 조항이 있어 전문이 꼭 필요) ③ ESP-IDF v5.5.2 `docs/en/COPYRIGHT.rst`(https://raw.githubusercontent.com/espressif/esp-idf/v5.5.2/docs/en/COPYRIGHT.rst) | 받은 파일을 `.cache/notice-staging/`에 두고 알려 주시면, 그 글로 `public/firmware/v1.29.0/NOTICE.txt`의 2·3절 고지 전문을 채워요(파일 내려받기는 Claude가 하지 않아요). 급하지 않아요 — 지금 고지에도 어떤 구성요소가 어떤 라이선스인지와 공식 주소는 모두 적혀 있어요 | 지금 고지(구성요소 목록 + 공식 링크) 그대로 두고, 미해결 125번에 남겨 둔다 |

## 현재 Phase

**Phase 3 ESP32 실습실 — P3-00~P3-11을 마치고, 적대적 검토까지 반영했다(2026-09-17 시작 → 2026-09-18 검토 반영).** P3-00에서 엔진을 Pyodide로 확정(PD-04 유지), P3-01 가상 보드 핵심, P3-02 보드 그림·첫 부품, 병렬 제작 준비, 여섯 구역(P3-03~P3-10)과 통합(P3-11)이 끝났고, **검토 3명(완료 기준 재현·초보자 사용성·안전/저작권)이 낸 26건(치명 1·중요 8·사소 17)을 근거를 확인해 모두 판단·반영**했다. **자동으로 확인할 수 있는 것은 모두 통과했다** — 시나리오 B(블록 끌어 놓기 → 실행 → 가상 터치 → LED → 코드 한 줄 수정)가 로컬·실사이트에서 통과한다. **실물 보드와의 비교는 모두 "확인 필요"다**(시나리오 C 포함) — 모의 시리얼·가상 보드에서 되는 것은 실물의 증거가 아니다(부록 B-2 4~6·9~30번 = 운영자 할 일 2번, `/labs/esp32/check/` 도우미가 안내한다). 다음은 Phase 4(통신 실습실, PLAN §8.4).

| 묶음(Phase 3) | 판정 | 증거 |
|---|---|---|
| P3-00 (실험) MicroPython WASM 포트 | 완료(2026-09-17, 소스 판독·Pyodide 실측으로 판정) | PD-04 유지 — 근거는 미해결 42, 비교·다시 볼 조건·가상 보드가 지킬 차이 표 14항목은 PLAN §8.3 P3-00 구현 메모 |
| P3-01 가상 보드 핵심 | 완료(2026-09-17, 실물 보드 비교는 부록 B-2 16번 — 확인 필요) | **흉내 모듈 단위 테스트 통과(완료 기준):** `tests/unit/lab/pyodide-board.test.ts` 19건 — Node 24.19.0의 실제 Pyodide 314.0.7 + `--experimental-wasm-jspi`로 ESP32 실습실 워커와 같은 파일·순서(install_available → reset_for_run → 코드 → run_idle)를 돌려 Pin 모드·값·repr·오류 문구(`invalid pin`·`pin can only be input`), 실행 전·중 입력과 풀업, sleep 없는 반복문의 입력(f015 모양), 핀 인터럽트, time 이름·ticks 넘침·`ticks_add` 한계·epoch 2000·`mktime`, 가상 시계(`sleep_ms(1)` 200번 = 가상 200ms 이상이면서 실제 2초 안), Timer 주기·ONE_SHOT·오류, 콜백 오류 뒤 계속, 코드 끝난 뒤 대기와 [정지], `const`·u-이름·errno·bluetooth 자리·`schedule` 대기열, CPython time 그대로, 핀 안내 5종, `board.state` 순서, 동기 진입점, 제한 모드를 확인했다. 그 밖에 `board-state`·`board-parts`·`python-modules`·`esp32-examples`·`runtime-client`(labId)·`pyodide-files`(패키지별 파일)·`explain`(board 풀이) 단위 테스트. **브라우저(Edge 153, 빌드 결과, 데스크톱 1366×768·모바일 375×812):** `tests/e2e/lab-esp32.spec.ts` — `/labs/esp32/`에서 `Pin(2, Pin.OUT).on()` [실행] → 상태 메시지 끝(end)·seq 2 이상, 내장 LED `data-visual-lit="true"`, 핀 표 GPIO2 "1 (HIGH)"·출력·"내장 LED"; 준비 단계가 끝난 뒤 opencv·numpy 요청 0건·허용 밖 주소 0건; BOOT 버튼을 마우스로 누른 채·Tab 뒤 Space를 누른 채면 코드가 0을 읽고 LED가 따라 켜짐, [정지] → 꺼진 모습; Timer 예제가 코드 끝난 뒤 phase idle로 LED를 3번 넘게 바꾸고 [정지]로 멈춤; 375px 가로 넘침 없음; dev 실습실에는 `machine`·`sleep_ms` 없음. 눈으로 본 화면(스크린샷): 보드 그림·켜진 LED·눌린 BOOT·초점 테두리·준비 패널 "준비 끝 · 12.9MB". **전체 검증(2026-09-17 운영자 PC):** Vitest 77파일 896건, `astro check` 0오류(339파일), 빌드(출처 검사 파일 291개·항목 24개, 번들 npm 15개, 보드 청크 17KB), 링크 검사 2,056개, 예제 대조 57/57, Playwright 전체 304 통과·140 건너뜀(프로젝트별로 나눈 검사)·실패 0(23.5분, `--workers=2`). **CI:** 첫 push(`30f9dda`)는 배포 35194558621 성공, 테스트 35194558689는 단위 890 통과·6 건너뜀, 브라우저 303 통과·140 건너뜀·**1 실패**(`module-hello` 모바일, 재시도도 실패 — P3-01과 무관한 Phase 2 검사의 시간 경쟁이며 P3-00 문서 push의 테스트 35187309907에서도 같은 실패). **재확인(2026-09-17 18시, 저장하고 정지 뒤):** `npm run build` 통과, `astro check` 0오류(339파일), Vitest 77파일 896건 통과(`pyodide-board` 19건 포함), 빌드 결과에 대고 Playwright `lab-esp32`·`lab-runtime`·`lab-loading`·`pages` 76 통과·56 건너뜀·실패 0, **실사이트** `lab-esp32` 데스크톱 4건 통과(내장 LED·BOOT 버튼·Timer 대기·dev 실습실 구분), 화면 캡처 눈 확인(LED "IO2 켜짐"·핀 표 GPIO2 출력 1 (HIGH) 내장 LED). `module-hello` 실패는 로컬 모바일에서 화면 CPU를 10배 느리게 하면 3번에 1번 재현(`after []`, 단추 누르기까지 566ms)했고, 파이썬이 누르기를 받을 때까지 기다리게 고친 뒤 10배·20배 느리게 8번 모두 통과, 보통 속도 데스크톱·모바일 5번씩 20건 통과. 그리고 **어떤 검사에서도 실행되지 않던 사이트 예제 01(첫 화면 예제 — 깜빡이기·interval 막대)·02(BOOT 버튼으로 LED)**를 `lab-esp32.spec.ts`에 더했다(01: LED 켜짐·꺼짐·켜짐, 막대 0.5 → 0.1이면 1.5초 동안 상태 메시지가 3번 → 11~14번, 화면 CPU 8배 느리게도 통과 / 02: 누르는 동안 GPIO0 = 0·LED 켜짐, 떼면 꺼짐). **CI 통과:** 고친 push(`465c208`) 테스트 35203248675 성공(단위 890 통과·6 건너뜀, 브라우저 304 통과·140 건너뜀·실패 0·flaky 0, 14.0분, 예제 스모크 영상처리 57개 문제 0, 시나리오 A 18.3초, 링크 2,056개)·배포 35203248690 성공, 예제 검사를 더한 push(`471fef4`) 테스트 35204663018 성공(브라우저 305 통과·141 건너뜀·실패 0·flaky 0, 13.9분, 시나리오 A 18.0초)·배포 35204663013 성공 |
| P3-02 보드 그림과 첫 부품 | 완료(2026-09-17, 실물 확인은 부록 B-2 10·12·13번 — 확인 필요) | **완료 기준 "f046, f015, f052, f053과 진동 알림 새 예제가 돈다"(Playwright — LED 켜짐·버튼 누름·진동 표시):** `tests/e2e/lab-esp32-parts.spec.ts` 10건(데스크톱 8·모바일 2 — 개발 서버 Edge 153에서 첫 실행 모두 통과, 빌드 미리 보기 전체 실행에서도 통과) — f046 사이드카 실습 방법 3단계가 보드 그림 위에 보임, 파일 그대로 [실행] → 내장 LED `data-visual-lit`와 GPIO2 핀 머리 `data-high`가 꺼짐·켜짐·꺼짐·켜짐(바뀌는 차례를 페이지 안에서 모아 확인), [정지] → 꺼짐, 콘솔 안내 0줄, 바깥 부품이 없어 브레드보드 없음·"보드에 붙은 부품만" 안내 / f015(기다리는 줄 없는 반복문) BOOT 버튼 Enter 누르고 있기·마우스 → LED 켜짐·꺼짐 / f053 배선도(터치 센서 부품·GPIO17 신호선·GND·3V3 선, 핀 머리 `data-wired`) → 마우스·Space 누르고 있는 동안 콘솔 `Touch value: 1`·핀 머리 빛남, 모바일은 CDP 터치 화면 입력(`touchStart`·`touchEnd`)으로 같은 결과 / f052 배선 목록 "주의: … 문자 LCD(16×2) … 아직 없어서" → [실행] 결과 error·콘솔 `machine.SoftI2C은(는) 가상 보드에 아직 없어요`(P3-04 전 기대 결과, 미해결 52) / 진동 알림 새 예제 → 사이트 배정 핀 "참고" 안내·`buzz_time` 막대, 터치 → `알림!` 한 번·진동 모터 `data-visual-on` true(`motion` shake, 이름 "진동 모터(GPIO19): 진동 중")·GPIO19 빛남 → 바뀌는 차례가 정확히 멈춤·진동·멈춤·진동·멈춤(두 번 떨고 멈춤) / 움직임 줄이기 → `motion` still·애니메이션 0개·"진동 중" 글 / 배선 오류 화면 검사(`page.route`로 예제 JSON의 배선만 틀리게) → "오류: GPIO5에 값을 보내는 부품(터치 센서)과 보드가 움직이는 부품(진동 모터)…"·"34~39번은 입력 전용…"·"주의: GPIO5은(는) … 스트래핑 핀…"·아직 없는 부품, 오류가 주의보다 먼저 / 파이썬 검사 → 콘솔 "17번 핀에는 터치 센서(값을 보내는 부품)… 출력(Pin.OUT)으로 정했어요"·"19번 핀에는 진동 모터… 입력(Pin.IN)으로 정했어요"·"출력으로 정하지 않아서 … 신호가 나가지 않아요"·"18번 핀을 출력으로 정했는데, 이 예제의 배선도에는 … 부품이 없어요" / 375px [그림 크게 보기] → 그림 폭 1.8배 넘게·그림 칸만 가로 스크롤·페이지 넘침 0·새로고침 뒤 기억. 기존 `lab-esp32.spec.ts` 8건도 개발 서버에서 통과, 예제 스모크 ESP32 4개 문제 0(`SMOKE_ONLY=f046,f015,f052,f053`). **눈으로 본 화면(Playwright 2~3배 캡처를 Read):** 데스크톱 첫 예제 보드만, 진동 알림 배선도(선 엇갈림 없음·레일 동그라미), 터치 누름·진동 중·핀 빛남, 두 겹 초점 테두리(브레드보드 위에서도 보임), f052 주의 상자, 휴대폰 맞춤·크게 보기, 보드 그림 위 "이 예제 실습 방법" 상자(데스크톱·휴대폰, 빌드 미리 보기 — 줄바꿈·넘침 0) — 크게 보기 CSS가 전역 `svg { max-width: 100% }`에 막혀 커지지 않던 것을 캡처로 발견해 고침. **전체 브라우저 테스트(빌드 → 미리 보기, 로컬 Edge, `--workers=2`):** 마무리 코드 316건 통과·148건 건너뜀(한쪽 화면 크기에서만 보는 검사)·실패 0(22.3분) — 실습 방법 상자를 더하기 전 빌드에서도 같은 316건 통과(23.0분). **CI:** 코드 push(`ca7a84a`) 배포 35215144501 성공(같은 push의 테스트 35215144491은 곧이어 올린 설명 고침 push에 밀려 취소 — 워크플로 `concurrency`), 설명 고침 push(`66c86b8`) 테스트 35215361881 성공(단위 923 통과·6 건너뜀, 타입 검사 0오류, 브라우저 316 통과·148 건너뜀·실패 0·flaky 0, 14.9분, 예제 스모크 ESP32 4개·영상처리 57개 문제 0, 시나리오 A 18.7초, 링크 2,057개)·배포 35215361903 성공. **실사이트(배포 뒤, Edge):** 진동 알림 예제 주소로 열기 → 핀 머리 30·▲ 5·브레드보드·선 8개(신호 2·보드 전원 2·부품 전원 다리 4)·실습 방법 3단계·사이트 배정 참고 안내, [실행] → 터치 센서 누름 → 진동 모터 "진동 중"·GPIO19 핀 머리 빛남·콘솔 `알림!`, 페이지 오류 0. **단위:** Vitest 83파일 929건(부품별 `board-part-<id>` 4파일, `board-layout`(핀 머리 표·선 겹침 0·엇갈림 0)·`board-wiring-spec`·배선 검사 14종·`esp32-examples`(배선 우선순위·이관 예제 사이드카)·`pyodide-board` 배선 안내 단계(실제 Pyodide)·사이드카·머리말·스키마·이관 도구), `astro check` 0오류(352파일, 힌트 4개는 이번에 고치지 않은 파일), `npm run build`(출처 검사 파일 300개·항목 24개, 번들 npm 15개, 보드 청크 38.0KB), 링크 검사 2,057개, 예제 대조 61/61 |
| 병렬 제작 준비(P3-03~P3-10 여섯 구역 앞) | 완료(2026-09-17 밤, 브라우저 확인은 Edge 153 — 블록 호환 대기의 브라우저·태블릿 실측은 P3-06, 실물 보드는 확인 필요) | **예제 이관:** f047~f051·f054~f081·f001·f007·f144 36개(`examples:verify` 97/97), ESP32 예제 스모크 40개가 사이드카 기대와 모두 맞음(개발 서버·빌드 결과 모두). **설치·등록:** Blockly 13.3.0·esptool-js 0.6.1(스텁 제외 PD-38 — 진짜 Vite 빌드로 스텁 글자 없음 확인 `tests/unit/esptool-stub-guard.test.ts`)·pako·tslib·atob-lite·@types/w3c-web-serial·esbuild, 출처 검사 통과(파일 393개·항목 31개·배포용 npm 14개), 개발 서버에서 Blockly `text_print` → `print('안녕')`·한국어 메시지와 esptool-js import를 Edge로 확인. **확장 자리:** Node 실제 Pyodide `pyodide-board-extension-points`(PWM·아날로그·부품 장치·아직 없는 모듈·/board/lib)·`pyodide-board-async-wait`(제한 모드 await 대기·입력·Timer·정지), `board-state`·`board-parts`·`board-audio`·`board-libraries`, 브라우저 `lab-esp32-extension-points.spec.ts`(PWM 밝기 40·핀 표 "PWM 출력"·board.device·한국어 ModuleNotFoundError), `lab-run-target.spec.ts`(실행 대상 — 실행·입력줄·정지·오류·떼기). **모의 시리얼:** `tests/unit/serial/` 53건(raw REPL·raw-paste 흐름 제어·배너·Ctrl-C·리셋·input·파일·자동 리셋 회로·명세 오류·선 뽑기·필터), `serial-mock.spec.ts`(Edge: 클릭 없는 requestPort SecurityError, 클릭 안의 CH340 선택, raw-paste 한글 출력, 선 뽑기 NetworkError·disconnect target, getPorts·다운로드 모드 플러그인). **개발 서버 여럿:** `--ignore-lock` 4500·4501 나란히 + spec 동시 통과, 잠금 서버가 있으면 다음 서버 멈춤(README 5.1). **전체 검증(2026-09-17 23시, 운영자 PC):** Vitest 92파일 1,011건, `astro check` 0오류(386파일), 빌드(33쪽, 서비스 워커 셸 128KB), 링크 검사 2,062개, 저장소 검사 통과, Playwright 전체(Edge 153, `--workers=2`) 320 통과·152 건너뜀·실패 0(23.2분 — 예제 스모크 영상처리 57·ESP32 40 문제 0). **커밋·CI:** `5c2dc53` `d07447c` `6e7a021` `d603a49` `fd86632` `b386a25` `3d6ed1a` → CI 테스트 35232487305 성공(Vitest 1,005 통과·6 건너뜀, astro check 0오류, Playwright 전용 Chromium 320 통과·152 건너뜀·실패 0·flaky 0, 15.6분, 링크 검사), 배포 35232487343 성공, 실사이트 `/labs/esp32/`에 새 예제 묶음(블루투스 통신 수업교안 실습)·`/licenses/blockly.txt` 200 확인 |
| P3-03 PWM·ADC 부품(구역 A) | 완료(2026-09-18, 실물 확인은 부록 B-2 6·10·12·14번 — 확인 필요) | `machine.PWM`·`machine.ADC`를 MicroPython v1.29.0 소스와 같은 범위·기본값·오류 문구로 흉내(해상도 되계산 `freq(1000)` → 998, LEDC 채널 16·타이머 8, 34~39번 `OSError (-258)`). 부품 6개(RGB LED·레이저·버저(Web Audio)·서보(프로필 2종 PD-15)·팬 모터(진리표·속도)·4채널 터치(조작 칸)). `servo_library.py` 원고 174쪽 복원. 사이드카 14개에 배선·실습 방법·실제 smoke, 사이트판 3개(f058·f059 판정 구간, f062 g 핀). **단위:** `tests/unit/board-pwm/`·부품 6개·`pyodide-board-pwm-adc.test.ts` 19건(Node 실제 Pyodide — duty·주파수·오류·채널·타이머, ADC 값·감쇠·해상도, 원본 f059·f060·f068·f073·f078·f080과 사이트판). **브라우저:** `lab-esp32-pwm-adc.spec.ts` 8건(데스크톱 7·휴대폰 1) |
| P3-04 I2C 표시 장치(구역 B) | 완료(2026-09-18, 실물 확인은 부록 B-2 9번과 LCD ROM·OLED 칩·EIO/ENODEV — 확인 필요) | `machine.SoftI2C`·`I2C`(가상 버스, 대답 없으면 `OSError: [Errno 19] ENODEV` + 한국어 까닭)·`RTC`. 문자 LCD는 PCF8574 → HD44780 바이트 해석이라 원본 `i2c_lcd.py`가 그대로 돌고 19자 줄 넘김까지 같다(글자 칸은 DOM `<tspan>`). OLED는 `framebuf`·`ssd1306`·`sh1106` 흉내 + SSD1306 명령 해석(두 이름 한 흉내, 사이트 5×8 글꼴). 사이트판 `i2c_lcd.py`(MIT 고지·`move_to` 별칭)와 사이드카 11개. **단위:** `tests/unit/board-i2c/` 15파일 127건(실제 Pyodide 버스·LCD 12·OLED 9·RTC 4). **브라우저:** `esp32-i2c.spec.ts` 6건(CDP 접근성 트리로 낭독기 이름·설명까지) |
| P3-05 네오픽셀·UART·MP3·콘솔 입력(구역 C) | 완료(2026-09-18, 실물 확인은 부록 B-2 11·12번과 DFPlayer 프레임·GRB — 확인 필요) | 네오픽셀(micropython-lib와 같은 동작 + `machine.bitstream`), UART 전체 흉내와 USB-UART 변환기·컴퓨터 시리얼 창(속도가 다르면 `reframe`이 실물처럼 깨뜨림, `ESP_FAIL` 문구를 원문대로 고침), MP3 모듈(DFPlayer 8·10바이트 프레임, 자체 작곡 3곡 합성 — PD-16), 보드 콘솔 `input()`(기다리는 동안 Timer·UART가 돈다 — P3-11에서 `lab.prompt`로 켜짐), `gorillacell_dcmotors.py` 원고 169쪽 PWM판 복원, 사이드카 12개. **단위:** `tests/unit/board-uart/` 11파일 73건(실제 Pyodide 5). **브라우저:** `esp32-uart.spec.ts` 8건 |
| P3-06 Blockly 블록 모드(구역 D) | 완료(2026-09-18, WebKit·iPad·Android와 실물 보드 실행은 확인 필요) | [블록]·[코드] 전환(누를 때만 Blockly `import()`), 한국어 블록(LED·BOOT·터치·진동·RGB·레이저·버저·서보·팬·LCD·OLED·네오픽셀·MP3·반복·조건·대기)과 도구 상자 10칸, 교과서와 같은 MicroPython 생성기, 블록 = 코드(편집칸 읽기 전용·[코드로 바꾸기]), **PD-27 블록 전용 호환 모드**(줄 수가 같은 await 실행판 — Node 실제 Pyodide 두 판과 Edge `?limited=1`에서 [정지] 62ms), 배선 알림. **단위:** `tests/unit/blocks/` 45건. **브라우저:** `esp32-blocks.spec.ts` 10건 + **시나리오 B**(아래) |
| P3-07·P3-08 실제 보드 연결·실행·저장(구역 E) | 완료(2026-09-18, **실물 보드는 아무것도 확인하지 않음** — 부록 B-2 17~23번) | Web Serial 통로·배너 판별 7가지·raw REPL/raw-paste·[정지] Ctrl-C 되풀이, [가상 보드]/[실제 보드] 탭과 `lab.setRunTarget`, [보드에 저장](mpremote `fs_writefile` 방식·SHA256 건너뜀·보드 뿌리), 실행 중 `input()`(영어·숫자·기호 + `\r`, 되울림 한 번 걸러 냄), boot.py 반복 되찾기·[보드 되찾기] 3단계·[boot.py 끄기], 실물에서 안 되는 코드 모양 안내 6가지. **단위:** `tests/unit/serial/` 184건(구역 E 131). **브라우저:** `esp32-real-board.spec.ts` 6건 + `esp32-real-board-run.spec.ts` 5건(가짜 `navigator.serial`) |
| P3-09 펌웨어 굽기(구역 F) | 완료(2026-09-18, **실물 굽기는 확인 필요** — 부록 B-2 24~26번) | esptool-js 0.6.1을 ROM 부트로더로만(PD-38 — 모자란 명령 4곳은 사이트가 채움: 8바이트 SPI_ATTACH·0x400 블록·ROM ASCII MD5·RTS 리셋), 원클릭 흐름(포트 선택 + 파일 받기 동시 → 칩 확인 → 크기·ESP32 머리·SHA-256 → (선택) 전체 지우기 → 압축 쓰기 → MD5 대조 → 재시작), 단계 7개·진행률·남은 시간, 한국어 오류 18가지와 [느린 속도로 다시 굽기], 파일 없을 때 선생님용 수동 방법. **P3-11에서 펌웨어 파일을 넣었다**(운영자 검증본 1,790,544바이트·SHA-256 e67ad601…95f6 → `public/firmware/v1.29.0/`, `sources.yaml`·NOTICE). **단위:** `tests/unit/firmware/` 74건 + Container 2건(실제 파일로 굽기까지). **브라우저:** `esp32-firmware.spec.ts` 13건(모의 ROM 부트로더) |
| P3-10 보드 준비 페이지(구역 F) | 완료(2026-09-18, 실물·드라이버·Linux는 확인 필요 — 부록 B-2 27~30번) | 4단계 흐름(준비물·케이블 → [보드 연결] → 연결이 안 될 때 → 펌웨어 굽기 → 첫 예제 2-1-1)과 결과 글 13가지, 장치 관리자 그림 3장면(직접 그린 SVG), 드라이버 공식 링크·자동 설치 근거·전산 담당 부탁 글[복사]·하드웨어 ID, Linux dialout·brltty·snap `raw-usb`, 같은 페이지 포트 넘기기(`apc:serial-port-release`), 키트 부품 표 이동(진동 모터 GPIO19). **단위:** `tests/unit/firmware/start-board-*` + Container. **브라우저:** `start-board.spec.ts` 9건 — **시나리오 C 흐름 전체**(펌웨어 없는 모의 보드 → 굽기 → 다시 연결·LED 세 번 → 첫 예제 → 실습실 [실제 보드] [실행] → GPIO2가 1·0을 오감 → [정지])가 한 검사로 이어진다 |
| P3-11 통합·시나리오 B·실물 점검 도우미 | 완료(2026-09-18) | **시나리오 B 통과**(`scenario-b.spec.ts`, 개발 서버·Edge 153): 블록 5개를 실제 마우스로 끌어 놓아 "터치 센서를 누르면 LED" 완성 → 생성 코드 정확 → [실행] → 가상 터치 누름 → LED 켜짐 → 떼면 꺼짐 → [코드로 바꾸기] → 한 줄(`== 1` → `== 0`) 고치면 바로 반대로 동작. **걸린 시간: 데스크톱 실습실 준비 3.8초 → 블록 모드 4.4초 → 블록 5개 완성 9.2초 → 실행 9.6초 → LED 켜짐 9.7초 → 코드로 바꾸기 10.2초 → 고친 코드 반영 10.6초**(휴대폰 4.5 → 5.0 → 9.9 → 10.2 → 10.4 → 10.8 → 11.2초), BOOT 버튼판("거꾸로 동작하는 버튼")도 통과. **실물 점검 도우미 `/labs/esp32/check/`:** 부록 B-2 Phase 3 항목을 화면 항목 22개로(예상 132분·추정), 배선이 있는 항목 12개는 가상 보드 그림을 꺼진 모습으로 한 장, [보드에 보내기]가 실물에 raw REPL로 코드를 보내고 눈으로 본 것을 예/아니오로 답하면 [결과 복사]가 PROGRESS에 붙일 표를 만든다. `esp32-board-check.spec.ts` 4건 — **모의 포트로 항목 22개를 끝까지**(코드가 모두 보드에서 돌고, 라이브러리가 필요한 항목은 보드에 파일을 먼저 올리고, `input()` 항목은 입력줄로 영어만 가고 한글은 빠지며, 답이 새로고침 뒤에도 남고 [기록 지우기]로 지워짐). **전체 검증(2026-09-18 05시, 운영자 PC):** `npm run build` 통과, `npm run check` 0오류(566파일), Vitest 147파일 1,485건 + Container 7건, 링크 2,088개, 저장소 검사 1,029개, `examples:verify` 97/97, **브라우저 전체 390 통과·222 건너뜀·실패 0·flaky 0(31.7분, Edge 153, `--workers=2`)** — 예제 스모크 97개 문제 0, 시나리오 A 28.0초, 첫 방문 Fast 3G 19.91MB. 처음 실행에서 7건이 실패해 모두 원인을 찾아 고쳤다(작업 로그의 "실패와 원인" 칸 — 그 가운데 하나는 제품 결함이었다: 실습실 카드 사전이 트레이스백 조건을 잃어버려 학생의 일반 IndexError에 네오픽셀 풀이가 붙던 것, 미해결 122). **공유 요청 반영 목록은 아래 작업 로그** |
| 검토 반영(P3-11 뒤) | 완료(2026-09-18) | 적대적 검토 3명의 지적 **26건(치명 1·중요 8·사소 17)**을 하나씩 근거를 확인해 판단했다. 24건은 그대로 또는 더 나은 방법으로 고쳤고, 2건(펌웨어 고지 전문·실물용 OLED 드라이버)은 **파일 내려받기가 필요해** 운영자 할 일 19번·미해결 64번으로 남겼다. 고친 것 가운데 제품에 남는 것: 결과 칸의 "콘솔에 결과가 나왔어요" 알림(치명 — `print()`뿐인 예제에서 아무 일도 없어 보이던 것), 편집칸 줄바꿈, 보드 그림 최소 폭 32rem과 넘침 표시, `board.notice`로 배선 어긋남 안내를 그림 옆에, 오류 사전 `level: notice`(파랑 안내 상자), 예제 한 줄 설명 표시, [보드에 저장] 덮어쓰기 확인 창, 고지 "초안" 빌드 차단, `repo-check.yml`. 검증: 빌드 통과(출처 검사 파일 412개·항목 33개), `npm run check` 566파일 0오류, `npm test` 147파일 **1,489건** + Container 7건, `npm run check:links` 2,088개, 저장소 검사 추적 파일 1,033개, `npm run test:e2e -- --workers=2` **393 통과·223 건너뜀·실패 0·흔들림 0(32.6분)**. 근거와 판단은 PLAN §8.3 "P3-11 검토 반영" 구현 메모와 미해결 125~127. **CI**: 테스트 35293745594 성공(단위 147파일 + 브라우저 393 통과·224 건너뜀 19.7분 + 링크 2,088개), 배포 35293745626 성공, **새 워크플로 "저장소 검사" 35293745653·35294097275 성공**(PROGRESS.md만 바뀐 push에서도 돌았다 — 이번에 막은 구멍이 실제로 막혔다는 증거). **실사이트**: `/`·`/labs/esp32/`·`/labs/esp32/check/`·`/start/board/`·`/help/errors/`·`/labs/vision/`·`/start/teacher/`·`/firmware/manifest.json` 모두 200, 오류 사전 "오류 풀이 93개", 고지에서 "초안" 0건, 실습실에 새 표시 6개가 모두 있고, 실사이트에서 시나리오 B 2건·ESP32 실습실 7건(새 콘솔 알림 검사 포함)·점검 도우미 3건·배선 어긋남 2건·모바일 6건 통과 |

**P3-02 판정(보드 그림과 첫 부품):** 아래 표의 "P3-02" 행.

**Phase 2 영상처리 실습실 완료(본체 2026-09-16, 검토 반영 2026-09-17).** P2-01 파이썬 워커·P2-02 코드 에디터·P2-03 카메라·창 흉내와 첫 실습·P2-04 슬라이더 규약(시나리오 A 자동 테스트 통과) 뒤, P2-05~P2-13을 7구역 병렬로 만들고 P2-14에서 통합했다. **PLAN §8.2의 Phase 2 완료 기준(시나리오 A 자동 테스트, 가짜 카메라)은 로컬·CI·실사이트에서 모두 통과**했고, 검토 반영에서 시나리오 A 검사를 "준비 중에 [실행]을 누르고, 결과 창과 조절 막대가 화면 안에 들어오는지"까지 보도록 강화했다. 표의 예제 가운데 `serial`을 쓰는 f084·f085만 Phase 3·4로 넘겼다(미해결 39). 사람 확인이 필요한 항목(실제 웹캠 — 운영자 할 일 1번, 모델 파일 — 17번, 기기 안 음성 인식·마이크 — 18번, Android Chrome·iPad Safari — 3번, 조판 삽화 권리 — 13번)은 "확인 필요"로 남기고, 승인 위임(DECISIONS O1)에 따라 기다리지 않고 Phase 3을 진행한다.

| 묶음(Phase 2) | 판정 | 증거 |
|---|---|---|
| P2-01 파이썬 워커 | 완료(2026-09-16) | 시험 페이지 `/labs/dev/runtime/`에서 `print('안녕')` 출력, `time.sleep` 반복이 [정지] 뒤 1초 안에 `KeyboardInterrupt`로 멈춤(실측 72ms), `while True: pass`가 1초 뒤 워커 재시작으로 멈추고 다시 실행됨, `input()` 대기 지점에 화면 값 전달, 제한 모드 안내, numpy 받기, `exit()` 정상 종료(콘솔 오류 0건), 허용 주소(jsDelivr) 밖 요청 0건 — Playwright(Edge 153, `tests/e2e/lab-runtime.spec.ts`). Node 실제 Pyodide 테스트(`--experimental-wasm-jspi`)로 `run_sync`·정지·`exit()` 확인 |
| P2-02 코드 에디터 | 완료(2026-09-16) | 시험 페이지 `/labs/dev/runtime/`(실습실 공통 틀 `LabShell`)에서 CodeMirror 6 편집칸(줄 번호·파이썬 구문 색·자리 글), 입력한 코드가 0.4초 뒤 `ai-physical-computing:editor:dev:<예제 id>`로 저장되고 새로고침 뒤 그대로 복원, [공유 링크]가 `#code=…&ex=…`를 만들고 새 탭에서 열면 같은 코드(주소의 #은 지워짐), [이 컴퓨터에서 내 기록 지우기] 확인 대화 뒤 localStorage·sessionStorage에 머리말 이름 0개·편집칸은 예제 원래 코드·글자 크기 기본값, 키보드만으로 Tab 들여쓰기 → Esc 뒤 Tab으로 편집칸 나가기 → Tab·Enter로 [실행]·[정지](정지 1단계 실측 99ms), 글자 크기 단계 저장, [초기화] 확인 대화, [예제 불러오기]·마지막 예제 기억, [.py 내려받기](hello.py 내용 일치), 좁은 화면에서 편집칸→입력·출력→조절 패널→콘솔 세로 쌓임 — Playwright(Edge 153, `tests/e2e/lab-editor.spec.ts` 7건 + `lab-runtime.spec.ts` 7건 통과). Vitest: 공유 링크 왕복·`+` 보존·길이 한계, 자동 저장 미루기·복원·막힌 저장 공간, 예제 목록 검사, 글자 크기 단계, 기록 지우기 범위, 저장소 검사 예외(24건 추가) |
| P2-03 카메라·창 흉내 모듈과 첫 실습 | 완료(2026-09-16, 실제 웹캠은 운영자 할 일 1번) | 영상처리 실습실 `/labs/vision/`에서 가짜 카메라(Edge 153에 `--use-fake-device-for-media-stream` + 코드로 그린 Y4M 640×480)로 첫 예제 `examples/vision/first-edge.py`를 [실행]하면 ① 파이썬 엔진 ② numpy ③ OpenCV 준비 단계가 끝나고, 입력 칸에 웹캠 미리 보기 `640×480 · 파이썬에 12~13fps로 전달`, 출력 창 `edges 640×480`에 Canny 결과가 그려짐(흰 에지 픽셀 비율 0.81~0.90%, 실측), 출력 화면에서 q 키 → `실행이 끝났어요.`, [정지] → KeyboardInterrupt(실측 183~198ms), 카메라 거부(`getUserMedia` → NotAllowedError) → 콘솔 `카메라 사용을 허용하지 않았어요 …` + 샘플 입력으로 자동 전환해 에지가 그려짐, 제한 모드(`?limited=1`)에서 샘플 입력을 켜 두면 `cap.read()`가 `(480, 640, 3)` 한 장을 받아 한 번 실행되는 코드가 돎, 사이트 밖 요청은 jsDelivr(Pyodide·numpy·opencv 휠)뿐, 페이지 오류 0건 — Playwright `tests/e2e/lab-vision.spec.ts` 5건(데스크톱 4·모바일 1). Node 실제 Pyodide+OpenCV(`tests/unit/lab/pyodide-cv2.test.ts`): cv2 창·카메라 함수가 사이트 것으로 바뀌고(오타 `destoyAllWindows`는 그대로 AttributeError), 첫 예제가 시험 입력으로 돌아 5장 뒤 q로 끝나며 `window.show`가 RGBA 64×48을 transfer 목록과 함께 보냄, `waitKey(0)` 대기 중 정지, 실행 전 키는 비워지고 실행 중 키는 순서대로, `imshow` 형식 변환(회색·float·BGRA)과 잘못된 모양 오류, 같은 창 초당 30장 제한, `cap.get/set/release`, 카메라 못 열 때 `(False, None)`, 파일 경로는 진짜 VideoCapture, 제한 모드 한 장 읽기. Vitest `tests/unit/lab/vision.test.ts` 12건(예제 목록·프레임 제한·fps·키 코드·샘플 장면·Y4M) |
| P2-04 슬라이더 규약 | 완료(2026-09-16, 실제 웹캠은 운영자 할 일 1번) | **시나리오 A 자동 테스트 통과**(`tests/e2e/scenario-a.spec.ts`, Edge 153 + 가짜 카메라): 홈 → [카메라로 바로 해보기]("준비 중" 없음) → 실습실 준비 5.9초 → [실행] → `edges` 창에 에지 6.9초 → threshold 슬라이더를 100 → 20으로 옮기면 코드가 `threshold = 20`으로 바뀌고 다음 프레임부터 희미한 네모의 테두리가 나타나 흰 픽셀 비율 0.86% → 0.96%(한 바퀴 평균), 다시 100으로 옮기면 0.85% — 홈부터 슬라이더 효과까지 9.8초, 총 15.7초(5분 기준). 조절 패널(`tests/e2e/lab-params.spec.ts` 3건): `# @slider`·`@select`·`@toggle` 줄마다 슬라이더(값·범위·설명 글, 화살표 키로 한 칸, `aria-valuetext` "101 (0부터 255까지)")·선택 상자·토글이 생기고 조작하면 코드의 그 자리 글자만 바뀜(`"blur"` 따옴표·`False`·소수 자릿수 유지), 잘못된 줄(범위 밖 값)은 "5번 줄: … 벗어나요" 경고, 코드를 고치면 패널이 따라옴, 실행 중 `time.sleep` 반복문에서 슬라이더 120 → 콘솔 `값 120`, 제한 모드 안내 "다음 [실행] 때 반영"과 코드에 적힌 값으로 실행. Node 실제 Pyodide(`pyodide-node.test.ts` params_* 4단계, JSPI·제한 모드 두 변형): `input()` 대기 뒤 전역 변수 갱신, 형 변환(int 7.6 → 8·float·str·bool), 예약어·잘못된 이름·사전 아님 무시, 바꿀 수 없는 값은 콘솔 알림, 실행 전 값은 버림, 전역 사전을 잇지 않으면 안 넣음, `get()` 지점에서도(양보 없이) 반영. Vitest `tests/unit/lab/params.test.ts` 22건(파서: SPEC 예 4개, 띄어쓰기·탭·쉼표, 한글 설명 앞·뒤, 간격 생략·int/float, CRLF 위치, 작은따옴표, 잘못된 형식 14가지 경고, 겹치는 이름, 값 글자 만들기·간격 맞추기), `example-meta.test.ts` 8건 |
| 병렬 제작 준비(P2-05~P2-13 앞) | 완료(2026-09-16) | **예제 이관(PD-33):** `node scripts/import-examples.mjs`가 원본 zip 3개(교과서·opmp·pg)의 코드 49개를 `examples/vision/u1/`(20)·`examples/vision/opmp/`(19)·`examples/desktop/`(10)에 CRLF → LF만 바꿔 쓰고 줄 수·SHA-256·구문(`python -c ast.parse`, 3.11.0) 대조 결과를 `scripts/examples-manifest.yaml`에 기록, 49개 모두 `python-ast` 통과·사이드카 `<이름>.meta.yaml` 생성. `npm run examples:verify` 49/49 일치(`npm test`에 포함). Vitest 15건(CP949 이름 zip 픽스처·CRC·홀로 있는 CR·UTF-8 아님·third-party/ 규칙·expect_syntax_error). 영상처리 실습실 [예제 불러오기]에 52개가 4묶음(`<optgroup>`)으로 보임(빌드 결과 확인). **흉내 모듈 폴더 규약:** 예시 모듈 `src/lab/modules/hello/`를 폴더만 두고 개발용 시험 페이지에서 `import apc_hello` → `greet` 요청 답 `안녕, 세계!`, `wave` 이벤트 → 패널 횟수 3, 패널 입력칸 → `name()`, [누르기] → `clicks()`(실행 전 값은 버려지고 실행 중 값만) — Playwright `tests/e2e/module-hello.spec.ts`(데스크톱·모바일), 영상처리 실습실에는 붙지 않음. Node 실제 Pyodide 7건(`pyodide-hello.test.ts`: 요청·이벤트·get/poll·틱 훅·동기 진입점), manifest 검사 6건. **패키지:** `@mediapipe/tasks-vision` 0.10.35(Apache-2.0)·`workbox-build` 7.4.1(MIT) 설치·`sources.yaml` 등록·고지 원문, WASM 4개(약 22MB)를 `public/vendor/mediapipe/0.10.35/wasm/`에 복사(git 제외)해 빌드 출처 검사·번들 검사 통과. **병렬 검증:** `PW_BASE_URL=http://localhost:4401/…`로 개발 서버(`ASTRO_DEV_BACKGROUND=1 npm run dev -- --port 4401`)에 대고 `module-hello`·`lab-vision` spec 6건 통과(가짜 카메라 에지 0.83%, 카메라 대기 중 정지 85ms). 전체 Playwright 데스크톱·모바일 통과(아래 작업 로그) |
| P2-05 로딩 전략과 캐시 | 완료(2026-09-16) | 빌드가 같은 사이트 Pyodide 예비본 7개(25.9MiB)를 `public/vendor/pyodide/314.0.7/`에 채우고(`npm run pyodide:fallback`, SHA-256 대조) `dist/sw.js`를 만든다(사전 캐시 셸 6개 128KB — PD-11 범위). Playwright `tests/e2e/lab-loading.spec.ts` 8건 통과: 진행률 패널·단계·1분 개념 카드·접힘, `?sw=off` 비상구, **두 번째 방문이 인터넷 없이 열림**(context.setOffline), [이 컴퓨터에 실습 파일 미리 받기]가 7개를 캐시에 저장, 막힌 jsDelivr 파일을 서비스 워커가 같은 사이트 예비본으로 바꿈, jsDelivr를 통째로 막아도 실습실이 열림, 점검 페이지 네트워크 항목은 [시험하기]를 누를 때만 접속, 375px 넘침 0. Vitest `tests/unit/loading/` 78건 |
| P2-06 한국어 오류 사전 | 완료(2026-09-16) | 데이터 한 파일(`content/help/errors/errors.yaml`, 묶음 6·풀이 47·예외 종류 30)로 실습실 카드와 `/help/errors/`를 함께 만든다. Playwright `tests/e2e/lab-errors.spec.ts` 17건 통과(데스크톱·모바일): NameError 카드에 종류·제목·"내 코드 2번째 줄"·[그 줄로 가기]·뜻·왜·고치는 법·원문 접기·사전 링크, 편집칸 줄 강조, 다른 오류 5가지가 각각 다른 항목, [정지]는 "오류가 아니에요", 375px에서 원문을 펼쳐도 넘침 0, 사전 페이지 항목 47개. Vitest `tests/unit/errors/` 57건(Node 실제 Pyodide로 45가지 오류를 다시 내 채집본과 대조) |
| P2-07 보충 차시 V1~V5 | 완료(2026-09-16) | 예제 5개(`examples/vision/supplement/`)·차시 5쪽(`content/lessons/u1/v1~v5.md`, SPEC §7.2 8칸·퀴즈 3문항)·직접 그린 SVG 5장. Playwright `tests/e2e/lab-supplement.spec.ts`: 다섯 예제가 가짜 카메라와 샘플 입력 두 가지로 끝까지 돌고 [q]로 정상 종료, 예제마다 조절 값이 실제로 먹음(V1 모눈 1.66%→6.56%, V2 평균 파랑 86→3, V3 흰 픽셀 6.3%→97.1%, V4 에지 0.85%→0.96%, V5 덩어리 0개), 사이트 밖 요청은 jsDelivr뿐, 차시 5쪽이 데스크톱·모바일에서 넘침 0. Vitest 견본 차시 검사에 V1~V5를 넣어 8칸·퀴즈·예제 파일을 함께 본다 |
| P2-08·P2-09 mediapipe(손·얼굴·자세) | 완료(2026-09-16, 실제 모델·웹캠은 운영자 할 일 1·17번) | 레거시 `mp.solutions` 모양 그대로 `Hands`·`FaceMesh`·`FaceDetection`·`Pose`·`drawing_utils`·`drawing_styles`. **카메라가 없어도** 합성 재생 입력(손 4·얼굴 3·자세 2 동작)으로 실습이 된다. Playwright `lab-mediapipe-hands.spec.ts` 8건·`lab-mediapipe-face-pose.spec.ts` 9건 통과(f119 얼굴 그물 20,706픽셀, f122 Opend↔Closed, f037 MAR 0.321~0.487, f035 Left·Center·Right, f042 어깨 차이 40픽셀 초과, 구글 주소 요청 0건). Vitest `tests/unit/mediapipe/` 138건(연결표를 설치된 npm 패키지와 대조, Node 실제 Pyodide 얼굴·자세 14단계). 추론 위치는 실험 뒤 화면(메인 스레드)으로 결정(PLAN 구현 메모) **검토 반영(2026-09-17):** 표가 적은 4-1-1 예제 f092~f094를 원본 zip에서 옮겨(`examples/vision/u4/`) 고치지 않고 확인 — f093 재생 얼굴에 초록 그물 11,852픽셀, f094 `refine_landmarks=True`의 468·473번까지 빨간 점 243픽셀(`tests/e2e/lab-textbook-u3-u4.spec.ts`). **f085는 첫 줄이 `import serial`이라 미확인**(Phase 3·4, 미해결 39). 인식 패널은 이제 코드가 mediapipe를 쓸 때만 열린다 |
| P2-10 러너 공통 | 완료(2026-09-16) | `src/lab/modules/runtime-extras/`(모든 실습실): 사이트가 그린 `mask.png`가 작업 폴더에 들어가 f039가 알파까지 읽고, 코드가 저장한 파일은 [내려받기]가 되며, `ImageFont.truetype('C:/Windows/Fonts/malgun.ttf')`가 사이트 글꼴(Pretendard 정적 .otf)로 이어지고, `cv2.py` 같은 이름은 넣지 못하게 막고, 콘솔 300조각이 넘으면 접는다. Playwright `tests/e2e/lab-runner.spec.ts` 10건 통과(f039 원본 3중 반복 596~3,071ms vs 사이트판 numpy 7~35ms, 픽셀 최대 차이 0~1). `exit()`·`__name__=='__main__'`은 Pyodide에 이미 있어 넣지 않았다 **검토 반영(2026-09-17):** 파일 패널은 코드가 파일을 읽고 쓰는 모양(imread·open·save·truetype…)이거나 [파일 넣기]를 썼을 때만 열린다. **표의 f084(가상 입력)는 `import serial`이라 미확인**(`input()` 부분은 실습실 입력줄로 됨 — Phase 3·4, 미해결 39) |
| P2-11·P2-12 가상 데스크톱 | 완료(2026-09-16) | 논리 해상도 1920×1080 가상 모니터·아이콘·메모장·그림판·오른쪽 클릭 메뉴·대화상자, `pyautogui` 흉내(PyAutoGUI 0.9.54와 같은 상수·걸음 계산), 가상 브라우저 창(검색 결과는 이 사이트의 진짜 쪽 목록에서만), 스페이스 키 미니게임 3종(자체 규칙), `screenshot()` → '내 파일' 카드·[내려받기]. Playwright `lab-desktop.spec.ts`·`lab-desktop2.spec.ts` 12건 통과(f121 입 벌리면 스페이스 → 점수 29, f127 코끝으로 가상 커서 이동). Vitest `tests/unit/pyautogui/` 68건 **검토 반영(2026-09-17):** 표가 적은 f090·f091·f095·f096·f097을 원본 zip에서 옮겨(`examples/vision/u3/`·`u4/`) 고치지 않고 확인 — f090 재생 손 "손가락 0~5개 펴기"에서 `screenshot.png`가 '내 파일'에 저장, f091 검지로 가상 커서 (960, 540) → (960, 453), f095 화면 크기 1920×1080을 읽고 기준점 뒤 커서 (960, 540) → (1639, 523), f096 EAR 글자·점, f097 재생 얼굴 "눈 깜빡이기"에서 "우클릭 실행!"(`lab-textbook-u3-u4.spec.ts` 3건, 로컬 약 1분) |
| P2-13 음성 인식 흉내 | 완료(2026-09-16, 실제 마이크는 확인 필요) | `speech_recognition.py`(파일 이름으로 가리기 — 내려받기 0바이트)로 f044·f045가 **고치지 않고** 돈다. 기본은 글자 입력, 서버 인식은 교사가 `/settings/`에서 켠 브라우저에서만 `<option>`이 생긴다(꺼져 있으면 DOM에 없음). Playwright `tests/e2e/lab-speech.spec.ts` 12건 통과(글자 입력 왕복, 안 들린 것으로 보내기, 제한 모드, [정지], 설정 켜기·끄기, 기록 지우기 뒤 꺼짐, 375px 넘침 0), Vitest `tests/unit/speech/` 37건(Node 실제 Pyodide로 f044·f045 파일 그대로 실행) **검토 반영(2026-09-17):** 기기 안 인식 확인은 [확인] 단추를 누를 때만(설정 페이지 첫 글자 "확인 전", 단추 [확인] → 물어본 뒤 [다시 확인]), 기기 안 인식으로 시작할 때 `processLocally`가 실제로 켜지지 않으면 인식을 시작하지 않고 f044가 원본대로 "서버 요청 실패."로 끝남(가짜 인식기로 `start()` 0회 확인). 음성 패널은 코드가 `speech_recognition`을 쓸 때만 열린다 |
| P2-14 차시 연결과 시나리오 A 점검 | 완료(2026-09-16, 실제 웹캠은 운영자 할 일 1번) | 차시의 따라하기 예제에 [이 자리에서 실습실 열기](누른 뒤에야 `<iframe …?example=…&embed=1>`을 만든다 — 누르기 전 전송 0바이트)와 [실습실에서 열기]("준비 중" 뗌). Playwright `tests/e2e/lesson-embed.spec.ts`: 누르기 전 실습실·Pyodide 요청 0건, 누르면 그 예제가 올라간 실습실이 열리고 머리글·바닥글은 숨음, 접기·펼치기, 넘침 0. **이관 예제 스모크** `tests/e2e/examples-smoke.spec.ts`: 49개를 실습실에서 한 번씩 실행해 파이썬 오류 0건(3.2분, 원본이 일부러 오류로 끝나는 f024만 사이드카 `smoke:`에 기록). **첫 방문 측정** `tests/e2e/first-visit.spec.ts`: 홈만 0.34MB(사전 캐시 셸뿐) / 첫 에지까지 19.83MB(사이트 0.89MB + jsDelivr 18.94MB, 허용 주소 밖 0바이트), 홈 → 실습실 0.8초 → 준비 끝 7.0초 → 첫 에지 8.1초, Fast 3G 실측 홈 4.1초·계산값 전체 약 216초. **CI(Chromium, Ubuntu)**: 단위 834건·브라우저 285 통과(1 flaky)·링크 2,097개, 예제 스모크 49개 문제 0, 전송량 19.83MB, 시나리오 A 총 15.3초. **실사이트**(배포 뒤 PW_BASE_URL로 확인): 시나리오 A 총 17.0초(준비 7.2초 → 첫 에지 8.4초 → 슬라이더 11.4초, 흰 픽셀 0.85% → 0.96% → 0.85%), 첫 방문 19.94MB(사이트 1.01MB + jsDelivr 18.94MB), 홈만 0.34MB **검토 반영(2026-09-17):** 시나리오 A 검사가 준비 중에 [실행]을 누르고(예약) 결과 창·threshold 막대가 1366×768 화면 안에 들어오는지까지 본다 — 로컬 총 32.2초(전체 스위트 2워커 병렬 중 — 준비 11.1초 → 첫 에지 17.6초 → 슬라이더 22.4초, 흰 픽셀 0.85% → 0.95% → 0.85%), CI 총 17.8초(준비 5.3초 → 첫 에지 8.4초 → 슬라이더 11.6초), 실사이트 총 17.0초로 통과(준비 중에 [실행]을 눌러 예약 → 준비 4.6초 → 첫 에지 8.1초 → 슬라이더 효과 11.1초, 흰 픽셀 0.86% → 0.96% → 0.85%). 예제 스모크 57개 로컬·CI 모두 문제 0개(로컬 4.9분). 첫 방문 전송량 로컬 19.83MB(사이트 0.90MB + jsDelivr 18.93MB, 홈만 0.34MB), 실사이트 19.95MB(사이트 1.02MB + jsDelivr 18.93MB, 허용 주소 밖 0바이트, 홈 → 첫 에지 13.0초, Fast 3G 계산값 약 217초·실측 홈 화면 3.8초). 실습실에서 "이 예제가 나오는 차시" 링크로 돌아가고, 없는 예제 이름은 한국어로 알리며(`data-example-missing`), 링크 검사가 `?example=` 값까지 본다. 임베드 틀은 화면 높이를 넘지 않는다(좁은 화면 80svh) |
| Phase 2 검토 반영(26건) | 완료(2026-09-17) | **치명 3:** ① 준비 중 [실행] 예약(`data-lab-run-pending`, 준비가 끝나면 저절로 실행) ② [실행] 뒤 결과 창과 첫 조절 막대를 한 화면에 함께 보이게 옮기기(`revealTogether` — 1366×768에서는 출력 화면 틀과 threshold 막대, 375×812에서는 출력 칸 전체와 막대, 움직임 줄이기면 바로) ③ 오류로 끝나면 안내 줄 "오류로 끝났어요: NameError — 내 코드 2번째 줄" + 풀이 카드로 옮기기(오류 줄 강조는 편집칸 안에서만 스크롤 — 브라우저 테스트에서 CodeMirror가 화면을 되돌리는 것을 발견해 고침). **중요 8:** f090~f097 이관·확인(f084·f085는 serial이라 넘김), 서비스 워커 SHA-256 대조(같은 크기로 바꿔치기한 `pyodide.mjs`를 버리고 예비본을 씀 — e2e), opencv-python MIT 고지, numpy 휠 구성요소 고지 16개, 카메라 거부 이유 남기기, 준비 패널을 첫 준비 동안 편집칸 위로, 흉내 모듈 패널은 쓸 때만, Tab으로 막 들어온 편집칸은 Tab 한 번에 지나감. **사소 15:** 없는 `?example=` 알림·링크 검사, 설정 "확인 전"·[확인], PLAN·MAINTENANCE 음성 문서, MediaPipe에서 옮긴 src 파일 출처 항목, 보충 SVG 출처 항목, 공유 링크 개인정보 주의, `processLocally` 확인, 요청 검사를 문맥 단위로, `.gitignore`에 SPEC.md, 좁은 화면 44px, 서비스 워커 실패 영어 문장 숨김, 차시 링크, 임베드 높이, 오류 사전 접기·찾기, 안쪽 용어("정지 2단계"·ms) 빼기. **검증:** Vitest 72파일 848건 통과, `astro check` 0오류(321파일), `npm run build`(출처 검사 파일 288개·항목 24개, 번들 npm 15개, 서비스 워커 셸 6개 128KB), 링크 검사 사이트 안 주소 2,052개(#위치 262·실습실 예제 7), 저장소 검사 652개, 예제 대조 57/57. **Playwright 전체 298 통과·136 건너뜀·0 실패**(Edge 153, 로컬 2워커 22.6분) — 그 전 전체 실행 두 번에서 결함 5가지를 잡아 고쳤다: ① 없는 `?example=` 알림이 한 번도 안 나옴(`undefined`를 `null`과 비교) ② 1366×768에서 출력 창만 보이고 threshold 막대는 화면 밖 → 결과와 첫 막대를 함께 보이기(`revealTogether`) ③ 모듈 패널 범위 스타일이 44px 규칙을 이김 ④ 재생 입력을 골라도 인식 패널이 닫혀 동작을 못 고름 ⑤ 오류 줄 강조가 카드로 옮긴 화면을 되돌림. 화면 실측([실행] 3초 뒤): 1366×768 캔버스 10~226·막대 595~708, 375×812 출력 제목 8·캔버스 91~347·막대 586~699(모두 화면 안). **CI** 테스트 35183067936 성공(단위 842+6 건너뜀, 브라우저 297 통과·1 flaky `lab-desktop2` 모바일 움직임 줄이기 → 원인(누른 횟수를 기다리지 않고 한 번 읽음)을 고쳐 반복 6회 통과), 배포 35183068002 성공, **실사이트** 시나리오 A·첫 방문·3·4단원 예제·차시 임베드 9건 통과. flaky를 고친 마지막 push(`1116682`) 뒤 **테스트 35184213303 성공(단위 842+6 건너뜀, 브라우저 298 통과·136 건너뜀·0 flaky, 13.4분, 예제 스모크 57개 문제 0, 시나리오 A 18.6초, 링크 2,052개)·배포 35184213172 성공** |

| 묶음 | 판정(2026-09-16 검토 + 반영) | 증거 |
|---|---|---|
| P1-01 저장소 연결과 첫 커밋 | 완료 | 원격 main = 로컬 HEAD, 추적 파일에 원본 폴더·원본 파일·`docs/SPEC.md` 0개(`git ls-files`, `gh api …/contents/docs`) |
| P1-02 Astro 뼈대 | 완료 | `npm run build`·`npm run check`(0 errors) 통과, `dist/` 링크에 base 붙음, CLAUDE.md 기술 스택·명령어 절이 실제와 같음 |
| P1-03 배포 파이프라인과 첫 배포 | 완료 | 배포 실행 성공 기록, 실사이트 HTTPS 200(아래 로그의 실행 번호) |
| P1-04 출처 등록부·저장소 검사·자동 출처 페이지 | 완료 | 미등록 파일·중복 매칭·`third-party/` 미등록 → 빌드 실패, 커밋 전 훅이 `.pdf`·6MB 파일을 막음. 검토 지적(넓은 `examples/**` 항목, 이미지·개인정보 검사 우회, mark.js·Pretendard npm 표기)을 이번에 고침 |
| P1-05 공통 레이아웃과 홈 | 완료 | 1366×768·375×812·320×568에서 버튼 3개가 첫 화면 안, Tab 순서·초점 표시, 대비 계산값 |
| P1-06 콘텐츠 컬렉션과 차시 템플릿 | 완료 | md 2개 + py 1개를 넣는 실험으로 코드 수정 없이 목록·페이지 생성 확인, 8칸 순서 검사 |
| P1-07 용어사전 틀 | 완료 | 26항목, 차시 본문 용어 초점 → 툴팁(이제 용어 위) → Enter로 `#agent` 이동 |
| P1-08 시작하기 3쪽과 점검 페이지 틀 | 완료(모바일 실측은 확인 필요) | 점검 10항목이 Chrome(Windows)에서 맞게 나옴. Android Chrome·iPad Safari는 기기가 없어 운영자 할 일 3번 |
| P1-09 스모크 테스트와 CI | 완료 | 로컬·CI Playwright 통과, 테스트 실패가 배포를 막지 않는 것을 실제 기록으로 확인 |
| P1-10 라이선스·안내 문서 틀 | 완료 | LICENSE·LICENSE-CONTENT.md·README·MAINTENANCE·CONTRIBUTING·이슈 양식 3종, 바닥글·기여 페이지 연결, 제외 조항 |
| P1-11 사이트 검색 | 완료 | "픽셀"·"서보" 검색으로 차시·실습실이 나오고, 용어사전 결과는 이제 항목(#pixel)으로 바로 감 |

## 완료

- **Phase 2 — 영상처리 실습실(진행 중):**
  - P2-01 파이썬 워커(2026-09-16): `src/lab/runtime/` — 설정 `config.ts`(Pyodide 314.0.7 jsDelivr 주소, 같은 사이트 예비본 자리, 정지 유예 1초, 양보 간격 16ms), 메시지 형식 `protocol.ts`, 다리 `bridge.ts`(정지 신호 경주 `raceStop`, 요청·답, 최신 값 `get`·쌓인 값 `poll`), 파이썬 도우미 `apc_runtime.py`(`block_on` = `pyodide.ffi.run_sync`, `time.sleep`·`input()` 대체, 짧은 sleep 모으기, 제한 모드 한국어 안내), 워커 `worker.ts`(모듈 워커에서 `import('…/pyodide.mjs')` → `loadPyodide({ indexURL })`, stdout·stderr 스트리밍, `loadPackagesFromImports`, `__main__` 전역, SystemExit는 정상 종료), 화면 쪽 `client.ts`(`PythonRuntime`: 상태 unloaded→loading→idle→running→stopping, 정지 1단계 메시지 → 1초 안에 안 멈추면 2단계 `Worker.terminate()`·재시작·패키지 다시 받기). 개발용 시험 페이지 `/labs/dev/runtime/`(코드 입력칸·[실행]·[정지]·콘솔·입력줄, `?limited=1`). 브라우저 저장 이름 규칙 `src/lib/storage.ts`(미해결 13번 해결). 출처 등록: Pyodide(MPL-2.0 고지 전문 `public/licenses/pyodide.txt`), CPython 3.14.2(PSF-2.0), NumPy 2.4.6, `/credits/`에 MPL-2.0·PSF-2.0 전문 링크. 확인한 사실: jsDelivr `v314.0.7/full/`에 pyodide.mjs·pyodide.asm.mjs·pyodide.asm.wasm·python_stdlib.zip·pyodide-lock.json이 있고(pyodide.asm.js는 없음) CORS·CORP 허용, 인터럽트 버퍼는 SharedArrayBuffer(COOP·COEP) 필요라 GitHub Pages에서 못 씀(협조적 정지로 대체), `exit()`·`KeyboardInterrupt`는 약속 거부와 별개로 워커 `unhandledrejection`으로 한 번 더 새어 워커가 삼킴. 검증 수치는 야간 작업 로그.
  - P2-02 코드 에디터(2026-09-16): 코드 에디터 `src/lab/editor/`(`python-editor.ts` — CodeMirror 6를 필요한 부품만 모아 만듦(basicSetup·자동 완성 화면 없음): 줄 번호, 현재 줄, 파이썬 구문 강조, 괄호 짝, 되돌리기, Tab 4칸 들여쓰기 + Esc 뒤 Tab으로 나가기(CodeMirror 내장 tab focus mode, 안내 문장을 aria-describedby로), Ctrl+Enter 실행, 한국어 자리 글; `theme.ts` — 차시 코드 블록과 같은 github-light-high-contrast 색(대비 계산값 주석)과 디자인 토큰; `font-size.ts` — 13·15·17·20·24px 단계, 저장 이름 `editor:font-size`). 실습실 공통 조작 `src/lab/controls/`(`lab-shell.ts` 컨트롤러 — `getLabController(root)`로 페이지가 받아 `on('done')`·`onRequest('camera.read', …)`·`runtime.setValue` 등을 쓴다; `examples.ts` 예제 목록 모양 `LabExample{id,title,code,file?,packages?}`(P2-03부터 examples/의 .py를 페이지가 읽어 넘김, 지금은 시험용 6개); `autosave.ts` 예제별 이름 `editor:<실습실>:<예제 id>`(0.4초 미룸, 저장됨·저장 안 됨 표시, 마지막 예제 기억); `share-link.ts` lz-string으로 `#code=…&ex=…`(2,000자 넘으면 .py 함께 안내, 16,000자 상한, URLSearchParams의 `+`→공백 변환을 피해 직접 읽음, 열면 #을 지움); `download.ts`; `records.ts` [이 컴퓨터에서 내 기록 지우기](`clearOurs` localStorage+sessionStorage, `apc:records-cleared` 이벤트)). 화면 틀 `src/components/lab/LabShell.astro`(SPEC §6.1 배치: 조작 줄 / 왼쪽 편집칸 / 오른쪽 위 `io` 슬롯 / 오른쪽 아래 `panel` 슬롯 / 아래 콘솔·입력줄 / 기록 지우기, 64rem 미만은 세로 쌓기, 공유·초기화는 `<dialog>`)와 `ClearRecordsButton.astro`(확인 대화, 도움말·교사용 페이지에도 넣어 "버튼은 실습실과 함께 생겨요" 약속 문장을 실제 버튼으로 바꿈). 개발용 시험 페이지를 이 틀로 교체(예제 6개, 실행기 정보는 `after` 슬롯). 출처 등록: CodeMirror 6 패키지 6개·Lezer 4개·style-mod·w3c-keyname·@marijn/find-cluster-break(MIT, 고지 원문 `public/licenses/codemirror.txt`), lz-string 1.5.0(MIT, `public/licenses/lz-string.txt`) — 번들에 실제로 들어간 14개만 npm 목록에(crelt는 의존성이지만 번들 밖). 저장소 검사에 `privacy_exceptions`(public/licenses/ 아래 고지 원문의 이메일 모양만, 미해결 21번) 추가. 번들 크기: 실습실 스크립트 청크 407KB(압축 전, 실습실 페이지에서만 받음).
  - P2-03 카메라·창 흉내 모듈과 첫 실습(2026-09-16): **파이썬 쪽 모듈 폴더 `src/lab/python/`**(`modules.ts`가 .py를 `import.meta.glob(?raw)`으로 묶어 워커가 Pyodide FS `/apc`에 씀; `apc_runtime.py`에 `request(raw=)`·`emit`(답 없는 알림, transfer)·`register_reset_hook` 추가; `apc_shims.py` 등록표 `SHIMS={'cv2':'apc_cv2'}` → 워커가 실행 직전 `install_available()`; `apc_cv2.py` — 진짜 opencv-python 위에 `VideoCapture`(0번은 화면 요청 `camera.open/read/set/release`, 문자열은 진짜)·`imshow`(회색·BGR·BGRA→RGBA, 같은 창 초당 30장, `window.show` 이벤트 transfer)·`waitKey`(양보 지점 + `poll('cv2.keys')`)·`namedWindow`·`getWindowProperty`(화면에서 닫으면 0.0)·`destroyWindow`·`destroyAllWindows` 등 14개 이름만 덮어씀). 워커·다리·클라이언트에 `event` 메시지(워커→화면, transfer)·`reply(value, transfer)`·패키지 진행 `phase/names`(+"No new packages to load" 한국어) 추가. **화면 `src/lab/vision/`**(`vision-lab.ts` `mountVisionLab` — 요청·이벤트 잇기, cap.read 초당 15장 제한 `FrameThrottle`, 준비되면 OpenCV 미리 받기와 단계 표시, 제한 모드 prefill; `sources.ts` 입력 소스 등록표 — 웹캠 getUserMedia 640×480 `facingMode user`·샘플(코드로 그린 움직이는 도형·글자·그라데이션)·내 그림 파일, 웹캠 실패 시 한국어 이유(거부·없음·사용 중·보안 연결 아님)와 샘플 자동 전환, P2-08은 `registerVisionSource()`로 재생 입력 추가; `windows.ts` 출력 창(창 이름별 캔버스·탭·[창 닫기]·fps, 키 캡처는 글자·ESC·Enter·Backspace·Space만); `frame.ts` 순수 논리; `examples.ts` — `examples/vision/**/*.py` → `LabExample[]`(첫 줄 주석 제목·둘째 줄 설명·파일 이름 id·opencv-python 미리 받기)). **페이지** `src/pages/labs/vision/index.astro`(자리 페이지 → LabShell + `VisionIo.astro` io 슬롯: 준비 단계·입력 소스·미리 보기·해상도·fps·출력 탭·화면 키 [q][d][r][ESC]·"영상은 이 컴퓨터 밖으로 나가지 않아요", BrowserNotice 켬, 1분 개념·잘 안 될 때), 첫 예제 `examples/vision/first-edge.py`(자체 제작, `threshold = 100  # @slider 0 255 1`). `_labs.ts`에 `open: true`(실습실 개요 카드 "열림", 자리 틀 안 씀), 학생용 시작하기 ③ "첫 실습 열기" 버튼. `LabShell.astro` 좁은 화면 넘침 수정(긴 예제 제목의 선택 상자 — 열 `minmax(0,1fr)`·선택 상자 `min-width:0`). 출처 등록: OpenCV 4.11.0.86(Apache-2.0, `public/licenses/opencv.txt`). 테스트: Playwright 가짜 카메라(`playwright.config.ts` + `tests/e2e/global-setup.ts`가 `scripts/gen-test-video.mjs`로 `.cache/test-camera/synthetic.y4m` 7.4MB 생성 — 커밋 안 함, PD-30). 확인한 사실: Pyodide opencv 휠은 창 백엔드 없이 빌드돼 `imshow`·`waitKey`·`namedWindow`가 `cv2.error "The function is not implemented …"`, `VideoCapture(0)`은 열리지 않음(Node 실제 Pyodide). 검증 수치는 판정표·야간 작업 로그.
  - P2-04 슬라이더 규약(2026-09-16): **조절 값 규약** `src/lab/params/`(`parse.ts` — 들여쓰기 없는 `이름 = 값  # … @slider 최소 최대 간격 …`·`@select 낱말…`·`@toggle` 줄을 읽어 값 글자의 위치·int/float(값·범위·간격에 소수점이 있으면 float)·설명(규약 앞뒤 글)을 뽑고, 틀린 줄(숫자 아님·최소 > 최대·간격 0·범위·선택지 밖·들여쓰기·예약어·겹침·모르는 규약·식)은 한국어 경고; `panel.ts` — 코드가 바뀔 때마다 다시 읽어 같은 요소는 값만 고치고, 조작하면 코드의 그 자리 글자를 바꿔 쓰고(`lab.replaceCode`, 커서·되돌리기 유지) 실행 중이면 `runtime.pushEvent('lab.params', {name, value, type})`). 화면 `src/components/lab/ParamPanel.astro`가 LabShell `panel` 슬롯의 기본 내용이라 모든 실습실·시험 페이지에 들어간다. **파이썬 쪽 반영 훅** `apc_runtime.py`: 워커가 코드를 돌리기 직전 `bind_run_globals(globals())`로 학생 코드의 전역 사전을 알리고(실행 전에 쌓인 값은 버림) 끝나면 `unbind_run_globals()`, `sync_params()`가 입력 확인 지점(`block_on` 뒤·`maybe_yield`)마다 쌓인 값을 형대로 바꿔 전역 변수에 넣는다(바뀔 때 한 번만 — 학생 코드가 반복문에서 같은 변수를 스스로 바꾸는 것을 방해하지 않음, 양보 없음). **예제 머리말 규약** `src/lab/README.md`·`src/lab/controls/example-meta.ts`(`readExampleMeta`: 첫 주석 줄 제목·둘째 줄 설명·`# @lesson slug`·`# @tags a, b`·"바꿔볼 것 3가지"·"왜 이런 결과가 나올까" 상자, 원본 이관 예제에는 머리말 없음 — PD-10 줄 번호). 첫 예제 `first-edge.py`에 `blur_size` 슬라이더·설명·상자 두 개, V4 예제에 `@lesson v4`·상자, 1-1-1 체험 예제에 `@lesson 1-1-1`. 홈 [카메라로 바로 해보기]의 "준비 중" 표시를 뗌(`home-content.ts`), 학생용 시작하기 ③ 문장을 조절 패널 동작에 맞춤. 가짜 카메라 영상에 바탕보다 밝기 20만 높은 희미한 네모를 더해(`scripts/gen-test-video.mjs`, 임계값 20에서만 테두리가 나옴 — 실제 OpenCV 계산 324픽셀) 슬라이더 효과를 잰다. 브라우저 테스트 `tests/e2e/scenario-a.spec.ts`(시나리오 A, 경과 시간 기록)·`lab-params.spec.ts`, 공통 도구 `tests/e2e/helpers/vision.ts`. 검증 수치는 판정표·야간 작업 로그.
  - 병렬 제작 준비(2026-09-16, P2-05~P2-13을 7명이 동시에 만들기 전): **① PD-33 예제 이관 도구** `scripts/import-examples.mjs` + `scripts/lib/import-examples.mjs`(목록·기록 `scripts/examples-manifest.yaml`: zip·멤버·대상·저작자 → 기록 `lines`·`sha256`·`syntax`·`syntax_checker`·`imported`, 주석 보존) + 자체 zip 읽기 `scripts/lib/zip-read.mjs`(중앙 디렉터리·deflate·CRC-32, UTF-8 비트가 꺼진 CP949 이름을 euc-kr로 읽음 — 자료 zip이 모두 그렇다). 원본 바이트를 CRLF → LF만 바꿔 쓰고 파이썬 줄 규칙으로 줄 수 대조, 구문은 이 PC의 파이썬 3.11 `ast.parse`(없으면 Node 가벼운 검사). 1단원 f026~f045 → `examples/vision/u1/<차시>-<주제>.py`(심화는 `-adv-`), opmp f118~f136 → `examples/vision/opmp/NN-<주제>.py`(zip 번호 = 계단 순서), pyautogui f016~f025 → `examples/desktop/NN-<주제>.py`. 제목·설명·차시·쪽·태그·패키지는 **사이드카** `<이름>.meta.yaml`(`src/lab/controls/example-sidecar.ts`, 빌드 전용 yaml 파서)에 두고 코드 파일은 한 글자도 안 고침. `src/lab/vision/examples.ts`가 `vision/`·`desktop/` 두 폴더를 읽고 사이드카 → 머리말 → 파일 이름 순으로 제목을 정하며 `EXAMPLE_GROUPS`(첫 실습·사이트 예제 / 1단원 교과서 실습 / OpenCV·MediaPipe 계단(교안) / 가상 데스크톱(pyautogui))로 `<optgroup>` 묶음(`LabExample.group`, LabShell 렌더링). `npm run examples:verify`가 `npm test`에 들어감. **② 흉내 모듈 폴더 규약** `src/lab/modules/<id>/{manifest.ts, index.ts, *.py, panel.astro?}` — `modules/types.ts`(계약), `modules/manifests.ts`(eager glob + 검사: id=폴더 이름, 이름 `<id>.` 접두, 붙박이·다른 모듈과 겹침·한 패키지 두 모듈 금지), `modules/host.ts`(labId에 맞는 모듈만 `index.ts` 지연 로딩 → `mount(ctx)`; ctx는 manifest에 적은 이름만 허용, dispose 때 훅 해제, `data-lab-modules`), `python/modules.ts`(`../modules/*/*.py`도 묶고 이름 겹침 검사, `SHIM_TABLE`), `apc_shims.register_shims`(워커가 load 때 넘김), `apc_runtime.register_tick_hook`(입력 확인 지점 훅, 양보 금지·재진입 방지), `VisionLab.onFrame`(카메라 프레임 훅)·`getVisionLab`, LabShell이 `panel.astro`를 `[data-lab-module-panel=<id>]`(placement `panel`|`wide`, 새 grid 줄 `modules`)에 hidden으로 그림. 예시 모듈 `hello`(dev 실습실에만: 요청 `hello.greet`, 이벤트 `hello.wave`, 값 `hello.name`, 채널 `hello.clicks`). **다리 수정:** `bridge.beginRun()`이 실행 전에 쌓인 push 값을 모두 버림(학생 코드가 처음 import하는 모듈은 첫 실행의 초기화 함수 등록이 늦어 실행 전 값이 새는 것을 hello 브라우저 테스트에서 발견 — Node 도우미 `pyodide-node-run.mjs`의 키 push를 실행 중(setup)으로 옮김). **③ 미리 설치·등록:** `@mediapipe/tasks-vision` 0.10.35(dependencies, Apache-2.0, 공식 저장소 LICENSE 원문 `public/licenses/mediapipe-tasks-vision.txt`), `workbox-build` 7.4.1(devDependency, MIT, `public/licenses/workbox.txt`), `scripts/vendor-assets.mjs`(`predev`·`prebuild`: WASM 4개 → `public/vendor/mediapipe/0.10.35/wasm/`, `.gitignore` `/public/vendor/`), `sources.yaml`에 Tasks Vision·MediaPipe 모델(`public/models/**`)·Workbox 항목과 Pyodide 예비본 경로(`public/vendor/pyodide/**`), `public/models/.gitkeep`, `scripts/repo-allowlist.yaml` large_files 안내. `idb-keyval`은 넣지 않음(Cache Storage·localStorage 규칙으로 충분). **④ 병렬 검증:** `playwright.config.ts`에 `PW_BASE_URL`(있으면 webServer 없이 그 주소, 하위 경로 검사), 포트 배정 A 4401 … G 4407과 공유 파일 목록·저장 이름 규칙은 `src/lab/README.md` 5절. 문서: `src/lab/README.md` 3~6절(아키텍처 그림·훅 표·메시지 형식·테스트 방법·금지 사항), `CLAUDE.md`, `MAINTENANCE.md` §2, PLAN §8.2 구현 메모.
- **Phase 1 — 프로젝트 뼈대와 첫 배포(2026-09-15~16):**
  - P1-01 저장소·첫 커밋, P1-02 Astro 7.3.2 뼈대, P1-03 배포 파이프라인(`deploy.yml`)과 첫 배포, P1-04 출처 등록부·저장소 검사·`/credits/`, 공통 기반(사이트 지도·주소 도우미·디자인 토큰·공통 레이아웃·Pretendard·콘텐츠 규칙·상자 문법·Playwright·Pagefind) — 야간 작업 로그 참고.
  - P1-05 홈: 큰 버튼 3개(1366×768·375×812에서 스크롤 없이 보이고 Tab으로 닿음, 320×568도 첫 화면 안), 직접 그린 흐름 SVG(동작 줄이기면 멈춘 그림, 움직여도 6초×3회 뒤 정지 + [그림 멈추기]), 카드 4개·원칙 3개.
  - P1-06 배우기: 목록(대단원 4 > 묶음 > 차시 카드 44장, 준비 중·원고 없음·보충 표시), 대단원 4쪽, 차시 틀(SPEC §7.2 8칸, 성취기준·시간·준비물·난이도 뱃지, 예제 코드, 서버 없이 채점하는 퀴즈, 교사용 접기, 이전·다음), 견본 1-1-1(원고 008~012쪽, 삽화 3장 눈 확인·메타데이터 제거)·V4 틀. 새 md 하나로 목록·페이지가 생기는 실험 통과(`content/lessons/u2/2-1-9.md`를 넣어 확인 뒤 지움). 틀 경고는 빌드를 멈추지 않고(PD-35), 두 md가 같은 주소면 빌드 오류. 자리 표시 `::예제`·`::퀴즈`.
  - P1-07 용어사전: 26항목(PLAN 20 + 원고 1단원 6), `/glossary/` 색인·항목 앵커·"나오는 차시", 본문 `:용어[…]` → 굵은 링크 + 툴팁(키보드 초점·마우스, Esc, 터치는 바로 이동). 두 단계 구조: remark는 표시 자리만, 페이지를 만들 때 `GlossaryScope`가 사전을 새로 읽음(Astro 콘텐츠 캐시가 CI에서도 되살아나 remark 단계에 풀이를 넣으면 옛 글이 남기 때문, withastro/action v6 `cache` 기본 true 확인). 샘플 차시 1-1-1에서 Tab → 툴팁 → Enter로 `#agent` 이동을 브라우저 테스트로 확인.
  - P1-08 시작하기 4쪽 + 점검 페이지(10항목 지원·미지원·확인 필요와 한국어 대처, JSPI는 52바이트 시험 모듈 실제 실행, [결과 복사]), 브라우저 권장 환경 안내 `BrowserNotice`(실습실 4곳·학생용·보드 준비).
  - P1-09 스모크·영역별 브라우저 테스트(데스크톱 1366×768·모바일 375×812), 링크 검사 `npm run check:links`(`scripts/check-links.mjs`), 테스트 워크플로 `.github/workflows/e2e.yml`(push·PR, 단위 테스트·타입 검사·Playwright Chromium·링크 검사, 브라우저 캐시 키 = @playwright/test 버전, 실패 보고서 업로드, 배포를 막지 않음).
  - P1-10 `LICENSE`(MIT)·`LICENSE-CONTENT.md`(CC BY-NC-SA 4.0)·README·MAINTENANCE·CONTRIBUTING·이슈 양식 3종, 바닥글·기여 페이지에서 라이선스 파일과 제3자 목록 연결.
  - P1-11 사이트 검색 `/search/`(Pagefind 1.5.2 API로 직접 만든 화면, `?q=`, 10개씩 더 보기, 키보드)와 머리글 검색 상자.
- **Phase 1 검토 반영(2026-09-16, 지적 18건 → 16건 반영):**
  - 안전: 저장소 검사가 래스터 이미지를 추적 파일 어디서든(tests/·.github/·뿌리) 잡고 글·코드 파일 안의 data: 주소 그림(SVG의 CSS url·feImage, 마크다운·Astro의 img)도 눈 확인 기록을 요구한다. 개인정보 검사에 UTF-16 파일 해독, 이메일·전화번호 모양, 문장 속 OneDrive 경로, 해시로만 적은 학교 이름(`scripts/privacy-needles.json`, 만들기 `node scripts/privacy-needle.mjs`)을 더했다(단위 테스트 6건 추가, 검사 시간 0.8초). 배포·테스트 워크플로의 액션을 릴리스 커밋 SHA로 고정하고 풀 리퀘스트도 `check:repo`를 돌린다.
  - 저작권: 1-1-1의 원고 009쪽 삽화 3장을 `third_party`로 등록(출판사 조판 삽화, 권리 귀속 확인 전 — 운영자 할 일 13번)해 `/credits/` 제3자 목록에 보이고 사이트 라이선스에서 뺐다. Pagefind 기본 화면 파일(pagefind-ui·modular·component·highlight, Svelte·bcp-47·adequate-little-templates·mark.js가 묶여 있음)을 빌드 뒤 지워 배포하지 않고(`scripts/prune-pagefind.mjs`), Pagefind MIT 고지 전문을 `public/licenses/pagefind.txt`로 배포해 `/credits/`에서 연결한다. Pretendard 항목의 잘못된 npm 표기를 지웠다. 운영자 자료 항목에 걸린 파일 머리에 저작권·라이선스 표기가 있으면 빌드가 참고 경고를 낸다(PLAN P1-04 완료 기준 문구도 폴더 약속에 맞게 고침).
  - 사용성: 홈의 자리 페이지로 가는 버튼 2개에 "준비 중" 표시, 소개 문장에 과목 이름, 카드의 알약 모양 칸을 쉼표 나열 글자로. 실습실 자리 페이지는 준비 중 상자 안에 둘러볼 곳 링크를 넣고 "사이트를 만드는 두 번째 단계(Phase 2)"처럼 되풀이 없이 쓰며, 차시의 [실습실에서 열기(준비 중)](보조 버튼)로 넘어오면 `?example=`을 읽어 "실습실이 생기면 여기서 열려요"를 보인다. 자리 페이지의 브라우저 권장 환경 안내는 실습실이 생길 때까지 끈다(휴대폰 첫 화면 1/3 차지, 점검 코드 6KB). 용어 툴팁은 위 자리가 있으면 용어 위에 놓는다(읽던 문장의 다음 줄을 가리지 않게). 용어사전 검색 결과는 "픽셀 Pixel — 용어사전" 제목으로 `#pixel`에 바로 가고(Pagefind `sub_results`), 항목의 다른 이름·함께 보면 좋은 낱말·나오는 차시와 출처 페이지의 사용 위치·경로·npm 칸은 색인에서 뺐다. 글자만 200%로 키워도 머리글이 넘치지 않게(nowrap 제거, 검색 상자 줄 바꿈), 바닥글 라이선스 링크 높이 24px 이상, 예제 코드 상자는 `role="region"`으로 이름을 읽힌다.
  - 성능: 글꼴을 받는 동안 보이는 맑은 고딕을 Pretendard와 같은 폭·높이로 맞춘 대체 글꼴 규칙(`src/styles/fonts.css`, size-adjust 86%·85.5%, 세로 지표 override)을 두어 조각이 도착해도 줄이 밀리지 않는다(Edge 153 측정: 한글 위주 문장 폭 차이 1% 안, `line-height: normal` 높이 같음). "3G 3초" 판정 기준(본문 첫 그리기)과 실측값은 PLAN §8.1 구현 메모. 전송량 자체(글꼴 조각 288~614KB)는 P2-05 서비스 워커 사전 캐시와 P6-02에서 다룬다.
  - 미룬 것: main 룰셋(저장소 설정 — 운영자 할 일 14번), 남은 미리 보기 서버 종료(자동 권한 판정이 막음 — 16번), macOS·Linux용 대체 글꼴 지표(미해결 17번), 계정 이름 범위는 문서화만(DECISIONS C10, 15번).
- **Phase 1에서 Claude가 정한 것(근거는 커밋 메시지·파일 머리말):**
  - 검색: 기본 UI 대신 API로 화면을 만듦(한국어 문구·디자인 토큰·접근성·코드 크기). 색인에서 뺀 것: 홈·검색·404 페이지, 준비 중 상자(ComingSoon), 교사용·정답 상자(학생 검색 요약에 지도 글·정답이 보이지 않게), 용어 툴팁 글. 낱말 나열은 가운뎃점 대신 쉼표.
  - 접근성: 마크다운 코드 블록 테마 `github-light-high-contrast`(기본 github-dark 주석 대비 3.05:1 → 모든 코드 글자색 5.04:1 이상, @shikijs/themes 4.4.3 색으로 계산). 점검 페이지 제목을 "학교 네트워크와 브라우저 점검"으로(좁은 화면에서 "·브라우저"로 줄이 바뀌던 문제).
  - 라이선스 범위: 안내 문서(README·MAINTENANCE·CONTRIBUTING·docs/)도 CC BY-NC-SA 4.0(PD-26에 없던 범위), `src/` 컴포넌트 안 인라인 SVG 그림은 코드와 떼어 쓸 수 없어 MIT, 그림 파일(`public/images/`)은 CC BY-NC-SA 4.0.
  - 점검 페이지: 창 너비 1024 CSS 픽셀 미만은 "확인 필요", 브라우저 판별은 `userAgentData` 우선(브랜드가 Chromium뿐이면 UA 글자 — CI Chromium 실측), JSPI는 이름 확인 + 실제 실행, Chrome·Edge인데 JSPI가 없으면 업데이트 안내, 휴대폰·태블릿 Web Serial과 Linux Web Bluetooth는 "확인 필요".
  - `BrowserNotice`는 코드를 실행하거나 보드를 연결하는 페이지(학생용, 보드 준비, 그리고 실습실이 실제로 생긴 뒤의 실습실 4곳)에만 두고 학습 페이지·예제 갤러리·자리 페이지에는 두지 않음(SPEC §9 "학습 페이지는 전부 읽힘", 2026-09-16 검토 반영으로 자리 페이지는 끔).
  - 링크 검사는 테스트 워크플로에서만 돌려 배포를 막지 않음(PD-35와 같은 이유: 새 차시의 링크 하나 때문에 배포가 멈추지 않게, 실패는 Actions에 남음).
  - P1-03 시험 파일 `public/_probe/`와 그 등록부 항목을 지움(응답 형식 확인은 끝났고 Phase 2부터 실제 `.mjs`·`.wasm`이 배포됨).
  - 홈: h1 "보고, 판단하고, 움직이는 인공지능을 만들어요", [카메라로 바로 해보기]만 주요 버튼, 21rem 미만에서 버튼 설명 숨김, 홈은 검색 색인 제외(다른 페이지 요약이라 결과가 겹침).
  - 용어: 없는 낱말은 빌드 경고만(PD-35), 서로 다른 두 항목이 같은 이름이면 빌드 오류. 항목별 개별 페이지 없이 한 페이지 + 앵커.
- Phase 0 산출물: `docs/INVENTORY.md`(자료 20개·코드 158개), `docs/CODE_MAPPING.md`(코드 158개 → 브라우저 실행 매핑, id 누락 없음), `docs/PLAN.md`(정보 구조, 기술 스택, 기술 결정 2건, Phase 1~6 작업 묶음, 위험, 결정 목록 PD-01~PD-37).
- **2차 검토 반영(2026-09-15):** 검토 지적을 하나씩 근거를 확인해 판정하고 세 문서에 반영했다. 주요 내용:
  - 교육과정: 과목 교육과정의 성취기준 15개를 인천광역시교육청 게시물에서 확인해 반영(INVENTORY §9.2), 차시 대응표 초안(PLAN §2.2, PD-21), 진동 모터를 1차 부품으로(PD-36).
  - 예제 이관: 코드 추출 사본은 158개 전부 줄 끝이 손상돼 쓰지 않고 원본 zip에서 옮긴다(PD-33).
  - 공개 안전: 경로 지정 커밋과 저장소 검사(PD-32), 공개 전 문서 점검과 학교명 비공개 기본값(PD-37), BT p5 얼굴 공개 제외, 교사용 자료실은 가린 편집본 PDF(PD-31).
  - 라이선스: 사이트 소프트웨어 MIT / 학습 자료·예제 CC BY-NC-SA 4.0 / 제3자 자료 제외(PD-26), `buzzer.py` 게임 음악 배열 제외, Apache·LGPL 고지 조건 보강.
  - 실습 범위: iPad·휴대폰용 블록 전용 호환 모드(PD-27)와 SPEC 이탈 기록, 카메라 없는 PC용 합성 랜드마크 재생(PD-30), 시나리오 B 입력은 터치 센서(PD-34), f113 레이저 끄기 논리 새로 추가(PD-23), MQTT 안전 규칙(PD-29).
  - 누락 묶음 추가: P1-11 사이트 검색, P3-11 실물 점검 도우미, P4-10 ESP32 통신 템플릿·블록, P4-11 예제 갤러리.
  - 외부 사실 갱신: Android·Firefox Web Serial, Pages 권한(`contents: read`)·REST·한도, 개인정보 안내의 제3자 처리(GitHub Pages·jsDelivr), CH340 자동 설치 가능성, `time_pulse_us`·UART 기본값, mediapipe 판 순서, BroadcastChannel 같은 출처, Teachable Machine 의존성.
- 운영자 결정 기록 `docs/DECISIONS.md`(O1~O8, C1~C9), 야간 규칙 `docs/OVERNIGHT.md`, `CLAUDE.md`, 원본 폴더를 뺀 `.gitignore`(이번 검토에서 고치지 않음).
- 환경: Node.js 24.19.0, GitHub CLI 로그인, 공개 저장소 생성(2026-09-15 확인: 파일 0개, Pages 꺼짐), 비공개 자료 저장소 업로드, Astro 7.3.2 최소 프로젝트가 현재 한글·공백 경로에서 빌드됨.
- 핵심 기술 결정: 실행 중 입력 전달은 JSPI(브라우저가 파이썬을 잠깐 멈췄다 이어 실행하는 기능, PD-01), Pyodide 314.0.7은 jsDelivr + 같은 사이트 예비본(PD-02), MediaPipe Tasks Vision 0.10.35 고정(PD-03), 가상 보드 1차 부품·예제별 배선(PD-05), 브릿지 규약(PD-06).

### Phase 2 — 영상처리 실습실 (2026-09-16 완료)

- **P2-01~P2-04:** 파이썬 워커(모듈 워커 + JSPI, 정지 2단계), 코드 에디터(CodeMirror 6, 자동 저장·공유 링크·내려받기·기록 지우기), 카메라·창 흉내(`apc_cv2.py`)와 첫 실습, 조절 값 규약(`# @slider`·`@select`·`@toggle`).
- **P2-05 로딩·캐시:** 같은 사이트 Pyodide 예비본 + 서비스 워커(사전 캐시는 셸만), 단계별 진행률·1분 개념 카드·[미리 받기], 점검 페이지 네트워크 항목, `?sw=off` 비상구.
- **P2-06 오류 사전:** 데이터 한 파일로 실습실 카드와 `/help/errors/`(47항목), 학생 코드 줄 찾기·줄 강조·짧은 트레이스백.
- **P2-07 보충 차시 V1~V5:** 예제 5개·차시 5쪽·직접 그린 SVG 5장, 용어사전 4항목(색공간·채널·블러·윤곽선) 추가.
- **P2-08·09 mediapipe:** 손·얼굴·자세 흉내와 합성 재생 입력(카메라 없이도 실습), 모델은 같은 사이트에서만 받는다.
- **P2-10 러너 공통:** 가상 파일(`mask.png`)·저장 파일 내려받기·한글 글꼴 경로 연결·이름 가림 경고·콘솔 접기.
- **P2-11·12 가상 데스크톱:** 가상 모니터·메모장·그림판·가상 브라우저·미니게임 3종·`screenshot()`.
- **P2-13 음성 인식 흉내:** 글자 입력 기본, 교사용 `/settings/`에서 켤 때만 서버 인식 선택지.
- **P2-14 통합:** 공유 파일 요청 판단·반영, 차시 실습실 임베드, 이관 예제 49개 스모크 테스트, 첫 방문 전송량·시간 측정.
- **검증(2026-09-16 운영자 PC):** Vitest 71파일 834건, `astro check` 0오류, 빌드(출처 검사 파일 270개·항목 23개, 번들 npm 15개), 링크 검사 2,097개, 저장소 검사, Playwright 전체 통과(데스크톱 1366×768·모바일 375×812).
- **검토 반영(2026-09-17):** 적대적 검토 26건 반영 — 실습실 흐름(준비 중 [실행] 예약·결과/오류로 화면 옮기기·준비 패널 위치·모듈 패널은 쓸 때만·Tab·44px·카메라 거부 이유·안쪽 용어), 차시↔실습실 오가기(차시 링크·없는 예제 알림·임베드 높이·링크 검사), 오류 사전 접기·찾기, 음성 설정 글·`processLocally` 확인, 서비스 워커 SHA-256 대조, 재배포 휠 고지(numpy·opencv-python), 출처 항목 바로잡기, 3·4단원 PC 쪽 예제 8개 이관. 검증은 판정표 "Phase 2 검토 반영" 행.

## 진행 중

**Phase 4 통신 실습실 — P4-01 브릿지 핵심과 병렬 제작 준비까지 끝(2026-09-18). 다음은 여섯 구역 A~F 병렬(README 5.2).**

- **병렬 제작 준비(2026-09-18):** 통신 예제 **30개**를 한 번에 옮기고(3단원 f082~f089·4단원 f098~f116·교안 f137·f147~f149·f157·f158·HW f002), 구역이 나중에 못 고칠 **공유 파일의 자리**를 모두 열어 두었다 — 보드 이벤트 `board.uart.tx`, machine 확장 자리 `ext/ble/`·`ext/network/`(+`network` 한국어 자리 안내, `NOT_YET_MODULES`에 `umqtt`·`esp32_ble_util`), 오류 사전 `comm` 묶음(항목 4개), 예제 갤러리 태그 규약 `src/lab/gallery/facets.ts`(단원·난이도·가상 보드 가능·통신 방식·부품·낱말 — 차시 md가 먼저, 없으면 사이드카). MQTT.js 5.15.2는 공식 저장소 LICENSE로 MIT를 확인해 등록·설치했다(고지 `public/licenses/mqtt.txt`에 의존성 44개 원문). 옮긴 코드에 박혀 있던 **BLE 주소 5곳은 자리표시자로 가렸다**(미해결 130). 구역·포트(A 4701 … F 4706, 2차 G 4707·H 4708)·공유 파일·사이드카 주인은 `src/lab/README.md` 5.2.
- **P4-01 브릿지 핵심(2026-09-18):** PLAN §7.2(PD-06)·§7.4(PD-29)·§7.6을 규칙마다 단위 테스트가 있는 코드로 옮겼다(`src/lab/bridge/`, 규약은 README 9절). 통로는 `direct`(같은 탭)·`tab`(BroadcastChannel) 둘이고, MQTT·BLE·Web Serial은 `registerBridgeChannel`로 끼운다.

**Phase 3 ESP32 실습실 — 검토 반영까지 끝(2026-09-18).**

- 끝나서 `main`에 있는 것: P3-00(엔진 판정 PD-04 유지), P3-01 가상 보드 핵심, P3-02 보드 그림·첫 부품, 병렬 제작 준비, 여섯 구역 P3-03~P3-10, 통합 P3-11, **검토 반영**.
- 검토 반영에서 한 일: 지적 26건을 하나씩 근거를 확인해 판단 → 24건 반영(2건은 파일 내려받기가 필요해 운영자 할 일) → 빌드·타입·단위·링크·브라우저 전체 검증 → 경로 지정 커밋·push → 배포·테스트 워크플로 성공 → 실사이트 확인·실사이트 시나리오 B.
- **남은 일은 실물 보드뿐이다:** 부록 B-2 4~6·9~30번(운영자 할 일 2번). `/labs/esp32/check/`가 항목마다 안내하고 [결과 복사]로 이 문서에 붙일 글을 만들어 준다. 굽기·드라이버는 `/start/board/`에서.
- 파일을 받아야 끝나는 것 두 가지: ① 펌웨어 고지 전문(운영자 할 일 19번, 미해결 125) ② 실물용 OLED 드라이버 `ssd1306.py`(미해결 64 — 키트 OLED 칩 확인 뒤).
- **마무리 검증(2026-09-18 10시 50분 KST):** 로컬 브라우저 전체 **393 통과·223 건너뜀·0 실패·0 흔들림(32.7분)**, 단위 147파일 1,489건 + Container 7건, `astro check` 566파일 0오류, 링크 2,088개, 저장소 검사 추적 파일 1,033개. CI 테스트 35293745594·배포 35293745626·저장소 검사 35293745653 모두 성공. 실사이트 주요 주소 200과 시나리오 B 통과.
- **검토 도중 찾아 고친 것 하나(회귀):** 편집칸이 줄을 접자 문서가 길어져 CodeMirror 가상 스크롤이 화면 밖 줄을 DOM에서 뺐고, DOM으로 편집칸 글을 읽던 브라우저 테스트 도우미가 깨졌다(2건 실패). 도우미가 CodeMirror 문서를 직접 읽게 고쳤다. 또 "콘솔에 결과가 나왔어요" 알림이 결과 칸 맨 아래라 화면 밖이던 것을 실사이트 캡처로 발견해, 보드 그림과 함께 보이도록 고치고 테스트도 "화면 안에 있는지"로 바꿨다(dba6210).

### 2026-09-18 07시 15분 — 운영자 계정 전환으로 정지 (검토 단계 도중)

운영자가 사용량 한도 때문에 **다른 Claude 계정으로 갈아타기로** 해서, 검토 3명 중 1명이 끝난 자리에서 워크플로(`wf_86bbf998-bc6`)를 멈췄다. 이어받는 순서는 `docs/HANDOFF.md`, 멈춘 자리의 자동 기록은 `.cache/resume/STATE.md`(10분마다 갱신).

- **끝난 검토:** `review:safety`(안전·저작권·개인정보). 결과를 `.cache/phase3-reviews/finished-reviews.md`에 옮겨 두었다(git 제외, 이 노트북). 재개하면 이 검토자는 캐시에서 돌아오므로 다시 돌지 않는다. 판정 대부분 통과, **못 지킨 것 2가지**: ① [보드에 저장]이 확인 창·백업 없이 `main.py`를 덮어씀 ② 저장소 검사 우회 구멍.
- **멈춘 검토 2명:** `review:criteria`, `review:ux` — 결과 전에 멈췄으므로 재개하면 처음부터 다시 돈다(약 40분). 커밋은 하지 않는 단계라 잃는 것은 시간뿐이다.
- **다음:** 워크플로 재개 → 검토 2명 다시 → `fix:final`이 세 검토 결과를 반영 → Phase 3 완료 → Phase 4 워크플로 새로 작성.
- **push가 멈췄던 일(원인 확인·해결됨):** 07시 무렵 두 번, Git Credential Manager(`credential.helper=manager`)가 `credential-manager get`에서 응답 없이 멈춰 push가 진행되지 않았다. **원인은 GCM이 운영자의 승인을 기다리고 있었던 것**이고(그 창은 Claude 화면에 보이지 않는다), 운영자가 승인한 뒤(07:25) 기본 설정 그대로 `git fetch` 0.6초·`git push` 정상으로 돌아왔다. 멈춘 동안에는 `gh` 로그인(keyring, scopes repo·workflow)으로 `git -c credential.helper= -c credential.helper='!gh auth git-credential' push origin main`처럼 우회해 올렸다(45328d5·b85e7d0). **다시 멈추면 먼저 운영자에게 승인 창을 확인해 달라고 하고**, 급하면 위 우회 명령을 쓴다. 멈춘 `git.exe`·`git credential-manager` 프로세스는 종료해도 된다.

### 2026-09-18 07시 40분 — 새 계정·새 세션에서 이어받음 (검토 재개)

운영자가 다른 Claude 아이디로 로그인해 "사이트 작업 재개"라고 했고, `docs/HANDOFF.md` 순서대로 이어받았다. 환경은 그대로였다(`node_modules`·`public/vendor`·원본 추출본·커밋 전 훅·절전 방지·10분 자동 기록 모두 살아 있음). 작업 트리 깨끗, `main` = `origin/main`.

- **통째 재개는 실패했고 즉시 멈췄다.** `resumeFromRunId: 'wf_86bbf998-bc6'`로 그대로 재개하니, 끝나서 **이미 커밋·배포까지 된** `build:E2`(실제 보드 실행·저장)와 `build:F2`(보드 준비 페이지)가 **새 캐시 열쇠로 다시 돌기 시작**했다. 이어 붙인 단계라 프롬프트 안에 앞 단계 결과 JSON이 박혀 있어 열쇠가 달라진 탓이다. 바로 `TaskStop`했고 **작업 트리는 손대기 전이라 깨끗했다**(확인함). 이 실측은 `docs/HANDOFF.md` 4번 "주의"에 적었다(커밋 bec08b5).
- **대신 남은 일만 담은 워크플로를 새로 썼다 — `wf_db55fe07-fd6`.** 끝난 14단계 결과는 진행 기록의 `journal.jsonl`에서 꺼냈고, 옛 스크립트의 머리(`COMMON`까지)와 args를 글자 그대로 옮겨 에이전트가 받는 맥락이 달라지지 않게 했다. 구성: 검토 `criteria`·`ux` 2명 병렬(포트 4611·4612) → `fix:final`. 앞 세션에서 끝난 `safety` 검토 결과는 스크립트에 상수로 박아 넘겼으므로 세 검토가 모두 수정 단계에 들어간다.
- **이 워크플로는 통째 재개해도 안전하다**(이어 붙인 제작 단계가 없고, `fix:final`은 중간에 멈췄다 다시 시작되는 것을 전제로 쓰여 있다). 재개에 필요한 것은 모두 제자리에 두었다 — 스크립트 사본 2곳(세션 `workflows\scripts\`, 안전 사본 `.cache\resume\`), args는 `.cache\resume\RESUME-ARGS.json`, 자동 기록 `.cache\resume\STATE.md`가 run id·스크립트 경로·단계별 시각을 10분마다 갱신한다.
- push는 정상으로 돌아왔다(깃 인증 승인 창 안 뜸, bec08b5).

### 2026-09-19 00시 05분 — Phase 4 진행 위치 (한도 대비 기록)

워크플로 `wf_26c7dddc-2bf`. **Core 2단계는 커밋·push 끝**(HEAD ba93a0e, origin과 같음). 2026-09-18 15시 26분에 한도로 15개 단계가 한꺼번에 죽었다가 21시 55분에 재개했고, 재개 뒤 **Build 8단계가 모두 끝났다**.

- **끝난 구역(보고는 `.cache/phase4-notes/`):** A 영상처리↔보드, B 가상 BLE, B2 Web Bluetooth, C Web Serial 데이터 포트, D MQTT·탭 통로, D2 대시보드, E 통신 템플릿·블록, F 예제 갤러리. 공유 파일 변경 요청 6건이 `.cache/phase4-requests/`에 있다(ble·gallery·mqtt·serialport·templates·vision).
- **지금 도는 것:** G 시나리오 F 예제, H 4단원 통합 화면. 그다음 통합 → 검토 3 → 수정.
- **커밋 안 된 작업 79개 파일**(구역은 커밋하지 않는다 — 통합 단계가 커밋). **`git clean`·`git checkout .` 금지.** 만일에 대비해 `.cache/resume/phase4-wip-snapshot.tar.gz`(199항목)에 사본을 떠 두었고 목록은 같은 폴더의 `phase4-wip-files.txt`다.
- **끊겨도 싸게 이어진다:** 끝난 8단계는 워크플로 캐시에 결과가 있어 재개하면 즉시 돌아온다. 재개 방법은 `docs/HANDOFF.md` 4번(run id·스크립트 사본·args 위치).

### 2026-09-19 00시 30분 — 운영자 요청으로 작업 정지 (내일 이어서)

Phase 4 워크플로 `wf_26c7dddc-2bf`를 멈췄다. 돌던 두 단계(G 시나리오 F 예제, H 4단원 통합 화면)는 결과 전이라 재개하면 처음부터 다시 돈다. 남은 개발 서버와 에이전트 프로세스는 모두 정리했다(4300~4800 포트에 듣는 서버 없음).

**어디까지 됐나 (10단계 끝)**

- Core: P4-01 통신 브릿지 핵심, 병렬 준비 — **커밋·push 끝**(origin과 같음).
- Build 8개: A 영상처리↔보드, B 가상 BLE, B2 Web Bluetooth, C Web Serial 데이터 포트, D MQTT·탭 통로, D2 대시보드, E 통신 템플릿·블록, F 예제 갤러리 — **워크플로 캐시에 결과가 있어 재개하면 즉시 돌아온다.** 보고 8개는 `.cache/phase4-notes/`, 공유 파일 변경 요청 6건은 `.cache/phase4-requests/`.
- 남은 것: G·H → 통합(요청 판단·전체 검증·시나리오 D·F·커밋·배포) → 적대적 검토 3 → 수정.

**작업이 날아가지 않게 해 둔 것 (세 겹)**

1. **원격 백업 가지 `wip/phase4-20260919`**(커밋 `2ca339e`) — 구역이 만든 파일 190개·27,500줄을 올렸다. `main`에는 넣지 않았고 배포·테스트 워크플로는 `main` push에서만 도니 사이트에 영향이 없다. 이 노트북이 통째로 없어져도 여기서 되살린다.
2. **이 컴퓨터 사본** `.cache/resume/phase4-wip-snapshot.tar.gz`(223항목, 목록 `phase4-wip-files.txt`)와 `.cache/resume/phase4-notes`·`phase4-requests` 복사본.
3. **작업 폴더의 파일 89개 그대로**(커밋 안 됨 — 통합 단계가 커밋할 몫). **`git clean`·`git checkout .` 금지.**

**백업에서 뺀 파일 1개:** `tests/unit/mqtt/brokers.test.ts`. 저장소 검사가 64번째 줄을 이메일 주소 모양으로 보고 막았다(아이디·비밀번호가 들어간 wss 주소라 골뱅이표가 들어간다) — "아이디·비밀번호가 든 주소는 막는다"를 확인하는 **시험용 값이라 실제 개인정보가 아닌 가짜 양성**이다. 허용 목록(`scripts/repo-allowlist.yaml`의 `privacy_exceptions`)에 넣을지는 맥락을 아는 통합 단계가 정한다. 파일 자체는 작업 폴더와 위 2번 사본에 그대로 있다.

**내일 이어받기:** `docs/HANDOFF.md` 4번대로 `Workflow({scriptPath: ".cache/resume/phase4-comm-lab-wf_26c7dddc-2bf.js" 또는 세션 사본, resumeFromRunId: "wf_26c7dddc-2bf", args: .cache/resume/RESUME-ARGS.json})`. 끝난 10단계는 캐시에서 돌아온다.

### 2026-09-24 17시 50분 — Phase 4 재개 (5일 만)

`docs/HANDOFF.md` 순서대로 확인: 작업 파일 89개 그대로, 로컬 = origin(ed31266), 백업 가지 `wip/phase4-20260919` 있음, 원본 추출본·스크립트 사본 3개(같은 내용)·진행 기록 모두 있음, 같은 세션. 다른 세션이 이 폴더에서 일하고 있지 않음을 확인(돌고 있는 node·git·개발 서버 없음). 자동 기록·절전 방지 도우미를 다시 띄우고(절전 방지 2026-09-25 05:46까지) `resumeFromRunId: 'wf_26c7dddc-2bf'`로 재개했다. **재개 뒤 새로 시작된 단계는 G 시나리오 F 예제·H 4단원 통합 화면 둘뿐이고, 끝난 10단계는 캐시에서 돌아왔다**(진행 기록에서 재개 뒤 붙은 줄만 세어 확인).

- 모델이 Opus 5.5로 바뀌었다. 워크플로 공통 프롬프트의 커밋 서명(Opus 5)은 고치면 캐시가 모두 무효가 되므로 그대로 두고, **커밋하는 두 단계(통합·수정)의 프롬프트에만** Opus 5.5 서명으로 덮어쓰는 줄을 넣었다.

## 다음 할 일 (순서대로 — 끝난 Phase 3은 `docs/PLAN.md` §8.3, 다음 Phase 4는 §8.4)

1. ~~**P3-00 (실험) MicroPython WASM 포트**~~ **끝남(2026-09-17): PD-04 유지** — 판정·근거는 미해결 42, 비교·다시 볼 조건·**가상 보드가 지킬 MicroPython 차이 표 14항목**(ticks 넘침 2**30·epoch 2000·단정밀도 float·errno 번호·u-이름·`const` 등)은 PLAN §8.3 P3-00 구현 메모. P3-01은 그 표의 "흉내" 항목을 보드 흉내 모듈 단위 테스트로 확인한다. 협조적 정지(`bridge.ts`)·동기 진입점 규칙(미해결 25)·흉내 모듈 폴더 규약은 Pyodide를 그대로 쓰므로 바뀌지 않는다.
2. ~~**P3-01 가상 보드 핵심**~~ **끝남(2026-09-17, 완료 기준 재확인·CI 테스트 35204663018 성공 — 작업 로그 맨 위)** — ESP32 실습실(`LabShell labId="esp32"` + `BoardIo.astro`), 보드 흉내 모듈 `src/lab/modules/board/`(모듈 폴더 이름은 예시로 적었던 `esp32-board` 대신 `board` — 미해결 43), `machine.Pin`·`Timer`·MicroPython판 `time`(import 훅 — 미해결 44)·`micropython.const`·u-이름·errno·bluetooth 자리, 가상 시계와 입력 확인 지점(미해결 45·46), 코드가 끝난 뒤 대기(미해결 47), 실습실별 파이썬 모듈·준비 범위(미해결 48), 메시지 `board.*`(미해결 49), 부품 레지스트리 `parts/<부품 id>/`(보드에 붙은 내장 LED·BOOT 버튼), 오류 사전 `board` 묶음, 사이트 예제 3개(미해결 50). 규약은 `src/lab/README.md` 7절, 구현 메모는 PLAN §8.3.
3. ~~**P3-02 보드 그림과 첫 부품**~~ **끝남(2026-09-17, CI 테스트 35215361881·배포 35215361903 성공 — 판정표 "P3-02" 행)** — 30핀 개발 보드 그림(스트래핑 핀 ▲·핀 강조·[그림 크게 보기]), 부품 `touch-digital`·`vibration-motor`(GPIO19 사이트 배정 — 미해결 7), 예제별 배선(차시 md·사이드카·머리말 `# @part` — 미해결 53)과 브레드보드 배선도(미해결 51), 배선 오류 한국어(화면 14종·파이썬 5종 — 미해결 54), 원본 예제 f046·f015·f052·f053 이관(f052는 P3-04까지 ImportError — 미해결 52), 진동 알림 새 예제, PWM 세기 자리(미해결 55), 부품별 단위 테스트 파일 규칙·부품 id 약속(미해결 56, README 7.5), 조절 패널 안내 글을 실습실 공통 문장으로. 규약은 `src/lab/README.md` 7.4·7.5, 구현 메모는 PLAN §8.3.
4. ~~**병렬 제작 준비 → P3-03~P3-10 여섯 구역 병렬 → P3-11 통합**~~ **끝남(2026-09-18 — 판정표 P3-03~P3-11 행, 작업 로그 맨 위)**. 아래는 그때 정한 규약이라 다음 부품·블록·차시를 더할 때 그대로 쓴다: 구역 표·공유 파일·사이드카 주인은 `src/lab/README.md` 5.1, 부품은 7.9 체크리스트, 확장 자리는 7.10(PWM `BOARD.set_pwm`, 아날로그 `read_millivolts`, 부품 장치 `register_part`·`set_device_state`·`on_device_input`, 조작 칸 `controls`, 소리 `sound`, 보드 라이브러리 `examples/esp32/lib/`, machine 확장 `ext/<기능>/`, 블록 호환 `wait_ns_async`, 실행 대상 `lab.setRunTarget`), 모의 시리얼은 8절. PLAN §8.3 표 순서(PWM·ADC 부품 → I2C 표시 장치 → 네오픽셀·UART·MP3 → Blockly 블록 모드와 블록 전용 호환 모드 실험 → Web Serial·raw REPL → 보드에 저장 → 펌웨어 굽기 → 보드 준비 페이지 → 시나리오 B와 실물 점검 도우미). Blockly 13.3.0·esptool-js 0.6.1은 설치·등록 끝(스텁 제외 PD-38). machine 주변장치(PWM·ADC·I2C·UART·RTC·`time_pulse_us`)는 `src/lab/modules/board/ext/<기능>/apc_board_<기능>.py` + `register_machine_export` 확장으로, 부품은 `parts/<부품 id>/` 폴더로 더한다(`src/lab/README.md` 7.5·7.6·7.9). 통합 때: 운영자가 받아 둔 펌웨어(`.cache/firmware-staging/`)·모델(`.cache/models-staging/`) 배치와 등록(`.cache/phase3-requests/main-session-operator-checks.md`), 구역 요청의 공유 파일 변경, 두 구역에 걸친 예제의 사이드카 smoke·"준비 중" 문장 맞추기.
5. ~~**Phase 3 검토 → 수정**~~ **끝남(2026-09-18 — 판정표 "검토 반영" 행, 구현 메모는 PLAN §8.3 "P3-11 검토 반영").** 검토 3명의 지적 26건을 판단·반영했다. 검토가 짚었지만 **다음 묶음으로 미룬 것**: ① 실물 보드 확인(부록 B-2 4~6·9~30번 — 운영자 할 일 2번) ② WebKit·iPad·Android 블록 전용 호환 모드 실측(미해결 91) ③ 영상처리 카메라 고르기·검은 영상 감지(미해결 121) ④ 실물용 OLED 드라이버(미해결 64) ⑤ 펌웨어 고지 전문(운영자 할 일 19번, 미해결 125) ⑥ 차시(P5)가 생기면 배선을 차시 md에서 읽는 길을 실제 차시로 확인.
6. **지금: Phase 4 통신 실습실(PLAN §8.4 P4-01~P4-11).** ~~P4-01 브릿지 핵심~~ **끝남(2026-09-18 — `src/lab/bridge/`, README 9절)**, ~~병렬 제작 준비~~ **끝남(2026-09-18 — 예제 30개 이관, 확장 자리, MQTT.js 등록·설치, 구역 표 README 5.2, 미해결 128~131)**. 다음은 여섯 구역 병렬(A 영상처리 → 가상 보드 4701 · B 가상 BLE 4702 · C 실제 기기 연결 4703 · D MQTT·탭 통로 4704 · E 대시보드 4705 · F 통신 템플릿·블록 4706)과 2차 G(시나리오 F·4단원 통합, 4707)·H(예제 갤러리, 4708). 아래는 원래 순서 — **P4-01 브릿지 핵심**(§7.2 규칙: 길이 검사·초당 10회·상태는 최신 값/이벤트는 대기열·원시 바이트, §7.6 원본 코드용 병합 규칙, 통로 인터페이스 — Vitest), **P4-02 영상처리 → 가상 보드**(PC 쪽 `serial` 흉내 + 탭 사이 통로 → f084·f085 ↔ f082·f083이 고치지 않고 짝지어 돈다. **설계는 이미 적어 두었다 — PLAN §8.4 "설계 메모(P3-05에서 미리 검토)"**: 컴퓨터 쪽은 USB-UART 변환기 부품의 흉내가 그대로 맡고, PC → 보드는 `board.device.input {kind:"send"}`, 보드 → PC는 새 이벤트 `board.uart.tx`를 더해 `BroadcastChannel`로 잇는다), **P4-03 가상 BLE**, **P4-04 Web Bluetooth**, **P4-05 Web Serial 데이터 포트**, **P4-06 MQTT·탭 통로**, **P4-07 대시보드**(시나리오 D), **P4-08 시나리오 F 예제**, **P4-09 4단원 통합 화면**, **P4-10 ESP32 통신 템플릿·통신 블록**, **P4-11 예제 갤러리**(시나리오 E 축소판). Phase 3이 남긴 규약을 그대로 쓴다: 보드 메시지 이름은 `board.`로 시작하고 manifest에 먼저 적는다(지금 일곱 개), 부품 = 폴더 하나(README 7.5·7.9), 블록 = 도구 상자 한 칸 + 생성기(7.11), 실행 대상 `lab.setRunTarget`(8.5).
7. **Phase 2에서 넘어온 일:** ① **f084·f085**(3단원 PC 쪽, `import serial`)를 P3-05 가상 UART·P3-07 Web Serial이 생기면 옮겨 확인한다(미해결 39 — P2-09·P2-10 표의 남은 완료 기준) ② 1-2-1 차시 md가 생기면 보충 V4 끝의 "다음은 무엇을 배울까" 링크를 차시 링크로(P5-05) ③ 모델 파일이 들어오면(운영자 할 일 17) 실제 웹캠에서 손·얼굴·자세 인식과 임계값 예제(f035·f037·f096·f097 EAR 0.1)의 체감을 본다 ④ (끝남) CI의 1 flaky(`lab-desktop2` 움직임 줄이기)는 키를 누른 직후 표시를 한 번만 읽던 검사 탓이라 고쳤다(95e2a2a, 다음 CI 0 flaky).

**모든 묶음의 공통 규칙(PLAN §8.0):** 커밋은 경로 지정, 예제는 원본 zip에서 옮기고 줄 수·구문 대조(추출 사본 `extracted/flat` 금지), 원고·교안 이미지는 한 장씩 눈 확인 기록, 테스트 랜드마크는 합성만 커밋, 코드를 바꾼 묶음은 push 전 Vitest·Playwright 통과.

### 클라우드 세션이 이어받을 때의 전제 (모두 갖춰졌을 때만 `docs/OVERNIGHT.md` 절차를 적용)

1. P1-01로 문서가 원격 `main`에 있다(2026-09-15 충족).
2. 운영자 할 일 10번(claude.ai GitHub 연결)이 끝나 저장 실패(HTTP 401)가 해결됐다.
3. OVERNIGHT §0-1의 종료 시각이 `2026-09-15T23:00:00Z`로 고정되어 있어 이후 실행은 첫 단계에서 끝난다 → 루틴을 다시 켜기 전에 운영자 확인 아래 로컬 세션이 그 줄을 그날 밤 시각으로 고쳐 커밋한다(이번 검토에서는 OVERNIGHT를 고치지 않음).
4. `docs/SPEC.md`가 원격에 없으면(운영자 할 일 7번 답 전) 클라우드 세션은 SPEC을 읽을 수 없다 → 7번 답 뒤에 켜거나, DECISIONS·PLAN만으로 할 수 있는 묶음으로 제한한다.
5. OVERNIGHT §1은 코드 위치로 `extracted/flat`을 적지만 줄 끝이 손상돼 있으므로 PLAN PD-33 규칙(원본 zip `originals/`에서 옮김)을 따른다.
6. Pages는 켜져 있다(2026-09-15 운영자 PC의 `gh`로 켬, 충족). 다시 꺼지면 워크플로만 두고 여기에 "운영자 PC에서 Pages 켜기 필요"를 적는다(`configure-pages`의 `enablement`는 `GITHUB_TOKEN`으로 안 됨, action.yml 확인).

## 미해결 결정·확인 사항 (Claude 몫, 모두 작업을 막지 않음)

| # | 항목 | 지금 기본값 | 언제 |
|---|---|---|---|
| 1 | 기술 확인: tasks-vision 0.10.35 모듈 워커 실행(P2-08). **Node에서 Pyodide `run_sync`는 확인 끝(2026-09-16, P2-01):** Node 24.19.0에서 `--experimental-wasm-jspi`(V8 옵션 이름 그대로, 기본 꺼짐 `--no-experimental-wasm-jspi`)를 주면 `WebAssembly.Suspending`·`promising`이 생기고(CI의 Node 24.20.0은 플래그 없이도 켜져 있어 끄는 쪽도 `--no-experimental-wasm-jspi`로 명시한다), 실제 Pyodide 314.0.7에서 `pyodide.ffi.can_run_sync()`가 참이며 `run_sync`로 sleep·input·정지 신호·`exit()`가 설계대로 돈다(`tests/unit/lab/pyodide-node.test.ts`, 플래그 없이는 제한 모드로 돌아 한 번 실행되는 코드만 됨). (glob 로더가 루트 `content/`를 읽는 것은 공통 기반 단계에서 실제 빌드로 확인함, 2026-09-16) | PLAN 기본안 | P2-08에서 |
| 2 | Android Chrome·iPad Safari의 JSPI·Web Serial 실측 | 기기가 없어 P1-08에서 "확인 필요"로 남김. MDN browser-compat-data(2026-09-16)는 `WebAssembly.Suspending`을 chrome_android·safari·safari_ios 모두 false로 적는다. 점검 페이지는 휴대폰·태블릿의 Web Serial도 "확인 필요"로 보인다 | 운영자 할 일 3번 결과가 오면 |
| 3 | 블록 전용 호환 모드(PD-27) 채택 여부 | **조건부 채택(2026-09-18, P3-06)** — 블록이 만든 코드를 고치지 않고 돌릴 때만 줄 수가 같은 await 실행판을 보내고, 코드 모드에서 고친 코드는 제한 모드 그대로다. Node 실제 Pyodide(JSPI판·제한 모드판)와 Edge `?limited=1`에서 확인([정지] 62ms). **진짜 JSPI 없는 브라우저(WebKit·iPad·Android)는 확인 필요**(미해결 91) | 실측 뒤 다시 |
| 4 | 차시 ↔ 성취기준 대응표 확정(PD-21) | 초안 | P5 차시 작성 때(운영자 할 일 5번 반영) |
| 5 | 휠에 함께 묶인 라이브러리의 고지(PD-19, FreeType·IJG 등) | **재배포하는 휠(numpy·OpenCV — 같은 사이트 예비본 `public/vendor/pyodide/`)은 해결(2026-09-17):** 휠 안 고지 원문을 모두 실었다 — numpy `dist-info/licenses/` 17개(최상위 LICENSE.txt는 `numpy.txt`, 나머지 16개는 `numpy-3rd-party.txt`, 파이썬으로 휠과 대조해 전부 포함 확인), OpenCV `LICENSE-3RD-PARTY.txt`(`opencv-3rd-party.txt`, FFmpeg LGPL-2.1+ 포함)와 패키징 MIT `LICENSE.txt`(`opencv-python.txt`, OpenCV 항목 license `Apache-2.0 AND MIT`). **받기만 하는 휠(Pillow 등 — 학생 브라우저가 jsDelivr에서 직접 받고 사이트는 재배포하지 않음)**은 Pillow MIT-CMU 고지(`pillow.txt`)만 싣고, 휠 안 구성요소(FreeType·libjpeg 등) 목록은 아직 확인하지 않았다 | P6-04(받기만 하는 휠을 예비본에 더하게 되면 그때 같은 방식으로 고지를 싣는다) |
| 6 | `ESP32BLE.py`가 2black0 구현(MIT)과 같은 코드인지 → MIT 고지 여부 | 제3자 항목으로 등록, 사이트 라이선스 제외 | P4-03 |
| 7 | 진동 모터 사이트 배정 핀 | **해결(P3-02, 2026-09-17): GPIO19.** 근거: ① Espressif ESP-IDF GPIO 문서(ESP32, v6.1, 2026-09-17 확인)의 표에서 GPIO19는 설명 칸이 비어 있다 — 스트래핑 핀(0·2·5·12·15), JTAG(12~15), SPI0/1 플래시·PSRAM(6~11·16·17), 입력 전용(34~39)이 아니다(13·14는 JTAG라 뺐다) ② 교과서·교안 자료 코드 158개가 쓰는 핀(0·2·4·5·12·15·16·17·18·21·22·23·25·26·27·32·33, 주석 속 1·3)에 없어 어느 차시 배선과도 겹치지 않는다(2-2-1 차시는 버저 15·터치 17) ③ 원고 118쪽 키트 사진에서 확장 보드 3색 헤더 줄과 VSPI 헤더에 19번이 나와 있다 ④ 30핀 개발 보드 아래 줄이라 배선도 선이 보드를 돌지 않는다. 부품 기본 핀(`parts/vibration-motor/part.ts` `defaultPins`)이고, 그 핀 그대로 쓰면 배선 목록에 "사이트가 정한 핀이에요 … 실물 키트에서는 아직 확인하지 않았어요" 참고가 나온다 | 부록 B-2 12번 실물 확인 결과로 확정·교체 |
| 8 | workbox 사전 캐시 기본값(공식 문서 확인 후 PD-11 설정) | **해결(2026-09-16 P2-05):** `workbox-build` 7.4.1의 `getManifest()`만 쓰고 Workbox 실행 코드는 배포하지 않는다(서비스 워커 논리는 `src/sw/sw.js` 한 파일). 기본값을 그대로 쓰지 않고 `globPatterns`·`maximumFileSizeToCacheInBytes`(160KB)·`dontCacheBustURLsMatching`을 적어 **셸 6개 128KB**만 사전 캐시한다(예산 700KB를 넘으면 빌드가 멈춘다). 글꼴 조각 92개는 넣지 않았다 | 끝 |
| 9 | 배포 번들 의존성 목록을 얻는 방법(P1-04) | Vite 공식 `build.license`를 브라우저용(client) 환경에만 켜고(Astro가 `.vite/`를 지워서 `dist/bundle-licenses.json`에 둠) 빌드 뒤 `postbuild`가 `sources.yaml`과 대조해 지운다. package-lock 기준은 번들에 안 들어가는 빌드 도구 187개까지 등록해야 해서 뺐다. 한계: JS 청크에 든 모듈만 잡힌다(npm에서 가져온 CSS·글꼴·이미지는 빌드 전 `dependencies` 등록 검사로만 보완), `npx astro build`로 직접 빌드하면 검사가 돌지 않는다, Astro·Vite가 환경 이름·옵션을 바꾸면 목록 파일이 없어져 빌드 실패로 알린다 | P6-04 출처 최종 점검, Astro·Vite를 올릴 때 |
| 10 | 로컬 브라우저 테스트에 쓰는 브라우저 | Playwright 1.63.0 전용 Chromium(1243)이 이 PC에 없다. 소프트웨어 내려받기는 직전에 운영자 확인을 받는 규칙이라 받지 않았고, `playwright.config.ts`가 설치된 Microsoft Edge(153.0.4234.32)를 자동으로 고른다(`PW_CHANNEL`로 바꿈). CI는 테스트 워크플로(`e2e.yml`)가 러너에 Chromium을 설치해 쓴다(2026-09-16부터) | 운영자 할 일 12번 답이 오면 `npx playwright install chromium` |
| 11 | Pagefind 한국어 검색 품질 | 2026-09-16 실측(`tests/e2e/search.spec.ts`): 낱말 앞부분으로 찾는다("서보" → "서보모터", "로그인" → "로그인이"). 조사는 떼어 주지 않는다("픽셀은" 1건, "픽셀" 4건 — 검색 화면에 "조사를 빼고" 안내). 가운뎃점으로 이은 낱말은 한 낱말로 묶인다(나열은 쉼표로). 어떤 낱말과도 맞지 않는 한국어 검색어는 앞부분이 맞는 결과를 보여 줘서 "결과 없음"은 영문으로만 시험한다. 이어 붙은 요소(dt·dd)의 글자가 붙는 문제는 공백으로 해결. 문장 가운데 인라인 `data-pagefind-ignore`(용어 툴팁)는 색인에서 빠진다("행동하면서"로 1-1-1이 나오지 않음 확인). 결과 주소의 base는 pagefind.js 주소에서 자동으로 붙는다. 검토 반영(같은 날): 용어사전은 `sub_results`로 항목 단위 결과("픽셀 Pixel — 용어사전" → `#pixel`), 항목 메타(다른 이름·함께 보면 좋은 낱말·나오는 차시)와 출처 페이지의 사용 위치·경로·npm 칸은 색인 제외 → "픽셀" 결과 4건 → 3건(출처 페이지 빠짐) | Pagefind를 올릴 때(P6-04) 다시 확인 |
| 12 | 용어 표시·상자 문법 경고가 나오는 때 | 사전에 없는 낱말 경고는 페이지를 만들 때 나와 빌드마다 보인다. remark 단계 경고(제목·링크·상자 제목 안 용어, 모르는 상자 이름)는 Astro 콘텐츠 캐시 때문에 그 md가 바뀐 빌드에서만 보인다(결과 HTML은 그 파일만으로 정해져 영향 없음) | P5-02 `check:lessons`에서 캐시와 상관없이 전체 검사 |
| 13 | 브라우저 저장 이름 규칙 공통 파일(시작하기 요청) | **해결(2026-09-16 P2-01):** `src/lib/storage.ts` — 머리말 `ai-physical-computing:`, `storageKey`·`readItem`·`writeItem`·`removeItem`·`readJson`·`writeJson`·`listOurKeys`·`clearOurs`(이 사이트 이름만 지우고 `localStorage.clear()`는 쓰지 않음, 저장이 막힌 브라우저에서도 오류로 멈추지 않음). `capabilities.ts`는 같은 값을 다시 내보내고 안내 닫기 기억 이름도 이 규칙으로 만든다 | P2-02부터 자동 저장(`editor:<실습실>:<예제 id>`)·글자 크기(`editor:font-size`)·기록 지우기가 쓴다(2026-09-16) |
| 14 | 차시 frontmatter `source` 필드(원고·코드만·보충, 배우기 제안) | 만들지 않음. "원고 없음" 딱지는 차례표(`src/components/lesson/curriculum.ts`)에 있는 차시에만 붙는다 | P5에서 차례표 밖 차시가 생길 때 |
| 15 | 띄어쓰기 사라짐 자동 검사 | Astro 7은 줄바꿈이 든 글자·태그 사이 공백을 지운다. 통합 단계에서 빌드 결과를 훑어 "글자 바로 뒤에 붙은 여는 인라인 태그" 22곳을 찾아 `{' '}`로 고치고 CLAUDE.md에 주의를 적었다. 이 검사를 링크 검사에 넣을지는 미정 | P6 품질 점검, 같은 문제가 다시 나오면 더 이르게 |
| 16 | 예제 갤러리의 빈 안내 자리 | 조건부 슬롯도 `Astro.slots.has('notice')`가 참이라 갤러리에 빈 `.page-notice`가 생긴다. 전역 CSS(`:has()`)로 높이 0이지만 `:has()`를 모르는 옛 브라우저에서는 16px 틈. 2026-09-16부터 실습실 자리 페이지 4곳도 안내를 꺼서 같은 빈 자리가 생긴다 | Phase 4 갤러리(P4-11) 작업 때 |
| 17 | macOS·Linux·Android용 대체 글꼴 지표 | `src/styles/fonts.css`의 'Pretendard Fallback'은 Windows 맑은 고딕만 맞췄다(이 PC에서 잴 수 있는 글꼴). Apple SD Gothic Neo·Noto Sans CJK KR의 폭·세로 지표는 그 운영체제에서 재야 한다. 그 밖의 운영체제는 지금처럼 운영체제 글꼴로 보이다가 Pretendard로 바뀐다(줄이 조금 밀림) | P6-02 성능 점검(운영자 PC 밖 기기가 있을 때) |
| 19 | Pyodide에서 `exit()`(SystemExit)·`KeyboardInterrupt`로 끝난 실행이 `runPythonAsync` 약속 거부와 별개로 워커의 `unhandledrejection`으로 한 번 더 새는 현상 | 워커(`src/lab/runtime/worker.ts`)가 두 종류만 삼킨다(브라우저 테스트가 콘솔 오류 0건으로 확인). 원인은 코드를 돌리는 asyncio 작업이 두 예외를 결과에 적고도 다시 던지는 것(asyncio 규칙) | Pyodide 판을 올릴 때 다시 확인(현상이 사라져도 삼키는 코드는 무해) |
| 18 | Pagefind WebAssembly 파일 속 Rust 크레이트 고지 | 배포하는 `wasm.*.pagefind`에 든 크레이트(라이선스 대부분 MIT/Apache-2.0)는 목록이 없다. Pagefind 저장소의 Cargo.lock으로 확인해 고지할지 P6-04에서 정한다. pagefind.js·pagefind-worker.js에는 다른 라이브러리 표시 없음(grep) | P6-04 출처 최종 점검 |
| 20 | 공유 링크 길이 한계(P2-02, Claude 결정) | `src/lab/controls/share-link.ts`: 주소가 2,000자를 넘으면 ".py 내려받기로 만든 파일도 함께 보내 주세요"를 붙이고(메신저·게시판이 긴 주소를 자르거나 링크로 만들지 않는 경우 대비 — Chrome·Edge 주소 표시줄 자체는 2MB까지 열림), 16,000자를 넘으면 만들지 않고 한국어 오류. 예제 50줄 안팎은 압축하면 600~1,200자. 공유 링크에는 코드만 담고 영상·입력값은 넣지 않는다(PLAN §10). 열면 주소의 #을 지워 새로고침 뒤에는 자동 저장본이 열린다(공유 코드를 고친 뒤 옛 코드로 되돌아가지 않게) | 실제 수업에서 링크가 잘리는 사례가 나오면 경고 길이 조정 |
| 22 | cv2 흉내 모듈의 창·카메라 동작 세부(P2-03, Claude 결정) | `src/lab/python/apc_cv2.py`: ① `cap.read()`는 화면이 답할 때까지 기다리므로 거의 항상 `(True, 배열)`, 입력 소스를 못 열었을 때만 `(False, None)`(f034류 `continue` 폭주 방지) ② `imshow`는 같은 창을 초당 30장까지만 보내고(화면이 못 그리는 영상은 버림), 잘못된 이미지는 빈도 제한과 상관없이 늘 `cv2.error` ③ `destroyWindow`·`destroyAllWindows`는 화면의 창을 남기고(결과 확인용) 새 실행이 시작될 때 지운다 ④ `cap.set(CAP_PROP_FRAME_WIDTH/HEIGHT)`만 화면에 부탁하고 다른 속성은 False ⑤ `VideoCapture(문자열)`은 진짜 OpenCV에 맡긴다(Pyodide FS의 영상 파일) ⑥ 0번이 아닌 카메라 번호는 열리지 않고 콘솔 안내 ⑦ `cap.release()`는 화면의 카메라를 끄지 않는다(미리 보기·다음 실행에 다시 씀 — [입력 끄기]·페이지 떠날 때 끔) ⑧ 키 입력은 출력 화면에 초점이 있을 때의 글자·ESC·Enter·Backspace·Space만(Tab·화살표·조합키는 브라우저에 둠) | 자료 예제를 실제로 돌리는 P2-07~P2-09에서 어긋나는 예제가 나오면 조정 |
| 23 | 카메라 없는 입력의 범위(P2-03) | 샘플 입력은 코드로 그린 움직이는 도형·글자·그라데이션(`SampleScene`) 한 종류, 파일 업로드는 그림 한 장(png·jpg·webp 등, 브라우저 메모리에만). 영상 파일 업로드·CC0 샘플 영상은 넣지 않았다(저작권 확인 부담, 그림 한 장으로 임계값·에지·블러 예제는 충분). 합성 랜드마크 재생 입력은 P2-08이 `registerVisionSource()`로 더한다 | P2-07 보충 예제에서 영상이 꼭 필요한 예제가 나오면 |
| 24 | 예제 목록 규칙(P2-03) | `examples/vision/**/*.py`를 빌드 때 모두 읽어 첫 줄 주석 = 제목, 둘째 줄 = 설명, 파일 이름(하위 폴더는 `폴더-파일`) = id, 정렬은 폴더 없는 파일 → 폴더 → 파일 이름(숫자 크기순). 1-1-1 체험 예제(`u1/1-1-1-sort-vs-group.py`, cv2 없음)도 목록에 들어간다(실습실에서 그대로 돈다). 차시 순서·계단 순서로 보여 주는 정렬 규약(`# @order` 등)은 만들지 않았다 | P2-07에서 예제가 6개 이상이 되면 정렬·묶음 규약을 정함 |
| 25 | 동기 진입점에서는 양보하지 않는다(P2-03, 실사이트에서 발견) | 워커가 동기 `runPython`으로 부르는 코드(실행 준비 `reset_for_run`과 흉내 모듈의 초기화 함수, `installShims`)는 `block_on`·`sleep`·`request`·`input`·`poll`·`get`처럼 양보하는 함수를 부르면 안 된다 — JSPI가 있어도 동기 진입점에서는 스택 전환이 거부된다(`RuntimeError: Cannot stack switch because the Python entrypoint was a synchronous function`). `apc_runtime.maybe_yield`는 `pyodide.ffi.can_run_sync()`(호출 문맥까지 봄)가 거짓이면 양보를 건너뛰고, 초기화용으로 양보 없는 `drain(channel)`을 둔다. 새 흉내 모듈(P2-08 mediapipe, P2-11 pyautogui, Phase 3 가상 보드)의 초기화 함수도 이 규칙을 따른다 | 새 흉내 모듈을 만들 때마다 `tests/unit/lab/pyodide-cv2.test.ts`의 동기 진입점 검사와 같은 검사를 둔다 |
| 26 | 조절 값 규약의 동작 세부(P2-04, Claude 결정 — SPEC §6.1은 "슬라이더를 움직이면 다음 프레임부터 즉시 반영"만 정함) | ① **패널이 코드의 값 글자도 바꿔 쓴다**(`threshold = 100` → `threshold = 20`): 코드와 패널이 늘 같은 값이라 다시 [실행]·[공유 링크]·[.py 내려받기]·제한 모드("다음 실행 때 반영")가 따로 규칙 없이 맞고, 학생에게 "슬라이더 = 코드의 숫자"라는 한 가지 모형만 남긴다. 되돌리기(Ctrl+Z)에도 남는다(드래그 한 번이 한 묶음). ② 실행 중 반영은 `setValue`(최신 값을 매 지점마다 덮어씀)가 아니라 **`pushEvent('lab.params')` 큐(바뀔 때 한 번)**: 학생 코드가 반복문 안에서 `threshold = threshold + 1`처럼 같은 변수를 스스로 바꾸는 실험을 패널이 매번 되돌리지 않게. ③ 실행 중이 아닐 때는 파이썬에 보내지 않고 도우미도 실행 준비 때 쌓인 값을 버린다 → [실행] 직후 패키지를 받는 동안 움직인 값은 코드에만 적히고 그 실행에는 안 들어갈 수 있다(다시 움직이면 됨, `panel.ts` 머리말). ④ 들여쓰기한 줄·식(`100 + 1`)·범위 밖 값·선택지 밖 값은 조절 요소를 만들지 않고 경고만(값을 바꿔 쓸 수 없거나 뜻이 흐려서). ⑤ 조절 패널은 LabShell 기본 내용이라 ESP32·통신 실습실·차시 임베드도 같은 규약을 쓴다 | 자료 예제를 돌리는 P2-07~P2-09·Phase 3에서 어긋나는 경우가 나오면 조정 |
| 27 | 예제 파일 머리말 규약(P2-04, `src/lab/README.md`) | 사이트가 만든 예제만 머리말(첫 주석 줄 제목·둘째 줄 설명·`# @lesson slug`·`# @tags`)과 "바꿔볼 것 3가지"·"왜 이런 결과가 나올까" 상자를 파일에 둔다. 원본에서 옮긴 예제(PD-33)에는 넣지 않는다(PD-10 줄 번호 보존 — 제목·설명은 차시 md의 `examples:` 항목). 갤러리(P4-11)·차시 임베드(P2-14)는 `readExampleMeta`로 읽는다. 상자를 md에도 둘지(PLAN §2.6은 md 안 상자를 말함)는 두 곳에 같은 글을 두지 않도록 P2-07에서 정한다(후보: 사이트 예제는 파일 상자를 md가 불러 보임) | P2-07 보충 예제·P4-11 갤러리 |
| 21 | 저장소 검사의 이메일 모양 예외(P2-02) | CodeMirror·Lezer의 MIT 고지 원문에는 저작자가 스스로 적은 주소가 들어 있어 검사에 걸린다. 고지 원문은 고치지 않아야 하므로 `scripts/repo-allowlist.yaml`에 `privacy_exceptions`(경로는 `public/licenses/` 아래만, 종류는 `email`만, 이유 필수)를 두고 `scripts/lib/repo-check.mjs`가 그 파일의 이메일 모양 검사만 건너뛴다(사용자 폴더·전화번호·비공개 이름 검사는 그대로, 단위 테스트 추가). 운영자·학생 정보에는 어떤 경우에도 쓰지 않는다 | 새 고지 파일을 넣을 때마다 항목을 따로 적음(패턴은 파일 하나씩). **2026-09-16 병렬 준비에서 `package-lock.json`도 예외 경로로 허용**: npm이 잠금 파일에 기록하는 다른 패키지의 deprecated 안내문(glob 11.1.0, workbox-build 의존성)에 저작자의 공개 주소가 들어 커밋이 막혔다. 잠금 파일은 사람이 고치지 않는 파일이라 이메일 종류만 예외로 두었다(`isPrivacyExceptionPathAllowed`, 운영자·학생 정보는 예외 없음) |

| 28 | 원본 이관 예제의 제목·설명 위치(병렬 준비, Claude 결정 — PLAN §2.6은 "예제 정보는 차시 md에") | 차시 md가 아직 없는 예제(opmp·pyautogui, 1단원 대부분)는 코드 파일 옆의 **사이드카** `<이름>.meta.yaml`에 둔다(`src/lab/README.md` 3절). 근거: ① 코드 파일은 줄 번호 보존을 위해 한 글자도 못 고침(PD-10) ② 한 예제 = 한 파일이라 7명이 동시에 고쳐도 부딪히지 않음(단일 목록 파일이면 충돌) ③ 목록 파일 `scripts/examples-manifest.yaml`의 `meta`는 처음 만들 때의 씨앗일 뿐 그 뒤 기준은 사이드카. md가 생기면 md의 `examples:` 항목이 우선(P2-14·P5에서 연결 규칙을 코드로) | P2-14 차시 임베드·P5 차시 작성 때 |
| 29 | 흉내 모듈 폴더 규약의 세부(병렬 준비, Claude 결정) | ① manifest.ts(순수 데이터)와 index.ts(화면)를 나눈 까닭: 워커 번들도 manifest를 읽어야(shims 표) 하는데 index.ts는 DOM·무거운 라이브러리를 import하므로 워커에 섞이면 안 됨 ② 요청·이벤트·채널 이름은 manifest에 적은 것만 ctx가 허용(오타·겹침을 빌드·실행 즉시 잡음) ③ index.ts는 실습실(labs)에 맞을 때만 지연 로딩(청크 분리 — hello는 별도 파일로 나옴) ④ panel.astro는 빌드 때 그 실습실 페이지에만 hidden으로 그리고 모듈이 showPanel()로 연다(placement `panel`/`wide`) ⑤ 붙박이 cv2는 폴더로 옮기지 않음(VisionIo와 강결합, 이름만 예약) ⑥ `bridge.beginRun()`이 실행 전 push 값을 모두 버린다(모듈 초기화 함수의 drain은 이중 안전장치) | P2-08~P2-13 모듈을 실제로 만들며 부족한 훅(예: 출력 창에 겹쳐 그리기, 워커 쪽 이벤트 구독)이 드러나면 types.ts에 더한다 |
| 30 | 병렬 검증 환경(병렬 준비) | `PW_BASE_URL`이 있으면 Playwright가 서버를 띄우지 않는다. 개발 서버는 검색 색인이 없어 `search*.spec`이 돌지 않고 첫 응답이 느리지만 Pyodide 워커·가짜 카메라·조절 패널·모듈은 미리 보기와 같게 돈다(2026-09-16 실측). Claude 에이전트 안에서는 `ASTRO_DEV_BACKGROUND=1`이 없으면 Astro 7이 백그라운드로 띄우다 30초 대기에 실패했다(빌드와 동시 실행 중 CPU 경합). 포트 배정(A 4401 … G 4407)은 README 5절 | 통합 단계에서 전체 `npm run test:e2e`(빌드 + 미리 보기)로 다시 확인 |
| 31 | `@mediapipe/tasks-vision` WASM의 배포 방법(병렬 준비) | npm 패키지(0.10.35)에 LICENSE 파일이 없어 공식 저장소 LICENSE 원문을 고지로 실었다. WASM 4개(약 22MB)는 저장소에 넣지 않고 `scripts/vendor-assets.mjs`가 `predev`·`prebuild`에서 `public/vendor/mediapipe/0.10.35/wasm/`으로 복사한다(PLAN §5.2 "빌드 때 npm 패키지에서 복사"). CI(withastro/action의 `npm install` → `npm run build`)에서도 prebuild가 돌아 배포물에 들어간다 — 첫 배포에서 `dist/vendor/mediapipe/…` 응답을 확인할 것. `vision_wasm_module_internal.*`는 복사하지 않았다(Tasks API가 쓰지 않는 파일로 판단, 필요하면 목록에 추가) | P2-08에서 FilesetResolver.forVisionTasks(withBase('vendor/mediapipe/0.10.35/wasm'))로 실제 로딩을 확인할 때 |
| 32 | 차시의 "실습실 임베드" 구현 방법(P2-14, Claude 결정 — SPEC §7.2는 "예제가 미리 로드된 상태"만 정함) | **누른 뒤에 만드는 iframe**으로 했다. 차시 한 쪽에 예제가 여럿이라 `LabShell`을 그대로 여러 개 넣으면 ① 실습실 컨트롤러가 페이지당 `[data-lab]` 하나를 전제로 주소(`?example=`·`#code=`)를 읽어 서로 간섭하고 ② 파이썬 워커·흉내 모듈이 예제 수만큼 뜨며 ③ 실습실 스크립트(약 400KB)와 오류 사전(50KB)이 모든 차시 페이지에 실린다. iframe은 문서가 나뉘어 ①②가 생기지 않고, **누르기 전에는 한 바이트도 받지 않는다**(느린 학교 망·휴대폰에서 차시 읽기가 무거워지지 않음). 같은 사이트라 자동 저장·[기록 지우기]·카메라 권한도 그대로다. `?embed=1`이면 `<html data-embed>`가 붙어 머리글·바닥글·빵부스러기와 `[data-embed-hide]`가 숨는다 | 차시가 늘어 임베드를 여러 개 여는 수업이 생기면 메모리·속도를 다시 본다 |
| 33 | 예제 스모크 테스트의 기대 결과 적는 곳(P2-14, Claude 결정) | 사이드카(`<이름>.meta.yaml`)에 선택 칸 `smoke:`를 뒀다(`input`·`outcome`·`error`·`seconds`·`skip`). 입력 소스는 적지 않으면 `tags`로 고른다(손·얼굴·자세·mediapipe면 재생 입력). 지금 적어 둔 것은 f024(`pyautogui.enter` AttributeError)뿐이고 나머지 48개는 "오류만 아니면 통과"다 | 예제가 늘어 기대 결과가 여러 종류가 되면 규약을 넓힌다 |
| 34 | 정적 한글 글꼴을 어디서 가져올지(P2-10·P2-14, Claude 결정) | Pyodide의 FreeType이 woff2를 못 열어(배포 중인 Pretendard 조각 92개 전부 `unknown file format`, Node 실제 Pyodide로 확인) `.otf` 한 개가 필요하다. **npm 패키지 `pretendard@1.3.9`(devDependency, OFL-1.1)에서 빌드 때 `public/vendor/pretendard/`로 복사**하는 쪽을 골랐다(저장소에 바이너리를 넣지 않고, 이미 배포 중인 글꼴과 같은 판이라 고지도 하나면 된다) | 글꼴을 바꾸면 `scripts/vendor-assets.mjs` 목록과 `SITE_FONT_CANDIDATES`를 함께 고친다 |
| 35 | 브라우저 테스트 병렬 수(P2-14) | 실습실 검사 하나하나가 브라우저 안에서 Pyodide(약 20MB)와 OpenCV를 띄운다. 기본 병렬 수(코어의 절반 = 4)로 돌리면 8GB 컴퓨터에서 서로 자원을 뺏어, **따로 돌리면 통과하는 검사가 전체 실행에서 실패**했다(2026-09-16 실측: examples-smoke·lab-loading). `playwright.config.ts`에서 동시 실행 수를 로컬 3·CI 2로 줄이고 재시도를 1회로 뒀다 | CI 러너나 작업 PC가 바뀌면 다시 잰다 |
| 36 | Fast 3G 속도 제한의 한계(P2-14) | CDP의 `Network.emulateNetworkConditions`는 **페이지 대상에만** 걸려 파이썬 워커가 받는 Pyodide 20MB는 느려지지 않는다(실측으로 확인). 그래서 느린 회선 수치는 **홈 화면(셸)까지가 실측**(4.1초)이고 전체는 잰 바이트를 1.6Mbit/s·지연 562.5ms로 나눈 **계산값 약 216초**로 적었다. 전송량 자체는 문맥 단위 `requestfinished` + `request.sizes()`로 워커·서비스 워커 몫까지 실측했다(19.83MB) | 브라우저 대상 전체에 속도 제한을 거는 방법이 생기면 실측으로 바꾼다 |
| 37 | 빌드가 jsDelivr에 기대게 된 것(P2-05·P2-14) | `prebuild`의 예비본 받기는 `.cache/pyodide-fallback` → `node_modules/pyodide`(코어 5개) → `.cache/pyodide-packages` 순서로 찾고, **휠 2개(numpy·OpenCV 13.7MB)만** 없을 때 jsDelivr에서 받는다. 그래서 캐시가 빈 CI 빌드에서 jsDelivr가 막히면 배포가 실패한다(전에는 빌드가 바깥 망을 쓰지 않았다). 지금은 조용히 반쪽짜리 예비본을 싣는 것보다 **시끄럽게 실패하는 쪽**을 골랐고, Actions 캐시를 두어 첫 성공 뒤에는 망을 쓰지 않는다 | 실제로 배포가 막히는 일이 생기면 "못 받으면 경고만 하고 계속"으로 바꾼다(그때는 예비본 없는 배포임을 로그에 크게 남긴다) |
| 38 | 기기 안 음성 인식 확인이 탭을 죽이는 브라우저가 있다(P2-14, CI에서 발견) | `SpeechRecognition.available({processLocally:true})`를 부르면 **기기 안 음성 인식 서비스가 없는 Chromium 계열**(Playwright가 쓰는 Chromium 등)이 `No binder found for interface media.mojom.OnDeviceSpeechRecognition`으로 렌더러를 죽인다 — try/catch로 막을 수 없다(브라우저가 탭을 끝내므로). 처음에는 실습실·설정 페이지가 **열자마자** 불러서 CI 브라우저 테스트 80건이 Page crashed로 실패했고, "패널을 건드릴 때"로 미뤘더니 글자 칸을 누르기만 해도 같은 일이 났다. 지금은 **[기기 안 인식 되는지 확인] 단추(실습실)·[다시 확인](설정)을 눌렀을 때만** 묻는다(상태 `unchecked`). 크롬·엣지(Windows)에서는 크래시 없이 결과가 나온다(Edge 153 실측 `unavailable`) | 브라우저가 고쳐지거나 "서비스가 있는지" 알 방법이 생기면 자동 확인으로 되돌린다. 그때까지는 단추를 눌러야 알 수 있다는 것을 설정 페이지 글에 적어 둔다 |
| 39 | `serial`(pyserial)을 쓰는 3단원 PC 쪽 예제 f084·f085(Phase 2 검토 반영, 2026-09-17) | 옮기지 않았다: 첫 줄이 `import serial`이라 시리얼 흉내가 없는 지금은 실습실에서 `ModuleNotFoundError`로 끝난다(학생 목록에 올리면 혼란). PLAN §8.2 표의 P2-09 "f085(보내기 제외)"·P2-10 "f084(가상 입력)"는 미확인으로 남긴다. f090~f097(3-1-4·4-1-1~4-1-3)은 옮겨 확인했다 | P3-05(가상 UART 선)·P3-07(Web Serial)에서 `serial` 흉내를 만들 때 옮겨 스모크·짝 예제(f082·f083)로 확인 |
| 40 | 첫 준비 동안 위에 올린 준비 패널이 제자리로 갈 때 화면이 한 번 움직인다(검토 반영, Claude 결정) | 파이썬을 받는 동안에는 준비 패널(진행률·1분 개념 카드)을 편집칸 위에 두고(`data-loading-intro="yes"`), 준비가 끝나 패널이 접히거나 [실행]을 누르면 입력·출력 아래 제자리로 돌린다. 그 순간 편집칸이 패널 높이만큼 위로 올라간다. 새 배치 종류를 만들지 않고(통합 때 거절한 까닭 그대로) CSS 격자 순서만 바꾼 결과이고, 진행률이 문서 y≈3,300px에 숨어 한 번도 안 보이던 문제보다 작다고 판단했다 | 교실에서 "읽던 코드가 움직인다"는 의견이 오면 "접힌 한 줄은 위에 남기기"로 바꾼다 |
| 41 | 개발 서버에 대고 브라우저 테스트를 돌릴 때 결과 폴더 위치(2026-09-17 관찰) | 저장소 안(`--output=.cache/…`)에 두었더니 실패 추적 파일(`traces/resources/*.html`)이 생길 때마다 개발 서버 감시기가 알아챘고(`[watch] .cache/…html`), 그 무렵 실습실 검사들이 파이썬 준비에서 90초씩 멈췄다(같은 검사는 빌드 결과에서 통과). 그 사이에 파일을 고친 것(HMR)도 겹쳐 원인을 하나로 단정하지 않았다. `src/lab/README.md` 5절에 "결과 폴더는 저장소 밖" 권고를 적었다 | Phase 3 병렬 제작 때 같은 증상이 나오면 `astro.config.mjs`의 `vite.server.watch.ignored`에 `.cache/**`를 넣는다 |
| 42 | P3-00 가상 보드 실행 엔진: MicroPython 공식 WebAssembly 포트 vs Pyodide(2026-09-17, Claude 결정) | **PD-04 유지 — 가상 보드는 Phase 2 Pyodide 러너(JSPI `block_on`·정지 1·2단계·흉내 모듈 폴더·오류 사전·예비본/서비스 워커)를 그대로 쓴다.** 근거(MicroPython `v1.29.0` 태그 소스·npm 레지스트리·PR 확인): ① npm판 `@micropython/micropython-webassembly-pyscript@1.29.0-6`(MIT)은 ES 모듈이라 모듈 워커에서 뜨고 `registerJsModule`·`FS`로 흉내를 넣을 수 있지만, JSPI·ASYNCIFY가 없어 `time.sleep`이 바쁜 대기(`mphalport.c`)라 자는 동안 버튼·센서 값과 [정지]가 워커에 닿지 않는다 ② 표준 입력이 비어 있어(`mp_hal_stdin_rx_chr() (0)`) `input()`에 화면 입력을 줄 수 없고, 동기 대기는 교차 출처 격리 헤더가 필요한 PD-01 (a) 경로다 ③ README가 "브라우저에서는 인터럽트가 구현되지 않았다"고 적어 동기 코드는 `Worker.terminate()`로만 멈춘다 → 입력을 읽는 반복문·`input()`을 쓰는 원고 예제 30개 안팎이 가상 보드에서 끝까지 안 돼 원칙 3을 못 지킨다. MicroPython의 이점(첫 전송 약 0.25MB vs Pyodide 코어 약 6.0MiB, `bytes + str`·`const`·ticks·오류 문구가 실물과 같음)은 흉내·안내로 좁히고, 웹어셈블리 포트도 배정밀도 float·epoch 1970이라 ESP32(단정밀도·2000)와의 차이가 다 사라지지는 않는다. **손으로 돌리는 실험은 하지 않았다**(패키지 내려받기는 운영자 확인 대상 — 실험 페이지·실험 코드 없음). CPython 쪽 차이는 Node의 Pyodide 314.0.7로 실측했다(`OSError(2, …)`가 `PermissionError`가 되고 `errno.ENODEV`가 43인 것 등). 다시 받을 때의 정보: npm `@micropython/micropython-webassembly-pyscript@1.29.0-6`, https://registry.npmjs.org/@micropython/micropython-webassembly-pyscript/-/micropython-webassembly-pyscript-1.29.0-6.tgz , 파일 7개 2,378,759바이트(`micropython.mjs` 110,120 + `micropython.wasm` 449,553 + settrace·ulab판 wasm 3개), integrity `sha512-kLwGPJFsLY/2aUPmzZxpvauFSmKc0SlJWVvvEttULR0poVQ6W9HV//1fUJyxrGWAxCVDw9aYqJ0LAog0ZLjYVA==`. 비교 전체·가상 보드가 지킬 차이 표 14항목은 PLAN §8.3 P3-00 구현 메모 | ① MicroPython JSPI판(PR #19594 "Add minimal JSPI support", 2026-09-17 병합 전)이나 협조적 양보(PR #19427)가 npm으로 나오면 운영자 허락을 받아 받고 `run_sync` 기반 sleep·`input()`·[정지]를 실제로 시험 ② P3-06 블록 전용 호환 모드(PD-27)가 JSPI 없는 태블릿의 Pyodide로 안 되거나 전송량이 문제일 때 그 모드에 한해 검토 |
| 43 | 가상 보드 부품 레지스트리 위치와 확장 규약(P3-01, Claude 결정 — 작업 지시는 "보드 아래 하위 폴더 또는 별도 모듈 폴더 중 하나를 골라 근거를 남김") | **보드 모듈 안의 부품 폴더** `src/lab/modules/board/parts/<부품 id>/part.ts`(+ 선택 `apc_part_*.py`·드라이버 `.py`). 까닭: 부품은 보드 핀 상태를 함께 읽고 입력을 보드에 넣는 보드의 일부다 — 흉내 모듈 폴더로 두면 부품마다 manifest·`<id>.*` 메시지 이름이 생기고, 모듈끼리 핀 상태를 나눌 통로가 없으며, 부품 수만큼 청크를 따로 받는다. 부품 폴더는 `import.meta.glob`으로 찾아 보드 청크 하나(빌드 약 17KB)에 묶이고 메시지는 보드의 `board.*` 이름을 함께 쓰므로 부품을 더해도 manifest·등록 파일을 고치지 않는다(`validatePartDefinitions`가 모양을 검사, `board-parts.test.ts`가 폴더 목록과 대조). 모듈 폴더 이름은 다음 할 일에 적었던 예시 `esp32-board` 대신 `board`(Phase 4 통신 실습실도 같은 보드를 붙일 수 있게 실습실 이름을 빼고 메시지 머리말 `board.`과 맞춤). `machine`의 주변장치(PWM·ADC·I2C·UART·RTC·time_pulse_us)는 부품이 아니라 `apc_board_<이름>.py` + `apc_board.register_machine_export` 확장으로 더한다 — `machine.py`를 여러 묶음이 함께 고치지 않게. 아직 없는 이름은 `from machine import PWM`에서 `ImportError: machine.PWM은(는) 가상 보드에 아직 없어요(실물 ESP32에는 있어요). …`(오류 사전 `board-not-emulated`), 흉내 계획이 없는 이름(DAC·SPI·WDT·deepsleep 등 — 자료 예제에서 안 씀)은 "이 기능은 실물 보드에서 확인해요" | P3-02에서 첫 외부 부품(디지털 터치·진동 모터)을 더할 때 규약이 모자라면 README 7.5와 함께 고침 |
| 44 | 학생 코드만 MicroPython판 `time`을 받게 하는 방법(P3-01, Claude 결정 — P3-00 차이 표 1·3번이 P3-01에 맡김) | `apc_board.install()`이 `builtins.__import__`를 감싸, import하는 쪽이 학생 코드(`__main__`, 작업 폴더 `/home/pyodide/`·보드 라이브러리 폴더 `/board/lib/`의 파일)일 때만 `time`·`utime` → `apc_board_time.py`의 모듈, `errno`·`uerrno` → newlib 번호 모듈(22개, 차이 표 10번), `bluetooth`·`ubluetooth` → 자리 안내 `ModuleNotFoundError`, `ustruct`·`umachine` 등 u-이름 16개 → 원래 모듈(차이 표 5번)을 준다. `sys.modules['time']`은 진짜 그대로라 표준 라이브러리·Pyodide 내부(C 코드의 `PyImport_Import`는 `sys.modules`를 봄)는 영향이 없다(Node 실측: 훅을 건 뒤에도 `importlib.import_module('time')`에 `perf_counter`가 있고 `logging`·`datetime`이 1970 기준 시각으로 돎). 버린 길: ① `time.py` 파일로 가리기 — 붙박이 모듈이라 안 됨 ② 진짜 `time`에 `sleep_ms` 등을 더하고 `time()`·`localtime()`을 2000 기준으로 바꾸기 — `logging`·`asyncio`·`datetime`이 깨질 수 있음 ③ 보드용 워커를 따로 만들기 — PD-04 러너·정지·캐시를 두 벌 유지(미해결 48). 남는 차이: `importlib.import_module('time')`처럼 `__import__`를 거치지 않는 코드는 진짜 `time`을 받는다(자료 예제에 없음). 영상처리·dev 실습실에는 훅이 없다(`lab-esp32.spec.ts`의 dev 실습실 검사: `'sleep_ms' in dir(time)`이 False, `import machine` 실패) | P3-04 사이트 라이브러리(`/board/lib/`)가 표준 라이브러리에 MicroPython판 객체를 넘겨 문제가 보이면 다시 봄 |
| 45 | 가상 시계의 규칙(P3-01, Claude 결정) | 가상 시각 = 실행 시작 뒤 학생 코드가 계산한 실제 시간 + sleep한 양. sleep이 실제로 기다린 시간(브라우저 타이머 늦음·짧은 sleep을 16ms씩 모아 기다리기)은 세지 않고, sleep이 끝나면 기준점을 다시 잡는다 → `t = ticks_ms(); sleep_ms(100); ticks_diff(ticks_ms(), t)`가 실물처럼 100(+계산 시간)이다. sleep 도중에 도는 틱 훅(입력 반영·Timer)은 그 sleep이 끝날 시각을 넘지 않는 시각을 본다 — 넘으면 Timer 주기를 건너뛰고 `ticks_us`가 거꾸로 가는 것을 Node 실측에서 발견해 고쳤다. 실행마다 0에서 시작(보드를 새로 켠 것처럼, ticks 넘침 2**30ms는 수업 시간 안에 오지 않음). RTC(`time.time()`·`localtime()`)는 실행 시작 때 PC 현지 시각(`tm_gmtoff`)에서 출발 — Thonny가 연결할 때 보드 시계를 PC 시각으로 맞추는 것과 같게(RTC 예제 f051에서 "지금 시각"이 보이게). 실물 전원 직후 값은 부록 B-2 6번. `ticks_cpu()`는 µs(실물은 CPU 사이클 수) | P3-03 이후 `RTC().datetime()`을 같은 시계로 흉내 낼 때 |
| 46 | Timer·핀 인터럽트 콜백을 부르는 때(P3-01, Claude 결정 — P3-00 차이 표 12번) | 입력 확인 지점(`time.sleep*`·출력 중이 아닌 핀의 `value()`·`ticks_*()`)에서만 대기열(실물 스케줄러 깊이 8, 넘치면 버림)의 콜백을 차례로 부른다. 틱 훅·대기 전 훅(동기 진입점)에서는 대기열에 넣기만 한다. sleep 중에는 다음 Timer 시각마다 깨어 제시간(가상 시각)에 부르고, 핀 인터럽트가 걸려 있으면 긴 sleep을 20ms 조각으로 나눠 버튼에 20ms 안에 반응한다(Node 실측: `sleep_ms(480)` 도중 100ms 간격으로 바꾼 두 핀의 입력 네 번이 trigger에 맞게 각각 제때 콜백). 콜백 안에서는 다른 콜백이 끼어들지 않고, 콜백 오류는 학생 코드 줄만의 트레이스백 + 안내 한 번 뒤 계속 돈다(실물과 같음). **남는 차이:** 양보 없는 계산 반복문·`input()` 대기 동안에는 콜백이 돌지 않고, 밀린 Timer 주기는 그 뒤 첫 확인 지점에서 한 번만 부른다(실물은 계산 중에도 제때 부름). 16ms보다 짧게 켰다 끈 핀은 화면에서 합쳐져 안 보인다(미해결 49 — 인터럽트 계산은 빠짐없이) | 부록 B-2 16번 실물 확인, 자료 예제에서 차이가 드러나면 P3-02~ 교사용 접기 |
| 47 | 코드가 끝난 뒤의 보드와 [정지] 뒤 모습(P3-01, Claude 결정 — PLAN §4.4 "스크립트가 끝난 뒤의 대기") | 학생 코드가 오류 없이 끝났는데 켜진 Timer·핀 인터럽트·쌓인 콜백이 있으면 워커가 같은 실행 안에서 `apc_runtime.run_idle()`로 [정지]까지 계속 돌린다(콘솔 안내 한 번, `board.state` phase `idle`) — 실물에서 Timer 예제가 스크립트 끝 뒤에도 깜빡이는 것과 같게(사이트 예제 `03-timer-blink.py`). 할 일이 없으면 곧바로 끝난다(영상처리 실습실은 훅이 없어 그대로). 오류·`exit()`로 끝나면 이어 돌지 않는다 — 오류 카드를 보고 고치는 흐름을 우선했다(실물은 REPL로 돌아가도 Timer가 돌 수 있어 **차이**, 부록 B-2 16번). [정지]·다시 시작이면 부품을 꺼진 모습으로, 스스로 끝나거나 오류면 마지막 핀 상태를 남긴다(실물에서 LED가 켜진 채 끝나는 것과 같음). 다음 [실행]은 보드를 새로 켠다(핀·Timer·시계 초기화 — Thonny의 [정지]/재시작에 가까움) | 부록 B-2 16번 결과로 오류 뒤 동작을 다시 봄 |
| 48 | 실습실마다 워커에 넣는 흉내 모듈과 준비 모듈이 받는 파일(P3-01, Claude 결정 — PD-04 "Pyodide 코어만 쓰는 별도 워커"를 어떻게 지킬지) | 워커 파일은 하나로 두고(러너·정지·예비본·서비스 워커 공유) 페이지마다 따로 띄우며, `load` 메시지의 `labId`로 manifest `labs`가 그 실습실을 포함하는 모듈의 `.py`·shims만 `/apc`에 쓴다(manifest 없는 붙박이 파일은 모든 실습실, labId가 없으면 예전처럼 전부 — `python-modules.test.ts`). 그래서 `machine.py`·`time` 훅은 영상처리·dev 실습실에 없고 `cv2` 흉내는 ESP32 실습실에 없다. 준비 모듈의 캐시 채우기·[이 컴퓨터에 실습 파일 미리 받기]는 LabShell `pyodidePackages`(뿌리 `data-lab-packages`) 범위만: ESP32는 `[]` → 파이썬 엔진 파일만("준비 끝 · 12.9MB"), 영상처리는 `opencv-python`(+ 의존 numpy), 속성이 없는 페이지(dev)는 예전처럼 전체. ESP32 실습실에서 opencv·numpy 요청 0건(`lab-esp32.spec.ts`) | P4-02(영상처리 + 가상 보드 한 화면)에서 두 실습실 모듈을 한 워커에 함께 넣을 때 `labId` 여러 개 규칙을 정함 |
| 49 | 가상 보드 ↔ 화면 메시지 모양(P3-01, Claude 결정 — PLAN §7 브릿지 규칙과의 관계) | 이름 다섯 개(`manifest.ts`): `board.state`(파이썬 → 화면 이벤트, 이번 실행에서 만진 핀 **전체 목록** + phase·seq·가상 시각 — 바뀐 핀만 보내지 않고 전체라 화면은 최신 것만 그리면 됨), `board.inputs`(화면 → 파이썬 최신 값, 실행 시작 때 읽음), `board.input`(화면 → 파이썬 쌓이는 값, 눌렀다 뗀 것도 순서대로), `board.wiring`(배선), `board.device`(부품 흉내 상태, P3-04~). 전송 시점은 바뀐 것을 모아 ① 파이썬이 실제로 기다리기 직전(대기 전 훅) ② 마지막 전송 뒤 16ms가 지난 쓰기 ③ 시작·대기 시작·끝 — 초당 약 60번이 넘지 않아 `while True: led.toggle()`도 화면을 막지 않는다. §7.2 규칙 5("상태는 최신 값만, 이벤트는 대기열")와 같은 뜻이고, §7의 UART·BLE·MQTT 글자 통로는 Phase 4가 `board.uart.*`처럼 따로 더한다. 형식은 `src/lab/README.md` 7.3·`state.ts` 머리말 | P3-02 배선도·P3-03 PWM 밝기에서 모양이 모자라면 `v`를 올려 고침 |
| 50 | ESP32 실습실 첫 화면·예제·오류 풀이(P3-01, Claude 결정) | 페이지: `BrowserNotice`(`_labs.ts` `browserNotice: true`)·설명 3줄·LabShell + 가상 보드(보드 그림·핀 표)·도움말 상자 3개(보드 없이 되는 것, 가상과 실물이 다른 점 — 콜백 시점·소수 자릿수·실행마다 새로 켬, 오류 사전·점검 페이지 링크). 홈의 가상 보드 버튼에서 "준비 중"을 뗐다(`home-content.ts`). 점검 도우미 `/labs/esp32/check/`는 P3-11까지 자리 페이지. 예제는 사이트가 만든 3개(내장 LED 깜빡이기 — `interval` 슬라이더, BOOT 버튼으로 LED, Timer 깜빡이기 — 코드가 끝나도 도는 모습)이고 원본 이관은 P3-02(f046·f015·f052·f053)부터 — 예제 스모크를 실습실별로 나눠 두어 옮기면 ESP32 실습실에서 돈다(지금 0개라 건너뜀). 보드 그림은 사이트가 그린 브랜드 중립 SVG(짙은 기판·"ESP32 가상 보드" 글자, 부품 글자 11px·상태 글자 10px, BOOT 누르는 자리 64×60, 초점 테두리 노랑 — 기판 위 대비 8.1:1 계산값). 오류 사전 `board` 묶음 풀이 6개: 문구는 MicroPython 그대로(`invalid pin` 등) 두고 풀이만 한국어, 영상처리 실습실의 `import machine`은 여전히 "이 실습실에 없는 모듈" 풀이(단위 테스트) | P3-02 차시 md 연결 때 첫 화면 문장을 차시 흐름에 맞춰 다시 봄. ~~휴대폰 폭에서 보드 그림 글자가 작게 보이는지~~ P3-02에서 [그림 크게 보기]로 해결(미해결 51) |
| 51 | 보드 그림·배선도 모양(P3-02, Claude 결정) | **보드:** 교과서 키트 보드(원고 118쪽 사진)와 같은 30핀 개발 보드의 핀 순서를 사이트가 직접 그린다(업체 이름·실크 문구 없음) — 학생이 실물 보드의 핀 번호 자리와 같게 익히도록. P3-01의 번호 없는 핀 머리 그림(한 줄 19개)은 버림. 스트래핑 핀 ▲(GPIO0은 BOOT 그림), 쓰는 핀 흰 고리·1이면 노란 빛·마우스를 올리면 설명(핀 머리 층은 화면 낭독기에서 숨기고 같은 내용을 핀 표 글자로), 내장 LED는 원고 123쪽 "D2 LED 녹색불"에 맞춰 초록. **배선도:** SPEC §6.2 "DevKit + 브레드보드 + 부품"대로 바깥 부품을 보드 아래 브레드보드에 한 줄로 놓고 신호선·전원 레일·부품 전원 다리를 긋는다. 키트 확장 보드(3색 헤더)만 그리는 방식은 버림 — 헤더가 없는 브레드보드 수업에서 전원이 어디서 오는지 보이지 않아서(키트 쓰는 법은 "그림 읽는 법" 한 줄). 선 길: 아래 줄 핀은 보드 밑, 위 줄 핀은 보드 위·오른쪽 바깥, 전원은 왼쪽 바깥 — 세로선 칸을 나눠 겹쳐 그려지지 않고(단위 테스트가 셈), 부품은 핀 순서로 놓아 흔한 배선은 엇갈리지 않는다. 바깥 부품이 없으면 브레드보드를 그리지 않고 한 줄 안내. **휴대폰:** 그림을 칸 폭에 맞추면 핀 번호가 약 7px라 [그림 크게 보기](48rem으로 펴 가로 밀기, 선택 기억 `module:board:zoom`)를 둔다 — 기본은 맞춤(한눈에 전체), 확대를 기본으로 하면 처음 보는 학생이 보드 오른쪽 절반을 못 본다 | 부품이 많아(5개 넘게) 한 줄이 너무 길어지면 두 줄 배치와 줄 사이 선 길을 더함(P3-03~P3-05 결과를 보고) |
| 52 | f052(터치 + 문자 LCD)를 P3-02에서 옮기되 끝까지 돌리지 않음(P3-02, Claude 결정) | PLAN §8.3은 f052를 P3-02 완료 기준과 P3-04(f047~f057) 양쪽에 적었다. f052의 2~4번 줄은 `SoftI2C`·`i2c_lcd.py`(사이트 제공 라이브러리 f011 + MIT 고지)라 LCD 바이트 해석은 P3-04의 핵심 일이다. P3-02에서 LCD를 앞당겨 만들면 곧 이어질 병렬 구역(P3-04)과 같은 일을 두 번 하게 되므로, P3-02는 **파일 이관·터치 센서 배선·"문자 LCD는 아직 없어요" 주의·실행하면 2번 줄에서 한국어 안내가 든 ImportError**까지로 했다(사이드카 `smoke: { outcome: error, error: ImportError }`, 설명 끝 "지금은 2번 줄에서 멈춰요"). 터치 센서 동작 자체는 같은 차시의 f053으로 끝까지 확인한다. 배선의 LCD 부품 id `lcd-i2c`(sda 21·scl 22)를 README 7.5에 약속해 두어 P3-04가 그 이름으로 부품을 만들면 f052 배선이 저절로 그려진다 | P3-04에서 f052가 끝까지 돌면 smoke와 설명의 "준비 중" 문장을 지움 |
| 53 | 예제 배선을 적는 곳과 우선순위(P3-02, Claude 결정 — PLAN §2.6은 "배선은 차시 md frontmatter") | 세 곳을 받고 **차시 md → 사이드카 → 머리말 `# @part`** 순서로 처음 찾은 것만 쓴다(`wiring-spec.ts`·`esp32/examples.ts`). 까닭: ① 2단원 차시 md가 아직 없고(원고 차시는 P5) 옮긴 예제는 코드에 줄을 더할 수 없어(PD-10) 사이드카가 필요 ② 사이트가 만든 예제는 "새 예제 = .py 하나"(원칙 6)라 머리말 ③ 차시가 생기면 md가 이긴다(PLAN §2.6). 모양은 PLAN §2.6 예시 `{ type: touch_digital, pin: 17 }`도 그대로 받고(밑줄 → 하이픈) 사이드카 모양 `{ part, id, pin, pins, label }`과 같게 맞춘다. 틀린 줄은 빼고 빌드 경고만(PD-35). 스키마 `partSchema`는 `part` 또는 `type`만 필수라 기존 차시 빌드가 그대로다 | 2-1-1·2-1-2·2-2-1 차시 md를 쓸 때(P5) 배선을 md로 옮길지 정함 |
| 54 | 배선 오류를 알리는 방법(P3-02, Claude 결정 — PLAN §6.1 "배선 오류 설명은 실물과 같은 예외") | **배선 자체의 문제는 화면 검사**(그림 아래 목록, 오류·주의·참고 — 스트래핑 핀의 바깥 부품은 주의, 한 핀에 입력·출력 부품·입력 전용 핀의 출력 부품은 오류, 한 핀에 출력 부품 여럿은 참고 등 14종, README 7.4 표), **코드가 배선과 어긋나게 핀을 쓰는 것은 파이썬 콘솔 안내**(값을 보내는 부품 핀을 출력으로, 움직이는 부품 핀을 입력으로, 출력으로 정하지 않고 쓰기, 배선에 없는 핀을 출력으로, 떠 있는 핀 읽기에 배선 힌트 — 핀마다 실행에 한 번). "부품이 없는 핀에 쓰기"·"출력 핀을 입력으로 읽기"를 예외로 만들지 않은 까닭: 실물 MicroPython은 이런 코드를 오류 없이 돌린다 — 가상 보드가 예외를 내면 실물에서 되는 코드가 가상에서 멈추고(PLAN §6.1 "가상이 실물보다 너그러우면 안 된다"의 반대 방향 어긋남), 학생이 배선도를 고치는 대신 코드를 이상하게 고치게 된다. 실물과 같은 예외는 실물이 내는 것만(I2C 주소 없음 ENODEV·범위 밖 duty ValueError — P3-03·P3-04). 배선을 받지 못한 실행(단위 테스트 일부)에서는 안내하지 않아 기존 안내 개수가 바뀌지 않는다 | 교실에서 안내가 너무 많다는 의견이 오면 "배선에 없는 핀을 출력으로"를 사이트 예제에서만 켬 |
| 55 | PWM 세기를 화면에 보이는 자리(P3-02, Claude 결정) | PD-36 "PWM이면 세기"와 내장 LED 밝기를 위해 `board.state` pins에 선택 칸 `duty`(0~1, 켜진 시간 비율)·`freq`(Hz)를 정하고, 화면은 `state.ts outputStrength`(HIGH 출력 1, PWM이면 duty, 멈춤·출력 아님 0)로 부품 모습(`brightness`·`strength` 0~100)을 만든다. `machine.PWM` 흉내 자체는 P3-03 몫이라 만들지 않았다(범위 밖 duty 오류 문구·`duty_u16`·버저 소리가 함께 정해져야 해서). 메시지 판 `v`는 올리지 않았다 — 없으면 예전과 같게 읽는 선택 칸이라. 단위 테스트는 가짜 `duty`로 LED 40%·진동 60%를 확인 | P3-03이 `apc_board_pwm.py`에서 `duty`를 채울 때 이름·범위가 맞는지 다시 봄 |
| 56 | 부품 단위 테스트 파일과 부품 id 약속(P3-02, Claude 결정 — P3-01 재확인 뒤 할 일 ⑦) | 부품 하나 = `tests/unit/lab/board-part-<부품 id>.test.ts` 하나(스냅샷 도우미 `tests/unit/lab/helpers/board-snapshot.ts`). `board-parts.test.ts`는 레지스트리·배선 검사만 보고 **부품 폴더마다 이 파일이 있는지 검사**한다 — 병렬 구역이 같은 테스트 파일을 고쳐 부딪히지 않게. 기존 내장 LED·BOOT 버튼 검사도 옮겼다. 사이드카·차시 md가 먼저 쓰는 부품 id(`touch-digital`·`vibration-motor`·`lcd-i2c`)는 확정, 나머지(`rgb-led`·`laser`·`buzzer`·`servo`·`fan-motor`·`touch-analog-4ch`·`oled-i2c`·`neopixel-ring`·`mp3-player`)는 README 7.5에 제안으로 적었다(그 묶음이 바꾸면 사이드카도 함께) | 병렬 준비 단계에서 구역별 부품 목록과 함께 확정 |
| 57 | esptool-js의 플래셔 스텁을 싣지 않기(병렬 제작 준비, Claude 결정 — PLAN PD-38) | esptool-js 0.6.1 패키지의 `lib/targets/stub_flasher/*.json`은 esptool v4.6.1 `flasher_stub` 바이너리로 **GPL-2.0-or-later**(esptool-js 저장소 `src/targets/stub_flasher/README.md`, 2026-09-17 확인 — PLAN §9.4의 옛 "Apache-2.0 또는 MIT" 기록을 고침). 사이트 라이선스(MIT·CC BY-NC-SA)와 섞어 배포하지 않으려고 ROM 부트로더와 직접 굽는다. 스텁 JSON을 import하는 길은 빌드·개발 서버 플러그인(`scripts/lib/esptool-stub-guard.mjs`)이 한국어 오류 모듈로 바꾸고 진짜 Vite 빌드로 스텁 글자가 번들에 없음을 확인(`tests/unit/esptool-stub-guard.test.ts`). ROM 굽기는 스텁보다 느리다(속도는 P3-09에서 재고 실물은 운영자 할 일 2번) | P3-09 |
| 58 | 부품 구역이 보드 핵심을 고치지 않게 둔 확장 자리의 모양(병렬 제작 준비, Claude 결정 — PD-39) | PWM은 핀 상태의 `pwm=(duty, freq)`(모드와 상관없이 전기를 냄, `Pin(…, 인자)`로 다시 정하면 v1.29.0 `machine_pin.c` init처럼 끊김), 아날로그는 부품이 거는 전압 `{mv}`(디지털 문턱 1.65V — 실물 VIH·VIL 사이의 들쭉날쭉함은 흉내 내지 않음), 부품 장치는 실행마다 factory로 새로 만들고 상태는 16ms 병합·최신 값만(`board.device {v,id,part,state}`), 화면 조작은 쌓이는 값 `board.device.input`(실행 전 값은 버림), 조작 칸은 SVG가 아닌 HTML(키보드·낭독기 기본), 소리는 페이지에 AudioContext 하나와 주 음량(끄기 저장 `module:board:sound`), 보드 라이브러리는 한 파일을 가상 `/board/lib`와 실물 저장이 함께 씀, 아직 없는 모듈은 표(`NOT_YET_MODULES`)로 안내하고 파일이 생기면 저절로 그 파일. 공유 테스트가 "아직 없음"에 기대던 곳(f052 LCD·machine.PWM/UART·neopixel)은 구역이 부품을 더해도 흔들리지 않게 바꿨다. 구역이 쓰다가 모자라면 요청 | P3-03~P3-05 |
| 59 | 실제 보드가 같은 [실행]·[정지]를 받는 방법(병렬 제작 준비, Claude 결정 — PD-39, SPEC §6.2 "같은 코드가 두 곳에서") | `lab.setRunTarget({ label, run(code, ctx), stop() })` — 끼우면 [실행]이 파이썬 준비를 기다리지 않고 대상으로, 콘솔 머리줄·결과 줄·입력줄(`ctx.prompt`)·'run'(`target` 이름)·'done'(오류 풀이 카드)·`data-state`(대상의 idle·running·stopping)·`data-run-target`이 보통 실행과 같다. 대상이 없으면 예전 경로 그대로(기존 검사 통과). 실행 중에는 대상을 못 바꾼다(오류). [가상 보드]/[실제 보드] 탭 화면·연결 단추 위치는 E 구역 설계 | P3-07·P3-08 |
| 60 | 블록 전용 호환 모드(PD-27)의 기다리기(병렬 제작 준비, Claude 결정) | 생성기가 만든 실행판이 `await apc_board.wait_ns_async(ns)`(Timer·핀 인터럽트·화면 입력·[정지]를 받음)·`await apc_runtime.sleep_async(초)`를 `runPythonAsync` 최상위에서 부른다 — JSPI 없이 도는지 Node 제한 모드로만 확인했다(`pyodide-board-async-wait.test.ts`). 브라우저·iPad Safari·Android Chrome 실측과 채택 여부는 P3-06(D 구역) | P3-06 |
| 61 | 모의 시리얼의 흉내 범위(병렬 제작 준비, Claude 결정) | Web Serial 명세 알고리즘(오류 이름·스트림·선 빠짐 NetworkError·opened 그대로 close 성공)과 MicroPython `pyexec.c`(raw REPL·raw-paste 창 128 + 첫 ``·SystemExit 트레이스백 없음)·ESP32 소프트 리셋 글·자동 리셋 회로(esptool ClassicReset → 다운로드 모드)를 따른다. 흉내 내지 않는 것: 보통 REPL 자동 들여쓰기·탭 완성, raw-paste 도중 구문 오류로 일찍 끝내기, ROM 부팅 글 전체, ROM 부트로더 규약(F 구역 플러그인), 버퍼 넘침, 한 번의 setSignals 안의 선 순서(한꺼번에), mini-python 밖 문법(scripts로 응답). 흉내에서 된 것은 실물 증거가 아니다(부록 B-2) | P3-07~P3-11 |
| 62 | 병렬 제작 전에 이관한 예제 36개의 사이드카(병렬 제작 준비, Claude 결정) | 제목·설명·차시·쪽·태그와 **지금 결과 그대로의 smoke 기대**만 적었다(SoftI2C·PWM·ADC·UART → ImportError, neopixel·사이트 라이브러리 → ModuleNotFoundError, 원본 결함 f062 TypeError·f074 SyntaxError, 핀만 쓰는 5개는 정상 — ESP32 스모크 40개가 기대와 모두 맞음). `parts`·`practice`와 결과가 바뀐 smoke·"준비 중" 문장은 주인 구역이 고치고(README 5.1 표), 두 구역에 걸친 예제(f058·f066·f072·f049·f056·f081·f001·f007·f075~f077)는 통합 때 맞춘다 | P3-03~P3-05, P3-11 |
| 63 | 한 작업 폴더의 개발 서버 여럿(병렬 제작 준비 확인, 2026-09-17) | `--ignore-lock` 서버 둘(4500·4501)이 나란히 뜨고 각각 spec을 동시에 돌려 통과. 잠금을 쓰는 서버(4502)가 있으면 다음 서버(4503)는 "Another astro dev server is already running."으로 멈춤. 에이전트 안에서 `--ignore-lock`은 `ASTRO_DEV_BACKGROUND=1`이 있어야 되고(Astro 7.3.2 `cli/dev/index.js`), `astro dev stop`은 잠금을 쓴 서버만 끈다. Windows에서 에이전트 백그라운드 작업을 멈춰도 node가 남아 포트로 PID를 찾아 껐다 | 병렬 제작 동안 |
| 64 | 실물 보드용 OLED 드라이버(`ssd1306.py`·`sh1106.py`) | **가상 보드는 해결(P3-04 — 부품 폴더의 흉내가 두 이름을 모두 준다). 실물 [보드에 저장]용 파일은 아직 싣지 않았다**: 후보는 micropython-lib `ssd1306.py`(MIT, 4,922바이트, git blob 37ad682d…, 서명이 코드 자료 f054~f058과 같음)와 robert-hh `sh1106.py`(MIT, 10,394바이트, blob 94cb2ca0… — 4번째 위치 인자가 `res`라 원고 서명과 다름). 싣기로 정하면 흉내 쪽 이름을 옮겨야 하고(`board-libraries.test.ts`가 같은 이름을 막는다) framebuf 흉내가 글자 설명을 버스로 넘기게 바꿔야 한다. 지금은 ① [실제 보드] [실행] **전에** 한국어로 알리고(`VIRTUAL_ONLY_MODULES`·`virtualOnlyModuleNotice` — 2026-09-18 검토 반영, 그 전에는 실행해 봐야 ImportError로 알았다) ② 오류 사전 `board-import-no-module`이 "Thonny로 같은 이름으로 올려요"를 안내하고 ③ 오류 사전 `module-not-found`의 "사이트 배포판을 써요" 문구에서 ssd1306을 빼 사실과 어긋나지 않게 했다. 파일 내려받기는 운영자 허락 뒤 | 키트 OLED 칩 확인(부록 B-2 9번) 뒤 |
| 65 | 펌웨어·모델 파일 배치 | **끝남(2026-09-18, P3-11).** 운영자가 받아 검증한 파일 6개를 `public/`으로 옮기고 크기·해시를 다시 대조했다 — 모델 5개(`public/models/`, MD5 모두 일치)와 펌웨어 1개(`public/firmware/v1.29.0/ESP32_GENERIC-20260824-v1.29.0.bin`, 1,790,544바이트·SHA-256 e67ad601…95f6). `sources.yaml`에 펌웨어 항목 2개(목록·펌웨어, 고지 `NOTICE.txt`)를 더하고 모델 항목의 주석·`fetched`를 고쳤다. 스테이징(`.cache/*-staging/`)은 그대로 둔다 | 끝 |
| 66 | PWM 흉내 범위(P3-03, Claude 결정) | 소스 그대로: 범위·문구·기본값(5000Hz·50%), LEDC 채널·타이머 고르기, 주파수마다 해상도·나눗수로 되계산한 읽기 값, `deinit` 뒤 inactive, 34~39번 `OSError (-258)`, Pin 재설정으로 끊김. 흉내 안 함: 새 duty가 다음 주기부터 반영되는 LEDC 지연, lightsleep 클럭, `invert`·deinit 뒤 쉬는 전압 | 실물 확인 뒤(부록 B-2 6번) |
| 67 | ADC 전압 → 값 모델(P3-03, Claude 결정) | 값(12비트) = 내림(전압 × 4095 ÷ 끝 전압), 끝 전압 ATTN_11DB 3300·6dB 1750·2.5dB 1250·0dB 950mV. 실물은 곡선·보정 차이로 수십씩 다를 수 있다 | 부록 B-2 10번 |
| 68 | PD-15 서보 프로필 고르기(P3-03, Claude 결정) | 배선 줄에 부품 옵션 칸이 없어 **이번 실행에서 부른 라이브러리**로 정한다(`servo_library` → servo40, 그 밖·라이브러리 없음 → mg90s). 교과서 예제는 모두 라이브러리를 쓰므로 원고대로 0·90·180°. 학생이 `servo.duty(40)`을 직접 쓰면 mg90s 기준 약 30°로 그려진다 | 배선 옵션 칸을 만들면 다시(아래 69번) |
| 69 | 배선 한 줄의 부품 옵션 칸(구역 A 요청 5, 선택) | 만들지 않았다. `WiringEntry.options`를 두면 예제 배선에 서보 프로필·RGB 극성을 적을 수 있다(PD-15 원래 뜻). 지금은 라이브러리 감지로 충분해 미룸 | 2-2-4 차시(P5)를 쓸 때 |
| 70 | 핀 상태 표의 ADC 핀 표시(구역 A 요청 5-b, 선택) | 만들지 않았다. `ADC(Pin(32))`를 만든 핀은 표에 "정하지 않음 · 1 (HIGH)"로 보인다("아날로그 입력(ADC) · 약 2.63V"가 맞는 설명) | 다음 부품 묶음 |
| 71 | 버저 소리(P3-03, Claude 결정) | PWM은 그 주파수의 펄스파(duty 50%면 사각파), 크기 = 0.07 × sin(π·duty). 핀이 계속 1이면 사이트가 정한 1000Hz 고정음(실물 능동·수동 버저 차이는 부록 B-2 12번). 음원 파일 없음(PD-16) | 실물 확인 뒤 |
| 72 | 4채널 터치 조작 방법(P3-03, Claude 결정) | 한 번 누르기 대신 조작 칸의 [패드 1~4] 누르고 있기 + "누르지 않을 때의 값" 막대. 패드 값 688·1535·2381·3263(원고 139쪽)이 정확히 읽히는 전압을 쓴다 | 부록 B-2 10번 |
| 73 | 팬 모터 모델(P3-03, Claude 결정) | 속도 = \|INA duty − INB duty\| × 100%, 방향은 큰 쪽, 둘 다 켜짐은 멈춤(브레이크 — 실물 모듈 확인 전), 기본 핀 25·26 | 부록 B-2 12번 |
| 74 | RGB LED 극성 표기(P3-03, Claude 결정) | 교과서 코드처럼 1이면 켜지는 방식(공통 음극)으로 그리고 그림에 "1이면 켜짐"을 적는다. 기본 핀 없음(예제마다 다름) | 부록 B-2 14번 |
| 75 | ESP32 사이트판(PD-10) 모양(P3-03, Claude 결정) | 제목·설명은 사이드카에만 두고 파이썬 파일에는 머리말을 넣지 않는다(줄 번호를 원본·원고와 맞추려고). 한 줄 안의 고침은 그 줄 끝 `# [사이트판]`, 줄 추가는 원고와 같은 자리. 목록에서 원본 제목 끝에 "(원본 파일)", 사이트판에 "(사이트판)". 이관 기록이 아니라 예제 스모크 대상이 아니다(구역 테스트로 지킨다) | Phase 4 사이트판을 만들 때 다시 |
| 76 | I2C 주소·핀이 틀릴 때의 오류 번호(P3-04, Claude 결정) | v1.29.0 SoftI2C 소스대로 모두 `OSError: [Errno 19] ENODEV` + 한국어 까닭. 원고 오류 표의 "EIO(5) = 핀 번호 오류"는 소스에서 나오는 길을 찾지 못해 흉내 내지 않았다 | 부록 B-2 9번(실물에서 어떤 번호가 나오는지) |
| 77 | 문자 LCD 흉내 수준(P3-04, Claude 결정) | 드라이버를 흉내 내지 않고 **선에 오간 바이트만** 해석한다(PCF8574 비트 → HD44780 니블). 그래서 원본 `i2c_lcd.py`가 그대로 돌고 19자 줄 넘김·커서·CGRAM·백라이트가 저절로 나온다. 16ms 병합이라 글자마다 메시지를 보내지 않는다 | 끝 |
| 78 | LCD 글자 모양(P3-04, Claude 결정) | HD44780 **A00** 문자표(0x5C → ¥ 등). 키트 LCD가 A00인지 A02인지는 확인 전. 한글은 `ord`의 아래 8비트가 그대로 가서 다른 글자가 된다(실물과 같음 — 사이드카에 "영어로 적어요") | 부록 B-2 9번 |
| 79 | OLED 흉내 방식(P3-04, Claude 결정) | 드라이버 흉내(`ssd1306`·`sh1106`)가 실제 명령 바이트를 보내고 장치가 SSD1306 명령표로 해석한다(주소 틀림·poweroff·invert·contrast가 명령으로 드러남). 글자는 `_apc_hint`로 넘겨 화면 낭독기 설명에 싣는다(실물 선에는 없는 가상 전용 통로) | 실물 드라이버를 싣기로 하면 다시(64번) |
| 80 | 두 이름 한 흉내(P3-04, Claude 결정) | 원고는 `sh1106`, 코드 자료는 `ssd1306`. 원고의 `sh1106.py`가 자료에 없어 스크린숏 서명(`SH1106_I2C(width, height, i2c, addr=0x3C, external_vcc=False)`)을 따르고 ssd1306 흉내를 물려받게 했다 | 부록 B-2 9번(키트 칩) |
| 81 | framebuf 글꼴(P3-04, Claude 결정) | 펌웨어의 `font_petme128_8x8.h`는 출처·라이선스 표기가 파일에 분명하지 않아(원칙 5) 옮기지 않고 사이트가 5×8 점 무늬를 그렸다. 칸(8×8)·자르기·UTF-8 바이트마다 한 칸 규칙은 실물과 같아 "몇 글자가 들어가나"는 같다 | 끝 |
| 82 | RTC 초기화 시점(P3-04, Claude 결정) | 가상 보드가 실행마다 보드를 새로 켜는 규칙(P3-01)과 같게 시계·RTC 메모리를 처음으로. 13월은 소스 표대로 다음 해 1월 + 안내, 0월·14월 이상은 0일로 계산 + 안내 | 부록 B-2 9번(실물이 소프트 리셋 뒤에도 이어지는지) |
| 83 | Pretendard가 숫자 사이의 x를 곱하기 기호로 그림(P3-04 발견) | `font-variant-ligatures: none`으로 끈다 — `global.css`의 `.prose`(본문·오류 카드·사전), 실습실의 실습 방법 상자·시리얼 창·점검 도우미. `font-feature-settings`로는 안 된다(캡처로 확인) | 16진수를 글로 쓰는 새 화면이 생기면 같은 규칙 |
| 84 | 네오픽셀 흉내 범위(P3-05, Claude 결정) | micropython-lib neopixel 0.1.0과 같은 동작(GRB `ORDER=(1,0,2,3)`, `write()`를 불러야 반영, bytearray 규칙). 신호 시간(ns)은 흉내 내지 않는다 | 부록 B-2 6번(펌웨어 내장 import·키트 링 GRB) |
| 85 | UART 흉내 범위(P3-05, Claude 결정) | 기본값 115200 8N1·txbuf/rxbuf 256·timeout 0, 번호 0~2(UART0은 REPL), repr의 나누개 속도 115201, 받을 칸 rxbuf+128, 바이트 도착 시각, 속도·비트가 다르면 `reframe`이 비트 단위로 깨뜨림. 흉내 안 함: UART0 읽기, rts·cts 흐름 제어의 실제 멈춤 | 부록 B-2 11번 |
| 86 | MP3 프레임 받아들이는 범위(P3-05, Claude 결정) | 체크섬 없는 8바이트(원고 방식)와 10바이트를 모두 받고 뒤의 0은 버린다. 틀린 체크섬은 무시 + 오류 응답. 실물이 8바이트를 받는지는 원고 결과 사진 근거뿐이라 **가상이 실물보다 너그러울 수 있다** | 부록 B-2 11번 |
| 87 | MP3 음원(P3-05, Claude 결정) | 사이트가 작곡한 멜로디 3곡(3.6·4.8·4.2초)을 Web Audio로 합성한다(PD-16 — 음원 파일 없음). 실물은 microSD의 곡을 튼다 | 끝 |
| 88 | 보드 콘솔 `input()`(P3-05, Claude 결정) | 실물은 `input()` 대기 중에도 Timer·IRQ·UART가 돌므로(`MICROPY_EVENT_POLL_HOOK`), ESP32 실습실만 `builtins.input`을 바꿔 `wait_ns(20ms)` 조각으로 기다린다. 화면 입력줄은 실습실 틀의 공개 `lab.prompt`(P3-11에서 더함) | 부록 B-2 19번(Thonny에서 실물도 그러는지) |
| 89 | USB-UART 변환기 전원 다리(P3-05 → P3-11 반영) | 실물은 자기 USB로 전원을 받고 TX·RX·GND만 잇는다. 부품 규약에 `power: { gnd, vcc: false }`를 더해 GND만 그린다 | 끝 |
| 90 | 시리얼 창의 받은 글자(P3-05, Claude 결정) | 새 실행마다 비운다(실물 시리얼 프로그램은 남긴다 — 헷갈림을 줄이려는 선택) | 교실 의견 뒤 |
| 91 | 블록 전용 호환 모드의 실기기 실측(P3-06) | **확인 필요.** Edge `?limited=1`은 엔진에 JSPI가 있어 완전한 흉내가 아니다(고친 코드도 `time.sleep` 동안 입력을 받고 [정지]가 105~115ms에 먹었다). `playwright.config.ts`에 `webkit` 프로젝트를 두어 CI가 WebKit으로 그 검사만 돌린다. iPad·iPhone·Android 실기기는 운영자 확인(할 일 3번) | CI WebKit 결과·운영자 기기 |
| 92 | 실습실 틀의 두 공식 자리(P3-06 요청 3 → P3-11 반영) | `lab.setRunCodeTransform(fn)`(블록 호환 모드가 보낼 코드를 고름)과 공개 `lab.prompt(label)`(보드 콘솔 input)을 틀에 더했다. 편집칸 읽기 전용은 아직 우회다(`mode-ui.ts`가 CodeMirror Compartment를 덧붙임 — 편집기가 `view.setState`로 상태를 통째로 바꾸면 사라진다, 지금 그런 곳은 없다) | 편집기를 고칠 때 `editor.setReadOnly`로 |
| 93 | 블록 작업판 저장(P3-06, Claude 결정) | `localStorage`에 JSON으로(`module:blocks:workspace`). 블록 type 이름을 바꾸거나 지우면 저장된 작업판을 못 읽고 기본 예시로 돌아간다(try/catch로 처리하지만 학생 작업판은 사라진다) | 블록 이름을 바꿀 때 조심 |
| 94 | 코드 → 블록 변환(P3-06) | 만들지 않았다(SPEC·PLAN에 없음). 코드 모드에서 고친 코드는 블록으로 돌아가지 않고, [블록]을 다시 누르면 마지막 작업판이 열린다(편집칸 코드가 다르면 dialog로 묻는다) | 요청이 오면 |
| 95 | 블록 생성 코드의 실물 동작(P3-06) | **확인 필요.** 생성 코드는 교과서 예제와 같은 API(서보 `ServoMotor`, 팬 `GORILLACELL_DCMOTORS`, MP3 프레임, LCD 0x20, 네오픽셀 16칸)지만 실물에서 돌려 보지 않았다 | 부록 B-2 항목과 함께 |
| 96 | 판별에 Ctrl-B를 더함(P3-07, Claude 결정) | PLAN 문구는 Ctrl-C·Enter지만 그것만으로는 `>>> `만 오고 버전 배너가 오지 않아, 리셋 없이 배너를 다시 찍는 Ctrl-B를 더했다. Enter는 Ctrl-C 뒤에 보내 `input()`을 기다리던 main.py에 빈 줄이 들어가지 않게 한다. 그래서 연결하면 보드에서 돌던 프로그램은 멈춘다(의도) | 부록 B-2 17번 |
| 97 | 실행마다 소프트 리셋(P3-07, Claude 결정) | 가상 보드가 매번 새로 시작하는 것과 같게 지난 Timer·PWM·핀을 정리한다(mpremote run 기본과 같음). 대가로 실행마다 boot.py가 다시 돌아 0.2~1초 더 걸리고 전역 변수가 이어지지 않는다 | 부록 B-2 17·20번 |
| 98 | [정지]가 확인되지 않을 때(P3-07, Claude 결정) | Ctrl-C를 0.5초 간격으로 6번 보낸 뒤 "멈춤을 확인하지 못함"으로 끝내고 busy 상태에서 [보드 되찾기]·EN 버튼을 안내한다. raw-paste 전송 중 정지할 때 보드의 받기 끝 `\x04`에 답하지 않는 것은 원문상 맞지만 실물 확인 전 | 부록 B-2 17·21번 |
| 99 | 포트 선택 창을 거르지 않음(P3-07·P3-09·P3-10, Claude 결정) | `requestPort`에 filters를 주지 않아 CH9102·CP210x 같은 다른 칩 보드도 고를 수 있다(블루투스 포트도 목록에 보인다 — 선택 창 도움말과 판별로 안내). 학교 관리 정책으로 Web Serial이 막힌 브라우저는 창 없이 곧바로 NotFoundError가 와서 "포트를 고르지 않았어요"로 보일 수 있다 | 부록 B-2 27·28번 |
| 100 | 실제 보드 탭 선택·연결을 기억하지 않음(P3-07, Claude 결정) | 늘 [가상 보드]로 시작한다(원칙 3, 공용 PC의 다음 학생이 선택 창을 만나지 않게). 새로 방문하면 [보드 연결]을 다시 눌러야 한다 | 교실 의견 뒤 |
| 101 | 한 포트에 주인 하나(P3-07·P3-10, Claude 결정) | `BoardConnection`은 실습실 real-board 모듈과 점검 도우미가 각자 하나만 만든다. 같은 페이지에서 두 곳이 열면 `port-in-use`가 난다. 보드 준비 페이지의 [보드 연결]은 늘 닫고, 굽기가 시작되면 창 이벤트로 포트를 넘긴다 | 부록 B-2 30번 |
| 102 | [보드에 저장] 자리와 방식(P3-08, Claude 결정 D1·D2·D3) | mpremote `fs_writefile` 방식(256바이트씩 `w(b'…')`, SHA256이 같으면 건너뜀) + 사이트가 더한 안전장치(임시 이름 `.part`에 쓰고 크기 확인 뒤 제자리, 표시 글자로만 출력 읽기). 자리는 **보드 뿌리**(교과서 Thonny 저장 자리 + ESP32 `sys.path`). [보드에 저장]은 main.py만 쓴다(boot.py 고르기 없음) | 부록 B-2 18번 |
| 103 | [실행]의 라이브러리 올리기(P3-08, Claude 결정 D4) | 코드가 부르는 사이트 라이브러리가 보드에 **없을 때만** 올린다(올리기 전 콘솔 안내). 있으면 건드리지 않고 사이트판과 다르면 안내만 한다(학생이 고친 파일을 덮지 않게). 그래서 교과서판 `i2c_lcd.py`가 있는 보드에서는 f050 `move_to`가 AttributeError다 | 부록 B-2 22번 |
| 104 | 보드 `input()` 규칙(P3-08, Claude 결정 D5) | 코드가 도는 동안 입력줄을 늘 열어 둔다(raw REPL에 "입력 기다림" 신호가 없다 — Thonny 셸과 같은 모양). 보낼 줄은 실물 readline이 받는 32~126 글자 + `\r`(한글은 빼고 안내, 250자 상한), 되울림은 한 번 걸러 낸다(10초 안에 맞추기 시작하지 않으면 포기) | 부록 B-2 19번 |
| 105 | 되찾기와 자동 실행 파일 끄기(P3-08, Claude 결정 D6·D7·D8) | boot.py 반복은 6초 뒤 Ctrl-C 0.6초 간격 최대 6번(되찾은 보드는 다음부터 0.5초). [보드 되찾기]는 Ctrl-C 되풀이 → RTS로 다시 켜며 되풀이 → EN 버튼 안내 3단계. [boot.py 끄기]는 지우지 않고 `boot_off.py`로 이름만 바꾼다. **반복문 첫 줄부터 멈춤 신호를 삼키는 코드는 되찾기가 실패할 수 있다**(그때는 "EN을 누른 채 [보드 되찾기]" → 펌웨어 다시 굽기 안내) | 부록 B-2 20·21번 |
| 106 | ROM만으로 굽기(P3-09, PD-38 실행) | esptool-js 0.6.1의 모자란 곳 네 군데를 사이트가 채웠다(8바이트 SPI_ATTACH·0x400 블록·ROM ASCII MD5·RTS 리셋). **ROM에는 전체 지우기 명령이 없어 FLASH_BEGIN의 "지울 크기"로 위치 0부터 지운다 — 실물 확인 전이라 그 선택은 기본 꺼짐** | 부록 B-2 25번 |
| 107 | 굽기 기본 속도 460800(P3-09, Claude 결정) | 속도 전환은 끝난 뒤 한 번 읽어 확인하고, 실패하면 "느린 속도로 다시 굽기"(115200)로 이끈다. 쓰는 도중 실패도 느린 속도를 권한다. 시간 어림: 압축 1,172,437바이트 = 0x400 블록 1,145개, 선로 시간만 460800bps 약 25초·115200bps 약 102초 | 부록 B-2 24번 |
| 108 | 펌웨어 파일 검증 순서(P3-09, Claude 결정) | 크기 → ESP32 이미지 머리(0xE9·세그먼트 수·칩 id 0) → SHA-256. 하나라도 틀리면 굽지 않는다. 목록은 빌드할 때 HTML에 심고(서비스 워커 캐시 문제 없음), 파일은 페이지를 열 때 HEAD 한 번 | 끝 |
| 109 | esptool-js 내부에 기대는 곳(P3-09, Claude 결정) | `loader._connectAttempt`·`transport.appendArray`를 연결하는 동안만 가로채 시도 기록과 "받은 글"을 모으고, `FLASH_WRITE_SIZE`를 바꿔 쓰고 `checkCommand`를 직접 부른다. 판을 올리면 `tests/unit/firmware/flasher.test.ts`가 가장 먼저 깨진다(버전 고정) | esptool-js를 올릴 때 |
| 110 | 펌웨어 고지(NOTICE.txt) 완성도(P3-09) | MicroPython v1.29.0 LICENSE 전문 + ESP32 빌드 설정으로 확인한 저장소 안 제3자 코드(littlefs BSD-3·oofatfs BSD-1·berkeley-db-1.xx BSD-4·ppp_set_auth.c Apache-2.0) + ESP-IDF v5.5.2 COPYRIGHT 요약·원문 주소. **BSD-4-Clause의 광고 조항은 원문 확인이 남았다** | 검토 때 |
| 111 | 보드 준비 페이지의 연결 확인(P3-10, Claude 결정) | 한 번 누르면 열기 → 판별 → 내장 LED 세 번(0.3초씩, 시험 코드는 저장하지 않음) → **늘 닫기**. 포트를 쥐고 있으면 굽기·실습실·Thonny가 못 연다. GPIO2에 다른 부품을 꽂아 두었으면 그 핀에 신호가 세 번 간다(화면 글로만 알린다) | 부록 B-2 27·30번 |
| 112 | "연결이 안 될 때" 안내에 접는 상자 금지(P3-10, 테스트 제약) | `tests/e2e/start.spec.ts`가 `details#port-not-found` 안의 `summary`를 하나로 보고 키보드로 접고 편다. 전산 담당 부탁 글·하드웨어 ID도 펼친 상자 안에 그냥 보인다. 안내를 늘릴 때 같은 제약을 지킨다(MAINTENANCE 4-5) | 끝 |
| 113 | 장치 관리자 그림의 이름·COM 번호(P3-10) | 직접 그린 SVG 세 장면에 "예"라고 밝혀 두었다(드라이버가 없을 때 보이는 이름은 보드마다 다르다) | 부록 B-2 28번 |
| 114 | 2-1-1 차시 링크(P3-10, Claude 결정) | 빌드할 때 `content/lessons`에 2-1-1이 있으면 그 차시로, 없으면 "II. 피지컬 컴퓨팅 단원 차례"로 잇는다(P5에서 차시 md가 생기면 코드 수정 없이 바뀐다) | P5 |
| 115 | 모의 ROM 플러그인의 "펌웨어 없음" 흉내(P3-10) | 모의 보드 객체의 받기·내보내기를 페이지 안에서만 감싼다(TypeScript private 메서드를 실행 중에 감쌈). 공유 모의 보드가 그 이름을 바꾸면 시나리오 C 검사가 먼저 깨진다 | 모의 보드를 고칠 때 |
| 116 | 실물 점검 도우미 항목의 범위(P3-11, Claude 결정) | 부록 B-2 Phase 3 항목을 화면 항목 22개로 옮겼다(예상 132분·추정). 항목 하나 = `src/lab/esp32/check/items.ts` 한 줄, 코드는 실물 MicroPython 이름만·끝없는 반복 금지. 사이트가 값을 판정하지 않고 사람이 예/아니오로 답한다(눈으로 보는 것이 목적) | 실물 확인 결과를 받은 뒤 항목을 다듬는다 |
| 117 | 점검 도우미의 배선 그림(P3-11, Claude 결정) | 가상 보드 그림을 **꺼진 모습으로 한 장** 그린다(누를 수 없다 — "선을 이렇게 꽂아요"만 보여 주면 되므로). 예제 배선도와 같은 자리·같은 색이다. 보드 그림 글자 CSS를 공용 파일 `src/styles/board-drawing.css`로 옮겨 실습실과 함께 쓴다 | 끝 |
| 118 | 1분 개념 카드를 실습실마다 다르게(P3-11 발견·결정) | ESP32 실습실에서 사진·에지 카드가 보이던 것을 보드 카드 5장(마이크로컨트롤러·핀·MicroPython·0과 1·PWM)으로 바꿨다(`cardsForLab`). 통신 실습실이 생기면 그 묶음도 더한다 | Phase 4 |
| 119 | `wait_ns`의 첫 flush를 16ms 규칙으로(P3-11, 구역 A 요청 3) | 짧은 sleep 반복문(f060)이 `board.state`를 초당 약 540개 보내던 것을 16ms 병합으로 바꿨다. **그보다 짧은 펄스는 그림에 안 보인다**(P3-01부터 알려진 차이, 미해결 49) | 끝 |
| 120 | 모의 보드에 `hashlib.sha256`(구역 E 요청 6-3, 선택) | 더하지 않았다. 브라우저·Node 공용 코드에 새 해시 구현을 넣어야 하고, [보드에 저장]의 "같은 파일 건너뜀" 갈래는 이미 정한 응답(`scripts`)으로 시험하고 있다. 실물 보드에 `hashlib`이 없으면 늘 다시 쓰기로 떨어진다(안전한 쪽) | 부록 B-2 18번 결과 뒤 |
| 121 | 영상처리 실습실의 카메라 고르기·검은 영상 감지(운영자 웹캠 요청 1·2·5~7) | 코드는 만들지 않았다. Phase 2 기능 변경이라 영상처리 전체 회귀가 필요해 P3-11 범위에서 미뤘다. 대신 **확인된 해결 방법**(교실 화면 공유 프로그램의 가상 카메라를 장치 관리자에서 끄기)을 `/help/`의 "카메라는 켜졌는데 화면이 까맣게만 보여요"와 `/start/teacher/` 체크리스트에 실었다. 만들 것: `enumerateDevices`로 [카메라 고르기], 가상 카메라 이름 뒤로 미루기, 2초 밝기·분산으로 검은 영상 안내, `/start/check/` 카메라 항목 | Phase 3 검토 뒤 한 묶음으로 |
| 122 | 오류 사전을 실습실 카드로 심는 길에서 트레이스백 조건이 사라졌던 것(P3-11 통합에서 브라우저 테스트가 찾음) | **고쳤다.** `catalogJson`은 camelCase(`tracebackPatterns`)로 쓰는데 `catalogFromJson`의 검사기는 데이터 파일 이름(`traceback_patterns`)만 받아, 카드 사전에서 "어느 파일에서 난 오류인지" 조건이 없어졌다(P2-06부터 있던 결함 — 그때는 그 조건을 쓰는 항목의 메시지 패턴이 좁아 가려져 있었다). 구역 C의 네오픽셀 항목이 들어오면서 학생의 일반 `IndexError: list index out of range`에 네오픽셀 풀이가 붙어 드러났다. 검사기가 두 이름을 모두 받게 하고, 단위 테스트가 JSON으로 되읽은 사전으로도 같은 풀이가 나오는지 보게 했다 | 끝 |
| 123 | 포트 이름 글이 두 곳에 있다(구역 E·F 요청 7·11) | 굽기 화면은 `src/lab/firmware/controller.ts`의 `describePort`(제조사 VID만 — "WCH CH340 계열(1a86:7523)"), 실습실·점검 도우미·보드 준비 페이지의 연결 칸은 `src/lab/serial/usb-chips.ts`의 `describePortInfo`("CH340(WCH) · USB 1a86:7523")를 쓴다. 두 글이 모두 맞고 브라우저 테스트가 각 글자를 보고 있어, P3-11에서는 통일하지 않고 남겼다(통일하면 `describePort`를 지우고 F의 테스트 문구를 함께 고친다 — 화면 글이 바뀌므로 캡처 확인 필요) | Phase 3 검토 때 |
| 124 | 단위 테스트 도우미의 결과가 파이프에서 잘렸다(P3-11 마무리 CI에서 드러남) | **고쳤다.** `tests/unit/<분야>/helpers/pyodide-*-run.mjs` 9개가 결과 JSON을 stdout에 쓰고 곧바로 `process.exit(0)`을 불러, 파이프 버퍼(리눅스 64KB)를 넘는 글이 잘렸다. 그래서 **리눅스 CI에서만** `board-i2c` LCD·OLED와 `pwm-adc` 세 검사가 "Unterminated string in JSON"으로 실패했다(pwm-adc 결과는 860,107자, 139~146KB 지점에서 끊김 — 윈도에서는 통과해 로컬에서 안 보였다). 공통 함수 `tests/unit/helpers/finish-json.mjs`가 글이 다 나갔다는 알림을 받은 뒤 끝내고(10초 안전장치), 보드 도우미는 그래도 못 읽으면 글자 수와 끝 40자를 한국어로 알려 준다. **새 도우미 스크립트도 `process.stdout.write`+`process.exit` 대신 이 함수를 쓴다** | 끝 |
| 125 | 펌웨어 고지(`public/firmware/v1.29.0/NOTICE.txt`)의 제3자 구성요소 라이선스 **전문** | **반만 했다.** "이 고지는 초안입니다 … 파일을 올리기 전에 채웁니다"라고 적힌 채 1.79MB 바이너리와 함께 배포되던 자기모순을 사실 기술로 고쳤고(2026-09-18 검토 반영), 같은 일이 되풀이되지 않게 **고지 파일에 "초안"·TODO가 있으면 출처 검사가 빌드를 멈춘다**(`scripts/lib/sources-check.mjs`의 `DRAFT_NOTICE_PATTERN`). 남은 일은 MIT·BSD 계열이 바이너리 재배포에 요구하는 고지 전문을 2·3절 구성요소마다 싣는 것인데, ESP-IDF v5.5.2 `COPYRIGHT.rst`·`LICENSE`와 MicroPython v1.29.0 `lib/berkeley-db-1.xx/LICENSE` 등을 **내려받아야** 해서(Claude는 내려받지 않는다) 운영자 할 일 19번으로 남겼다 | 운영자 할 일 19번 답 뒤 |
| 126 | 코드 모드에서 부품을 고르는 화면(UI) | 만들지 않았다. 2026-09-18 검토 반영으로 **편집칸 머리말 `# @part`를 다시 읽는 것**까지 했다(예제를 고쳤거나 빈 칸에서 쓴 코드일 때만 — `wiringFromEditorCode`). 그래서 학생이 `# @part touch-digital 17`을 적으면 부품이 그려지고 누를 수 있다. 목록에서 부품을 골라 붙이는 화면은 블록 모드가 이미 그 일을 하고 있어 미뤘다 | 차시 md(P5)에서 필요해지면 |
| 127 | 4채널 터치 값 막대 한 칸 45 | 화살표 키만 쓰는 학생이 판정 구간(500~800 …)에 닿게 step 1 → 45로 키웠다(4095를 정확히 나누는 값이라 End 키가 끝값 4095에 닿는다 — 2026-09-18 검토 반영). 실물 측정값이 오면(부록 B-2 10번) 구간과 함께 다듬는다 | 부록 B-2 10번 뒤 |
| 128 | `ESP32BLE.py`의 MIT 고지 | 원본 그대로 `examples/esp32/lib/third-party/ESP32BLE.py`에 옮기고 출처는 "원 권리자 보유 — 운영자 사용 허락(O5)"로 등록했다(TechToTinker 025 글 계열). 2black0/MicroPython-ESP32-BLE(MIT)와 같은 코드로 확인되면 `i2c_lcd.py`처럼 MIT 고지를 더한 사이트판으로 바꾼다 — 대조하려면 파일을 받아야 해서 운영자 확인이 필요하다(INVENTORY §8, PLAN §6.5) | 운영자가 저장소 LICENSE를 확인해 줄 때 |
| 129 | 보드 라이브러리 이름에 대문자 허용 | `import ESP32BLE`가 되려면 파일 이름이 `ESP32BLE.py`여야 해서, 이관 도구의 대상 경로 규칙과 `board-libraries.ts`의 이름 규칙을 **`examples/esp32/lib/` 아래에서만** 대문자까지 받게 풀었다(이름 겹침은 대소문자를 가리지 않고 비교 — Windows·macOS 파일 이름 때문). 예제 파일 이름은 그대로 소문자만(PD-09) | 끝남(2026-09-18) |
| 130 | 옮긴 코드의 기기 주소 가리기 | 원본을 고치지 않는다는 PD-10의 **유일한 예외는 개인정보**다(DECISIONS 저작권 예외). 이관 도구에 `privacy: [mac]`을 더해 BLE·MAC 주소를 같은 글자 수의 `XX:XX:XX:XX:XX:XX`로 바꾼다(줄·칸 위치 그대로). 실제로 가린 파일은 f089·f100·f104·f114·f158 다섯 개(각 1곳). 선언하지 않았는데 주소가 있으면 이관이 멈추고, 저장소 전체를 보는 단위 테스트도 있다. 브라우저는 주소로 연결하지 않으므로(PLAN §7.3) 학습에 값이 필요 없다 | 끝남(2026-09-18) |
| 131 | MQTT.js 브라우저 번들의 전이 의존성 고지 | 브라우저는 `package.json` exports의 browser 조건으로 **미리 묶인 한 파일**(`dist/mqtt.esm.js`)을 받는다 — esbuild로 확인하니 번들에 들어가는 npm 패키지는 `mqtt` 하나다. 그래서 의존성(mqtt-packet·readable-stream·ws·lru-cache·ieee754 …)의 코드는 그 파일 안에 있고 번들 검사에 이름이 따로 나오지 않는다 → 고지 `public/licenses/mqtt.txt`에 의존성 44개의 LICENSE 원문을 모아 덮었다. P4-06이 실습실에 붙인 뒤 `npm run build` 번들 검사로 실제 목록을 다시 본다(PLAN P6-04) | P4-06에서 |

## 야간 작업 로그

| UTC 시각 | 세션(로컬·클라우드) | 한 일 | 커밋 | 실패와 원인 |
|---|---|---|---|---|
| 2026-09-18T00:40Z | 로컬(새 계정 이어받기) | **Phase 3 검토 반영**: 적대적 검토 3명(완료 기준 재현·초보자 사용성·안전/저작권)의 지적 **26건(치명 1·중요 8·사소 17)**을 근거를 하나씩 확인해 판단하고 24건을 반영했다(2건은 파일 내려받기가 필요해 운영자 할 일 19번·미해결 64). 굵직한 것: ① **콘솔이 화면 밖이라는 것을 구조로 인정**(치명) — 결과 칸 아래 "콘솔에 결과가 나왔어요" + 마지막 3줄 + [콘솔 보기 ↓], 콘솔 제목 "새 출력 N줄" 배지 ② **편집칸 줄 접기**(주석이 데스크톱 38%·휴대폰 69% 잘려 있었다) ③ **`board.notice`**(일곱 번째 메시지)로 배선 어긋남 안내를 그림 아래 칸에도 ④ 오류 사전 **`level: notice`**(고칠 것 없는 안내는 파랑 상자) ⑤ **[보드에 저장] 덮어쓰기 확인 창**(`confirmOverwrite`·`BoardSaveCancelled`) ⑥ 보드 그림 최소 폭 32rem·넘침 그늘·[원래 크기로] ⑦ 예제 한 줄 설명 표시 ⑧ 편집칸 `# @part` 다시 읽기 ⑨ **초안 고지 빌드 차단**·`repo-check.yml`(PROGRESS.md만 바뀐 push도 저장소 검사) ⑩ `repo-check.mjs`의 리터럴 NUL 제거 ⑪ 자르기 호환 규칙 오탐·조사(`withParticle`)·4채널 터치 막대 45·`dir(machine)`·부록 B-2 7번·오류 사전 항목 수 93. **회귀 하나를 찾아 고쳤다**: 줄 접기로 문서가 길어지자 CodeMirror 가상 스크롤이 화면 밖 줄을 DOM에서 빼서, DOM으로 편집칸 글을 읽던 테스트 도우미가 깨졌다(브라우저 2건 실패) — 도우미가 CodeMirror 문서를 직접 읽게 고쳤다. | 7e1abe5·51b8ffa·b939677·6b45e68·b801c97·3c4dc8e·ec2d142·3427367·7d21179 | 브라우저 1회차 2건 실패(위 회귀), 고친 뒤 2회차 393 통과, 알림 위치까지 고친 3회차 **393 통과·223 건너뜀·0 실패·0 흔들림(32.7분)**. 단위 테스트는 브라우저 실행과 동시에 돌리면 6건이 자원 경쟁으로 실패했고(따로 돌리면 통과), 기계가 한가할 때 147파일 1,489건 모두 통과 |
| 2026-09-17T21:30Z | 로컬 | **Phase 3 P3-11 마무리 검증**: ① **실사이트 확인** — `/`(38,263바이트)·`/labs/esp32/`(236,371)·`/labs/esp32/check/`(27,763)·`/start/board/`(75,058)·`/help/errors/`(252,684)·`/licenses/blockly.txt`·`/firmware/manifest.json`·`/models/hand_landmarker.task`(7,819,105) 모두 200. ② **실사이트 시나리오 B 통과** — 블록판 16.3초·BOOT 버튼판 11.3초(데스크톱 1366×768, `PW_BASE_URL`로 GitHub Pages 주소). ③ **점검 도우미도 실사이트에서 통과** — 4건(모의 포트로 항목 22개 끝까지 32.4초, 휴대폰 375px 넘침 없음). ④ **CI 판정** — 첫 push(99c341d)의 테스트 35270913000에서 단위 테스트 세 개가 **리눅스에서만** 실패(원인은 오른쪽 칸), 브라우저 테스트·링크 검사 작업은 성공. 고쳐서 다시 밀어 테스트 35274533190 성공(단위 147파일 1,477통과·8건 건너뜀 108초, `astro check` 566파일 0오류, 브라우저 390통과·223건너뜀·흔들림 0건 15.3분, 링크 검사 2,088개)·배포 35274533239 성공. ⑤ **로컬 재검증** — Vitest 147파일 1,485건 + Container 2파일 7건 통과, `astro check` 566파일 0오류, 저장소 검사 1,030개. ⑥ 사이드카 27개가 `git status`에 고친 것처럼 보였던 것은 예전 CRLF 시절 크기가 색인에 남아 있던 것으로(내용은 HEAD와 같음) `git add --renormalize`로 색인만 새로 썼다 — 커밋할 내용은 없었다. | 9d0bdcb | **리눅스에서만 나던 실패의 원인**: `spawnSync`로 띄우는 도우미 스크립트가 결과 JSON을 파이프에 쓰고 곧바로 `process.exit(0)`을 불러 64KB 뒤가 잘렸다(미해결 124에 자세히). 그 밖: push 한 번이 `git-credential-manager`에서 6분 넘게 멈춰(CPU 15초) 프로세스를 끊고 `gh auth git-credential`을 **한 번만** 붙여 올렸다(git 설정은 바꾸지 않음) |
| 2026-09-17T19:30Z | 로컬 | **Phase 3 P3-03~P3-11 통합**: ① **공유 파일 요청 판단·반영**(여섯 구역의 `.cache/phase3-requests/*.md`) — 오류 사전에 항목 34개와 새 묶음 `flash`(항목 101개·묶음 8개, 오류 사전 단위 테스트 4파일 58건 통과 — `catalog.test.ts`는 16건), 실물 MicroPython 문구 패턴(`isn't defined`·`unsupported types for __add__`), 실습실 틀의 공개 `lab.prompt`(구역 C의 보드 콘솔 input이 켜짐)·`lab.setRunCodeTransform`(구역 D의 호환 모드가 우회를 버리고 이 자리를 씀), 보드 모듈의 `data-board-wiring-override`(구역 D 배선 알림 — 시나리오 B가 통과하게), `visual.summary`를 화면 낭독기 이름에(부품 6개의 `aria-description` 우회 제거), `wait_ns`의 첫 flush를 16ms 규칙으로(f060 초당 540개 → 병합), [정지]·조절 패널 안내를 실행 대상에 맞게(CSS·`PARAM_NOTE_TARGET`), 보드 라이브러리 자리 글을 `/lib` → 보드 뿌리로 + 펌웨어 굳힘 모듈 이름 금지 목록, 부품 전원 다리 `vcc: false`(USB-UART 변환기), 모의 보드의 readline ASCII 제한·소프트 리셋 순서(실물과 같게 — 그 결과 `sys.exit()`·`machine.soft_reset()`이 같은 바이트라 `softReboot` 표지로 알린다), 부품 테스트 3개를 `tests/unit/lab/`로 옮김, Blockly 도구를 `tests/e2e/helpers/blocks.ts`로, `playwright.config.ts` `webkit` 프로젝트 + CI 설치, `astro.config.mjs` `APC_VITE_CACHE_DIR`, `.gitignore` `__pycache__`, `npm test`에 Container 테스트, 예제 스모크에 동작 제한 시간. ② **파일 배치**: 운영자가 검증한 모델 5개·펌웨어 1개를 `public/`으로 옮기고 크기·해시 재대조 + `sources.yaml` 등록(운영자 할 일 17번 완료, 미해결 65 해결). ③ **실물 점검 도우미 `/labs/esp32/check/` 새로 만듦**: 항목 22개(`items.ts`)·배선 그림 12개(`wiring-figure.ts` — 보드 그림을 꺼진 모습으로)·판정과 [결과 복사](`report.ts`)·화면(`check-page.ts`·`BoardCheckHelper.astro`), `/start/teacher/`에서 연결. 보드 그림 글자 CSS를 공용 `src/styles/board-drawing.css`로 옮겼고(점검 도우미에서 핀 이름이 겹쳐 보여 찾음), 1분 개념 카드를 실습실마다 다르게(`cardsForLab` — ESP32는 보드 카드 5장). ④ **시나리오 B 통과**(데스크톱 10.6초·휴대폰 11.2초, BOOT 버튼판 포함). ⑤ **전체 검증(운영자 PC, 2026-09-18 05시)**: `npm run build` 통과(출처 검사 파일 412개·항목 33개, 번들 npm 19개, 서비스 워커 셸 128KB), `npm run check` 0오류·0경고(566파일), `npm test` Vitest 147파일 **1,485건** + Astro Container 2파일 7건, `npm run check:links` 2,088개(#위치 280개), 저장소 검사 추적 파일 1,029개, `examples:verify` 97/97, **`npm run test:e2e -- --workers=2` 390 통과·222 건너뜀·실패 0·flaky 0(31.7분)** — 예제 스모크 영상처리 57·ESP32 40 문제 0, 시나리오 A 28.0초, 시나리오 B 데스크톱 18.9초·휴대폰 19.8초(빌드 결과·첫 방문처럼 새로 받는 상태), 첫 방문 Fast 3G 19.91MB·요청 138건, 점검 도우미 4건(모의 포트로 항목 22개 끝까지). 화면 캡처 눈 확인(데스크톱 1366·휴대폰 375 — 점검 도우미 처음·연결됨·항목·복사 글, 실습실 가상 보드·블록 모드·[실제 보드] 탭, 보드 준비·굽기 화면). ⑥ **문서**: CLAUDE.md 기술 스택 9줄·명령어 3줄, PLAN §8.3 구현 메모 9개·차이 표 9번·부록 B-2 14줄(17~30번), MAINTENANCE 2-2·2-3·4-3·4-4·4-5, `src/lab/README.md` 5.1 표·7.1 API 7행·7.5 부품 15개·7.11 블록 모드·8.4~8.6. **거절·미룸**: 모의 보드 `hashlib`(미해결 120), 영상처리 카메라 고르기·검은 영상 감지(미해결 121 — 안내만 넣음). | (아래 커밋 목록) | **브라우저 전체 실행이 처음에는 7건 실패**했고 모두 원인을 찾아 고쳤다: ① 오류 사전을 카드로 심는 길에서 트레이스백 조건이 사라져 학생의 일반 `IndexError`에 네오픽셀 풀이가 붙음(제품 결함 — 미해결 122, `catalog-schema.ts` 고침) ② 사전이 47 → 101항목이 되어 사전 페이지 높이 절대값 검사가 걸림(항목 수에 비례한 한도로) ③ 4채널 터치의 화면 낭독기 이름이 `visual.summary`로 좋아져 옛 문구 기대가 깨짐 ④ 점검 도우미가 실제 화면이 되어 자리 페이지 검사 대상에서 빠져야 함(`_labs.ts` `open: true`) ⑤ 도움말 질문이 하나 늘어 목록 검사 ⑥ WebKit이 깔려 있지 않은 이 컴퓨터에서 `webkit` 프로젝트가 브라우저를 못 띄워 실패(설치돼 있을 때만 프로젝트를 만들게). 고친 뒤 전체 재실행 390 통과·실패 0·flaky 0. 그 전 실행의 `esp32-uart` f007 flaky 1건은 8GB 메모리에서 두 워커가 Pyodide를 동시에 받아 90초 안에 준비되지 않은 것(재시도 통과, 재실행에서는 없음) |
| 2026-09-17T14:15Z | 로컬 | **Phase 3 병렬 제작 준비(P3-03~P3-10 여섯 구역 앞)**: ① 예제 36개(f047~f051·f054~f081·f001·f007·f144)를 PD-33 도구로 이관 — 사이드카는 제목·설명과 지금 결과 그대로의 smoke 기대, ESP32 스모크 40개 기대 일치 ② Blockly 13.3.0·esptool-js 0.6.1·@types/w3c-web-serial 1.0.8·esbuild 0.28.2 설치, 출처 등록·고지 5개, esptool-js 플래셔 스텁이 GPL-2.0-or-later임을 저장소 README로 확인해 싣지 않기로(PD-38 — 빌드 플러그인과 진짜 빌드 테스트) ③ 가상 보드 확장 자리(PWM·아날로그·부품 장치·조작 칸·소리·보드 라이브러리·아직 없는 모듈·machine 확장 위치 `ext/`)와 부품별 Pyodide 단계 파일 방식, 공유 테스트가 "아직 없음"에 기대던 곳을 구역 작업에 흔들리지 않게 ④ 블록 전용 호환 모드 대기 `wait_ns_async`·`sleep_async` ⑤ 실습실 실행 대상 `lab.setRunTarget` ⑥ 모의 시리얼(Web Serial 명세·MicroPython pyexec.c 원문 확인, Node 53건·Edge spec) ⑦ README 5.1(구역·포트 A 4501~F 4506·공유 파일·사이드카 주인)·7.9(부품 체크리스트)·7.10(확장 자리)·8(모의 시리얼), 오류 사전 구역 자리, PLAN PD-38·PD-39·구현 메모, 미해결 57~65 ⑧ 개발 서버 여럿(`--ignore-lock`) 실측. 검증: Vitest 1,011건·astro check 0오류·빌드·링크 2,062·저장소 검사·Playwright 전체 320 통과(실패 0) | `5c2dc53` `d07447c` `6e7a021` `d603a49` `fd86632` `b386a25` `3d6ed1a`(CI 테스트 35232487305·배포 35232487343 성공) | 모의 보드 첫 판에서 호스트가 보낸 `
`을 실행 중에 버려 raw REPL에 못 들어감 → 실물처럼 stdin에 두었다가 REPL이 이어 읽게 고침. 포트가 같은 차례에 열려 부팅 출력이 새어 들어옴 → 명세대로 open 결과를 다음 작업 차례에. 창 넘침 판정이 같은 조각 안의 ``을 셈 → 조각이 끝난 뒤에 몫을 더함. 부품 확장 spec에서 소리 단추와 보드 칸 상태가 같은 속성 이름(`data-board-sound`)이라 선택자가 둘을 잡음 → 상태를 `data-board-sound-state`로. Playwright `page.evaluate`가 사용자 조작으로 돌아 SecurityError 검사가 통과하지 않음 → init script로 시험 |
| 2026-09-17T11:00Z | 로컬 | **P3-02 보드 그림과 첫 부품**: ① 보드 그림 — `layout.ts`(30핀 핀 머리 표·보드에 붙은 부품 자리·배선도 계획 순수 함수)·`board-drawing.ts`(기판·핀 이름·스트래핑 핀 ▲·핀 강조·브레드보드·레일·선)·`view.ts`(부품 배치·누르는 자리·두 겹 초점 테두리·[그림 크게 보기])·`BoardIo.astro`(컨테이너 쿼리 배치·배선 목록·그림 읽는 법) ② 부품 — `touch-digital`(누르는 동안 1, GPIO17)·`vibration-motor`(GPIO19 사이트 배정, 떨림·움직임 줄이기·세기), 내장 LED 밝기·초록, BOOT ▲ ③ 배선 — `wiring-spec.ts`(차시 md·사이드카·머리말 `# @part`), `resolveWiring` 검사 14종(`WiringIssue`), `wiringValue`(label·directions·known), `apc_board.py` 배선 안내 5종, `board.state` `duty`·`freq` 자리(`outputStrength`), `content-schemas.ts` `partSchema`(part 또는 type) ④ 예제 — 이관 도구가 목록 `meta`의 `parts`·`smoke`·`practice`를 사이드카로 옮기게 넓히고 f046·f015·f053·f052를 옮김(줄 수·ast 대조), 진동 알림 새 예제 `04-touch-vibration-alert.py`, 예제마다 실습 방법 3단계(사이드카 `practice`·머리말 "실습 방법" 상자 → 보드 그림 위 "이 예제 실습 방법") ⑤ 조절 패널 안내 글을 실습실 공통으로 ⑥ 테스트 — 부품별 단위 테스트 파일 4개·`board-layout`·`board-wiring-spec`·배선 검사·예제 목록·Pyodide 배선 안내 단계, 브라우저 `lab-esp32-parts.spec.ts` 10건 ⑦ 문서 — README 7.4·7.5·1~3·6절, PLAN §6.1 PD-36·§6.2·§8.3 P3-02 구현 메모·부록 A·B-2 12번, INVENTORY §4.1, CLAUDE.md, MAINTENANCE §2, 미해결 7·51~56. 검증은 판정표 "P3-02" 행 | 51ceb1d 25c1649 58904d7 436b578 ca7a84a 66c86b8 + 이 커밋(CI·실사이트 기록) | ① 한 번에 긴 파이썬 수정 글을 bash heredoc으로 넘기면 셸이 따옴표를 짝짓지 못해 멈춤(두 번) — 수정 스크립트를 파일로 써서 돌림. 같은 방식으로 넘긴 테스트 글의 `\n`이 실제 줄바꿈이 되어 테스트 파일 두 개가 문법 오류 → 바로 고침 ② 개발 서버가 .astro 스타일 변경(속성 이름 바꿈)을 새로고침해도 옛 CSS로 보냄 → 서버를 다시 띄움 ③ [그림 크게 보기]를 눌러도 그림이 안 커짐 — 전역 `svg { max-width: 100% }`가 폭을 막음(캡처 눈 확인으로 발견, `max-width: none`) ④ 줌 단추와 그림 칸이 같은 `data-board-zoom` 속성을 써서 테스트 선택자가 둘을 잡음 → 칸 속성을 `data-board-zoom-level`로 |
| 2026-09-17T09:05Z | 로컬 | **P3-01 완료 기준 재확인(저장하고 정지 뒤 이어서)**: 커밋 `43f2554`·`8df27bb`·`29843a8`·`30f9dda`·`e6c02ec`를 읽고 작업 지시 6항목(실습실 화면·Pyodide 코어만·보드 흉내 모듈·부품 레지스트리·테스트·검증)과 대조 — 빠진 기능은 없고, **CI 테스트 워크플로만 실패 상태**였다. 로컬 재검증: 빌드 통과(53초), `astro check` 0오류, Vitest 77파일 896건, 빌드 결과(미리 보기 4331)에 대고 `lab-esp32`·`lab-runtime`·`lab-loading`·`pages` 76 통과·실패 0, 실사이트 `lab-esp32` 데스크톱 4 통과, 화면 캡처 눈 확인. CI 실패 원인 분석: `tests/e2e/module-hello.spec.ts`(Phase 2)가 파이썬 `time.sleep(0.8)` 한 번 안에 [누르기]를 눌러야 하는데 CI 모바일에서 늦음 — 코드가 같은 `1116682`에서는 통과했고 문서만 바꾼 P3-00 push(35187309907)에서도 재시도까지 실패. 로컬에서 화면 CPU를 10배 느리게(CDP `Emulation.setCPUThrottlingRate`) 하면 3번에 1번 `after []`로 재현(콘솔 글 알아채기 398ms + 누르기 566ms). 검사를 "0.1초마다 확인하며 최대 15초 기다림"으로 바꾸고 `after [3]`(실행 전 두 번은 버려지고 실행 중 세 번째만)까지 보게 했다 → 10배·20배 느리게 8/8, 보통 속도 20/20 통과. `src/lab/README.md` 4.6 테스트 방법 표에 "실행 중 화면 입력은 시간 창 대신 기다리기" 줄 추가. 대조 중 찾은 빈틈: 사이트 예제 `01-first-blink.py`(실습실 첫 화면 예제)·`02-boot-button-led.py`를 실행하는 검사가 하나도 없었다 → `lab-esp32.spec.ts`에 데스크톱 검사 하나(01 깜빡임과 interval 막대 효과 — 상태 메시지 순서 번호가 는 양으로 재고 빨라질 때까지 되풀이, 02 BOOT 버튼 누르는 동안 LED) — 로컬 3/3, 화면 CPU 1·4·8배 느리게 6/6. push 뒤 **테스트 35203248675 성공**(단위 890·6 건너뜀, 브라우저 304 통과·140 건너뜀·실패 0·flaky 0, 14.0분, 스모크 57개 문제 0, 시나리오 A 18.3초, 링크 2,056개)·배포 35203248690 성공, **테스트 35204663018 성공**(브라우저 305 통과·141 건너뜀·실패 0·flaky 0, 13.9분, 새 예제 검사 통과, 시나리오 A 18.0초)·배포 35204663013 성공 | 215eaae · 465c208 · 471fef4 + 이 커밋 | 원인은 P3-01 코드가 아니었다. 진단용 검사·설정(시간 재기·CPU 느리게)은 `.cache/diag-hello/`(git 제외)에만 두었다 |
| 2026-09-17T07:25Z | 로컬 | **P3-01 가상 보드 핵심**: ESP32 실습실 `/labs/esp32/`을 실제 실습실로(LabShell `labId="esp32"`·`pyodidePackages={[]}` + `BoardIo.astro`, 브라우저 권장 환경 안내, 홈 버튼 "준비 중" 뗌). 보드 흉내 모듈 `src/lab/modules/board/`(labs `['esp32']`): `machine.py`(Pin·Timer, 아직 없는 이름은 ImportError + 한국어)·`micropython.py`·`apc_board.py`(핀·가상 시계·콜백 대기열·import 훅·확장 불러오기)·`apc_board_time.py`, 화면 `index.ts`·`view.ts`·`state.ts`·`parts.ts`와 부품 폴더 2개(내장 LED·BOOT 버튼). 러너: `load` 메시지 `labId`로 실습실별 `.py`·shims, `apc_runtime`의 `peek`·대기 전 훅·코드 끝난 뒤 훅(`run_idle`), 준비 모듈의 패키지 범위(`data-lab-packages`). 오류 사전 `board` 묶음 6개, 사이트 예제 3개, 예제 스모크 실습실별 분리. 이름·값·오류 문구는 MicroPython v1.29.0 소스(`ports/esp32/machine_pin.c`·`machine_pin.h`·`machine_timer.c`·`modtime.c`, `extmod/modtime.c`, `shared/timeutils/timeutils.c`, `py/modmicropython.c`·`objmodule.c`)와 newlib errno로 맞췄다. 문서: `src/lab/README.md` 7절, PLAN §8.3 P3-01 구현 메모·부록 B-2 16번, CLAUDE.md, 미해결 43~50. 검증은 판정표 "P3-01" 행(Vitest 77파일 896건, Playwright 304 통과·실패 0) | 43f2554 8df27bb 29843a8 + 이 커밋 | ① Node 실측에서 sleep 도중 도는 틱 훅이 실제 경과 시간을 봐서 Timer 주기를 건너뛰고 `ticks_us`가 거꾸로 감 → 그 sleep이 끝날 시각을 넘지 않게(`Clock.begin_wait`) ② 긴 sleep 동안 핀 인터럽트 콜백이 sleep 끝까지 밀림 → 인터럽트가 걸려 있으면 20ms 조각으로 잠 ③ 단위 테스트 전체와 함께 돌리면 Timer 간격 17ms·제한 모드 검사 5초 초과 → 허용 범위와 시간 한도 조정 ④ 입력 반영 검사가 부하에서 시간으로 넣은 입력과 경쟁 → 파이썬이 보낸 표시를 받은 뒤 입력을 넣게 ⑤ 오류 사전 목록 검사가 줄 전체("ValueError: invalid pin")에 `^invalid pin$`을 못 맞춤 → 패턴 앞을 "줄 처음 또는 ': '"로 ⑥ 화면 확인에서 부품 글자가 작고 BOOT 누르는 자리가 좁음 → 글자 11·10px, 투명 누름 사각형, `:focus` 노란 테두리 ⑦ `.astro` 줄바꿈 공백 사라짐 2곳 → `{' '}` |
| 2026-09-17T05:50Z | 로컬 | **P3-00 MicroPython WebAssembly 포트 판정(Phase 3 첫 묶음)**: npm 레지스트리(최신 `1.29.0-6`, PyScript판, MIT, 파일 7개)와 MicroPython `v1.29.0` 태그의 `ports/webassembly` 소스(README·`api.js`·`Makefile`·variant 설정·`mphalport.c/h`·`mpconfigport.h`·`library.js`)·코어 소스(`readline.c`·`objstr.c`·`modtime.c`·`smallint.h`·`objexcept.c`)·ESP32 포트 설정·newlib errno·genrst 차이 문서·열린 PR #19594·#19427을 읽고, CPython 쪽을 Node의 Pyodide 314.0.7로 실측해 **PD-04 유지**로 판정했다(미해결 42). PLAN §8.3에 구현 메모와 **가상 보드가 지킬 MicroPython 차이 표 14항목**, §6.1·부록 A PD-04·§11 위험 9·부록 B-2 6번(소수 자릿수·epoch 실물 확인 추가), CODE_MAPPING §3.8.2(`bytes + str` 소스 확인)·§4.3 판정 줄을 고쳤다. 자료 ESP32 예제 검색: `KeyboardInterrupt` 처리 0곳, `sleep_ms`·`sleep_us` 22파일, `const` 6파일, u-이름 5파일, `ticks_ms` 뺄셈 f113 | (이 커밋) | 패키지를 내려받지 않아(운영자 확인 대상) 손으로 돌리는 실험 페이지는 없다 — `input()`이 끝나지 않는다는 점은 소스 판독만. 다시 볼 조건은 JSPI판 npm 배포 |
| 2026-09-17T05:10Z | 로컬 | **Phase 2 검토 반영(재개·완료)**: 중단됐던 수정 단계의 작업 트리(수정 45개 + 새 파일 5개)를 `git diff`로 하나씩 읽어 26건과 대조 — 손대 둔 것 24건(S3는 PROGRESS 미해결 5번 몫이 남음), 손대지 않은 것 2건(C1 3·4단원 예제 이관, C4 PLAN·MAINTENANCE 음성 문서). f090~f097 8개를 원본 zip에서 옮기고(`examples/vision/u3/`·`u4/`, 이관 57개) `lab-textbook-u3-u4.spec.ts`로 예제마다 하려던 일을 확인, 서비스 워커 SHA-256 대조 e2e(같은 크기로 바꿔치기한 `pyodide.mjs`), `processLocally` 무시 엔진 e2e, 링크 검사 `example-not-found` 단위 테스트, `revealTogether` 단위 테스트, PLAN §8.2 구현 메모·MAINTENANCE 음성 설정 절·README·CLAUDE 갱신. 검증은 판정표 "Phase 2 검토 반영" 행. 커밋 8개(68a5a10 안전·출처, d73074d 예제 이관, bcaf79f 실습실 흐름, 2c286c2 차시·사전·설정, 708d5fa 문서, 6b4e642 보완, 1bbd139 문서 보완, + 테스트·PROGRESS) → 배포 35183068002·테스트 35183067936 성공(1 flaky) → 실사이트 시나리오 A 17.0초 → flaky 원인(키 누른 직후 한 번 읽기)을 고쳐 push → 테스트 35184213303(0 flaky)·배포 35184213172 성공 | 68a5a10 d73074d bcaf79f 2c286c2 708d5fa 6b4e642 1bbd139 95e2a2a 1116682 | ① 개발 서버에 대고 돌린 첫 확인이 파이썬 준비에서 90초씩 멈춤 — 결과 폴더를 저장소 안(`.cache/…`)에 두어 감시기가 추적 파일을 알아채고 그 사이 파일도 고쳐 HMR이 겹침(미해결 41). ② 첫 전체 실행은 메모리 부족(여유 0.9GB, 다른 프로그램)으로 느려져 중단하고 2워커로 다시 돌림. ③ 전체 실행에서 WIP의 결함 5가지(판정표)를 잡음 — WIP 테스트 가운데 셋(시나리오 A가 준비 끝 idle을 기다림, 키보드 검사가 들여 쓴 코드를 실행, 준비 패널 검사의 30초 한도)도 고침 |
| 2026-09-16T12:20Z | 로컬 | **Phase 2 push·CI·실사이트 확인**: 영역별 커밋 11개를 push(`f705660..c4e76a0`) → 배포 35096439200 성공, 테스트 35096439171 실패. 두 가지를 고쳐 다시 push했다 — ① **CI에서 실습실 검사 80건이 Page crashed**: 기기 안 음성 인식 확인(`available({processLocally:true})`)이 그 서비스가 없는 Chromium에서 렌더러를 죽였다(로그의 `No binder found for interface media.mojom.OnDeviceSpeechRecognition`). 확인을 **단추를 누를 때만** 하게 고쳤다(미해결 38번). ② Node 판에 따라 JSPI를 끄지 못해 mediapipe·pyautogui의 제한 모드 검사 4건이 실패 → 끄지 못하면 건너뛴다. 예제 스모크는 10개마다 페이지를 새로 열게 해 저사양 CI에서 탭이 죽지 않게 했다. 배포 35100608569·테스트 35100608523 **모두 성공**(브라우저 285 통과·1 flaky `lab-desktop2` 움직임 줄이기, 10.5분). 실사이트에서 시나리오 A와 첫 방문 측정을 다시 돌려 통과(총 17.0초, 19.94MB) | 5267c36 · 40f9639 | 첫 CI 실패의 원인 두 가지는 **로컬(Windows·Edge 153)에서는 나지 않는 것**이었다 — 브라우저(Chromium 빌드)와 Node 판 차이. 병렬 구역이 로컬 Edge로만 확인한 몫이 통합에서 드러났다 |
| 2026-09-16T11:00Z | 로컬 | **Phase 2 통합(P2-14)**: 병렬 7구역의 공유 파일 변경 요청 7건(.cache/phase2-requests/)을 하나씩 판단해 반영했다 — 빌드 앞뒤에 예비본 받기·서비스 워커 만들기(package.json), 같은 사이트 Pyodide 예비본 켜기(config.ts), 점검 페이지 네트워크 항목 연결, 사이트 전체 서비스 워커 등록(BaseLayout), 사이트 지도에 `/help/errors/`·`/settings/` 등록, 문제 해결의 "준비 중" 상자를 오류 사전 링크로, 검색 항목 단위 결과(anchorPages), `.lab-button`을 :global로(슬롯·모듈 패널까지), 공용 `setEditorCode`를 키 입력 방식으로, `module-hello.spec` 기대값, `EXAMPLE_GROUPS`에 보충 묶음, 중복 V4 예제 삭제, `.prose figcaption` 스타일, 용어 4항목(색공간·채널·블러·윤곽선), `apc_runtime.register_finish_hook`, Actions 캐시(Pyodide 예비본). 등록: OpenCV 3rd-party 고지(FFmpeg LGPL-2.1+)·Pillow 12.2.0·Pretendard 정적 글꼴(npm devDependency → public/vendor/), 모델 5개는 내려받지 않고 받는 곳·크기·MD5와 허용 목록만 준비(운영자 할 일 17번). P2-14: 차시 실습실 임베드(누른 뒤 만드는 iframe + `?embed=1`), 이관 예제 49개 스모크 테스트, 첫 방문 전송량·시간 측정. 실측: 전송량 19.83MB(홈만 0.34MB), 홈 → 첫 에지 8.1초, Fast 3G 홈 4.1초·계산값 216초 | 6e8abb8(공통 연결·등록) bb16cb3(로딩·캐시) 19cd735(오류 사전) 085bcc9(mediapipe) 23bc0cd(러너) 6d50af8(가상 데스크톱) 21baadc(음성·설정) af124c2(차시 임베드) a07fa44(테스트·CI) abc9d60(문서) c4e76a0(보충 V1~V5) 5267c36·40f9639(CI 크래시 수정) | 전체 Playwright 첫 실행에서 12건 실패 — 모두 통합으로 달라진 것(차시 순서·질문 수·[결과 복사] 둘·실습실 링크 이름)과 **병렬 실행 자원 경합**(따로 돌리면 통과)이었다. 기대값을 실제 화면에 맞추고 동시 실행 수를 로컬 3·CI 2로 줄여 해결(미해결 35번) |
| 2026-09-16T05:45Z | 로컬 | **병렬 제작 준비(P2-05~P2-13 앞)**: ① PD-33 예제 이관 도구(`scripts/import-examples.mjs` + `scripts/lib/{import-examples,zip-read}.mjs`, 목록·기록 `scripts/examples-manifest.yaml`)로 원본 zip 3개의 코드 49개를 CRLF → LF만 바꿔 옮기고 줄 수·SHA-256·`ast.parse`(파이썬 3.11.0) 대조 — 49개 모두 `python-ast` 통과, 사이드카 49개 생성, `--verify` 49/49 ② 흉내 모듈 폴더 규약(`src/lab/modules/`: types·manifests·host, 예시 `hello`, `python/modules.ts` 두 폴더 묶음, `apc_shims.register_shims`, `apc_runtime.register_tick_hook`, `VisionLab.onFrame`·`getVisionLab`, LabShell 모듈 패널·`<optgroup>`) ③ `@mediapipe/tasks-vision` 0.10.35·`workbox-build` 7.4.1 설치·등록, `scripts/vendor-assets.mjs`, `public/models/` 자리 ④ `PW_BASE_URL`. 검증: Vitest 40파일 407건(새 47건: zip·이관 15, 사이드카 4, manifest 6, hello 실제 Pyodide 7, 다리 1 …), astro check 0 오류, `npm run build`(출처 검사 파일 240개·항목 22개, 번들 14개), 링크 검사 1,530개, 전체 Playwright(빌드 + 미리 보기, Edge 153) 178 통과·90 건너뜀(5.3분; 시나리오 A는 4개 워커 병렬 부하에서 총 56.0초 — 5분 기준 안), 개발 서버(`PW_BASE_URL`, 포트 4401)에서 `module-hello`·`lab-vision` 6건(에지 0.83%, 정지 85ms), 스크린숏 1366·375 눈 확인(모듈 패널이 조절 패널 아래, 넘침 없음). 실사이트: `/labs/vision/` `<optgroup>` 4개·예제 52개, `/labs/dev/runtime/` hello 패널, `vendor/mediapipe/0.10.35/wasm/vision_wasm_internal.wasm` 200(`application/wasm`, 11,153,617바이트), 고지 파일 2개 200, `/credits/`에 Tasks Vision·MediaPipe 모델·Workbox 항목 | 57d16d4(이관 도구·예제 49개·사이드카), 9778e62(모듈 폴더 규약·hello·다리 수정), 5a2ef6e(패키지·자산·PW_BASE_URL·저장소 검사 예외), 0df67d5(문서) → 배포 35061711236 성공, 테스트 35061711239 성공(단위·타입, Chromium 브라우저 테스트, 링크 검사) | 첫 전체 Playwright에서 `module-hello` 2건 실패: 실행 전에 누른 값이 첫 실행에 들어옴(`before [1, 2]`) — 학생 코드가 처음 import하는 모듈은 첫 실행의 `reset_for_run` 뒤에야 초기화 함수를 등록하므로 drain이 안 됨 → `bridge.beginRun()`이 push 큐를 모두 비우게 고침(미해결 29번 ⑥). 그 여파로 `pyodide-node-run.mjs`의 실행 전 키 push 검사가 어긋나 실행 중(setup) push로 옮김. `npm run dev`가 에이전트 감지로 백그라운드 기동 → 30초 대기 실패 → `ASTRO_DEV_BACKGROUND=1`로 해결(미해결 30번) |
| 2026-09-16T04:40Z | 로컬 | **P2-04 슬라이더 규약 → 시나리오 A 자동 테스트**: 조절 값 규약 파서 `src/lab/params/parse.ts`(값 글자 위치까지 뽑음, int/float 판별, 한글 설명, 경고 14종)와 조절 패널 `panel.ts` + `ParamPanel.astro`(LabShell `panel` 슬롯 기본 내용 — 모든 실습실이 받음), 값 변경 → 코드의 그 자리 글자 바꿔 쓰기(`lab.replaceCode`·에디터 `replaceRange`) + 실행 중이면 `pushEvent('lab.params')`, 파이썬 도우미 `bind_run_globals`/`unbind_run_globals`/`sync_params`(워커가 `runPython('…bind_run_globals(globals())', {globals})`로 학생 전역 사전을 잇고 `block_on`·`maybe_yield`에서 반영), 예제 머리말 규약 `src/lab/README.md`·`example-meta.ts`, 첫 예제에 blur 슬라이더·상자, 홈 버튼 "준비 중" 뗌, 학생용 ③ 문장, 시나리오 A 테스트·조절 패널 테스트·`helpers/vision.ts`. 검증: Vitest 35파일 363건(+30: 파서 22, 머리말 8, Node Pyodide params 4단계), `astro check` 0 오류, `npm run build`(번들 14개 그대로), Playwright 전체(아래 실패 참고), 링크 검사, 실패 스크린숏에서 패널 모양 눈 확인(이름·설명·값·범위·도움말). **시나리오 A 실측(Edge 153, 새 컨텍스트, 캐시 없음):** 홈 → 실습실 준비 5.9초 → 첫 에지 6.9초 → 슬라이더 효과 9.8초, 총 15.7초, 흰 픽셀 비율 100:0.86% → 20:0.96% → 100:0.85%. 전체 Playwright(병렬 실행, 174 통과·90 건너뜀, 4.7분)에서는 총 55.0초(0.86% → 0.95% → 0.86%). push → **배포 35056546102·테스트 35056546117 모두 성공**(CI Chromium: 시나리오 A 총 16.5초 — 준비 6.8초·첫 에지 8.4초·슬라이더 효과 11.1초, 비율 0.85% → 0.94% → 0.86%; Playwright 174 통과·90 건너뜀 2.0분; 단위 362건). **실사이트 확인(curl):** `/labs/vision/`에 조절 패널 뿌리(`data-lab-params data-count="0"`)·첫 예제의 `blur_size` 슬라이더·도움말, 홈 카메라 버튼에 "준비 중" 없음(남은 표시는 ESP32 1개), 학생용 ③ 새 문장, 실습실 스크립트 `application/javascript` 200. 로컬 미리 보기 375px에서 자바스크립트로 잰 배치: 가로 넘침 0px, 패널 343px(좌우 16px), 슬라이더 2개 각 310px·조절 막대 44px 높이, `aria-valuetext` "100 (0부터 255까지)"(브라우저 창이 숨겨져 스크린숏은 빈 화면 — 지표로 대신 확인) | 89381a5(규약 파서·조절 패널·파이썬 반영 훅·첫 예제·머리말 규약), 03e8f0a(시나리오 A·조절 패널 테스트·가짜 카메라 희미한 네모·홈 버튼), 9650c70(문서) + 이 기록 커밋 | ① 첫 시나리오 A 실행이 실패: threshold 20의 비율(0.82%)이 100(0.91%)보다 낮음. 원인 둘 — `ensureTestVideo`가 파일 **크기**만 비교해 희미한 네모를 더한 뒤에도 P2-03 때 만든 옛 영상을 그대로 씀(바이트 전체 비교로 고침), 그리고 같은 임계값에서도 장마다 비율이 0.80~0.91%로 흔들림(원이 사선을 지나며 가림 — Node 실제 OpenCV로 8장 계산: 20과 100의 차이는 어느 장이든 정확히 324픽셀) → 한 바퀴 넘게(24장) 평균 낸 비율을 비교하게 고침 ② 첫 예제 머리말의 설명 문장 속 `"# @slider …"` 낱말을 파서가 "주석만 있는 줄의 규약"으로 경고 → 주석만 있는 줄은 규약으로 **시작할 때만** 경고(문장 속 낱말은 지나감) |
| 2026-09-16T03:40Z | 로컬 | **P2-03 카메라·창 흉내 모듈과 첫 실습**(10:00~10:52 KST 중단분을 이어서 완성): 작업 트리의 WIP(파이썬 모듈 폴더 `src/lab/python/`, `apc_cv2.py`, `apc_shims.py`, `modules.ts`, 워커·다리·클라이언트의 `event`/transfer, Node cv2 테스트)를 하나씩 검토해 고쳤다 — ① Node cv2 테스트가 `waitKey(0)`에서 영원히 기다림: 키를 실행 **전에** 넣었는데 `reset_for_run`이 실행 전 키를 비우는 설계라서 → 도우미에 `setup`(실행 중에 넣기) 추가, "실행 전 키는 비워진다"도 검사 ② 시험 입력 mock이 2ms마다 답해 imshow의 같은 창 초당 30장 제한에 걸려 5장 중 1장만 화면으로 감 → 화면과 같은 67ms 간격으로 답함 ③ `imshow`가 빈도 제한에 걸리면 잘못된 이미지도 조용히 넘어가던 결함 → 검사(`_check_image`)를 제한 앞으로 ④ Node에는 postMessage가 없어 "transfer 뒤 버퍼가 빈다"를 검사할 수 없음 → "버퍼가 transfer 목록에 들어간다"로 바꾸고 실제 옮김은 브라우저 테스트로. 그 위에 화면(`src/lab/vision/` 5개, `VisionIo.astro`, 실습실 페이지, 첫 예제), 가짜 카메라 테스트(Y4M 생성기·globalSetup·`lab-vision.spec.ts`), 실습실 개요 "열림"·학생용 시작하기 ③ 버튼, OpenCV 출처 등록을 만들었다. 좁은 화면(375px)에서 긴 예제 제목의 선택 상자가 실습실 틀의 열을 500px로 키워 125px 넘침 → `.lab`에 `grid-template-columns: minmax(0,1fr)`, 선택 상자 `min-width:0`(LabShell). 검증: Vitest 33파일 333건(Node 실제 Pyodide+OpenCV 4초), astro check 0 오류, `npm run build`(출처 검사 19항목·번들 14개), Playwright 전체 170 통과·86 건너뜀(4.3분, Edge 153) — lab-vision 5건: 에지 픽셀 0.81~0.90%, 카메라 대기 중 정지 183~637ms(병렬 실행 부하에 따라), q 키·거부 전환·제한 모드·jsDelivr 밖 요청 0건. 스크린숏(1366·375)으로 배치 확인 뒤 "No new packages to load" 한국어화, 입력 제목 간격, 예제 주석 줄 길이를 다듬음. push → 배포 35052914288·테스트 35052914323 성공. **실사이트 확인(Edge 브라우저 창, 샘플 입력):** `/labs/vision/` JSPI 있음·패키지 준비 끝 → [실행] → `edges 640×480 · 12fps · 81장`, 흰 에지 픽셀 0.78%, [정지] 23ms — 그런데 콘솔에 `[알림] 흉내 모듈 초기화 중 오류: RuntimeError: Cannot stack switch because the Python entrypoint was a synchronous function`가 남았다. 원인: 워커가 실행 준비 `reset_for_run()`을 동기 `runPython`으로 부르는데 그 앞의 패키지 받기로 마지막 양보 뒤 16ms가 넘어 있어, cv2 초기화 함수의 `poll()`(입력 확인 지점)이 양보를 시도 → JSPI가 동기 진입점에서는 스택 전환을 거부. Node 테스트는 `beginRun` 직후 초기화해 16ms가 안 지나 잡지 못했다. 고침(두 겹): `apc_runtime.maybe_yield`가 `pyodide.ffi.can_run_sync()`(호출 문맥까지 봄)가 거짓이면 양보하지 않음 + 초기화 함수는 양보 없는 `drain()`을 씀. Node 테스트에 40ms 뒤 동기 초기화·동기 진입점 poll 검사를 더해 두 겹을 각각 뺐을 때 실패하는 것을 확인, 브라우저 테스트에 콘솔 알림 없음 검사 추가 수정 push → 배포 35053598754·테스트 35053598717 성공, 실사이트 다시 확인(새 워커 청크 `worker-D1amVKdM.js`, 샘플 입력 [실행] → 콘솔에 실행 표시만 있고 초기화 알림 없음, `edges 640×480 · 13fps · 71장`, 흰 에지 픽셀 0.78%, [정지] 12ms) | 9861f20(흉내 모듈·워커·Node 테스트·OpenCV 출처), f9d0e53(실습실 화면·첫 예제·개요·시작하기), 6931b79(가짜 카메라 테스트), c5cfb11(문서), 4743c6f(동기 진입점 수정), 47b3585(문서) | Node cv2 테스트 첫 실행이 240초 시간 초과(원인 ①), Playwright 모바일 2건 넘침(원인은 위), 실사이트 첫 실행의 초기화 알림(원인은 위) — 모두 고침 |
| 2026-09-16T01:20Z | 로컬 | **P2-02 코드 에디터**: CodeMirror 6(@codemirror/state 6.7.5·view 6.43.12·language 6.12.4·commands 6.11.1·lang-python 6.2.1, @lezer/highlight 1.2.3)와 lz-string 1.5.0을 npm 레지스트리·패키지 LICENSE로 확인해 정확한 버전으로 설치하고 `sources.yaml`에 등록(번들에 들어간 전이 의존성 8개 포함, MIT 고지 원문 `public/licenses/codemirror.txt`·`lz-string.txt`). 에디터 `src/lab/editor/`(부품 조합, 차시 코드 블록과 같은 색, Esc 뒤 Tab, 글자 크기 단계), 공통 조작 `src/lab/controls/`(컨트롤러·예제 목록·자동 저장·공유 링크·내려받기·기록 지우기), 화면 틀 `src/components/lab/LabShell.astro`·`ClearRecordsButton.astro`, 개발용 시험 페이지 교체, 도움말·교사용 페이지에 기록 지우기 버튼, 저장소 검사 `privacy_exceptions`(고지 원문 이메일). lz-string은 CommonJS뿐이라 이름 있는 가져오기가 Node·Rollup에서 안 잡힐 수 있어 `compress.ts`가 이름 공간의 `default`를 골라 쓴다. 검증: Vitest 31파일 319건(24건 추가), `astro check` 0 오류, `npm run build`(번들 npm 14개 등록 확인, 실습실 청크 407KB), 링크 검사 1,523개 통과, Playwright 로컬(Edge 153) 실습실 두 파일 14 통과(정지 1단계 99ms, 공유 링크 새 탭 복원, 기록 지우기 뒤 저장값 0개, 키보드만으로 실행·정지), 전체 165 통과·82 건너뜀(5.6분), 스크린숏 5장(1366·375 화면, 공유·기록 지우기 대화, 도움말 페이지) 눈 확인. 경로 지정 커밋 4개 push(훅 통과) → 배포 35044023974 성공, 테스트 35044023978 성공(단위·타입 검사, CI Chromium 브라우저 테스트, 링크 검사). 실사이트: `/labs/dev/runtime/`·고지 파일 2개·`/credits/`(CodeMirror·lz-string 항목)·도움말 버튼 200, 실습실 스크립트 `application/javascript`, Edge 창 자동 조작으로 편집 → 저장됨 → [실행] 출력 "실사이트 42" → 새로고침 복원 → 공유 링크 새 탭 같은 코드(#) 제거, 콘솔 오류 0건 | c6ddebd, 5b855bf, 11a3c78, e28df79 | 전체 실행에서 `search.spec.ts`의 머리글 검색 1건이 Pagefind 준비 5초 초과로 실패 → 따로 돌리면 통과(14/14). 원인은 같은 시간에 Pyodide를 받는 실습실 테스트 4개가 CPU를 차지해 검색 색인 준비가 늦어진 것으로 판단, 그 기대의 대기 시간만 15초로 늘림. 화면 확인 중 조절 패널 자리 글이 `display: grid`에서 낱말마다 줄로 나뉘어 보여 `<p>`로 감싸고 flex로 고침 |
| 2026-09-16T00:30Z | 로컬 | **P2-01 파이썬 워커**(08:30 KST 중단분을 이어서 완성): 작업 트리의 WIP 파일 18개를 `wip/phase2-snapshot`과 대조(모두 같음)한 뒤 하나씩 검토해 고쳤다 — ① `client.ts`: Pyodide 받기에 실패한 뒤 다시 `load()`해도 새 워커를 띄우지 않던 순서 문제(실패 알림이 같은 틱에 오면 약속 기억을 먼저 지운 뒤 덮어씀 → `#beginLoad`가 거부된 뒤에 지움) ② 시험 페이지 타입 오류(`hasJspiApi`에 DOM `WebAssembly`를 직접 넘김 → `envFromWindow`) ③ Node 도우미가 제한 모드에서 정지 신호를 받을 수 없는 반복문을 돌려 멈추던 문제(`needsJspi` 단계 건너뛰기) ④ `exit()`·`KeyboardInterrupt`가 약속 거부와 별개로 워커 밖으로 새는 것(unhandledrejection)을 워커가 삼키고 Node 도우미도 같은 처리 ⑤ task 취소 테스트의 가짜 워커가 답을 보내던 오류 ⑥ 정지 2단계 안내 문구를 `STOP_GRACE_MS`에서 계산. 공식 문서로 확인: Pyodide 워커 안내(모듈 워커만, `import('…/full/pyodide.mjs')`), `run_sync`·`can_run_sync`(JSPI + `runPythonAsync` 안에서만, 실험 기능), 인터럽트 버퍼(SharedArrayBuffer·COOP/COEP 필요), `setStdout({ write })`, `loadPackagesFromImports`(배포 패키지만). jsDelivr `v314.0.7/full/` 파일 5개 200·CORS·CORP 확인(`pyodide.asm.js`는 404), `sys.stdout.line_buffering` 참(줄마다 콘솔로 감). 검증: Vitest 28파일 295건(실제 Pyodide를 띄우는 Node 테스트 3건 포함, 8초), `astro check` 0 오류, `npm run build`(번들 npm 패키지 0개, 워커 청크 14.7KB), Playwright 159 통과·75 건너뜀(Edge 153, 3.3분 — lab-runtime 7건: print·NameError·SyntaxError·`exit()` 뒤 콘솔 오류 0건, 정지 1단계 72ms, 정지 2단계 재시작 뒤 재실행, `input()`, 제한 모드, numpy, 허용 주소 밖 요청 0건), 링크 검사 통과(사이트 안 주소 1,512개) | a17ccc5, 12dde41, 48b45fe, 7d64868 + 테스트 플래그 수정 커밋 | 이전 시도(08:30 중단분)의 실패 4건 원인 = 위 ①③⑤와, 새어 나온 KeyboardInterrupt로 Node 도우미가 죽던 것(④). push 뒤 배포 35040170348 성공(검사 19초·빌드 22초·배포 8초, 실사이트 시험 페이지·워커 청크·/credits/ 고지 링크 200), 테스트 워크플로 35040170394는 브라우저 테스트·링크 검사 성공·단위 작업 실패 1건: CI Node 24.20.0은 JSPI가 플래그 없이도 켜져 있어 "플래그 없이 = 제한 모드" 가정이 깨짐 → 끄는 쪽도 `--no-experimental-wasm-jspi`로 명시해 다시 push |
| 2026-09-15T13:30Z | 로컬 | P1-01 공개 전 문서 점검(PD-37): 올릴 9개 파일에서 학교명(지역 이름·"○○고등학교" 형태)·사용자 폴더 경로·AppData·MAC 주소·이메일·전화번호 검색 → 실제 노출 0건(검색어 설명 문장 1건, 일반 계정 경로 예시 `C:/Users/COM` 1건만). 인명은 저작자 표기(박상진·김석전)만. `OVERNIGHT.md`의 고정 종료 시각·`extracted/flat` 안내·Pages 켜기 방법을 PLAN §13.1 6·7번대로 고침 | (첫 커밋) | — |
| 2026-09-15T14:05Z | 로컬 | P1-03 배포 파이프라인과 첫 배포: `.github/workflows/deploy.yml` 추가. 빌드 작업(checkout v7 → configure-pages v6 → withastro/action v6, 권한 contents·pages 읽기, Node 24, Astro 익명 통계 끔)과 배포 작업(deploy-pages v5, 권한 pages·id-token 쓰기, 환경 github-pages)으로 나눔. 네 액션의 최신 주 버전이 PLAN과 같음을 각 저장소 releases/latest·action.yml로 확인, `enablement`는 쓰지 않음(Pages 이미 켜짐). main push(P1-02 커밋 7a4631f 포함) → 실행 34979015090 성공(빌드 16초, 배포 27초). `curl -sSfL` 홈 HTTPS 200·제목 확인(SPEC의 첫 배포 기준, Phase 1 전체는 P1-04~P1-11 뒤). 시험 파일 `public/_probe/` 응답 형식: `.mjs` = `text/javascript; charset=utf-8`, `.wasm` = `application/wasm`. 크롬 계열 브라우저 창에서 `import()`와 `WebAssembly.instantiateStreaming` 성공 | 3dd1a0a | 실패 없음(재시도 0회). 참고: 끝 슬래시 없는 주소는 301로 `/`가 붙은 주소로 이동, `dist/.gitkeep`이 공개 주소에서 200(P1-04에서 처리) |
| 2026-09-15T15:15Z | 로컬 | P1-04 출처 등록부·저장소 검사·자동 출처 페이지: `sources.yaml` 6항목(운영자 원고·예제, 자체 제작 그림·시험 파일, Astro, Pretendard 자리. 넓은 항목은 `third-party/` 폴더를 뺌), `npm run build` 앞뒤 출처 검사(`scripts/check-sources.mjs`: 파일·dependencies 등록과 저작자가 다른 중복 매칭 / 빌드 뒤 번들 의존성은 미해결 9번 방식), 저장소 검사(`scripts/check-repo.mjs` + `.githooks/pre-commit`, `npm install`·`npm ci`가 훅을 켬, deploy.yml `저장소 안전 검사` 작업을 배포가 기다림), `/credits/`(라이선스별 묶음·제3자 목록·사이트 라이선스 제외 표시, 본문 컴포넌트 분리), Vitest 37건 추가(총 41건). 확인: `public/`에 미등록 파일 → `npm run build` 종료 코드 1(한국어 오류) → 지우고 0, `.pdf`·6MB 파일 스테이징 → 훅이 커밋을 막음(Git Bash·PowerShell, HEAD 그대로), 실행 34987093979 성공(검사 15초·빌드 24초 병렬, 배포 12초), `/credits/` 200, `/.gitkeep`·`/bundle-licenses.json` 404 | 150130b | 실험 1·2에서 Vite 번들 목록 파일이 남지 않음 → 원인: Astro가 client 환경의 build 설정을 새로 만들고 빌드 결과의 `.vite/`를 지움 → `configEnvironment` 플러그인과 `.vite/` 밖 파일 이름으로 해결. `astro check`가 테스트의 Node 내장 모듈 타입 오류 21건 → `@types/node` 24.13.4 추가로 0건 |
| 2026-09-15T19:16Z | 로컬 | **Phase 1 통합·배포**: 병렬 제작 5구역(홈·배우기·용어사전·시작하기·기타 문서와 검색)을 합치고 공유 파일 요청을 판단해 반영했다. ① 차시 본문을 `GlossaryScope`로 감싸 용어 툴팁 연결(P1-07 완료 기준) ② 검색 색인 규칙(ComingSoon·교사용·정답 상자에 `data-pagefind-ignore`, 사이트 지도 설명의 가운뎃점 → 쉼표, 출처·용어사전의 붙은 글자 공백) ③ 코드 블록 테마 `github-light-high-contrast`(모든 코드 글자색 대비 5.04:1 이상) ④ 320px 머리글 한 줄(67→65px) ⑤ 바닥글에서 LICENSE·LICENSE-CONTENT.md 연결 ⑥ 실습실 4곳에 브라우저 권장 환경 안내 ⑦ 점검 페이지 제목 "학교 네트워크와 브라우저 점검" ⑧ `public/_probe/` 시험 파일과 등록부 항목 삭제 ⑨ 용어사전 "나오는 차시" 주소를 배우기 규칙으로 ⑩ PLAN의 Silicon Labs 주소·`configure-pages` 설명 수정. 다음 단계로 미룬 요청: 저장 이름 규칙 공통 파일(P2-02, 미해결 13번), 차시 `source` 필드(P5, 미해결 14번). P1-09: 링크 검사 `scripts/check-links.mjs`(`npm run check:links`)와 테스트 워크플로 `.github/workflows/e2e.yml` 추가. 로컬 검증: 빌드 통과, `astro check` 0 오류·0 경고, Vitest 23파일 244건, Playwright 150 통과·66 건너뜀(Edge 153, 2.7분), 링크 검사 사이트 안 주소 1,458개(#위치 188개) 통과, 스크린숏(1366×768·375×812·320×568) 눈 확인. 영역별 경로 지정 커밋 7개 push(a3334c4..6df7f86) → 배포 실행 35010369791 성공(검사 13초·빌드 26초·배포 11초). CI 브라우저 테스트 실패를 고쳐(796f88c) 배포 35012350162와 테스트 35012350102 모두 성공(CI Chromium 151 통과·67 건너뜀 45.6초, 단위 244건, 링크 검사 통과). 실사이트 `curl`: 홈·시작하기·점검·배우기·차시 2개·용어사전·실습실·실물 점검·교사용 자료실·문제 해결·출처·검색·기여 200, 없는 주소 404, 지운 시험 파일 404 | 1582b18, 9f7e136, f808251, e59130a, e3dd225, 8e48c2e, 6df7f86, 796f88c | ① 링크 검사가 404 페이지의 `og:url`(없는 주소 `/404/`)을 찾아냄 → noindex 페이지는 대표 주소를 적지 않게 고침 ② 화면 확인에서 Astro 7이 줄바꿈이 든 글자·태그 사이 공백을 지워 "알리려면문제"처럼 붙은 곳 22개 발견 → `{' '}`로 고치고 CLAUDE.md에 주의 추가 ③ 점검 페이지 제목이 좁은 화면에서 "·브라우저"로 줄이 바뀜 → "와"로 ④ **CI 브라우저 테스트 5건 실패(로컬 Edge에서는 통과)**: Playwright 기본 헤드리스 셸이 브랜드를 `HeadlessChrome`으로 알려 컴퓨터 화면에서도 "확인 필요"와 안내가 뜸(4건) → `HeadlessChrome`을 Chrome으로 판정(단위 테스트 추가), 리눅스 한글 대체 글꼴의 넓은 평균 글자 폭 때문에 검색 입력칸 기본 너비가 칸을 넓혀 375px에서 58px 넘침(1건) → `grid minmax(0, 1fr)`와 입력칸 `width: 100%`. 둘 다 로컬에서 재현(헤드리스 셸 브랜드 실측, 입력칸 크기를 키워 95px 넘침)한 뒤 고쳐서 확인 ⑤ 첫 테스트 실행에서 브라우저 캐시 저장 단계가 실패한 작업이라 건너뛰어짐 → 캐시를 되살리기·저장으로 나눔 ⑥ 병렬 제작 담당들이 띄운 `astro preview` 서버 3개(포트 4330·4336·4339)가 남아 있다. 멈추는 명령이 자동 권한 판정에서 거절돼 그대로 뒀다(테스트 포트 4329와 겹치지 않아 영향 없음, 창을 닫거나 다시 켜면 사라진다) |
| 2026-09-15T20:30Z | 로컬 | **Phase 1 검토 반영·재배포**: 완료 기준·안전·사용성 검토의 지적 18건을 근거를 직접 확인해 판정 → 16건 반영, 2건은 운영자 할 일(14번 main 룰셋 = 저장소 설정, 16번 남은 미리 보기 서버 종료 = 다른 프로세스 종료 권한). ① 저장소 검사(`scripts/lib/repo-check.mjs`): 래스터 이미지 눈 확인을 추적 파일 전체와 글·코드 파일 안 data: 그림까지, 개인정보 검사에 UTF-16 해독·이메일·전화번호·문장 속 OneDrive·학교명 해시(`scripts/privacy-needles.json`, `scripts/privacy-needle.mjs`) 추가, 단위 테스트 6건 ② 출처: 1-1-1의 조판 삽화 3장을 `third_party`로, 쓰지 않는 Pagefind 화면 파일 7개를 빌드 뒤 삭제(`scripts/prune-pagefind.mjs`)하고 MIT 고지 `public/licenses/pagefind.txt` 배포·`/credits/` 고지 링크, Pretendard npm 표기 삭제, 운영자 항목 파일의 저작권 표기 참고 경고 ③ 사용성·접근성: 홈 버튼 "준비 중" 표시·과목 이름·카드 칩 제거, 자리 페이지 준비 중 상자 안 링크·`?example=` 안내·되풀이 문장 수정·브라우저 안내 끔, 차시 [실습실에서 열기(준비 중)] 보조 버튼과 코드 상자 `role="region"`, 용어사전 검색 결과 항목 단위(`sub_results`)·항목 메타와 출처 개발 정보 색인 제외, 툴팁 용어 위, 머리글 글자 200% 넘침 0(nowrap 제거), 바닥글 링크 24px ④ 성능: 'Pretendard Fallback'(맑은 고딕 size-adjust 86%/85.5% + 세로 지표 override, Edge 153 측정 폭 차이 1% 안·줄 높이 같음) ⑤ 워크플로 액션 SHA 고정·PR에서도 `check:repo` ⑥ PLAN §8.1 구현 메모·P1-04 문구·§8.0 훅 주의·§10 계정 이름, DECISIONS C10, CLAUDE·CONTRIBUTING·MAINTENANCE. 검증: Vitest 24파일 255건(11건 추가), `astro check` 0 오류, Playwright 로컬 152 통과·68 건너뜀(Edge 153, 2.8분), 링크 검사 HTML 26·CSS 9에서 1,466개 통과, 스크린숏 9장 눈 확인(홈 320·375·1366, 자리 페이지 375, 검색, 툴팁 1366·375, 출처), 글자만 200%에서 4페이지 넘침 0. 경로 지정 커밋 5개 push(f55b277..68a5acd, 훅 우회 없음) → 배포 35020093568 성공(검사 14초·빌드 26초·배포 9초), 테스트 35020093696 성공(CI Chromium 152 통과·68 건너뜀 1.1분, 단위 255건, 링크 검사 통과). 실사이트 `curl`: 주요 주소 22개 200, 지운 `pagefind-ui.js`·`pagefind-highlight.js` 404, 없는 주소 404, 새 내용(준비 중 표시 2개·제3자 항목·고지 링크·`role="region"`·대체 글꼴 CSS) 확인 | f481671, 1981406, 4082243, aab7afe, 68a5acd | 실패 없음. 참고: ① 남은 `astro preview` 3개 종료 명령이 자동 권한 판정에서 거절됨(운영자 할 일 16번) ② 새 이메일 검사가 처음에 `a@2x.webp` 같은 파일 이름을 이메일로 잡아 파일 확장자 모양 도메인을 제외 ③ 기존 테스트 픽스처의 OneDrive 문자열이 넓어진 검사에 걸려 조각으로 이어 붙이게 고침 |
| 2026-09-15T16:40Z | 로컬 | 공통 기반(병렬 제작 5명이 고치지 않을 파일): ① 사이트 지도 `src/config/nav.ts`(PLAN §2.1 주소 19개와 대단원 4개, 머리글 메뉴 6개)와 주소 도우미 `src/lib/url.ts`(`withBase`) ② 디자인 토큰·전역·상자 스타일(파랑 강조색 하나, WCAG 대비 계산값을 주석에 적음: 본문 17.61·링크 5.96·초점 7.01, rem 글자 단계, 움직임 줄이기) ③ Pretendard 1.3.9 **가변 글꼴 다이내믹 서브셋** 자체 호스팅: 공식 태그 v1.3.9 파일 94개를 npm 파일과 크기·SHA-256으로 대조한 뒤 그대로 둠. 고른 근거(CSS의 유니코드 범위로 계산): 머리글·바닥글 글자만 8조각 201KB, 차시 한 편 크기의 글 13~18조각 333~471KB, 긴 문서 23~27조각 602~715KB인데 정적 서브셋 400+700은 늘 525KB이고 2,350자만 담김. 실사이트 홈 실측 woff2 10개 256KB, font-display swap ④ `BaseLayout`(lang ko, OG, 건너뛰기 링크, `data-pagefind-body`, head·notice 슬롯)·머리글(75rem 미만 접는 메뉴, aria-expanded, Esc)·바닥글(버전, 라이선스 두 줄과 제외 문구, 저작자, 출처·저장소·Issues), 부품 ComingSoon·Breadcrumb·Callout·PageLinks와 검색 상자 자리, 자리 페이지 17개와 404, 출처 페이지를 레이아웃으로 감쌈 ⑤ 콘텐츠: `src/content.config.ts`(루트 `content/lessons`·`content/glossary` glob 로더, 시험 파일로 실제 빌드 확인 뒤 지움), `src/config/content-schemas.ts`(PLAN §2.6 필드와 한국어 오류 문장 — 틀리면 `astro sync`가 파일 위치와 함께 한국어로 알림), @astrojs/markdown-remark 7.3.1 unified 처리기 + remark-directive 4.0.0, 상자 문법 10종(`src/lib/remark-boxes.mjs`), 용어 문법 `:용어[말]{항목=id}` 결정과 스텁 ⑥ Playwright 1.63.0 설정·스모크·검색 색인 테스트 ⑦ Pagefind 1.5.2 postbuild 색인(18페이지), 출처 등록부 12항목(Pagefind 화면 파일에 묶인 Svelte 4·bcp-47·adequate-little-templates 포함). 확인: Vitest 84건, `astro check` 오류 0, Playwright 16 통과·4 건너뜀(Edge 153, 데스크톱 1366×768·모바일 375×812, 화면 캡처 눈 확인), 실행 34995787531 성공(검사 14초·빌드 26초·배포 10초), 실사이트 curl(홈 제목·바닥글 문자열, 출처 새 항목, 글꼴 font/woff2, 404 페이지)과 브라우저 창(글꼴 적용, 검색 결과 주소 `/ai-physical-computing/credits/`) | b65d5a1, 42176f1, 3593ee7, d53bc7c, ee6e0ab | `astro check` ts(2367): `as const`로 좁혀진 base를 ''와 비교 → string으로 받아 해결. 예제 경로 `examples/…`가 정규식을 통과 → refine 추가. 상자 안 상자는 **바깥** 상자에 콜론을 더 써야 함(안쪽에 더 쓰면 바깥 상자가 일찍 닫힘, 설명 고침). Pagefind는 baseUrl을 넘기지 않아도 pagefind.js 주소에서 base를 알아냄(처음 테스트 가정이 틀려 고침). **Astro 7 `astro preview`가 AI 에이전트 환경을 알아채 스스로 백그라운드로 떠서 테스트 뒤에도 남음** → `ASTRO_PREVIEW_BACKGROUND`·`--ignore-lock`·서버 재사용 끔으로 해결. Playwright 전용 Chromium 1243 없음 → 내려받지 않고 Edge 사용(미해결 10번) |
