# CLAUDE.md

## 프로젝트
AI 피지컬 컴퓨팅 오픈랩(가칭) — 2022 개정교육과정 인공지능·피지컬 컴퓨팅(ESP32) 오픈 교육 사이트.
완전 정적 사이트, 백엔드 없음, GitHub Pages 배포, 전국 무료 배포 목적.

- 전체 요구사항: `docs/SPEC.md` — **세션 시작 시 반드시 읽기.**
- 결정 기록: `docs/DECISIONS.md` — **세션 시작 시 반드시 읽기.** 운영자 결정(승인 위임, 원본 자료 전부 사용 허락 등). SPEC과 충돌하면 이 문서가 우선.
- 진행 상황: `PROGRESS.md` — 세션 시작 시 읽고, 작업 묶음이 끝날 때마다 갱신.
- 이어받기: `docs/HANDOFF.md` — **계정·대화·컴퓨터가 바뀐 뒤 처음 열었으면 반드시 읽기.** 어디에 무엇이 있고(이 노트북에만 있는 파일 포함) 멈춘 워크플로를 어떻게 이어받는지. 운영자가 "사이트 작업 재개"라고 하면 이 순서대로.
- 계획·자료 분석: `docs/PLAN.md`, `docs/INVENTORY.md`, `docs/CODE_MAPPING.md`
- 클라우드 야간 루틴으로 실행 중이면: `docs/OVERNIGHT.md` 절차를 따른다.
- 원본 자료: **읽기 전용. 절대 수정·삭제 금지.** 사이트에 쓸 것만 골라 `content/`, `examples/`, `public/`으로 변환.
  - 운영자 PC: 프로젝트 루트의 원본 폴더들(`교과서_안/` 등, git 제외)
  - 그 밖(클라우드 등): 비공개 저장소 `songdocomputerpark-lang/ai-physical-computing-materials`(`originals/` 원본, `extracted/` 텍스트·코드·사진 추출본)
- 저장소: https://github.com/songdocomputerpark-lang/ai-physical-computing → Pages https://songdocomputerpark-lang.github.io/ai-physical-computing/

## 절대 원칙 (요약, 상세는 SPEC §2)
1. 설치 제로 — 파이썬은 Pyodide, ESP32는 Web Serial. 브라우저만으로 끝.
2. 서버 제로 — 백엔드·DB·로그인·유료 API 키 추가 금지. 상태는 localStorage.
3. 하드웨어 없어도 100% — 모든 피지컬 실습은 가상 ESP32 보드로 완료 가능. 실제 보드는 같은 코드.
4. 초보자 우선 — 한 페이지 한 개념, 용어 즉시 풀이, 그림(직접 만든 SVG) 우선.
5. 저작권 깨끗 — 외부 자료는 `sources.yaml` 등록 후에만 사용. 애매하면 직접 만든다.
6. 유지보수 쉬움 — 새 차시 = md 1개 + py 1개. 코드 수정 없이 추가 가능해야 함.
7. 한국어 — UI·문서·오류 설명·커밋 메시지는 한국어, 코드 식별자는 영어.

## 기술 스택
확정안 요약. 버전·근거·라이선스 전체는 `docs/PLAN.md` §3.1, 결정 번호(PD)는 부록 A. 버전은 `^` 없이 정확히 고정한다(`.npmrc`의 `save-exact=true`).
- **설치됨(P1-02):** Astro 7.3.2(정적 출력) + TypeScript 6.0.3 + @astrojs/check 0.9.10(TypeScript 7은 이 검사 도구가 받지 않아 보류), Vitest 5.0.0, yaml 2.9.1(등록부 읽기, P1-04). Node.js 22.12.0 이상(운영자 PC 24.19.0).
- **설치됨(공통 기반):** 마크다운 처리기 @astrojs/markdown-remark 7.3.1(unified) + remark-directive 4.0.0 — Astro 7 기본 처리기(Sätteri) 대신 `astro.config.mjs`의 `markdown.processor: unified(...)`로 상자 문법·용어 문법 플러그인을 쓴다. Pagefind 1.5.2(빌드 뒤 `dist/pagefind/` 색인), Pretendard 1.3.9(`public/fonts/pretendard/`, 공식 가변 글꼴 다이내믹 서브셋 파일 그대로), @playwright/test 1.63.0.
- **공통 기반 파일(여러 페이지가 함께 쓰므로 바꿀 때 영향 범위를 확인):** 사이트 지도 `src/config/nav.ts`, 주소 도우미 `src/lib/url.ts`(`withBase()` — 사이트 안 링크는 이것으로만 만든다), 검색 설정 `src/config/search.ts`, 콘텐츠 규칙 `src/config/content-schemas.ts` + `src/content.config.ts`(루트 `content/lessons`·`content/glossary`), 상자 문법 `src/lib/remark-boxes.mjs`(교사용·정답 상자는 검색 색인 제외), 용어 문법 `src/lib/remark-glossary.mjs`(1단계 표시 자리), 디자인 토큰·전역 스타일 `src/styles/`, 공통 레이아웃 `src/layouts/`(BaseLayout·머리글·바닥글). 쓰는 부품은 `src/components/common/`(ComingSoon·Breadcrumb·Callout·PageLinks).
- **Phase 2 실습실 기반(P2-01):** 파이썬 실행기 `src/lab/runtime/` — 화면은 `client.ts`의 `PythonRuntime` 클래스만 쓴다(`load`·`run`·`stop`·`setValue`·`pushEvent`·`writeFile`·`loadPackages`, 이벤트 state·ready·progress·stdout·stderr·notice·request·done). 워커 `worker.ts`(모듈 워커, Pyodide를 `config.ts`의 jsDelivr 주소에서 `import()`), 메시지 형식 `protocol.ts`, 파이썬↔JS 다리 `bridge.ts`, 파이썬 쪽 도우미 `apc_runtime.py`(`block_on`·`sleep`·`input`·`request`·`get`·`poll` — 흉내 모듈은 이 함수만 쓴다). Pyodide 버전·주소·정지 유예(1초)·양보 간격(16ms)은 `config.ts` 한 곳. 브라우저 저장 이름 규칙 `src/lib/storage.ts`(머리말 `ai-physical-computing:`, `clearOurs()`는 이 사이트 이름만 지운다). 개발용 시험 페이지 `src/pages/labs/dev/runtime/`(주소 `/labs/dev/runtime/`, noindex·검색 색인 제외·사이트 지도에 없음, `?limited=1`이면 제한 모드). Node 단위 테스트 `tests/unit/lab/`(실제 Pyodide를 `--experimental-wasm-jspi`로 띄움), 브라우저 테스트 `tests/e2e/lab-runtime.spec.ts`(jsDelivr 접속 필요).
- **Phase 2 실습실 화면(P2-02):** 실습실 공통 틀 `src/components/lab/LabShell.astro`(props `labId`·`examples`·`initialExampleId`, 슬롯 `io`(오른쪽 위 입력·출력)·`panel`(오른쪽 아래 조절 패널)·`after`; 조작 줄 [실행] [정지] [초기화] [예제 불러오기] [공유 링크] [.py 내려받기], 콘솔·`input()` 입력줄, [이 컴퓨터에서 내 기록 지우기]) — 화면 논리는 `src/lab/controls/lab-shell.ts`(페이지는 `getLabController(root)`로 받아 `on('code'|'run'|'done'|…)`, `onRequest(kind, handler)`, `runtime`·`editor`를 쓴다). 코드 에디터 `src/lab/editor/`(CodeMirror 6 부품 조합 `python-editor.ts`, 색·모양 `theme.ts`(차시 코드 블록과 같은 색), 글자 크기 단계 `font-size.ts`). 조작 부품 `src/lab/controls/`(예제 목록 모양 `examples.ts` — 새 예제는 .py 파일 하나를 목록에 넘기면 됨, 자동 저장 `autosave.ts`(`editor:<실습실>:<예제 id>`), 공유 링크 `share-link.ts`(lz-string, `#code=…&ex=…`, 2,000자 경고·16,000자 상한), 내려받기 `download.ts`, 기록 지우기 `records.ts` + `src/components/lab/ClearRecordsButton.astro`(도움말·교사용 페이지에도 있음)). 브라우저 테스트 `tests/e2e/lab-editor.spec.ts`, 도구 `tests/e2e/helpers/lab.ts`. CodeMirror·lz-string 고지 원문은 `public/licenses/`(고지 원문의 저작자 이메일은 `scripts/repo-allowlist.yaml`의 `privacy_exceptions`로만 허용).
- **Phase 2 영상처리 실습실(P2-03):** 페이지 `src/pages/labs/vision/index.astro`(`examples/vision/**/*.py`를 `import.meta.glob(?raw)`으로 읽어 `src/lab/vision/examples.ts`가 `LabExample[]`로 — 새 예제 = 그 폴더에 .py 하나, 첫 줄 주석이 제목·둘째 줄이 설명·파일 이름이 id), 입력·출력 칸 `src/components/lab/VisionIo.astro`(LabShell의 `io` 슬롯), 화면 논리 `src/lab/vision/vision-lab.ts`(`mountVisionLab(root)` — 파이썬 요청 `camera.open`·`read`·`set`·`release`와 이벤트 `window.show`·`open`·`close`, 키 입력 `pushEvent('cv2.keys', 코드)`, 창 닫기 `pushEvent('cv2.window', {name, closed})`, cap.read 초당 15장 제한, 제한 모드는 실행 직전 `setValue('camera.info'·'camera.frame')`), 입력 소스 `sources.ts`(웹캠 getUserMedia 640×480·샘플(코드로 그린 도형)·내 그림 파일, `registerVisionSource()`로 P2-08 재생 입력 추가, 웹캠 실패 시 한국어 이유 + 샘플로 자동 전환), 출력 창 `windows.ts`(창 이름별 캔버스·탭, 키 캡처), 순수 논리 `frame.ts`. **파이썬 쪽 모듈 `src/lab/python/`:** `modules.ts`가 폴더의 .py를 `?raw`로 묶어 워커가 Pyodide FS `/apc`(sys.path 맨 앞)에 씀 — `apc_runtime.py` 도우미(`request`·`emit`·`get`·`poll`·`sleep`·`register_reset_hook`), `apc_shims.py` 등록표(`SHIMS = {'cv2': 'apc_cv2'}`, 워커가 실행 직전 `install_available()`), `apc_cv2.py`(진짜 opencv-python 위에 `VideoCapture`·`imshow`·`waitKey`·`namedWindow`·`getWindowProperty`·`destroyWindow`·`destroyAllWindows` 등 창·카메라 이름만 덮어씀 — Pyodide의 opencv 빌드에는 창 백엔드가 없어 원래는 cv2.error). 새 흉내 모듈 = `apc_<이름>.py` + SHIMS 한 줄 + protocol.ts 머리말에 요청·이벤트 종류. **초기화 함수(`register_reset_hook`)와 `install()`은 워커가 동기 `runPython`으로 부르므로 양보하는 함수(`poll`·`get`·`sleep`·`request`·`block_on`)를 쓰지 않는다(동기 진입점에서는 JSPI 스택 전환이 거부됨) — 쌓인 값을 버릴 때는 `apc_runtime.drain(channel)`(PROGRESS 미해결 25번).** 테스트 `tests/unit/lab/{pyodide-cv2,vision}.test.ts`(Node 실제 Pyodide+OpenCV, 휠은 `.cache/pyodide-packages/`에 캐시), `tests/e2e/lab-vision.spec.ts`(가짜 카메라: `playwright.config.ts`의 `--use-fake-device-for-media-stream`, `tests/e2e/global-setup.ts`가 `scripts/gen-test-video.mjs`로 `.cache/test-camera/synthetic.y4m`(코드로 그린 도형, 커밋 안 함)을 만듦). OpenCV 4.11.0.86은 `sources.yaml`(Apache-2.0, `public/licenses/opencv.txt`).
- **Phase 2 조절 패널·예제 규약(P2-04):** 조절 값 규약 `src/lab/params/`(`parse.ts` 파서 — `이름 = 값  # @slider 최소 최대 간격`·`@select 낱말…`·`@toggle`, 들여쓰기 없는 줄만, 값 글자 위치·int/float·설명·한국어 경고; `panel.ts` 화면 논리 — 코드가 바뀌면 다시 읽고, 조작하면 코드의 그 자리 글자를 바꿔 쓰며(`lab.replaceCode`) 실행 중이면 `runtime.pushEvent('lab.params', {name, value, type})`). HTML `src/components/lab/ParamPanel.astro`는 LabShell `panel` 슬롯의 기본 내용(모든 실습실·시험 페이지에 들어감). 파이썬 쪽 반영: `apc_runtime.py`의 `bind_run_globals`(워커가 실행 직전에 학생 전역 사전을 넘김)·`sync_params`(`block_on` 뒤·`maybe_yield`에서 쌓인 값을 형대로 바꿔 전역 변수에, 양보 없음). **예제 파일 머리말 규약과 파라미터 반영 훅 설명은 `src/lab/README.md`**(`src/lab/controls/example-meta.ts`의 `readExampleMeta`: 제목·설명·`@lesson`·`@tags`·"바꿔볼 것 3가지"·"왜 이런 결과가 나올까" 상자 — 원본에서 옮긴 예제에는 머리말을 넣지 않음). 브라우저 테스트 `tests/e2e/scenario-a.spec.ts`(SPEC §13 시나리오 A, 경과 시간 기록)·`lab-params.spec.ts`, 도구 `tests/e2e/helpers/vision.ts`. 가짜 카메라 영상(`scripts/gen-test-video.mjs`)의 희미한 네모(밝기 100)는 시나리오 A의 임계값 판정용이라 지우지 않는다.
- **Phase 2 병렬 제작 준비(P2-05~P2-13 앞, 2026-09-16):** 규약 전체는 `src/lab/README.md`(3~6절). ① **흉내 모듈 폴더** `src/lab/modules/<id>/{manifest.ts, index.ts, *.py, panel.astro?}` — 등록 파일 수정 없이 자동 발견(`modules/manifests.ts`가 manifest를 모아 검사, `modules/host.ts`가 실습실(labId)에 맞는 모듈의 `index.ts`만 지연 로딩해 `mount(ctx)`, `python/modules.ts`가 폴더의 .py를 워커 `/apc`에 넣고 `SHIM_TABLE`을 `apc_shims.register_shims`에 넘김, `LabShell.astro`가 `panel.astro`를 `[data-lab-module-panel=<id>]`(placement `panel`|`wide`)에 hidden으로 그림). ctx 훅: `onRequest`·`onEvent`·`setValue`·`pushEvent`(manifest에 적은 이름만)·`onLab`·`vision()`(→ `VisionLab.onFrame` 카메라 프레임 훅)·`panel`/`showPanel`·`storageName`(`module:<id>:…`)·`notice`. 파이썬 쪽 훅: `apc_runtime.register_reset_hook`(실행 시작, 양보 금지)·`register_tick_hook`(입력 확인 지점마다, 양보 금지). 예시 모듈 `modules/hello/`(dev 실습실에만), 테스트 `tests/unit/lab/{modules,pyodide-hello}.test.ts`·`tests/e2e/module-hello.spec.ts`. 붙박이 cv2(`python/apc_cv2.py`)는 폴더 규약 이전 것으로 그대로 둔다. ② **예제 이관 도구(PD-33)** `scripts/import-examples.mjs`(+`scripts/lib/{import-examples,zip-read}.mjs`): 목록·기록 `scripts/examples-manifest.yaml`, 원본 zip 멤버를 CRLF→LF만 바꿔 `examples/`에 쓰고 줄 수·`ast.parse`(파이썬 3 있으면) 대조, 제목·설명은 옆의 **사이드카** `<이름>.meta.yaml`(`src/lab/controls/example-sidecar.ts`, 빌드 전용 — 브라우저 코드에서 import 금지). 1단원 f026~f045 → `examples/vision/u1/`, opmp f118~f136 → `examples/vision/opmp/`, pyautogui f016~f025 → `examples/desktop/`(영상처리 실습실 목록이 `EXAMPLE_GROUPS` 묶음(`<optgroup>`)으로 보임). `npm run examples:verify`가 `npm test`에 들어 있다. ③ **미리 설치·등록:** `@mediapipe/tasks-vision` 0.10.35(dependencies, WASM은 `scripts/vendor-assets.mjs`가 `predev`·`prebuild`에서 `public/vendor/mediapipe/0.10.35/wasm/`으로 복사 — git 제외), `workbox-build` 7.4.1(devDependency), 모델 자리 `public/models/`(sources.yaml 항목 있음, 5MB 초과는 `scripts/repo-allowlist.yaml` large_files). ④ **병렬 검증:** `PW_BASE_URL=http://localhost:44xx/ai-physical-computing/ npx playwright test <내 spec>`이면 빌드·미리 보기 서버 없이 그 주소를 시험(포트 배정 A 4401 … G 4407은 README 5절). Claude 에이전트 안에서 `npm run dev`는 `ASTRO_DEV_BACKGROUND=1`을 붙여 앞(foreground)에서 띄운다(Astro 7이 에이전트를 감지하면 백그라운드로 띄우다 30초 대기에 실패할 수 있음).
- **Phase 2 로딩·캐시(P2-05):** 예비본 내려받기 `scripts/fetch-pyodide-fallback.mjs`(prebuild — 첫 cv2 실습에 필요한 7개 25.9MiB를 `public/vendor/pyodide/<판>/`에, 표는 `src/lab/loader/pyodide-files.ts`의 이름·크기·SHA-256, 커밋하지 않음), 서비스 워커 `src/sw/sw.js` + `scripts/build-sw.mjs`(postbuild — workbox-build의 `getManifest()`로 **셸 목록만** 만들고 Workbox 실행 코드는 배포하지 않음. 사전 캐시 예산 700KB·파일 하나 160KB를 넘으면 빌드가 멈춤). 규칙: Pyodide는 캐시 우선 → 막히면 CDN↔같은 사이트 예비본을 **파일 하나 단위로 자동 교체**, `_astro/`·글꼴·`vendor/`·`models/`는 캐시 우선, HTML·Pagefind는 네트워크 우선. 설정은 `src/lab/loader/constants.ts` 한 곳(캐시 이름·용량 한도·메시지 이름). 화면은 실습실 모듈 `src/lab/modules/loading/`(단계별 진행률·받은 양·1분 개념 카드 5장 `src/lab/loader/cards.ts`·[이 컴퓨터에 실습 파일 미리 받기])와 `src/lab/loader/{stages,probe,sw-client}.ts`. 점검 페이지의 네트워크 항목은 `src/components/start/network-check/`(누를 때만 접속, 항목은 `items.ts`). 비상구: 주소에 `?sw=off`. 사이트 전체 등록은 `BaseLayout.astro`의 스크립트.
- **Phase 2 오류 사전(P2-06):** 데이터 한 파일 `content/help/errors/errors.yaml`(2026-09-24 기준 묶음 9·풀이 114 — **세는 법은 그 파일의 `groups`·`entries` 길이**. 보드 오류는 `board` 묶음, 펌웨어 굽기는 `flash` 묶음, 통신(시리얼·블루투스·MQTT·브릿지)은 `comm` 묶음 21항목, 교사가 항목만 더하면 사전 페이지와 실습실 카드에 함께 나온다. 항목에 `level: notice`를 적으면 "오류가 아닌 안내"로 파랑 상자로 그린다). 읽기·고르기 `src/lab/errors/`(`catalog-schema`·`catalog-build`·`catalog-data`(빌드 전용)·`traceback.ts`(사이트·Pyodide 내부 프레임을 걷어내 학생 코드 줄을 찾음)·`explain.ts`·`highlight.ts`(CodeMirror 줄 강조, 취소 가능한 이벤트 `apc:lab-error-line`)). 화면은 모듈 `src/lab/modules/errors/`(모든 실습실, placement `wide` — 콘솔 위 전체 폭 카드), 사전 페이지 `/help/errors/`(`src/pages/help/errors/`, 검색 항목 단위 결과 — `src/config/search.ts`의 `anchorPages`).
- **Phase 2 mediapipe 흉내(P2-08·P2-09):** `src/lab/modules/mediapipe/` — 파이썬 `apc_mediapipe.py`(레거시 `mp.solutions` 모양: `Hands`·`FaceMesh`·`FaceDetection`·`Pose`·`drawing_utils`·`drawing_styles`, 결과 없으면 `None`), 연결표 `face-connections.ts`·`apc_mp_tables.py`(생성 파일 — `node scripts/gen-landmarks.mjs`로 다시 만든다), 화면 엔진 `task-engine.ts` + `{hands,face,pose}-engine.ts`(모델은 늘 같은 사이트 `public/models/*.task`, 없으면 '모델 없음' 안내 후 재생 입력). **카메라 없이도 되는 재생 입력**(PD-30): `synthetic-{hands,face,pose}.ts`가 계산으로 만든 좌표(사람 촬영 데이터 아님)를 `replay-source.ts`가 입력 소스 'replay'로 등록, 동작 10개(2026-09-24 얼굴 `face-wink` "윙크·두 눈 감기(클릭)" 추가 — 4단원 클릭을 카메라 없이). 좌우 반전은 `mirror.ts`. 모델 파일은 운영자가 내려받아 `public/models/`에 둔다(운영자 할 일, 없어도 사이트는 깨지지 않음).
- **Phase 2 러너 공통(P2-10):** `src/lab/modules/runtime-extras/`(labs `'*'` — 모든 실습실). 작업 폴더는 `/home/pyodide`: 사이트가 그린 `assets/mask.svg` → 브라우저가 PNG로 그려 `mask.png`로 넣고(저장소에 래스터 없음), [파일 넣기]로 올린 파일도 같은 폴더에. 파이썬이 저장한 파일은 파일 패널에 [내려받기]로 올라온다(실행 시작·0.25초 틱·실행 끝 `apc_runtime.register_finish_hook`). `PIL.ImageFont.truetype`의 없는 글꼴 경로(`C:/Windows/Fonts/malgun.ttf` 등)는 사이트 글꼴로 바꾼다 — Pyodide의 FreeType이 woff2를 못 열어 정적 `Pretendard-Regular.otf`가 필요하고, `scripts/vendor-assets.mjs`가 npm 패키지 `pretendard`(devDependency)에서 `public/vendor/pretendard/`로 복사한다. 라이브러리 이름을 가리는 파일(`cv2.py`)은 넣지 못하게 막고, 콘솔은 300조각을 넘으면 접는다.
- **Phase 2 가상 데스크톱(P2-11·P2-12):** `src/lab/modules/desktop/`(placement `wide`) — 논리 해상도 1920×1080 가상 모니터(`model.ts` 순수 논리 + `render.ts` 캔버스), 파이썬 `pyautogui.py`(PyAutoGUI 0.9.54와 같은 상수·걸음 계산, 없는 함수는 AttributeError + 한국어 힌트), 표준 라이브러리를 가리는 `webbrowser.py`(가상 브라우저 창 — 검색 결과는 `src/config/nav.ts`의 진짜 쪽 목록에서만 만든다, 포털 모사 금지), 스페이스 키 미니게임 3종 `games.ts`(자체 규칙, 원작 게임 요소 없음), `screenshot()`은 Pillow 그림 + '내 파일' 카드([내려받기], 같은 이름 0.5초 제한).
- **Phase 2 음성 인식(P2-13):** `src/lab/modules/speech/` — 파이썬 파일 이름이 곧 import 이름인 `speech_recognition.py`(Pyodide에 없는 패키지라 `shims` 대신 파일 이름으로 가린다), 흐름은 ① 글자 입력(기본) ② 기기 안 인식(브라우저가 되면) ③ 서버 인식(교사가 `/settings/`에서 켠 브라우저에서만 선택지가 **생김**). 설정 화면 `src/pages/settings/`·`src/components/settings/`, 저장 이름 `module:speech:server-recognition`([기록 지우기]가 함께 지움).
- **Phase 2 차시 연결(P2-14):** 차시의 따라하기 예제(`src/components/lesson/LessonExamples.astro`)에 [이 자리에서 실습실 열기] — **누른 뒤에야** `<iframe src="/labs/vision/?example=<파일>&embed=1" allow="camera">`를 만든다(차시 페이지는 누르기 전까지 실습실 파일을 한 바이트도 받지 않고, 한 쪽에 예제가 여럿이어도 문서가 나뉘어 서로 간섭하지 않는다). `?embed=1`이면 `BaseLayout`이 `<html data-embed>`를 붙이고 `global.css`가 머리글·바닥글·빵부스러기와 `[data-embed-hide]`를 숨긴다. 예제 스모크 `tests/e2e/examples-smoke.spec.ts`(이관 예제(2026-09-17 기준 57개)를 실습실별로 한 번씩 실행 — 영상처리는 `examples/vision/`·`examples/desktop/`, ESP32는 `examples/esp32/` 이관분(P3-02부터), 기대 결과는 사이드카의 `smoke:` 칸), 첫 방문 전송량·시간 `tests/e2e/first-visit.spec.ts`.
- **Phase 3 가상 보드 핵심(P3-01):** 규약 전체는 `src/lab/README.md` 7절, 결정 근거는 `docs/PLAN.md` §8.3 P3-01 구현 메모·PROGRESS 미해결 43~50. ESP32 실습실 `src/pages/labs/esp32/index.astro`(`LabShell labId="esp32" pyodidePackages={[]}` + io 슬롯 `src/components/lab/BoardIo.astro`, 예제 `examples/esp32/*.py` → `src/lab/esp32/examples.ts`, 점검 도우미 `/labs/esp32/check/`는 P3-11에서 채움). 보드는 흉내 모듈 폴더 `src/lab/modules/board/`(labs `['esp32']`): 파이썬 `machine.py`(`Pin`·`Timer`, 아직 없는 주변장치는 ImportError + 한국어)·`micropython.py`·`apc_board.py`(핀·가상 시계·콜백 대기열·import 훅·확장 불러오기)·`apc_board_time.py`(MicroPython판 time), 화면 `index.ts`·`view.ts`(보드 SVG·핀 표·입력 부품 조작)·`state.ts`(메시지 모양)·`parts.ts`(부품 레지스트리·배선 검사). **부품 하나 = `src/lab/modules/board/parts/<부품 id>/part.ts` 폴더 하나**(등록 파일·manifest 수정 없음, 선택 `apc_part_*.py`), machine 주변장치는 `apc_board_<이름>.py` + `apc_board.register_machine_export`. 메시지는 `board.state`(파이썬 → 화면, 핀 전체 목록, 16ms 병합)·`board.inputs`(최신 값)·`board.input`(쌓이는 값)·`board.wiring`·`board.device` 다섯 이름(병렬 제작 준비에서 `board.device.input`을 더해 여섯, 2026-09-18 검토 반영에서 `board.notice`(배선 어긋남 안내를 그림 아래 칸에도)를 더해 일곱). **`time`은 `sys.modules`를 바꾸지 않고 import 훅으로 ESP32 실습실의 학생 코드에만 MicroPython판을 준다**(errno·u-이름·bluetooth 자리도 같은 훅). 러너 쪽 추가: 워커 `load` 메시지의 `labId`(그 실습실 모듈의 `.py`·shims만 `/apc`에), `apc_runtime`의 `peek`·`register_wait_hook`·`register_idle_hook`/`run_idle`(코드가 끝나도 Timer가 남으면 [정지]까지 — 대기 전 훅은 양보 금지, idle 훅은 비동기 진입점이라 기다려도 됨), LabShell `pyodidePackages`(준비 모듈의 캐시 채우기·[미리 받기] 범위). 콜백은 입력 확인 지점(`sleep*`·입력 핀 읽기·`ticks_*`)에서만 돈다. 오류 사전 `board` 묶음. 테스트 `tests/unit/lab/pyodide-board.test.ts`(+`helpers/pyodide-board-run.mjs`, Node 실제 Pyodide)·`board-state`·`board-parts`·`python-modules`·`esp32-examples`, 브라우저 `tests/e2e/lab-esp32.spec.ts`.
- **Phase 3 보드 그림·첫 부품(P3-02):** 규약은 `src/lab/README.md` 7.4·7.5, 근거는 PLAN §8.3 P3-02 구현 메모·PROGRESS 미해결 7·51~56. 보드 그림은 교과서 키트와 같은 30핀 개발 보드 핀 순서를 사이트가 그린 것(`src/lab/modules/board/layout.ts` `HEADER_PINS`·`planBoardDrawing` 순수 함수 → `board-drawing.ts` DOM → `view.ts`): 스트래핑 핀 ▲, 쓰는 핀 흰 고리·1이면 노란 빛(`[data-board-header]`의 `data-used`·`data-high`·`data-wired`), 바깥 부품이 있으면 보드 아래 브레드보드·레일·꺾은선 배선도(`[data-board-wire]`), [그림 크게 보기](`data-board-zoom-level`, 저장 이름 `module:board:zoom`). **배선은 예제마다** 차시 md frontmatter `examples[].parts`(PLAN §2.6의 `type`·`pin`도 받음) → 사이드카 `parts` → 머리말 `# @part 부품 핀` 순서(`wiring-spec.ts`, `esp32/examples.ts`의 `wiringByFile`)이고, 검사 결과는 `WiringIssue`(오류·주의·참고, 종류 표는 README 7.4 — 스트래핑 핀·한 핀에 입력·출력 부품·입력 전용 핀·모르는 부품 등)로 그림 아래 `[data-board-problems] li[data-code]`에, 코드가 배선과 어긋나게 핀을 쓰면 `apc_board.py`가 콘솔에 한 번씩 안내(`board.wiring`의 `label`·`directions`·`known`). 부품 `touch-digital`(누르는 동안 1, 기본 GPIO17)·`vibration-motor`(기본 GPIO19 사이트 배정·실물 확인 전, 떨림은 움직임 줄이기면 멈춤, 세기는 `board.state` pins의 `duty` 자리 — PWM은 P3-03). 부품 정의 새 칸 `anchors`·`power`·`defaultPinsNotice`, 누르는 자리·두 겹 초점 테두리는 `view.ts`가 붙인다. **부품 단위 테스트는 부품마다 `tests/unit/lab/board-part-<부품 id>.test.ts`**(도우미 `tests/unit/lab/helpers/board-snapshot.ts`). 예제: 원본 f046·f052·f053 → `examples/esp32/u2/`, f015 → `examples/esp32/hw/`(사이드카 `parts`·`smoke` — 이관 목록 `meta`에 적으면 도구가 옮김), **f052는 문자 LCD(P3-04) 전까지 2번 줄 ImportError가 기대 결과**, 진동 알림 새 예제 `examples/esp32/04-touch-vibration-alert.py`. 예제마다 실습 방법(사이드카 `practice`·머리말 "── 실습 방법 ──" 상자 → `LabExample.practice`)이 보드 그림 위 `[data-board-practice]`에 보인다. 브라우저 테스트 `tests/e2e/lab-esp32-parts.spec.ts`(배선을 일부러 틀리게 볼 때는 `page.route`로 실습실 HTML의 예제 JSON만 바꿈, 손가락 입력은 CDP `Input.dispatchTouchEvent`).
- **Phase 3 병렬 제작 준비(P3-03~P3-10 여섯 구역 앞, 2026-09-17 — PD-38·PD-39):** 구역·포트(**A 4501 PWM·ADC … F 4506 펌웨어 굽기**)·공유 파일·예제 사이드카 주인은 `src/lab/README.md` 5.1, 부품 제작 체크리스트 7.9, 확장 자리 한눈에 7.10, 모의 시리얼 8절. ① **예제 36개 한 번에 이관**(f047~f051·f054~f081·f001·f007·BT f144 → `examples/esp32/u2/`·`hw/`·`bt/`, 이관 97개, 사이드카 smoke는 지금 결과 그대로 — ESP32 스모크 40개 통과, `parts`·`practice`는 구역이 채움). ② **미리 설치:** Blockly 13.3.0(`blockly/core`·`blocks`·`python`·`msg/ko`, 소리·커서 파일은 `scripts/vendor-assets.mjs` → `public/vendor/blockly/13.3.0/media/`, 경로 `src/lab/vendor-paths.ts` `blocklyMediaPath()`), esptool-js 0.6.1(**플래셔 스텁은 GPL-2.0-or-later라 싣지 않음 — PD-38**, 스텁 import는 `scripts/lib/esptool-stub-guard.mjs`가 한국어 오류 모듈로 바꿈, `astro.config.mjs` `optimizeDeps`), 의존성 pako·tslib·atob-lite 등록, `@types/w3c-web-serial` 1.0.8(`src/lab/serial/web-serial.d.ts` — TypeScript 6은 @types를 저절로 넣지 않음), esbuild 0.28.2(devDependency). ③ **보드 확장 자리**(`apc_board.py`·`state.ts`·`part-types.ts`·`parts.ts`·`view.ts`·`index.ts`·`BoardIo.astro`): PWM `BOARD.set_pwm`(핀 항목 `mode 'pwm'`), 아날로그 전압 `{ mv }`·`read_millivolts`, 부품 장치 `register_part` → `wired_devices`·`set_device_state`(`board.device {v,id,part,state}`)·`on_device_input`(새 메시지 `board.device.input` — 이름 여섯), 부품 조작 칸 `controls(host, api)`, 소리 `sound: true` + `board-audio.ts getBoardAudio()`(`[data-board-sound]`, 상태 `data-board-sound-state`), 보드 라이브러리 `examples/esp32/lib/**` → 워커 `/board/lib`(`src/lab/esp32/board-libraries.ts`·`board-library-files.ts`, 실물 저장은 `librariesNeededBy`), 아직 없는 모듈 표 `NOT_YET_MODULES`, machine 확장 위치 `modules/board/ext/<기능>/apc_board_<기능>.py`. Node Pyodide 단계는 부품마다 `tests/unit/lab/helpers/board-steps/<이름>.mjs` + `runBoardSteps`(`helpers/pyodide-board.ts`, 공유 도우미 `--steps=`). ④ **블록 전용 호환 모드 자리** `apc_runtime.sleep_async`·`apc_board.wait_ns_async`(JSPI 없이 최상위 await — `pyodide-board-async-wait.test.ts`). ⑤ **실행 대상** `lab.setRunTarget({ label, run(code, ctx), stop() })`(`lab-shell.ts` — 실제 보드가 같은 [실행]·[정지]·입력줄·결과·'run'(`target`)/'done'을 받음, 뿌리 `data-run-target`, `tests/e2e/lab-run-target.spec.ts`). ⑥ **모의 시리얼** `src/lab/serial/mock/`(`MockSerialPort`·`FakeSerial`·`MicroPythonDevice`(raw REPL·raw-paste·자동 리셋 회로)·`mini-python.ts` — 테스트 도구, 배포 번들에 없음), Node 테스트 `tests/unit/serial/`(기준 호스트 `helpers/raw-repl-host.ts`), Playwright `tests/e2e/helpers/serial.ts`의 `installSerialMock(page, 설정, { plugins })`·`serialMock(page)`(esbuild로 묶어 `addInitScript`, 플러그인 `tests/e2e/helpers/serial-plugins/`), `tests/e2e/serial-mock.spec.ts`. **Playwright `page.evaluate`는 사용자 조작이 있는 것으로 돈다**(클릭 없는 requestPort 시험은 init script로). 오류 사전 `board` 묶음에 구역별 항목 자리 표시. 공유 테스트 중 "아직 없음"에 기대던 곳(f052 LCD·machine.PWM/UART·neopixel)은 구역이 부품을 더해도 흔들리지 않게 바꿨다.
- **Phase 3 PWM·ADC 부품(P3-03):** machine 확장 `src/lab/modules/board/ext/pwm/apc_board_pwm.py`·`ext/adc/apc_board_adc.py`(MicroPython v1.29.0 소스와 같은 범위·기본값 5000Hz·50%·오류 문구, 해상도로 되계산한 읽기 값 `freq(1000)` → 998, LEDC 채널 16·타이머 8, 34~39번은 `OSError (-258, 'ESP_ERR_INVALID_ARG')`). 부품 `parts/{rgb-led,laser,buzzer,servo,fan-motor,touch-analog-4ch}/` — 핀 신호는 `ext/pwm/pwm-signal.ts` `pinSignal()`로 읽고(off·low·high·pwm과 duty·주파수·펄스 폭), 버저 소리는 `parts/buzzer/buzzer-voice.ts`(Web Audio 펄스파, 음원 파일 없음 — PD-16), 서보 프로필은 이번 실행에서 부른 라이브러리로 정한다(PD-15: `servo_library` → servo40, 그 밖 mg90s). 4채널 터치는 조작 칸(`controls`)의 [패드 1~4] 누르고 있기 + 값 막대. 보드 라이브러리 `examples/esp32/lib/servo_library.py`(원고 174쪽 복원)·`examples/esp32/lib/third-party/buzzer.py`(원본 f008에서 게임 음악 선율 목록 `mario`만 뺀 사이트판 — 2026-09-26 미해결 175, 교과서 157쪽 확인 코드는 `examples/esp32/u2/2-2-1-buzzer-library.py`). 사이트판 예제 3개(f058·f059 판정 구간, f062 g 핀 줄 — 줄 번호를 원고와 맞춤). 테스트 `tests/unit/board-pwm/`·`tests/unit/lab/pyodide-board-pwm-adc.test.ts`·`tests/e2e/lab-esp32-pwm-adc.spec.ts`.
- **Phase 3 I2C 표시 장치(P3-04):** `machine.I2C`·`SoftI2C`·`RTC`는 보드 확장 `src/lab/modules/board/ext/{i2c,rtc}/`(가상 I2C 버스 — 부품 장치의 `i2c_address`·`i2c_write`·`i2c_read`·`i2c_hint`, 대답 없으면 `OSError: [Errno 19] ENODEV` + 한국어 까닭). 문자 LCD `parts/lcd-i2c/`는 PCF8574 비트 → HD44780 니블을 바이트 수준으로 해석해 원본 `i2c_lcd.py`가 그대로 돌고(19자 줄 넘김까지 같음), 글자 칸은 `<tspan data-lcd-cell="행-열">` DOM 글자다. OLED `parts/oled-i2c/`는 `framebuf`·`ssd1306`·`sh1106` 흉내가 SSD1306 명령 바이트를 보내고 장치가 해석한다(두 이름 한 흉내, 사이트가 그린 5×8 글꼴). 화면 낭독기 설명은 부품의 `<desc data-part-desc>`. 보드 라이브러리 사이트판 `examples/esp32/lib/third-party/i2c_lcd.py`(MIT 고지 + `move_to` 별칭). 테스트 `tests/unit/board-i2c/`·`tests/e2e/esp32-i2c.spec.ts`.
- **Phase 3 네오픽셀·UART·MP3·보드 콘솔(P3-05):** 부품 `parts/neopixel/`(학생용 `neopixel.py` + `machine.bitstream` 흉내 `apc_board_bitstream.py`, 16구 링·GRB), `parts/uart/`(`machine.UART` 전체 흉내 `apc_board_uart.py` + USB-UART 변환기와 컴퓨터 시리얼 창 — 글자·바이트 보내기, 속도가 다르면 `reframe`이 실물처럼 깨뜨림), `parts/mp3/`(DFPlayer 프레임 8·10바이트 해석, 곡은 `melodies.ts`의 자체 작곡 3곡 합성 — PD-16). 보드 콘솔 `src/lab/modules/board-console/`는 ESP32 실습실 `builtins.input`을 MicroPython 규칙으로 바꾸고, 실습실 틀의 `lab.prompt`로 입력줄을 열어 **기다리는 동안에도 Timer·UART가 돈다**(실물과 같게). 보드 라이브러리 `examples/esp32/lib/third-party/gorillacell_dcmotors.py`(원고 169쪽 PWM판 복원). 곡 끝은 가상 시각 알람(`apc_board.register_wake_hook` — 2026-09-26, C34)이 긴 sleep 안에서도 제시각에 멈춘다. 테스트 `tests/unit/board-uart/`·`tests/e2e/esp32-uart.spec.ts`.
- **Phase 3 블록 모드(P3-06):** 흉내 모듈 `src/lab/modules/blocks/`(ESP32 실습실, 코드 칸 제목 아래 [블록]·[코드] 전환 — 누를 때만 Blockly를 `import()`), 논리 `src/lab/blocks/`(한국어 사용자 정의 블록·도구 상자 10칸(Phase 4에서 통신 칸이 더해져 11칸)·MicroPython 생성기 — 화면 코드와 **줄 수가 같은 실행판**), 화면 `src/components/lab/blocks/BlocksPanel.astro`. 블록 = 코드: 블록 모드의 편집칸은 읽기 전용 생성 코드 칸이고 [코드로 바꾸기]가 예제 칸에 저장한다. 블록 전용 호환 모드(PD-27, 조건부 채택): JSPI가 없고 편집칸이 생성 코드와 글자까지 같을 때만 `apc_blocks.py` 실행판(최상위 await)을 보낸다(`lab.setRunCodeTransform`). 블록이 쓰는 부품 배선은 실습실 뿌리 속성 `data-board-wiring-override`로 알리고 보드 모듈이 그린다(`src/lab/blocks/board-link.ts`). 테스트 `tests/unit/blocks/`·`tests/e2e/esp32-blocks.spec.ts`·`tests/e2e/scenario-b.spec.ts`, 도구 `tests/e2e/helpers/blocks.ts`.
- **Phase 3 실제 보드(P3-07·P3-08):** `src/lab/serial/`(`serial-channel`·`raw-repl`·`raw-repl-follow`·`banner`·`board-connection`·`board-run-target`·`board-files`·`board-input`·`board-recovery`·`compat`·`usb-chips`·`support`) + 모듈 `src/lab/modules/real-board/`(입력·출력 칸 위 [가상 보드]/[실제 보드] 탭, `lab.setRunTarget`, [보드에 저장]·[보드 되찾기]·[boot.py 끄기]) + 화면 부품 `src/components/lab/real-board/`. [실행]마다 raw REPL 소프트 리셋 → raw-paste, [정지]는 Ctrl-C 되풀이, 포트 선택 창은 거르지 않는다. 파일 쓰기는 mpremote `fs_writefile` 방식(256바이트씩 `w(b'…')`, SHA256이 같으면 건너뜀)이고 자리는 **보드 뿌리**(교과서 Thonny 저장 자리 + ESP32 `sys.path`). `input()`은 코드가 도는 동안 입력줄을 열어 영어·숫자·기호 + `\r`만 보내고 되울림을 한 번 거른다. 테스트 `tests/unit/serial/real-board-*`·`tests/e2e/esp32-real-board*.spec.ts`.
- **Phase 3 펌웨어 굽기(P3-09):** 화면 `src/components/lab/firmware/FirmwareFlasher.astro`(보드 준비 페이지 3단계, HTML `src/lab/firmware/markup.ts`, 움직임 `controller.ts`), 굽기 순서 `src/lab/firmware/flasher.ts`(esptool-js 0.6.1을 **ROM 부트로더로만** — PD-38, 모자란 명령은 `rom.ts`: 8바이트 SPI_ATTACH·0x400 블록·ROM MD5·RTS 리셋), 목록 `public/firmware/manifest.json`(`manifest.ts` 검사, 빌드 때 `manifest-file.ts`가 HTML에 심음), 검증 `verify.ts`(크기·ESP32 이미지 머리·SHA-256) + `md5.ts`, 오류 풀이 `errors.ts`(오류 사전 묶음 `flash`), 지원 브라우저 `support.ts`, 받기 `download.ts`. 펌웨어 파일은 `public/firmware/v1.29.0/`(운영자가 공식 주소에서 받아 검증한 파일 — `sources.yaml`·`NOTICE.txt`). 테스트: 모의 ROM 부트로더 `src/lab/firmware/mock/esp32-rom.ts`(+ Playwright 플러그인 `serial-plugin.ts`), `tests/unit/firmware/`(Astro Container 그리기는 `npx vitest run --config tests/unit/firmware/vitest.container.config.mjs` — `npm test`에 들어 있다), `tests/e2e/esp32-firmware.spec.ts`.
- **Phase 3 보드 준비 페이지(P3-10):** `src/pages/start/board/index.astro`(1단계 준비물·케이블 → 2단계 [보드 연결] → 연결이 안 될 때 `details#port-not-found` → 3단계 펌웨어 굽기 → 4단계 첫 예제 2-1-1을 실습실 [실제 보드]에서). 부품 `src/components/start/board/`(연결 확인 `ConnectCheck.astro` + 화면 `connect-check.ts`·순서 `board-check.ts`(구역 E `BoardConnection`)·글 `connect-view.ts`, 안내 `PortHelp.astro`(**안에 `<details>`를 넣지 않는다** — `tests/e2e/start.spec.ts`), 장치 관리자 그림 `DeviceManagerFigure.astro`, 키트 표 `KitPartsTable.astro`+`kit-parts.ts`, 사이트 밖 주소·확인 기록 `links.ts`). 같은 페이지에서 포트를 넘기는 약속 `src/lab/firmware/port-release.ts`(창 이벤트 `apc:serial-port-release`). 테스트 `tests/unit/firmware/start-board-*.test.ts`·`port-release.test.ts`·`tests/e2e/start-board.spec.ts`(모의 ROM 플러그인의 `__APC_ESP32_BOARD_OPTIONS__ = { board: { firmware: 'none' } }`으로 시나리오 C 흐름).
- **Phase 3 통합·실물 점검 도우미(P3-11):** 실물 점검 도우미 `/labs/esp32/check/`(`src/pages/labs/esp32/check/index.astro` + `src/components/lab/check/BoardCheckHelper.astro`) — 항목은 `src/lab/esp32/check/items.ts` 한 파일(부록 B-2 번호·예상 시간·배선·시험 코드·예/아니오 질문), 배선 그림은 가상 보드 그림을 꺼진 모습으로 한 장 그리는 `wiring-figure.ts`, 판정·[결과 복사] 글은 `report.ts`(PROGRESS에 붙일 마크다운 표), 화면 논리 `check-page.ts`(구역 E `BoardConnection`·`provisionLibraries`·`prepareBoardInputLine`을 그대로 씀, 저장 이름 `board-check:answers`). 보드 그림 글자·부품 모양 CSS는 공용 파일 `src/styles/board-drawing.css`(실습실 `BoardIo.astro`와 점검 도우미가 함께 씀 — 스크립트가 그린 SVG라 범위 스타일이 닿지 않는다). 1분 개념 카드는 실습실마다 다르다(`src/lab/loader/cards.ts` `cardsForLab` — ESP32는 보드 카드 5장). 테스트 `tests/unit/esp32-check/`·`tests/e2e/esp32-board-check.spec.ts`(모의 포트로 항목 전체를 끝까지).
- **Phase 2 검토 반영(2026-09-17):** 실습실 사용성 규칙 — [실행]은 파이썬을 받는 동안에도 눌러 예약(`lab-shell.ts`, 이벤트 `run-pending`, 단추 `data-lab-run-pending`), 실행하면 io 슬롯의 `[data-lab-reveal-on-run]`(넓은 결과 칸)·`[data-lab-reveal-on-run-min]`(최소 칸)과 첫 조절 막대를 한 화면에 함께 보이게 옮기고(`src/lab/controls/reveal.ts`의 `revealTogether`·`revealElement` — 이미 보이면 안 움직임), 오류로 끝나면 안내 줄(`lab.showMessage`)에 적고 풀이 카드로 옮긴다(편집칸 줄 강조는 편집칸 안에서만 스크롤). 첫 준비 동안 준비 패널은 편집칸 위(`data-loading-intro`). **흉내 모듈 패널은 코드가 그 모듈을 쓸 때만 연다**(`src/lab/modules/panel-when-used.ts`의 `showPanelWhenUsed` — mount에서 `showPanel()`을 바로 부르지 않는다). `?example=`의 차시 링크(`LabExample.lesson`, 페이지가 `getCollection('lessons')`로 만듦)·없는 예제 알림, 링크 검사 `example-not-found`. 서비스 워커는 Pyodide 7개를 SHA-256으로도 대조(`build-sw.mjs`가 `pyodide.hashes`를 새김). 교과서 3·4단원 PC 쪽 예제 f090~f097을 `examples/vision/u3/`·`u4/`로 이관(57개, `EXAMPLE_GROUPS`에 묶음 두 줄) — f084·f085는 `serial`이라 Phase 3·4에서. 확인 `tests/e2e/lab-textbook-u3-u4.spec.ts`, 스모크 일부만 `SMOKE_ONLY=f090,f091`.
- **Phase 3 검토 반영(2026-09-18):** 적대적 검토 3명(완료 기준·사용성·안전)의 지적 26건을 반영했다. **실습실 공통:** 콘솔이 화면 밖이면 결과 칸 아래에 "콘솔에 결과가 나왔어요"와 마지막 3줄·[콘솔 보기 ↓](`[data-lab-io-output]`)·콘솔 제목 배지(`[data-lab-console-new]`)를 띄운다(`lab-shell.ts`, 콘솔이 보이면 IntersectionObserver가 지움), 편집칸은 줄을 접는다(`EditorView.lineWrapping`), 예제 한 줄 설명을 조작 줄 아래에 보인다(`[data-lab-example-description]`), 실행 대상이 상태 줄을 잠깐 바꿀 수 있다(`LabRunContext.setStatus`). **오류 사전:** 항목 칸 `level: notice`(고칠 것이 없는 안내는 빨강 대신 파랑 — [정지]·파이썬 다시 시작), [정지] 카드에서 카메라 얘기를 뺐다. **가상 보드:** `board.notice` 메시지로 배선 어긋남 안내를 그림 아래 "배선 확인" 칸에도, 편집칸에서 고친 코드의 `# @part`를 다시 읽어 배선도를 그린다(`wiringFromEditorCode`), 보드 그림 최소 폭 32rem + 넘침 그늘·"옆으로 밀어 보세요"(`[data-board-stage-wrap]`·`[data-board-scroll-hint]`), [그림 크게 보기] ↔ [원래 크기로], 배선 경고 조사는 `withParticle`, 4채널 터치 값 막대 한 칸 45(4095를 정확히 나눔). **실제 보드:** [보드에 저장]이 보드의 파일을 바꿔 쓰기 전에 확인 창(`confirmOverwrite`·`BoardSaveCancelled`), 사이트가 실물용 파일을 주지 못하는 모듈 안내(`VIRTUAL_ONLY_MODULES` — OLED 드라이버), 자르기 호환 규칙 오탐 수정. **검사:** 고지 파일이 "초안"이면 빌드를 멈춘다(`sources-check.mjs`), `repo-check.mjs`의 리터럴 NUL 제거(코드 검색에 보이게), `repo-check.yml` 워크플로 추가.
- **Phase 4 통신 실습실(P4-01~P4-11, 2026-09-24 통합):** 규약 전체는 `src/lab/README.md` 9절(브릿지 9.0~9.8, MQTT 9.9, 대시보드 9.10, 컴퓨터 쪽 흉내 9.11, 가상 BLE 9.12, Web Bluetooth·USB 데이터 포트 9.13, 템플릿·블록 9.14, 4단원 9.15, 갤러리 9.16)·5.2(실제로 만든 것 표)·8.7(가짜 블루투스), 결정 근거는 `docs/PLAN.md` §8.4 구현 메모·통합 구현 메모. **브릿지 핵심** `src/lab/bridge/`(메시지 규칙·보낼 차례 `BridgeOutbox`(초당 10회·상태 병합·이벤트 보존)·통로 등록표 `registerBridgeChannel` — 붙박이 `direct`·`tab`(BroadcastChannel, iframe 한 화면 모드 포함), 등록 통로 `mqtt`·`ble`·`serial`, 접두어 12글자(l·o·0·1 없음, `sessionStorage` 기본·주소 `?bridge=`)). **컴퓨터 쪽(영상처리 실습실) 흉내**: `modules/serial-pc/serial.py`(pyserial — 파일 이름이 곧 import 이름), 새 예제용 `modules/vision-bridge/finger-count/bridge.py`(`bridge.send`·`event`·`send_bytes`·`receive`), `modules/ble-pc/{bluetooth,bluetooth_lib}.py`(같은 문서의 가상 보드가 먼저, 없으면 블루투스 칸(`web-bluetooth`)으로 이어진 실제 보드 — 다른 탭으로는 아직 안 보냄), 두 실습실을 잇는 선·[보내기] 패널 `modules/vision-bridge/`(`BOARD_EXAMPLES`·`BOARD_PAIR_OF`, 보드에 UART 부품 없이 `ble`만 있으면 받은 바이트를 `apc:ble-write`로). **보드 쪽**: 부품 `parts/ble/`(상태 LED 12, 조작 칸이 상대 기기 노릇, 창 이벤트 `apc:ble-write`·`apc:ble-notify`), 확장 `ext/ble/`(`bluetooth`·`ubluetooth`)·`ext/network/`(`network.WLAN`·`umqtt.simple`), `apc_part_uart.py`가 이벤트 `board.uart.tx`를 보냄, 보드 라이브러리 `examples/esp32/lib/third-party/esp32_ble_util.py`(사이트 배포본). **실제 기기**: Web Bluetooth `src/lab/ble/` + 모듈 `web-bluetooth`(클릭 안에서만 선택 창, 광고 이름으로 고름 — 주소 안 씀, 가짜 블루투스 `src/lab/ble/mock/`), USB 데이터 포트 `src/lab/serial/data-port/` + 모듈 `data-port`(보드 REPL과 따로 두 번째 포트, 이름표). **MQTT** `src/lab/mqtt/` + 모듈 `mqtt`(ESP32 실습실): 기본은 같은 컴퓨터 탭, 공개 중계 서버는 고를 때만 + 경고 늘 보임(PD-29), MQTT.js는 [연결] 때 `import('mqtt')`, 토픽 `<접두어>/<장치>/<rx|tx>`(가상은 통로가 접두어를 붙이고 이미 붙어 있으면 한 번 더 붙이지 않음 — 실물 코드는 직접 적음). **페이지**: 통신 실습실 안내 `/labs/iot/`(`src/pages/labs/iot/index.astro`, 짝 예제 링크를 빌드 때 사이드카에서), 대시보드 `/labs/iot/dashboard/`(`src/lab/dashboard/` — 위젯 그래프·게이지·스위치·기록, 새 위젯 = `defaults.ts` 한 줄 + `widgets.ts` 한 갈래), 4단원 통합 실습실 `/labs/unit4/`(`src/lab/unit4/` — 한 문서에 두 실습실, [함께 실행], 성능 재기 `perf.ts`, 주소 `?pair=<짝 이름>`으로 두 칸을 함께 채움 — 2026-09-26 미해결 179), 예제 갤러리 `/labs/gallery/`(`src/lab/gallery/` — 빌드 때 카드, 태그 차례 차시 md → 사이드카 → 규칙, 주소 `?comm=ble` 등). **예제**: 이관 30개(3·4단원·교안 — BLE 주소는 `XX:XX:XX:XX:XX:XX`), 사이트판(`…-site.py` — 머리말 없음·줄 수 같음·고친 줄 끝 `# [사이트판]`, 이관 목록 밖이라 spec이 지킨다: 3-1-2 `bridge-vision-board.spec.ts`, BLE 5개 `esp32-ble.spec.ts`), 통신 템플릿 `examples/esp32/templates/`(블록 `src/lab/blocks/comm/`, 기본값 `plan.ts`의 `COMM_*`), 시나리오 F `examples/{vision,esp32}/u4/c3-*.py`(보충 C3 `content/lessons/u3/c3.md` — Phase 5 통합에서 공개). 차시 예제 링크는 파일 첫 폴더로 실습실을 고른다(`labOfExampleFile`, 링크 검사 `example-wrong-lab`). 점검 페이지의 MQTT 항목은 확인된 공개 중계 서버에 WebSocket으로 연결만 해 본다(`items.ts` `probeWebSocket`). 테스트 `tests/unit/{bridge,bridge-serial,board-ble,mqtt,dashboard,gallery}/**`·`tests/unit/serial/data-port-*`·`tests/unit/blocks/comm-*`, 브라우저 `tests/e2e/{bridge-vision-board,scenario-f,dashboard,mqtt,esp32-ble,web-bluetooth,data-port,esp32-comm-blocks,unit4,examples-gallery}.spec.ts`(시나리오 D = `dashboard.spec.ts`, F = `scenario-f.spec.ts` — 재생 입력이라 카메라 없이). **`page.routeWebSocket`은 `page.goto` 전에 건다**(새 문서에 넣는 스크립트라 연 뒤에 걸면 진짜 서버로 나간다 — 2026-09-24 확인).
- **Phase 5 원고 이미지 추출 도구(P5-01, 2026-09-25):** 원고 그림은 차시마다 따로인 그림 목록 `content/lessons/<단원 폴더>/<차시>.images.yaml`(허용 목록 + 눈 확인 기록 — 여러 구역이 서로 다른 파일만 고친다)에 적은 것만 `npm run images:extract -- <차시>`가 원본에서 꺼내 픽셀만 WebP로 다시 인코딩해 `public/images/lessons/<차시>/`에 쓴다(CLI `scripts/extract-lesson-images.mjs`, 흐름 `scripts/lib/lesson-images-extract.mjs`, 규칙·검사 `scripts/lib/lesson-images.mjs`, 파이썬 일꾼 `scripts/lib/lesson_images_worker.py` — PyMuPDF·Pillow·numpy, 개발 도구). 출판 편집 삽화·컷(원본 그림 XMP 이름표 `…(삽)`·`…(컷)`)은 **결정 O10으로 운영자 자료** — 차시 그림 폴더에 바로 두고(`third_party: publisher`는 목록 검사가 오류로 막음, 도구는 `checks.rights`에 기록만), `third_party: <키>` → `third-party/<키>/`는 그 밖의 허락받은 제3자 그림에만(sources.yaml `third_party` 항목이 `public/images/lessons/*/third-party/<키>/**`로 덮음). 출판사가 아닌 권리자(스톡) 표기가 있는 그림은 **결정 C11로 꺼내지 않는다**(도구가 멈춤, 운영자 저자 이름은 제3자로 보지 않음). 차시 그림 폴더의 SVG는 sources.yaml "사이트가 직접 그린 그림"이 맡는다. 제외 쪽 `scripts/image-exclusions.yaml`(INVENTORY §6 — never: BT p5·학급 게시물·U1 021쪽, region: 본인 확인 대상이 아닌 얼굴(U1 013·015·016·033)·경로·기기 주소 쪽은 region + `privacy_override` + 얼굴 쪽 엄격 얼굴 검사일 때만. 운영자 본인 얼굴 쪽과 BT p1 학교명은 O9·O11로 뺐고(16747e1), BT p84 셸 속 기기 주소는 통합에서 더함. 구역은 고치지 않는다). 얼굴 검사 `scripts/lib/face-check.mjs`는 사이트와 같은 OpenCV 4.11(Pyodide 휠 캐시 `.cache/pyodide-packages/`를 읽기만 — 이 PC의 파이썬 OpenCV 5.0에는 CascadeClassifier가 없다)로 경고만 하고, 제외 쪽에서 잘라 낸 그림은 하나라도 나오면 지우고 멈춘다. 도구가 `file`·`sha256`·`checks`를 목록에 적고 그림이 바뀌면 `reviewed`를 지운다. 쪽 미리 보기 `npm run images:show -- U1 14`(`.cache/lesson-images/show/`), 원본 없는 검사 `npm run images:check`(= `tests/unit/lesson-images.test.ts`, 실제 추출은 파이썬이 있을 때만 `lesson-images-extract.test.ts`). 절차는 MAINTENANCE.md 3-1, 구역용 요약 `.cache/phase5-notes/core-p5-01.md`.
- **Phase 5 차시 틀과 검사(P5-02, 2026-09-25):** 새 차시 쓰는 법은 `MAINTENANCE.md` 1절(설정 칸·8칸·상자·용어·그림·퀴즈·교사용·검사 규칙 표 1-6), **본보기는 기준 차시 `content/lessons/u1/1-1-1.md`**. 규칙 한 곳 `src/components/lesson/lesson-rules.ts`(규칙 이름 `fm-*`·`sec-*`·`box-*`·`teacher-box`·`img-alt`·`md-tilde`(범위 물결표 두 개가 GFM 취소선이 됨 — 본문 범위는 `\~`, 오류)·`md-bold`(`**[단추]**을`처럼 굵게가 글자로 남음, 참고) 등, HTML 나무 도우미 `html-tree.ts`) — 빌드(`[lesson].astro`)는 모두 `[차시 틀] 파일: [규칙] 문장` **경고로만**, `npm run check:lessons`(`scripts/check-lessons.mjs` → `scripts/lib/check-lessons.mjs`: 같은 규칙 + 예제 파일·그림 파일·원고 그림 눈 확인 기록·그림 목록 alt·용어사전·상자 이름 오타·같은 주소)는 **오류면 실패**(PD-35 — `npm test`에는 규칙 단위 테스트만, 실제 차시 엄격 검사는 CI "테스트" 워크플로의 한 단계). 성취기준 표·PLAN §2.2 대응표는 `src/config/standards.ts`(15개 코드·영역·사이트 요약·차시 45개 대응(IV-프로젝트 포함), 교육과정 게시물은 sources.yaml `reference` — 인천광역시교육청 승인 교육과정은 결정 O12로 확인). 일부러 비운 차시(보충 V1~V5·P1·선택 1-4-3·대단원 마무리 3)는 `UNMAPPED_REASONS`에 짧은 까닭을 두어 화면에 "해당 없음(보충 차시)"·"해당 없음(대단원 마무리)"처럼 + 자료실 까닭 링크, 까닭 없이 빈 차시만 "성취기준 코드 확인 중"(`lesson-data.ts` `standardsEmptyText`, DECISIONS C8). 교사용 접기 끝 "이 차시의 원고와 자료"는 저절로 붙는다(`LessonTeacherInfo.astro`·`teacher-info.ts` — 원천·"원고 없음: 사이트가 코드 기준으로 쓴 본문"(근거는 예제 코드·주석 + `handouts`가 있으면 수업 교안 + I단원이면 대단원 마무리 문항)·사이트가 새로 쓴 읽기 자료·성취기준·원본 안내(갤러리·자료실·원고 정정 목록·진짜 PC 링크)·가린 편집본 `handouts`(PDF 자리 `src/components/lesson/handouts.ts`)). 발표 모드 `present.ts`·`LessonPresent.astro`(`data-present-off`, 단계 = 제목 → `##` → `###` → 도전 과제·예제 → 퀴즈 문항, 교사용 제외, →←·Home·End·Esc). 새 상자 `:::생성형AI`(도전 과제 칸에만, 끝에 `GENAI_NOTE` 저절로). frontmatter 새 칸 `source`·`handouts`는 Astro 콘텐츠 캐시가 옛 값을 되살릴 수 있어 `?? []`로 읽는다. 테스트 `tests/unit/lesson/{lesson-rules,check-lessons,present,teacher-info}.test.ts`·`tests/unit/standards.test.ts`, 브라우저 `tests/e2e/lesson-template.spec.ts`. Phase 5 병렬 제작 구역 표는 `src/lab/README.md` 5.4, 요청 폴더 `.cache/phase5-requests/`.
- **Phase 5 검토 반영(2026-09-25):** 예제가 여럿인 차시는 `::예제[1]`·`::예제[2,3]`로 예제를 코드 읽기 옆에 나눠 놓는다(`lesson-html.ts` `placeExampleSlots`, 틀린 번호는 `body-plan`). 150줄 넘는 예제는 frontmatter `examples[].focus: "26-28, 31-32"`로 발췌(`src/components/lesson/example-code.ts` — 예제 끝 실습실 안내 주석은 차시 코드 칸에서 뺌, 규칙 `example-focus`). 설정 칸·사이드카의 따옴표 없는 글 속 ` #`은 YAML 주석이 되어 글이 잘린다 → 규칙 `fm-yaml-comment`(`scripts/lib/yaml-comment-traps.mjs`, 저장소 YAML은 `tests/unit/lesson/yaml-comment-traps.test.ts`). 새 상자 `:::더알아보기`(접힘 참고 표). 발표 모드는 화면보다 긴 단계를 "(이어서)"로 저절로 나눈다(`present.ts` `splitTallSteps`). 흉내 모듈은 하나씩 따로 설치하고(`apc_shims.last_failures()`), 패키지를 받는 중이면 실패 알림을 내지 않는다(`worker.ts` `packageLoadsInFlight`). 키트 업체 제품 그림은 `third-party/kit-vendor/`(sources.yaml 제3자 항목). 50분 넘는 교과서 차시는 나누지 않고 `:::참고[이 시간의 기본과 심화]` + 교사용 "두 차시로 운영할 때"(PLAN §8.5 검토 반영 메모).
- **Phase 5 차시 콘텐츠(P5-03~P5-13, 2026-09-25 통합):** 차시 45편 = 차례표 전체(`npm run check:lessons -- --complete` 통과) — `content/lessons/u1/`(1-1-1~1-4-3, 보충 V1~V5, `review.md`), `u2/`(2-1-1~2-2-4, 읽기 자료 `2-1-r.md`, `review.md`), `u3/`(3-1-1~3-1-4, 보충 P1·C1~C3 — C1~C3는 옛 `content/lessons/supplement/`에서 옮김, `review.md`), `u4/`(4-1-1~4-2-2, 프로젝트 안내 `project.md` = 차례표 `u4-project` 칸의 `IV-프로젝트`(`kind: reading`, `source: supplement`)). 그림은 사이트 SVG + 원고 그림 28장(그림 목록 15개), 용어사전 87개(새 낱말 57개 — 표제어·별칭이 다른 항목과 겹치지 않게). 새 예제: 사이트판 `examples/vision/u1/1-1-2-hands-first-site.py`·`1-4-1-adv-raise-hand-site.py`·`examples/esp32/u2/2-1-5-neopixel-check-site.py`, 체험 `examples/vision/u3/3-1-1-bytes-converter.py`·`examples/esp32/u3/c1-mqtt-remote.py`. 원고의 설치 절차는 차시에서 교사용 자료실 "진짜 PC에서 돌리기"(`/teacher/real-pc/#…`)로 링크한다. 합성 얼굴 재생 입력의 좌우 반전은 `mirror.ts`의 `faceMirrorPairs()`(478점 좌우 짝, 가운데 선은 제자리)로 점 번호까지 바꾼다 — 코드가 `cv2.flip(frame, 1)`로 뒤집으면 출력 창 왼쪽 눈이 코드의 `LEFT_EYE_POINTS`(33번 무리)(`SYNTHETIC_FACE_VERSION` 2, 고정 좌표 `tests/fixtures/landmarks/face-pose.json`). 구역 보고 `.cache/phase5-notes/zone-*.md`, 통합 보고 `.cache/phase5-notes/integration.md`.
- **Phase 5 교사용 자료실(P5-14, 2026-09-25):** 페이지 `src/pages/teacher/`(첫 화면·`guides/`(+대단원 `[unit]`)·`standards/`·`corrections/`·`real-pc/`·`faq/`), 부품·순수 함수 `src/components/teacher/`(자료실 메뉴·카드 목록 `teacher-pages.ts` — 아래 페이지는 사이트 지도 `nav.ts`가 아니라 여기서, 차시 교사용 접기 꺼내기 `teacher-guides.ts`(id 앞머리 `guide-u<대단원>-<slug>-`, 꺼낸 글 밖 #링크는 차시 주소로), 모으기 `teacher-lessons.ts`, 데이터 검사 `teacher-data-schema.ts`). **지도 요약·성취기준 표의 차시·원고 정정의 차시별 모음·생성형 AI 과제 모음은 차시 md에서 빌드 때 저절로** — 차시 교사용 칸의 `### …바꾼 곳`·`…코드 파일`·`…쓴 근거` 부분이 원고 정정 목록에 모인다(`isChangeSection`). 사람이 적는 데이터는 `content/teacher/assessment.yaml`(과목 정보·평가 방법·성취기준마다 볼 것·비운 차시의 긴 까닭 `unmapped` — `UNMAPPED_REASONS`와 같은 차시를 가리키는지 단위 테스트)·`corrections.yaml`(원고 정정 — 쪽 그림으로 확인한 것만)·`real-pc.md`(설치 안내, 판 번호)·`content/help/faq-teacher.md`(선생님 FAQ, `### 질문` 하나 = 질문 하나, 검색은 질문 단위 `anchorPages`). 교사용 시작하기(`/start/teacher/`)는 성취기준 영역 표에서 자료실 `#std-area-N`으로 잇는다. 테스트 `tests/unit/teacher/teacher-guides.test.ts`(순수 함수 — 구역의 e2e 파일을 Vitest로 옮김)·`tests/e2e/teacher.spec.ts`. 고치는 법 MAINTENANCE.md 3-4.
- **Phase 5 가린 편집본 교안(P5-14, PD-31):** `scripts/redact-handouts.py`(PyMuPDF 가림 주석으로 글자·그림 픽셀까지 지움, 메타데이터·XMP·태그 구조·링크·첨부 제거, 사진은 다시 인코딩) + 기록 `scripts/handout-redactions.yaml`(가릴 곳·쪽별 눈 확인 — 가린 내용 자체는 적지 않음) → `public/teacher/handouts/bt-lesson-plan-redacted.pdf`(91쪽)·`pyautogui-slides-redacted.pdf`(23쪽). 쪽 번호는 원본과 같다(차시 `handouts`의 `#page=`, 이름은 `src/components/lesson/handouts.ts`). 교안 p9·p84·p91 본인 얼굴(O9)·p1 학교명(O11)은 가리지 않고, p5 홍보 이미지 인물·기관 계정 경로·기기 주소·학급 게시물·바탕화면은 가린다. 명령 `npm run handouts:build|check|preview`(운영자 PC 개발 도구 — 파이썬 3.11 + PyMuPDF, PPTX 변환만 Windows PowerPoint, `check`는 원본 없이). 커밋 전 저장소 검사 `handout-review`(`scripts/lib/repo-check.mjs` — 스테이징된 기록의 SHA-256·모든 쪽 기록), PDF는 `repo-allowlist.yaml` `original_formats`의 두 파일만. 절차 MAINTENANCE.md 3-3.
- **Phase 5 통합 결정 반영(2026-09-25):** 예제 코드 MIT(O13 — `LICENSE-CONTENT.md`·`README.md`·`src/config/site.ts`·`/credits/`, `third-party/`는 원래 조건), 출판 편집 삽화는 운영자 자료(O10 — 옛 `third-party/publisher/` 그림 7장을 차시 폴더로 옮김), `ESP32BLE.py` MIT 고지(C12 — 이관 도구의 두 번째 예외 `notice:`(주석 줄만, 원본 앞에 붙이고 줄 수·SHA-256은 붙인 뒤 파일로 기록), 고지 원문 `public/licenses/esp32ble.txt`). 결정 기록은 `docs/DECISIONS.md` C8·C10~C12·O9~O13.
- **Phase 6 병렬 제작 준비(2026-09-26):** 구역 표는 `src/lab/README.md` 5.5(A 성능 4901·B 접근성 4902·C 출처·개인정보 4903·D 유지보수 문서 4904·E 오프라인판 4905(2차)·F 실습실 잔여 4906, 검토 4911~4913 — 구역마다 고칠 수 있는 파일 목록), 요청 폴더 `.cache/phase6-requests/<구역>.md`, 준비 보고 `.cache/phase6-notes/core-p6-prep.md`(기준 측정값). **빌드 환경 변수는 `src/config/site.ts`(`resolveBuildSettings`) 한 곳에서 읽는다:** `APC_BASE`(사이트 하위 경로 — 없으면 `/ai-physical-computing`, `/`면 뿌리. 브라우저 번들에는 `astro.config.mjs`가 `vite.define`의 `__APC_BASE__`로 새김 → `withBase`·서비스 워커·Pyodide 예비본 주소가 따름, 전체 주소 `absoluteUrl()`·canonical·og:url은 늘 공개 사이트 `siteConfig.publicBase`)·`APC_OUT_DIR`(결과 폴더 — 없으면 `dist`, Astro가 비우는 폴더라 `dist`·`dist-이름`·`.cache/` 아래만 받음 → `outDir`·번들 검사·`scripts/search-index.mjs`(Pagefind + `prune-pagefind.mjs`)·`build-sw.mjs`·`check-links.mjs`가 따름). Git Bash는 `/`를 `C:/Program Files/Git/`로 바꾸므로 `MSYS_NO_PATHCONV=1`(바뀐 값은 한국어 오류로 멈춤). 마크다운 플러그인 목록은 `src/lib/markdown-plugins.mjs` 한 곳(빌드·`check:lessons`가 함께 씀) — rehype 자리 `src/lib/rehype-lesson-polish.mjs`(구역 A, 출력이 바뀌면 `REHYPE_LESSON_POLISH_VERSION`을 올려 콘텐츠 캐시를 비움)와 줄바꿈 막기 클래스 `.nowrap`(`global.css`). 새 명령 `npm run build:offline`(오프라인판 — 아래 P6-07 항목)·`npm run test:a11y`·`npm run perf:measure`(`scripts/run-e2e-group.mjs` — `tests/e2e/<무리>*.spec.ts`가 없으면 "아직 없어요", 있으면 Playwright + `APC_E2E_GROUP`). `@axe-core/playwright` 4.13.0(devDependency, MPL-2.0 — 배포물 밖이라 sources.yaml 등록 안 함). 테스트 `tests/unit/build-env.test.ts`.
- **Phase 6 성능(P6-02, 2026-09-26):** 판정 = Chrome DevTools "3G"(옛 Slow 3G — 400kbit/s·지연 2,000ms)에서 학습 페이지 FCP 3,000ms 안, 쪽마다 3번 가운데 값(DECISIONS C15, "Slow 4G"(옛 Fast 3G)는 비교용). 규칙 한 곳 `scripts/perf-rules.mjs`(조건·한도·측정 쪽 목록 `PERF_PAGES`·무거운 라이브러리 표식), 측정 `tests/e2e/perf-pages.spec.ts`(실습실 밖 쪽의 Pyodide·MediaPipe·Blockly·MQTT.js·CodeMirror·esptool-js 요청 0 — 전체 실행은 대표 쪽, `perf:measure`는 차시 45편까지 · 원고 그림 자리 · 차시 번호 줄바꿈)·`perf-timing.spec.ts`(perf 무리에서만 — FCP·첫 문단·LCP·DCL·첫 그리기를 막은 파일, 되풀이 `APC_PERF_RUNS`)·`perf-lab-modules.spec.ts`. **CSS는 HTML 안**(`astro.config.mjs` `build.inlineStylesheets: 'always'`)이고 **글꼴 CSS는 첫 그리기를 막지 않는다**(`BaseLayout.astro` `media="print"` → `all` + `<noscript>`) — DECISIONS C16, `_astro/*.css` 파일이 없어 서비스 워커 셸은 JS·글꼴 CSS·아이콘·홈만. **흉내 모듈 쓸 때 받기:** manifest `load: { group, code, query?, windowEvents?, order? }`(`src/lab/modules/types.ts`, 계획 `manifests.ts` `moduleLoadPlan`, 받기 `host.ts` — 코드 낱말·주소·창 이벤트·파이썬 요청이 부르면 무리째 받고 order 차례로 mount, 뿌리 `data-lab-modules-loaded`·`-waiting`·`-loaded-by`, [미리 받기]는 `prefetchLazyModules`로 받기만) — 통신 모듈 여섯(무리 `comm`)만 쓴다(C17). 마크다운 출력 다듬기 `src/lib/rehype-lesson-polish.mjs` 판 2: 마크다운 그림에 파일에서 읽은 width·height, 글 속 차시 번호를 `<span class="nowrap">`(C14).
- **Phase 6 접근성(P6-03, 2026-09-26):** 판정 = axe 4.13.0 기본 규칙 전부 × 모든 쪽 × 두 화면 폭에서 critical·serious 0(C18). 검사 `tests/e2e/a11y.spec.ts`(사이트 지도·대단원·교사용 자료실·차시 md 전부·404 — 새 차시가 저절로 들어옴, 도구 오판은 `KNOWN_FALSE_POSITIVES`에 규칙 + 요소로만, 바닥글 판 표기)·`a11y-states.spec.ts`(누르거나 실행한 뒤의 화면)·`a11y-keyboard.spec.ts`(Tab 걷기 — 덫·초점 보임·가림, 여닫기, 확대 200%·글자 200%, 움직임 줄이기). 오래 걸리는 것은 `APC_E2E_GROUP=a11y`(= `npm run test:a11y`)에서만. 규약(C19): 넘칠 수 있는 칸은 `data-scroll-focus="이름"`(`src/components/common/scroll-focus.ts` — 넘치는 동안만 Tab 차례, `BaseLayout`이 모든 쪽에서 설치), 역할 없는 `<pre>`·`<code>`에 aria-label 금지, 실습실 콘솔 `role="log"`·`aria-live="off"`, 한 쪽에 실습실 틀이 둘이면 `LabShell`의 `contextLabel`(4단원 화면 "컴퓨터 칸"·"보드 칸"), 대시보드 위젯은 [설정]의 자리·크기 단추로도 옮김(WCAG 2.2 2.5.7). 바닥글 `[data-site-version]`과 "바뀐 점 기록"(저장소 `CHANGELOG.md`).
- **Phase 6 출처·라이선스(P6-04, 2026-09-26):** 고지 전문은 `public/licenses/` 25개 + `public/firmware/v1.29.0/NOTICE.txt` + `public/fonts/pretendard/LICENSE.txt` = 27개 — 목록은 `/credits/` "고지 전문 파일 모음"(`src/lib/credits.ts` `buildCreditsView().noticeFiles`). 실행 파일 안 구성요소 고지(C21): 펌웨어 `micropython-esp32-firmware.txt`, Pyodide 코어 `pyodide-core-3rd-party.txt`, opencv·Pillow 휠 `pyodide-wheels-3rd-party.txt`(FFmpeg 4.4.1 LGPL — PD-19·C22, IJG·FreeType 문장), 검색 엔진 wasm `pagefind-wasm-3rd-party.txt`(**GPL-3.0-only 크레이트 pagefind_microjson** — C23, 빌드가 `dist/pagefind/NOTICE-THIRD-PARTY.txt`로도 복사), MediaPipe wasm `mediapipe-wasm-3rd-party.txt`. 문서에 적으라는 문장은 `REDISTRIBUTION_NOTICES` → `/credits/` "다시 나누는 파일에 꼭 함께 알리는 것"(영어 원문 `lang="en"`). 한국어 머리말 고지는 UTF-8 BOM(단위 테스트). 출처 검사: json·yaml의 `license:` 칸 이름은 저작 표시가 아니고, 번들 SPDX 식은 괄호·AND·OR·WITH를 풀어 대조(C25). 가린 편집본은 끝에 출처·라이선스 쪽(`credits_page` — 블루투스 교안 92쪽·PyAutoGUI 슬라이드 24쪽, 원본 쪽 대조 `npm run handouts:compare -- --baseline <옛 편집본 폴더>`). 브라우저 `tests/e2e/credits.spec.ts`.
- **Phase 6 개인정보 최종 점검(P6-05, 2026-09-26):** 저장소 검사(`scripts/lib/repo-check.mjs`)의 기기 주소 찾기는 `findDeviceAddresses` 한 곳(C26 — MicroPython bytes 출력·hexlify·주소 낱말 옆 목록까지, 문맥 창 80자·전송 줄 예외), 그림은 `inspectImageMetadata` + `inspectRasterLeftovers`(끝 뒤 바이트·PNG 모르는 조각·썸네일·형식 모름). 손으로 돌리는 훑기(C27): `npm run check:repo -- --worktree`(스테이징 전 작업 폴더)·`-- --history`(git 기록 전체 — 확인해 둔 blob은 `scripts/repo-allowlist.yaml` `history_reviewed`)·`-- --dist <폴더>`(빌드 결과의 이 컴퓨터 절대 경로·개인정보 모양·그림, `vendor/`는 참고로만 — `build:offline`과 CI "테스트" 워크플로가 부름). 커밋 전 훅은 그대로 인덱스만. 테스트 `tests/unit/repo-check.test.ts`(미탐·오탐 사례, 합성 print 결과 2,000개×3).
- **Phase 6 유지보수 문서(P6-06, 2026-09-26):** `MAINTENANCE.md`(맨 위 "하려는 일로 찾기" 표, 0 준비 두 방법(웹 화면만/내 컴퓨터), 1 새 차시(차례표에 없는 새 차시·보충 번호 `B1` 규칙 C30, GitHub 웹 화면·github.dev로 올리기), 4 출처 등록 차례, 5 배포 확인, 6 빌드 실패(원인별 실제 메시지), 8 판 올리기(Pyodide·MediaPipe·Blockly·MQTT.js·esptool-js·펌웨어 4-4·Astro·Actions·Node), 9 도메인 연결(CNAME 파일 없음), 10 판 번호와 CHANGELOG, 11 연 1회 점검, 12 오프라인판 — **기존 절 번호는 바꾸지 않고 덧붙임**), `CONTRIBUTING.md`, `CHANGELOG.md`(판 기록 — 규칙 C20: 개발 중 Phase N 끝 = `0.N.0`, Phase 6 완료 = `1.0.0`, 판은 `package.json` `version` 한 곳, 2026-09-26 통합에서 0.5.0), 이슈 양식 `.github/ISSUE_TEMPLATE/{1-lab-error,2-lesson-error,3-example-proposal,4-privacy-copyright}.yml`(파일 이름 앞 숫자 = 고르기 화면 차례, 목록은 `src/pages/contribute/_issue-templates.ts` — C29), 풀 리퀘스트 양식 `.github/pull_request_template.md`.
- **Phase 6 오프라인 배포판(P6-07, 2026-09-26):** `npm run build:offline` = `scripts/build-offline.mjs`(10단계 — 파이썬 패키지 확인 `scripts/lib/offline-packages.mjs` → `npm run build -- --config scripts/offline/astro.config.offline.mjs`(보통 설정 + `vite.define` `__APC_OFFLINE__`)를 `APC_BASE=/`·`APC_OUT_DIR=.cache/offline/site`로 → `build-sw.mjs --offline` → Pyodide 오프라인 표 채우기 → 링크 검사 → `checkOfflineSite` → 안내 파일 → 개인정보 검사(`runBuildOutputCheck`) → zip(`scripts/lib/offline-zip.mjs`, Node 내장 zlib) → CRC 대조) → `.cache/offline/apc-offline-<판>.zip` + 요약 `.json`(저장소에 넣지 않음). 오프라인 표시는 `src/lab/runtime/config.ts`의 `OFFLINE_BUILD` 하나(파이썬 엔진·미리 받기가 같은 사이트만, `ALLOWED_REMOTE_ORIGINS` 빈 목록, 음성은 글자 입력만 — 온라인·개발 서버·Node에서는 false). Pyodide 표 `PYODIDE_OFFLINE_EXTRA_FILES`(Pillow)·`pyodideFileTable(offline)`(`src/lab/loader/pyodide-files.ts`), 서비스 워커 설정 `offline: true`면 CDN으로 바꾸지 않음. zip 안: `시작하기.bat`(ASCII만 — 한국어는 `START_BAT_MESSAGES` → `\uXXXX` → PowerShell)·`읽어보세요.txt`(BOM)·`server/serve.ps1`(PowerShell 5.1 `HttpListener`, localhost·127.0.0.1만·관리자 권한 없이·포트 자동·같은 판 재사용·Host 거르기, BOM)·`server/serve.py`·`site/`·LICENSE 둘(C31·C32). MIME 표 세 곳이 같음(`scripts/lib/offline-site.mjs` `OFFLINE_MIME_TYPES`), zip 안 이름 상한 130글자. 확인 `npm run test:offline`(= `scripts/offline/verify-offline.mjs [--server py]` — zip 풀기 → `시작하기.bat` → 인터넷을 막은 Edge로 `tests/e2e/offline.spec.ts`, CI는 건너뜀). 단위 `tests/unit/offline/`. 릴리스에 올리는 것은 운영자 확인 뒤(C33). 절차 MAINTENANCE.md 12절.
- **Phase 6 통합(2026-09-26):** 구역 요청을 반영하며 판을 **0.5.0**으로(바닥글·점검 [결과 복사]), 새 명령 `npm run test:offline`·`npm run handouts:compare`, 4단원 화면 칸 이름(`contextLabel`), MQTT·가상 데스크톱 패널의 이름 규약, 대시보드 [설정]의 자리·크기 단추(`[data-dash-place-from]`·`[data-dash-place-key]`), 스크립트가 만드는 스크롤 칸 4곳의 `data-scroll-focus`, [미리 받기]가 쓸 때 받는 모듈도 받음(`src/lab/modules/loading/index.ts`), 오프라인판 음성 글자 입력만(`availableModes`의 `offline`), CI "테스트" 워크플로의 빌드 결과 개인정보 훑기 한 단계. 시나리오 A~F·성능·접근성 결과표는 `PROGRESS.md` "시나리오·품질 결과표", 결정은 DECISIONS C13~C34, 구현 메모는 PLAN §8.6.
- **Phase 1 화면:** 홈 `src/components/home/`, 배우기 `src/pages/learn/`·`src/components/lesson/`(차시 틀 규칙은 `lesson-html.ts`·`curriculum.ts` 머리말), 용어사전 `content/glossary/`·`src/components/glossary/`, 시작하기·점검 `src/pages/start/`·`src/components/start/`·`src/lib/capabilities.ts`, 브라우저 권장 환경 안내 `src/components/compat/BrowserNotice.astro`(실습실·학생용·보드 준비 페이지의 notice 슬롯), 사이트 검색 화면 `src/components/search/`, 실습실 자리 `src/pages/labs/_labs.ts`, 라이선스 `LICENSE`(MIT)·`LICENSE-CONTENT.md`(CC BY-NC-SA 4.0), 안내 `README.md`·`MAINTENANCE.md`·`CONTRIBUTING.md`.
- **용어 표시:** 새 낱말 = `content/glossary/<영문-이름>.md` 하나. 본문에는 `:용어[낱말]`(조사는 대괄호 밖)을 쓰고, 사전에 없는 낱말은 빌드 경고만 낸다. 마크다운 본문을 보여 주는 페이지는 `GlossaryScope`로 감싼다(용어사전을 빌드마다 새로 읽어 풀이를 붙임 — Astro 콘텐츠 캐시가 remark 결과를 되살리므로 풀이를 remark 단계에서 넣지 않는다).
- **코드 블록 색:** Shiki 테마 `github-light-high-contrast`(`astro.config.mjs`, 코드 글자색 대비 모두 5.04:1 이상).
- **.astro 띄어쓰기 주의:** Astro 7은 줄바꿈이 든 글자와 태그 사이 공백을 지운다. 글 뒤에서 줄을 바꿔 `<a>`·`<strong>`·`<code>`를 쓰거나 `</dt>`·`<dd>`처럼 요소를 이어 쓰면 "알리려면문제"처럼 붙으므로, 줄 끝에 `{' '}`를 붙이거나 한 줄에 적는다(2026-09-16 통합 단계에서 22곳 발견·수정).
- **출처·저장소 검사(P1-04):** `sources.yaml`(출처 등록부) → 빌드 전후 검사와 `/credits/` 자동 생성(다른 저작자 파일은 `third-party/` 폴더 + 항목 따로, 밖에 두면 참고 경고). 커밋 전 훅·CI의 저장소 검사(`scripts/check-repo.mjs`, 허용 목록 `scripts/repo-allowlist.yaml`, 이미지 눈 확인 기록 = 차시 그림 목록 `content/lessons/**/*.images.yaml`(P5-01)과 옛 공용 기록 `scripts/image-allowlist.yaml`을 git 인덱스(스테이징한 내용)에서 함께 읽고 기록의 sha256·그림 속 메타데이터(EXIF·XMP·ICC·글 조각)까지 본다 — 추적 파일 전체의 래스터 이미지와 글·코드 파일 안 data: 그림, 개인정보 모양: 사용자·OneDrive 경로·MAC·이메일·전화번호, 학교 이름은 `scripts/privacy-needles.json`에 해시로만 두고 `node scripts/privacy-needle.mjs <이름>`으로 만든다. 학교명은 결정 O11로 공개해도 되지만 문서·주석·커밋 메시지에 학교 전체 이름을 적을 필요는 없다(검사는 그대로 막는다). 가린 편집본 PDF(`public/teacher/handouts/*.pdf`)는 `scripts/handout-redactions.yaml`의 SHA-256·쪽별 눈 확인 기록을 스테이징된 내용에서 대조한다(`handout-review`)). `git commit --no-verify` 금지(노출 전 관문은 훅뿐).
- **글꼴 대체:** `src/styles/fonts.css`의 'Pretendard Fallback'(맑은 고딕을 Pretendard 폭·높이에 맞춤)이 글꼴을 받는 동안 보인다. Pretendard 파일은 고치지 않는다.
- **검색 결과:** 용어사전은 항목 단위(`sub_results`, `src/config/search.ts`의 `anchorPages`). 빌드 뒤 `scripts/prune-pagefind.mjs`가 쓰지 않는 Pagefind 화면 파일을 지운다(다른 라이브러리가 묶여 있어 배포하지 않음).
- **사이트 설정 한 곳:** `src/config/site.ts`(이름·설명·저작자·주소·라이선스·버전) → `astro.config.mjs`가 읽는다. `base: '/ai-physical-computing'`, `trailingSlash: 'always'`(내부 링크는 `import.meta.env.BASE_URL` 뒤에 `/`로 끝나는 경로를 붙인다).
- **파이썬 실행:** Pyodide 314.0.7(모듈 워커) — jsDelivr 고정 주소 + 같은 사이트 예비본(PD-02), 실행 중 입력 전달은 JSPI 기본(PD-01). 설치됨(P2-01): 브라우저는 CDN에서 직접 받고, npm `pyodide@314.0.7`은 타입 선언과 Node 단위 테스트에만 쓰는 devDependency(배포 번들에 없음).
- **비전 AI:** MediaPipe Tasks Vision 0.10.35 고정(PD-03, 1.0.x는 사용 지표 전송 때문에 쓰지 않음).
- **블록·에디터:** Blockly 13.3.0(Python 생성기 — 설치됨 2026-09-17, 블록 모드는 P3-06), CodeMirror 6.
- **ESP32:** Web Serial API + raw REPL 자체 구현(P3-07·P3-08, 모의 시리얼 `src/lab/serial/mock/`), 펌웨어 굽기 esptool-js 0.6.1(설치됨 2026-09-17 — ROM 부트로더로만, 스텁 제외 PD-38), 블루투스는 Web Bluetooth.
- **통신:** MQTT.js 5.15.2(WebSocket, 설치됨 2026-09-18 — [연결]을 누를 때만 받음), 같은 컴퓨터 탭끼리는 BroadcastChannel(PD-17), 실제 보드는 Web Bluetooth(Nordic UART Service)·Web Serial 두 번째 포트(USB-UART 변환기).
- **그 밖:** Pagefind 1.5.2(검색), Pretendard 1.3.9(글꼴, 자체 호스팅), 순수 CSS, Playwright 1.63.0(브라우저 테스트), GitHub Actions → GitHub Pages(배포).
- 아직 설치하지 않은 것은 해당 묶음에서 위 버전으로 설치하고, 배포물에 들어가는 것은 먼저 `sources.yaml`에 등록한다.
- npm 설치 스크립트 허용 목록은 `package.json`의 `allowScripts`(지금은 esbuild만). 새 경고가 나오면 그 패키지를 확인한 뒤 `npm approve-scripts 이름`.

## 명령어
프로젝트 루트에서 실행한다. 운영자 PC 셸에는 Node.js가 PATH에 없을 수 있으니 먼저 붙인다 — Git Bash: `export PATH="/c/Program Files/nodejs:$PATH"`, PowerShell: `$env:Path = "C:\Program Files\nodejs;" + $env:Path`.
- 의존성 설치: `npm ci`(package-lock.json 그대로). 새 패키지는 `npm install 이름@정확한버전`
- 개발 서버: `npm run dev` → http://localhost:4321/ai-physical-computing/ (앞의 `predev`가 `scripts/vendor-assets.mjs`로 `public/vendor/`에 MediaPipe WASM과 Pretendard 정적 글꼴을 복사한다). Claude 같은 AI 에이전트 안에서는 `ASTRO_DEV_BACKGROUND=1 npm run dev -- --port 44xx`처럼 앞(foreground)에서 띄운다(Astro 7이 에이전트를 감지하면 백그라운드로 띄우다 "Dev server failed to start within 30s"로 실패할 수 있음, 2026-09-16 확인). **한 작업 폴더에는 개발 서버가 하나만 뜬다**(`Another astro dev server is already running.`) — 여럿이 함께 쓸 때는 먼저 뜬 주소를 `PW_BASE_URL`로 쓰거나 `--ignore-lock`을 붙여 잠금 파일을 건드리지 않고 띄운다. 병렬 제작 포트 배정(Phase 6: A 4901 … D 4904·E 4905(2차)·F 4906·검토 4911~4913 — `src/lab/README.md` 5.5, Phase 5: A 4801 … F 4806·2차 G 4807·H 4808은 5.4, Phase 4: A 4701 … F 4706·2차 G 4707·H 4708은 5.2, Phase 3: A 4501 … F 4506은 5.1, Phase 2 기록 A 4401 … G 4407은 5.3). `--ignore-lock` 서버는 같은 폴더에서 나란히 뜨지만 에이전트 안에서는 `ASTRO_DEV_BACKGROUND=1`과 함께만 되고 `astro dev stop`이 찾지 못하므로, 다 쓰면 포트로 PID를 찾아 끈다(`Get-NetTCPConnection -LocalPort 45xx -State Listen` → `Stop-Process`, 2026-09-17 확인). 서버가 여럿이면 `APC_VITE_CACHE_DIR=.cache/vite-45xx`를 서버마다 다르게 주어 의존성 미리 묶기 폴더를 나눈다(같은 폴더를 함께 쓰면 `504 (Outdated Optimize Dep)`가 되풀이되며 실습실이 뜨지 않는다 — 2026-09-18 실측)
- 빌드: `npm run build`(결과는 `dist/`. 앞에서 `vendor-assets`·`fetch-pyodide-fallback`(예비본 7개를 `public/vendor/pyodide/`에)·출처 검사, 뒤에서 번들 검사·Pagefind 색인·`build-sw`(`dist/sw.js`)가 돈다), 빌드 결과 미리 보기: `npm run preview` → http://localhost:4321/ai-physical-computing/ . Claude 같은 AI 에이전트 안에서 실행하면 Astro 7이 알아서 백그라운드 서버로 띄우므로, 다 보면 `npx astro preview stop`으로 닫는다(`npm run test:e2e`는 앞에서 돌게 막아 두었다)
- 타입 검사: `npm run check`(astro check, 오류 0이어야 한다)
- 단위 테스트: `npm test`(vitest run, `tests/unit/**/*.test.ts` + Astro 컴포넌트를 실제로 그리는 Container 테스트 `vitest run --config tests/unit/firmware/vitest.container.config.mjs` — 이름이 `*.container-test.ts`라 기본 설정으로는 돌지 않는다)
- 브라우저 테스트: `npm run test:e2e`(Playwright, `tests/e2e/`. 이관 예제(2026-09-24 기준 123개 — 영상처리 64(데스크톱 10 포함)·ESP32 59, 라이브러리 4개는 빼고 셈)를 실습실에서 한 번씩 돌리는 `examples-smoke.spec.ts`(약 10분)와 첫 방문 전송량·시간을 재는 `first-visit.spec.ts`가 들어 있다 — 둘 다 데스크톱에서만 돈다. 프로젝트는 desktop·mobile과 `webkit`(블록 전용 호환 모드만 — 로컬에 WebKit이 없으면 그 검사는 건너뛴다)) — `npm run build` 뒤 `astro preview`(포트 4329, `PW_PORT`로 바꿈)를 띄워 데스크톱 1366×768·모바일 375×812로 돈다. `PW_BASE_URL=http://localhost:4401/ai-physical-computing/ npx playwright test tests/e2e/<내 spec> --project=desktop`처럼 주소를 주면 빌드·서버 없이 그 주소(각자 띄운 개발 서버)를 시험한다(병렬 제작용, 개발 서버에는 검색 색인이 없어 search 테스트는 돌지 않음). 브라우저는 Playwright 전용 Chromium이 설치돼 있으면 그것, 없으면 Windows에서 설치된 Microsoft Edge를 쓴다(`PW_CHANNEL=msedge|chrome|chromium`으로 정함). 같은 폴더에서 동시에 두 번 돌리지 않는다(dist/를 새로 빌드함)
- 링크 검사: `npm run check:links` — `dist/`의 사이트 안 링크·그림·글꼴·#위치가 모두 있는지, base(`/ai-physical-computing/`)가 빠진 주소가 없는지 본다(먼저 `npm run build`). 다른 base·결과 폴더로 빌드했으면 같은 `APC_BASE`·`APC_OUT_DIR`를 주고 돌린다. 규칙은 `scripts/lib/link-check.mjs` 머리말
- 예제 이관(PD-33): `npm run examples:import`(원본 zip → `examples/`, 목록·기록 `scripts/examples-manifest.yaml`, 운영자 PC에서만 — 다른 컴퓨터는 `-- --materials <원본 폴더>`), `npm run examples:verify`(원본 없이 기록과 대조, `npm test`에 포함). 옮긴 코드 파일은 고치지 않고 제목·설명은 옆의 `<이름>.meta.yaml`에 적는다(`src/lab/README.md` 3·6절)
- 차시 틀 검사(P5-02): `npm run check:lessons`(모든 차시 — 초안 제외, 아직 없는 차시는 목록만), `-- 1-2-1 v4`(몇 개만), `-- --drafts`, `-- --complete`(차례표의 차시가 모두 있어야 통과 — Phase 5 완료 기준). 오류면 종료 코드 1, 빌드는 같은 규칙을 경고로만. 규칙 표는 MAINTENANCE.md 1-6
- 원고 그림(P5-01): `npm run images:show -- <U1|U2A|U2B|U2C|U3|BT|PPT> <쪽>`(좌표 격자·그림 번호 미리 보기), `npm run images:extract -- <차시>`(그 차시 목록의 그림만 꺼냄 — 운영자 PC 원본 폴더, 다른 컴퓨터는 `-- --materials <originals 폴더>`, 파이썬은 `APC_PYTHON`), `npm run images:check`(원본 없이 목록 검사, `npm test`에 포함). 절차는 MAINTENANCE.md 3-1
- 가린 편집본 교안(P5-14·P6-04): `npm run handouts:build`(다시 만들기 — 원본은 운영자 PC)·`npm run handouts:check`(원본 없이 검사)·`npm run handouts:compare -- --baseline <옛 편집본 폴더>`(원본 쪽이 쪽 그림 픽셀·쪽 글자까지 같은지 — 덧붙인 쪽만 새로 눈 확인, C24). 파이썬 PyMuPDF 필요(개발 도구). 절차는 MAINTENANCE.md 3-3
- 가린 편집본 교안(P5-14): `npm run handouts:build`(`-- bt`·`-- ppt` 하나만, 운영자 PC 원본 폴더 — 다른 컴퓨터는 `-- --materials <originals 폴더>`), `npm run handouts:preview -- bt`(쪽 그림 `.cache/handouts/<문서>/pNN.png` — 쪽별 눈 확인), `npm run handouts:check`(원본 없이 저장소의 편집본만). 파이썬 3.11 + PyMuPDF·Pillow·numpy·fontTools(`python` 명령), PPTX 변환만 Windows PowerPoint. 절차는 MAINTENANCE.md 3-3
- 같은 사이트 자산 복사: `npm run vendor`(`scripts/vendor-assets.mjs` — npm 패키지의 MediaPipe WASM과 Pretendard 정적 글꼴을 `public/vendor/`로, git 제외). `npm run dev`·`npm run build` 앞에서 자동으로 돈다
- Pyodide 예비본만 채우기: `npm run pyodide:fallback`(`--check`면 빠진 것만 알림, `--offline`이면 네트워크 없이 로컬 복사만). 서비스 워커만 다시 만들기: `npm run sw`(빌드 결과가 있어야 한다)
- CI: `.github/workflows/deploy.yml`(사이트 배포: 저장소 검사·빌드 → 배포)과 `.github/workflows/e2e.yml`(워크플로 이름 "테스트": push·PR마다 단위 테스트·타입 검사·Playwright 전용 Chromium 브라우저 테스트·링크 검사·빌드 결과 개인정보 훑기(`check-repo.mjs --dist dist`), 배포를 막지 않음 — PD-35), `.github/workflows/repo-check.yml`(워크플로 이름 "저장소 검사": 앞의 둘이 `paths-ignore`로 건너뛰는 `PROGRESS.md`·`.agent/**`만 바뀐 push에서도 `check:repo`가 돌게 — 2026-09-18 검토 반영). 코드를 바꾼 묶음은 push 전에 로컬에서 `npm test`·`npm run test:e2e`를 통과시킨다
- 검색 색인만 다시 만들기: `npm run search:index`(`scripts/search-index.mjs` — 결과 폴더(`APC_OUT_DIR`, 기본 `dist`)에 Pagefind를 돌리고 쓰지 않는 화면 파일을 지운다). `npm run build`가 뒤(postbuild)에서 자동으로 돌린다
- 출처 검사: `npm run check:sources` — `npm run build`가 앞(prebuild)에서 자동으로 돌리고, 뒤(postbuild)에서는 배포 번들 의존성을 검사한다. `npx astro build`로 직접 빌드하면 이 검사가 돌지 않는다
- 저장소 검사: `npm run check:repo` — 커밋 전 훅(`.githooks/pre-commit`)과 배포 워크플로가 같은 검사를 한다. 손으로 돌리는 훑기(P6-05, C27): `-- --worktree`(스테이징 전 작업 폴더 전체), `-- --history`(git 기록 전체), `-- --dist <폴더>`(빌드 결과 — 연 1회 점검과 오프라인판 zip 직전). 훅은 `npm install`·`npm ci`가 `git config core.hooksPath .githooks`로 켠다(확인: `git config --get core.hooksPath`)
- 오프라인 배포판: `npm run build:offline` → `.cache/offline/apc-offline-<판>.zip`(약 25~40초, 저장소에 넣지 않음). 확인 `npm run test:offline`(= `node scripts/offline/verify-offline.mjs`, 약 1분, Edge — `-- --server py`는 파이썬판 서버). 안내 글·서버 스크립트만 고쳤으면 `node scripts/build-offline.mjs --skip-build`. 만든 zip을 GitHub Release에 올리는 것은 운영자 확인 뒤(DECISIONS C33). 절차는 MAINTENANCE.md 12절
- 접근성 검사: `npm run test:a11y`(`tests/e2e/a11y*.spec.ts` 세 파일 두 화면 크기 — axe 모든 쪽·누른 뒤 화면·키보드·확대·움직임 줄이기, `APC_E2E_GROUP=a11y`) · 성능 측정: `npm run perf:measure`(`tests/e2e/perf*.spec.ts`를 desktop·워커 1로 — 빌드 → 미리 보기, **다른 검사 없이 혼자** 돌린다(C15), 되풀이 `APC_PERF_RUNS`(기본 3), 실사이트는 `PW_BASE_URL=https://songdocomputerpark-lang.github.io/ai-physical-computing/ npm run perf:measure`). 두 명령 뒤에 Playwright 인자를 붙일 수 있다(`-- --project=desktop`). 두 무리의 대표 검사는 `npm run test:e2e`에서도 돈다

## 작업 규칙
- 계획 → 구현 → 실제 브라우저 테스트 → 보고. 한 번에 한 Phase. (승인은 운영자가 위임함 — `docs/DECISIONS.md` O1. 결정은 근거와 함께 기록하고 진행)
- 브라우저 전용 기능(Pyodide, 카메라, Web Serial)은 브라우저에서 확인하기 전에 "완료"라고 하지 않는다.
- 확실하지 않은 API·패키지·라이선스는 공식 문서로 확인. 추측으로 쓰지 않는다.
- 학생이 보는 모든 문장은 "고1이 처음 읽어도 이해되는가?"로 검토한다.
- 작은 커밋 단위. 커밋 메시지는 한국어로 "무엇을 왜".
